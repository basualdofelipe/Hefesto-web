---
phase: 12.5-demo-login-and-code-readiness-for-portfolio
plan: "06"
subsystem: database
tags: [migration, seed, demo-user, auth, postgres, typeorm]
dependency_graph:
  requires:
    - phase: 12.5-01
      provides: demo-login endpoint that authenticates against demo@nemea.app
    - phase: 12.1
      provides: 1773000000000-CreateRolesAndMigrateUsers migration (roles table + ADMIN role uuid)
  provides:
    - "1773200000000-SeedDemoUser.ts: idempotent migration that inserts demo@nemea.app as active ADMIN user"
  affects: [nemea-back/database, fresh-clone-setup, demo-login-path]
tech-stack:
  added: []
  patterns: ["TypeORM MigrationInterface with parameterized raw SQL", "ON CONFLICT DO NOTHING for idempotent seed inserts", "Role id looked up by name (not hardcoded uuid)"]
key-files:
  created:
    - nemea-back/src/database/migrations/1773200000000-SeedDemoUser.ts
  modified: []
key-decisions:
  - "SeedDemoUser queries ADMIN role id by name (not hardcoded uuid) — uuid generated at CreateRolesAndMigrateUsers runtime, not fixed"
  - "ON CONFLICT (\"email\") DO NOTHING for idempotency — email has UQ constraint from CreateUserTable migration"
  - "Migration timestamp 1773200000000 > last existing 1773100000000 (AddSortOrderToProductSizes), no gap"
  - "Task 2 (live-DB application + psql verification) deferred per user decision — no reachable Postgres at execution time"
patterns-established:
  - "Seed migration pattern: SELECT role id by name → parameterized INSERT with ON CONFLICT DO NOTHING → down() DELETE by email"
requirements-completed: [R3]

# Metrics
duration: ~5min
completed: "2026-05-26"
---

# Phase 12.5 Plan 06: SeedDemoUser Migration Summary

**New TypeORM migration `1773200000000-SeedDemoUser.ts` seeding `demo@nemea.app` as an active ADMIN user via idempotent parameterized INSERT — live-DB application deferred pending Postgres access.**

## Performance

- **Duration:** ~5min
- **Started:** 2026-05-26
- **Completed:** 2026-05-26
- **Tasks:** 1 of 2 (Task 2 deferred — see below)
- **Files modified:** 1

## Accomplishments

- Migration file `1773200000000-SeedDemoUser.ts` created and committed (`ecc70e1` in nemea-back)
- `up()` queries the ADMIN role id by name (safe against uuid variability), then inserts `demo@nemea.app` with `ON CONFLICT ("email") DO NOTHING` for idempotency
- `down()` deletes the demo user by email for clean rollback
- `npx tsc --noEmit` and `npm run lint` both pass with zero errors
- No existing migration file touched (immutability constraint satisfied)

## Task Commits

Each task was committed atomically:

1. **Task 1: Write SeedDemoUser migration** - `ecc70e1` (feat) — nemea-back sub-repo

**Plan metadata:** `5cec01e` (docs: partial SUMMARY — checkpoint) → final SUMMARY commit: TBD

## Files Created/Modified

- `nemea-back/src/database/migrations/1773200000000-SeedDemoUser.ts` — TypeORM seed migration for demo@nemea.app; up()/down() with ADMIN role lookup and idempotent INSERT

## Decisions Made

- ADMIN role id is looked up by name (`SELECT id FROM "roles" WHERE "name" = 'ADMIN' LIMIT 1`) because the uuid was generated at CreateRolesAndMigrateUsers runtime — hardcoding would break on any fresh clone
- `ON CONFLICT ("email") DO NOTHING` targets the unique constraint on users.email (established in CreateUserTable migration) — keeps re-runs safe without a guard query
- Timestamp `1773200000000` is the next increment after `1773100000000-AddSortOrderToProductSizes.ts`

## Deviations from Plan

None — Task 1 executed exactly as written. Task 2 deferred per user decision (see below).

## Pending / Deferred Items

### DEFERRED: Task 2 — Live-DB Application and Verification (R3 runtime evidence)

**Status:** Deferred — no reachable Postgres at execution time. User explicitly chose to skip.

**What remains:** Apply the migration against a live Postgres and confirm:
1. `npm run migration:run` from nemea-back completes without error
2. `SELECT u.email, u.is_active, r.name FROM users u JOIN roles r ON r.id = u.role_id WHERE u.email = 'demo@nemea.app'` → one row, `is_active = t`, `name = ADMIN`
3. Re-run migration (idempotency check) → no error, still exactly one demo row
4. (Optional) POST to `/api/auth/demo-login` with `DEMO_LOGIN_ENABLED=true` → 200 + JWT with populated permissions

**Surfaces in:** `/gsd:verify-work` for phase 12.5. R3 is code-complete but lacks runtime evidence until this step is executed.

**How to run:**
```bash
# From nemea-back, with DATABASE_URL pointing at a reachable Postgres:
npm run migration:run
psql "$DATABASE_URL" -c "SELECT u.email, u.is_active, r.name FROM users u JOIN roles r ON r.id = u.role_id WHERE u.email = 'demo@nemea.app';"
```

## Issues Encountered

None — Task 1 built and linted cleanly on first attempt.

## User Setup Required

**Postgres required for deferred Task 2.** Once a local or Railway Postgres is reachable with `DATABASE_URL` set:
- `cd nemea-back && npm run migration:run`
- Verify with psql query above
- Optionally test demo-login POST endpoint

## Next Phase Readiness

- Migration code is complete and committed — a fresh clone running `npm run migration:run` will have the demo user after all migrations run in order
- Live-DB runtime verification (Task 2 / R3 runtime evidence) is deferred; surfaces as a gap for `/gsd:verify-work`
- No blockers for downstream work — the migration file is present and passes tsc + lint

---

## Self-Check

- [x] `1773200000000-SeedDemoUser.ts` exists: confirmed via `ls migrations/`
- [x] Task 1 commit `ecc70e1` exists: confirmed via `git log` in nemea-back
- [x] `npx tsc --noEmit` — zero errors (verified during Task 1 execution)
- [x] `npm run lint` — passes with zero warnings (verified during Task 1 execution)
- [x] No historical migration modified (git status confirmed new file only)
- [ ] Task 2 (live-DB apply + psql verify) — DEFERRED per user decision

## Self-Check: PASSED (code-only scope; Task 2 runtime verification deferred)

---
*Phase: 12.5-demo-login-and-code-readiness-for-portfolio*
*Completed: 2026-05-26*

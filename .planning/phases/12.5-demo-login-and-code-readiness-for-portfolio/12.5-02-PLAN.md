---
phase: 12.5-demo-login-and-code-readiness-for-portfolio
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - nemea-back/src/products/products.service.spec.ts
  - nemea-back/src/supplies/supplies.service.spec.ts
  - nemea-back/src/tiendanube-config/tiendanube-config.service.spec.ts
autonomous: true
requirements: [R4, R7]
must_haves:
  truths:
    - "products.service.spec.ts asserts SKU generation, BOM active-record swap, and duplicate-SKU conflict behavior"
    - "supplies.service.spec.ts asserts toggleStatus guards and create-with/without-initialPrice behavior"
    - "tiendanube-config.service.spec.ts asserts rate string→number parsing, gateway-not-found, and append-only tax config"
    - "npm test in nemea-back stays 100% green including the three new specs"
    - "D-06: the three backend specs are unit tests with mocked repositories"
    - "D-07: no integration-against-real-Postgres in these unit specs"
  artifacts:
    - path: "nemea-back/src/products/products.service.spec.ts"
      provides: "Behavior spec for ProductsService"
      contains: "describe('create'"
    - path: "nemea-back/src/supplies/supplies.service.spec.ts"
      provides: "Behavior spec for SuppliesService"
      contains: "describe('toggleStatus'"
    - path: "nemea-back/src/tiendanube-config/tiendanube-config.service.spec.ts"
      provides: "Behavior spec for TiendanubeConfigService"
      contains: "describe('updateGatewayRate'"
  key_links:
    - from: "products.service.spec.ts"
      to: "ProductsService"
      via: "Test.createTestingModule with mocked repos + EntityManager"
      pattern: "getRepositoryToken"
    - from: "tiendanube-config.service.spec.ts"
      to: "TiendanubeConfigService"
      via: "5 mocked repos, no EntityManager"
      pattern: "getRepositoryToken"
---

<objective>
Add behavior-focused unit specs for the three substantial untested backend services (`products` 504 lines, `supplies` 274 lines, `tiendanube-config` 257 lines). Covers R4 and contributes to R7 (suite green). Unit tests with mocked repositories per D-06 — no real Postgres (D-07).

Purpose: Bring the codebase to a professional portfolio bar by testing the real domain logic (SKU/BOM/price history, supply status guards, rate parsing) that currently has zero coverage.
Output: three `.spec.ts` files that assert inputs→outputs/state (not call-spying), all green under `npm test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-SPEC.md
@.planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-RESEARCH.md
@.planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-PATTERNS.md

<interfaces>
<!-- From RESEARCH.md Finding 5 + PATTERNS.md. Repos/managers each service injects. -->

ProductsService repos to mock: productRepo, bomRepo (SuppliesPerProductHistory), priceHistoryRepo (ProductPriceHistory), productTypeRepo, productNameRepo, productFinishRepo, productColorRepo, productSizeRepo, supplyRepo, EntityManager.
  EntityManager: mock `transaction: jest.fn().mockImplementation(cb => cb(mockManager))`; mockManager = { findOne, find, create, save, update }.
  Conflict path: 23505 → ConflictException. Build err via Object.assign(new QueryFailedError('', [], new Error()), { code: '23505' }).

SuppliesService repos to mock: supplyRepo, priceHistoryRepo, supplyTypeRepo, supplierRepo, bomRepo, EntityManager (for create-with-initialPrice transaction).

TiendanubeConfigService repos to mock: gatewayRepo, gatewayRateRepo, installmentRateRepo, taxConfigRepo, planRepo. NO EntityManager. getInstallmentRates/getLatestGatewayRates use repo.query() — add `query: jest.fn()` to those mocks.

Invariant: jest.clearAllMocks() wipes mockImplementation/mockReturnThis — re-attach EntityManager.transaction and any chained mocks AFTER clearAllMocks in beforeEach.
UUID fixtures MUST be valid v4 (zod .uuid() rejects literals like 'product-uuid').
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: products.service.spec.ts — SKU, BOM swap, conflict</name>
  <files>nemea-back/src/products/products.service.spec.ts</files>
  <read_first>
    - nemea-back/src/products/products.service.ts (system under test — generateSkuCode ~line 54-62, create, createBatch, update, updateBom, addPrice, findAll)
    - nemea-back/src/users/users.service.spec.ts (full file — module-setup analog)
    - nemea-back/src/scenarios/scenarios.service.spec.ts (multi-dep injection analog, lines 1–80)
    - .planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-PATTERNS.md (section: products.service.spec.ts — imports, mock repos block, module setup, priority cases)
    - .planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-RESEARCH.md (Finding 5 — Products testable-logic table)
  </read_first>
  <behavior>
    - create(): with catalog fixtures (type/name/finish/color/size each carrying skuCode) the saved product's skuCode equals the concatenation `type.skuCode + '.' + name.skuCode + '.' + finish.skuCode + '.' + color.skuCode + '.' + size.skuCode`.
    - create(): when save rejects with a 23505 QueryFailedError → ConflictException.
    - updateBom(): deactivates prior BOM entries and inserts the new active set (assert the saved/updated records reflect the active swap).
    - updateBom() or create(): NotFoundException/validation error when a referenced supply is inactive/missing.
    - addPrice(): appends a new price-history record (append-only; prior records untouched).
  </behavior>
  <action>
    Create products.service.spec.ts mirroring the users.service.spec.ts module setup. Register ProductsService plus all nine `getRepositoryToken(...)` repo mocks and `{ provide: EntityManager, useValue: mockEntityManager }` exactly as enumerated in the interfaces block. In `beforeEach`: build the module, `jest.clearAllMocks()`, then re-attach `mockEntityManager.transaction.mockImplementation(cb => cb(mockManager))`. Write `describe('create')` (SKU concatenation assertion + 23505→ConflictException), `describe('updateBom')` (active-record swap + inactive/missing supply rejection), and `describe('addPrice')` (append-only). Assert resulting saved entities / thrown exception types — not merely that a method was called. Use valid-v4 UUID fixtures. Do NOT pad trivial pass-through CRUD (SPEC: coverage is informational).
  </action>
  <verify>
    <automated>cd nemea-back && npm test -- --testPathPattern products.service.spec</automated>
  </verify>
  <acceptance_criteria>
    - products.service.spec.ts exists with `describe('create')`, `describe('updateBom')`, `describe('addPrice')` blocks.
    - The SKU test asserts the concatenated skuCode value (not a spy on save).
    - The conflict test asserts `ConflictException` is thrown on a 23505 error.
    - `npm test -- --testPathPattern products.service.spec` exits 0.
  </acceptance_criteria>
  <done>Spec exists, asserts behavior, green; SKU/BOM/conflict/append-only logic covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: supplies + tiendanube-config service specs</name>
  <files>nemea-back/src/supplies/supplies.service.spec.ts, nemea-back/src/tiendanube-config/tiendanube-config.service.spec.ts</files>
  <read_first>
    - nemea-back/src/supplies/supplies.service.ts (SUT — findOne, create, toggleStatus, addPrice)
    - nemea-back/src/tiendanube-config/tiendanube-config.service.ts (SUT — parseGatewayRate, updateGatewayRate, updateInstallmentRate, getTaxConfig, updateTaxConfig, updatePlan, getInstallmentRates)
    - nemea-back/src/roles/roles.service.spec.ts (simple-deps analog for supplies)
    - nemea-back/src/scenarios/scenarios.service.spec.ts (multi-repo, no-EntityManager analog for tiendanube-config)
    - .planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-PATTERNS.md (sections: supplies.service.spec.ts, tiendanube-config.service.spec.ts — priority cases)
    - .planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-RESEARCH.md (Finding 5 — Supplies + TiendanubeConfig testable-logic tables)
  </read_first>
  <behavior>
    - SuppliesService.toggleStatus(): throws when deactivating a supply that is in an active BOM; throws when reactivating a supply whose supplier is inactive; succeeds deactivating a supply not in any active BOM.
    - SuppliesService.create(): creates supply + price atomically when initialPrice provided (transaction path); creates supply only when initialPrice absent.
    - TiendanubeConfigService.updateGatewayRate(): NotFoundException when gateway absent; parses ratePercent string→number and appends a new rate record.
    - TiendanubeConfigService.getTaxConfig(): returns null when no active config exists.
    - TiendanubeConfigService.updateTaxConfig(): appends a new record (append-only) parsing ivaRate + iibbRate; prior record not modified.
  </behavior>
  <action>
    Create supplies.service.spec.ts mirroring roles.service.spec.ts setup: register SuppliesService + getRepositoryToken mocks for supplyRepo, priceHistoryRepo, supplyTypeRepo, supplierRepo, bomRepo, and EntityManager (transaction mock for create-with-initialPrice). Write `describe('toggleStatus')` (three cases above) and `describe('create')` (with/without initialPrice). Re-attach the transaction mock after clearAllMocks.
    Create tiendanube-config.service.spec.ts mirroring scenarios.service.spec.ts setup: register TiendanubeConfigService + five repo mocks (gatewayRepo, gatewayRateRepo, installmentRateRepo with `query: jest.fn()`, taxConfigRepo, planRepo) — NO EntityManager. Write `describe('updateGatewayRate')` (not-found + string→number parse+append), `describe('getTaxConfig')` (null when none active), `describe('updateTaxConfig')` (append-only parse). Assert outputs/state and exception types, not call-spying. Valid-v4 UUID fixtures.
  </action>
  <verify>
    <automated>cd nemea-back && npm test -- --testPathPattern "supplies.service.spec|tiendanube-config.service.spec"</automated>
  </verify>
  <acceptance_criteria>
    - supplies.service.spec.ts has `describe('toggleStatus')` asserting the in-active-BOM and inactive-supplier guards throw, and a happy deactivate case.
    - tiendanube-config.service.spec.ts has `describe('updateGatewayRate')` asserting NotFoundException and a parsed numeric rate appended; `describe('getTaxConfig')` returns null; `describe('updateTaxConfig')` append-only.
    - tiendanube-config spec registers NO EntityManager provider.
    - `npm test -- --testPathPattern "supplies.service.spec|tiendanube-config.service.spec"` exits 0.
  </acceptance_criteria>
  <done>Both specs exist, assert behavior, green.</done>
</task>

<task type="auto">
  <name>Task 3: backend suite + coverage green gate (R7)</name>
  <files>nemea-back/src/products/products.service.spec.ts, nemea-back/src/supplies/supplies.service.spec.ts, nemea-back/src/tiendanube-config/tiendanube-config.service.spec.ts</files>
  <read_first>
    - .planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-VALIDATION.md (Sampling Rate + R7 row)
  </read_first>
  <action>
    Run the full backend suite and coverage to confirm the three new specs integrate green with the existing 121 tests and that a coverage report is produced without errors. Coverage is informational — do NOT add or modify production code to inflate a percentage, and do NOT introduce a coverage threshold gate (SPEC: coverage reported, not gated). Fix only genuine spec defects surfaced by the full run.
  </action>
  <verify>
    <automated>cd nemea-back && npm test && npm test -- --coverage</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 in nemea-back (existing + 3 new specs all green).
    - `npm test -- --coverage` produces a report and exits 0 without errors.
    - No coverage threshold gate added to jest config.
  </acceptance_criteria>
  <done>Full backend suite green; coverage report generated cleanly; R7 (backend half) satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds test files only — no runtime trust boundary changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.5-TST | Tampering | test fixtures leaking into prod paths | accept | Spec files are test-only; no production source modified; jest scopes them out of the build |
| T-12.5-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies (SPEC constraint); jest/@nestjs/testing already installed; no install tasks |
</threat_model>

<verification>
- `npm test` exits 0 in nemea-back; `npm test -- --coverage` produces a report.
- Each new spec asserts inputs→outputs/state (reviewer confirms no pure call-spying).
- No production `.ts` files modified by this plan.
</verification>

<success_criteria>
- R4 satisfied: three behavior-focused service specs exist and pass.
- R7 (backend) satisfied: full backend suite green, coverage report clean.
</success_criteria>

<output>
Create `.planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-02-SUMMARY.md` when done.
</output>
</output>

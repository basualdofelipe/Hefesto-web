---
status: partial
phase: 12.5-demo-login-and-code-readiness-for-portfolio
source: [12.5-VERIFICATION.md]
started: "2026-05-26T23:30:16Z"
updated: "2026-05-26T23:30:16Z"
---

## Current Test

[awaiting human testing]

## Tests

### 1. Demo login end-to-end round-trip (R2)
expected: With `DEMO_LOGIN_ENABLED=true` (nemea-back/.env) and `NEXT_PUBLIC_DEMO_LOGIN_ENABLED=true` (nemea-front/.env.local), both servers running and the demo user seeded: opening `/login` shows the "Entrar como demo" button below the Google button; clicking it authenticates and lands on `/` with admin permission cards populated. Google login still works (sign out, sign in with a whitelisted account). Setting `NEXT_PUBLIC_DEMO_LOGIN_ENABLED=false` hides the button. With `DEMO_LOGIN_ENABLED=false` on the backend, `POST /api/auth/demo-login` returns 4xx with no token. Confirm the demo path rejects any non-demo email even when enabled (CR-01 fix): `curl -X POST $API/api/auth/demo-login -d '{"email":"basualdofelipe@gmail.com"}'` returns 401.
result: [pending]

### 2. Seed migration applied to live DB (R3)
expected: From `nemea-back/`, `npm run migration:run` applies `1773200000000-SeedDemoUser` and inserts `demo@nemea.app` with `is_active = true` and the ADMIN role. Verify: `psql "$DATABASE_URL" -c "SELECT u.email, u.is_active, r.name FROM users u JOIN roles r ON r.id = u.role_id WHERE u.email = 'demo@nemea.app';"` returns exactly one row (`t`, `ADMIN`). Re-running `npm run migration:run` is idempotent — no error, still exactly one demo row.
result: [pending]

## Summary

total: 2
passed: 0
issues: 0
pending: 2
skipped: 0
blocked: 0

## Gaps

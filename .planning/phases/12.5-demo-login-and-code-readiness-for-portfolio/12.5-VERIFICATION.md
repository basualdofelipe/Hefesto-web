---
phase: 12.5-demo-login-and-code-readiness-for-portfolio
verified: 2026-05-26T21:00:00Z
status: human_needed
score: 10/10 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 9/10
  gaps_closed:
    - "R10: ScenarioEditorClient.tsx:56 bare 'tarjeta_debito_credito' literal replaced with TN_PAYMENT_TARJETA (commit 0eeebc3)"
    - "R10: GatewayPlanSelector.tsx:83 bare 'tarjeta_debito_credito' default replaced with TN_PAYMENT_TARJETA (commit 8bc624a)"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Run demo login end-to-end smoke: start backend with DEMO_LOGIN_ENABLED=true, start frontend with NEXT_PUBLIC_DEMO_LOGIN_ENABLED=true, navigate to /login, click 'Entrar como demo', confirm authenticated session lands on / with admin permissions populated"
    expected: "Session is established with populated permissions from the demo@nemea.app account; protected routes are accessible; the Google login path still works normally"
    why_human: "Requires both servers running, live Postgres with migration applied — impossible to verify at static analysis time"
  - test: "Run seed migration against a real Postgres: cd nemea-back && npm run migration:run, then verify: SELECT u.email, u.is_active, r.name FROM users u JOIN roles r ON r.id = u.role_id WHERE u.email = 'demo@nemea.app'"
    expected: "One row returned: email=demo@nemea.app, is_active=t, name=ADMIN. Re-running migration produces no error (idempotency)."
    why_human: "Requires a reachable Postgres instance — no live database was available at execution time (explicitly deferred per user decision)"
---

# Phase 12.5: Demo Login and Code Readiness for Portfolio — Verification Report

**Phase Goal:** The repo is clone-and-run for a reviewer without a whitelisted Google account (env-gated demo login alongside Google OAuth) and the codebase reaches a professional portfolio bar: 100%-green test suite in both repos with behavior coverage of the untested business logic, App Router error/loading boundaries, no internal process comments in source, and Tiendanube slug literals centralized in application code.

**Verified:** 2026-05-26T21:00:00Z
**Status:** human_needed — all 10 code deliverables VERIFIED; 2 live-runtime items deferred by explicit user decision (R2 end-to-end round-trip, R3 seed migration on live DB)
**Re-verification:** Yes — third pass; closes the last R10 gap (GatewayPlanSelector.tsx:83, commit 8bc624a)

---

## Re-verification Summary

Previous verification (2026-05-26T23:55:00Z) found one remaining R10 code gap: `GatewayPlanSelector.tsx:83` retained `?? 'tarjeta_debito_credito'` as a bare literal default, because this phase-12.2 file was not in the original R10 research inventory and was not addressed by the first fix commit (`0eeebc3`).

Commit `8bc624a` (2026-05-26) closes that gap: it adds `import { TN_PAYMENT_TARJETA } from '@/constants/tiendanube'` at line 16 and replaces the literal at line 84 with `?? TN_PAYMENT_TARJETA`. The diff was 2 insertions + 1 deletion on `GatewayPlanSelector.tsx` only — no behavior change (constant holds the identical value `as const`).

This re-verification confirms:
- The fix is in place and correct
- A comprehensive grep for bare slug literals in comparison/default positions across all application code returns zero matches
- Both test suites remain 100% green (24/24 frontend, 153/153 backend) — confirming the fix introduced no regressions
- No new gaps were discovered

**Score change:** 9/10 → 10/10. All code deliverables are verified. Status remains `human_needed` due to the two live-runtime deferred items (R2, R3).

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | POST /api/auth/demo-login returns { accessToken, user } with populated permissions when DEMO_LOGIN_ENABLED=true for the seeded demo email | VERIFIED | `auth.service.ts:75-108` — endpoint exists, pinned to `DEMO_USER_EMAIL='demo@nemea.app'`, returns AuthResponseDto |
| 2 | POST /api/auth/demo-login returns 4xx and no token when DEMO_LOGIN_ENABLED is false or unset | VERIFIED | `auth.service.ts:76-78` — `configService.get<string>('DEMO_LOGIN_ENABLED') !== 'true'` throws UnauthorizedException before any DB lookup |
| 3 | Demo path is pinned to demo@nemea.app only — no privilege escalation via arbitrary email | VERIFIED | `auth.service.ts:80-83` — `if (email !== DEMO_USER_EMAIL)` rejects before DB lookup; `DEMO_USER_EMAIL` exported const at line 15 |
| 4 | Frontend /login shows "Entrar como demo" button; one-click authenticates demo user | VERIFIED (code) / HUMAN (runtime) | `DemoLoginButton.tsx` exists; `login/page.tsx:79` gates on `NEXT_PUBLIC_DEMO_LOGIN_ENABLED === 'true'`; `auth.ts` Credentials provider wired. End-to-end requires live environment |
| 5 | Fresh migration:run + seed creates active demo user with ADMIN role | VERIFIED (code) / HUMAN (runtime) | `1773200000000-SeedDemoUser.ts` exists; queries ADMIN role by name; idempotent via `ON CONFLICT ("email") DO NOTHING`. Live-DB run deferred per user decision |
| 6 | products.service.spec.ts, supplies.service.spec.ts, tiendanube-config.service.spec.ts exist and assert behavior | VERIFIED | All three files exist. Products: 11 tests (SKU generation, BOM active-record swap, price history append-only). Supplies: 10 tests (toggleStatus guards, create with/without initialPrice). TiendanubeConfig: 10 tests (rate parsing, gateway-not-found, append-only tax config) |
| 7 | CalculadoraClient and ScenarioEditorClient test suites exist, assert visible behavior, and pass | VERIFIED | Both test files exist. CalculadoraClient: 3 tests (forward, inverse, debounce). ScenarioEditorClient: 4 tests (override entry, ownership gate) |
| 8 | Home page test rewritten — render(await Home()) + mocked @/auth, asserts permission cards + empty-state | VERIFIED | `page.test.tsx` uses `jest.mock('@/auth')` and `render(await Home())`. 3 tests: admin permissions renders cards, no-permissions empty-state, partial permissions. No 'NEMEA'/subtitle assertions remain |
| 9 | npm test exits 0 in both repos | VERIFIED | Backend: 153/153, 15 suites — confirmed live in this re-verification. Frontend: 24/24, 5 suites — confirmed live in this re-verification |
| 10 | error.tsx and loading.tsx exist under (app)/ | VERIFIED | `error.tsx` — `'use client'`, exports `AppError({ error, reset })`, renders `error.digest`, "Reintentar" button. `loading.tsx` — plain Server Component with `Loader2 animate-spin` |
| 11 | grep WR-[0-9]\|IN-A[0-9]\|CRITICAL: review fix over nemea-front/src and nemea-back/src returns zero matches | VERIFIED | Exact acceptance criterion grep executed: `grep -rE 'WR-[0-9]\|IN-A[0-9]\|CRITICAL: review fix' nemea-front/src nemea-back/src` — zero matches |
| 12 | Tiendanube slug comparisons/defaults import from a single constants module per repo — no bare slug in comparison/default positions in app code | VERIFIED | GatewayPlanSelector.tsx:83 gap CLOSED (commit 8bc624a: imports TN_PAYMENT_TARJETA at line 16, uses at line 84). Comprehensive grep for bare slug literals in comparison/default positions across all application code returns zero. `PAYMENT_METHOD_LABELS` key in `types.ts:53` and `@ApiProperty` examples in DTOs are exempt (display label dict key / Swagger doc — not branching logic). Literals in migrations are immutably exempt. Literals in test fixtures are exempt (mock data). Comments containing slug names are exempt. |
| 13 | PermissionsGuard and @RequirePermission are unchanged | VERIFIED | No guard file touched during phase 12.5 |
| 14 | CR-01 security fix: demo-login pinned to demo@nemea.app, any other email rejected before DB lookup | VERIFIED | `DEMO_USER_EMAIL` exported constant at `auth.service.ts:15`. `validateDemoLogin` checks `email !== DEMO_USER_EMAIL` before DB lookup |
| 15 | npx tsc --noEmit passes in both repos | VERIFIED | Both exit code 0 confirmed by prior verification; suites remain green (no new TS source changed in commit 8bc624a beyond the one-line import + constant reference) |

**Score:** 15/15 truth checks verified. Truths 4 and 5 are CODE COMPLETE / HUMAN NEEDED for the live runtime component only.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `nemea-back/src/auth/dto/demo-login.dto.ts` | DemoLoginDto with @IsEmail() email field | VERIFIED | Single `email!: string` with `@IsEmail()` and `@ApiProperty` |
| `nemea-back/src/auth/auth.service.ts` | validateDemoLogin() method, env-gated, pinned to DEMO_USER_EMAIL | VERIFIED | Method at line 75; DEMO_USER_EMAIL const at line 15 |
| `nemea-back/src/auth/auth.controller.ts` | POST demo-login, @Public(), delegates to validateDemoLogin | VERIFIED | `@Post('demo-login') @Public() @HttpCode(200)` at line 48 |
| `nemea-back/src/auth/auth.service.spec.ts` | 6 validateDemoLogin cases including CR-01 pin cases | VERIFIED | 6 cases in describe('validateDemoLogin') |
| `nemea-back/src/config/env.validation.ts` | DEMO_LOGIN_ENABLED Joi field, default 'false', NOT required | VERIFIED | `Joi.string().valid('true', 'false').default('false')` at line 12 |
| `nemea-back/src/database/migrations/1773200000000-SeedDemoUser.ts` | Idempotent seed migration, ADMIN role by name | VERIFIED | File exists; queries ADMIN role by name; `ON CONFLICT ("email") DO NOTHING` |
| `nemea-back/src/products/products.service.spec.ts` | Behavior spec: SKU generation, BOM swap, price history | VERIFIED | 11 tests covering all 3 behaviors |
| `nemea-back/src/supplies/supplies.service.spec.ts` | Behavior spec: toggleStatus guards, create variants | VERIFIED | 10 tests |
| `nemea-back/src/tiendanube-config/tiendanube-config.service.spec.ts` | Behavior spec: rate parsing, gateway-not-found, append-only | VERIFIED | 10 tests |
| `nemea-front/src/auth.ts` | Credentials provider alongside Google; jwt/signIn callbacks extended | VERIFIED | Credentials provider at line 23; credentials branches in signIn (line 63) and jwt (line 89) callbacks |
| `nemea-front/src/app/(auth)/login/DemoLoginButton.tsx` | Server Action, hardcoded DEMO_EMAIL, "Entrar como demo" label | VERIFIED | `'use server'` inner function; `DEMO_EMAIL = 'demo@nemea.app'`; signIn('credentials', { email: DEMO_EMAIL }) |
| `nemea-front/src/app/(auth)/login/page.tsx` | Env-gated demo button render | VERIFIED | `process.env.NEXT_PUBLIC_DEMO_LOGIN_ENABLED === 'true'` guard at line 79 |
| `nemea-front/src/app/(app)/calculadora/__tests__/CalculadoraClient.test.tsx` | RTL tests: forward, inverse, debounce | VERIFIED | 3 tests asserting visible behavior and fetch endpoint calls |
| `nemea-front/src/app/(app)/escenarios/[id]/__tests__/ScenarioEditorClient.test.tsx` | RTL tests: override entry, ownership | VERIFIED | 4 tests |
| `nemea-front/src/app/(app)/__tests__/page.test.tsx` | Async RSC pattern, mocked @/auth, 3 permission cases | VERIFIED | `jest.mock('@/auth')` + `render(await Home())` + 3 test cases |
| `nemea-front/src/app/(app)/error.tsx` | 'use client', AppError, reset button, error.digest | VERIFIED | All properties present |
| `nemea-front/src/app/(app)/loading.tsx` | Plain Server Component, Loader2 spinner | VERIFIED | No 'use client'; Loader2 with animate-spin |
| `nemea-back/src/constants/tiendanube.ts` | 3 slug constants | VERIFIED | TN_GATEWAY_PAGO_NUBE, TN_PLAN_ESENCIAL, TN_PAYMENT_TARJETA |
| `nemea-front/src/constants/tiendanube.ts` | 3 slug constants | VERIFIED | TN_PLAN_ESENCIAL, TN_GATEWAY_PAGO_NUBE, TN_PAYMENT_TARJETA |
| `nemea-front/src/components/scenarios/GatewayPlanSelector.tsx` | TN_PAYMENT_TARJETA imported and used as default | VERIFIED | Line 16: import; line 84: `const newMethod = methods[0] ?? TN_PAYMENT_TARJETA` (commit 8bc624a) |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `auth.controller.ts demoLogin` | `authService.validateDemoLogin` | controller delegates | WIRED | Line 62: `return this.authService.validateDemoLogin(dto.email)` |
| `auth.service.ts validateDemoLogin` | `configService.get('DEMO_LOGIN_ENABLED')` | env-gate read | WIRED | Line 76: `this.configService.get<string>('DEMO_LOGIN_ENABLED')` |
| `auth.service.ts validateDemoLogin` | `DEMO_USER_EMAIL` guard | email pin | WIRED | Line 81: `if (email !== DEMO_USER_EMAIL)` — CR-01 fix confirmed |
| `DemoLoginButton.tsx` | `POST /api/auth/demo-login` | Credentials provider authorize() | WIRED | `auth.ts:33-39` fetches the backend endpoint |
| `login/page.tsx` | `DemoLoginButton` | env-gate conditional | WIRED | Line 79: `NEXT_PUBLIC_DEMO_LOGIN_ENABLED === 'true'` |
| `calculadora.service.ts` | `TN_GATEWAY_PAGO_NUBE` | constants import | WIRED | No bare literal in comparison (line 117 uses the constant) |
| `scenarios.service.ts` | `TN_GATEWAY_PAGO_NUBE, TN_PAYMENT_TARJETA, TN_PLAN_ESENCIAL` | constants import | WIRED | Lines 27-29 import; lines 281-285 use |
| `GatewaySelectors.tsx` | `TN_PLAN_ESENCIAL` | constants import | WIRED | Line 16 import; line 31 `DEFAULT_PLAN_SLUG = TN_PLAN_ESENCIAL` |
| `TiendanubeConfigClient.tsx` | `TN_PLAN_ESENCIAL` | constants import | WIRED | Line 29 import; line 39 use |
| `ScenarioEditorClient.tsx` | `TN_GATEWAY_PAGO_NUBE, TN_PAYMENT_TARJETA, TN_PLAN_ESENCIAL` | constants import | WIRED | Lines 13-16 import; lines 59-65 use (commit 0eeebc3) |
| `GatewayPlanSelector.tsx` | `TN_PAYMENT_TARJETA` | constants import | WIRED | Line 16 import; line 84 use as default (commit 8bc624a) |

---

### Data-Flow Trace (Level 4)

Not applicable for this phase's deliverables. The core artifacts are auth endpoints, spec files, boundary files, and constant modules — none are components that render from a dynamic data source requiring a data-flow trace.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Backend test suite 100% green | `npm test` in nemea-back | 153/153, 15 suites | PASS |
| Frontend test suite 100% green | `npm test` in nemea-front | 24/24, 5 suites | PASS |
| Process comment grep (backend) | `grep -rE 'WR-[0-9]\|IN-A[0-9]\|CRITICAL: review fix' src/` in nemea-back | zero matches | PASS |
| Process comment grep (frontend) | `grep -rE 'WR-[0-9]\|IN-A[0-9]\|CRITICAL: review fix' src/` in nemea-front | zero matches | PASS |
| R10 GatewayPlanSelector fix confirmed | Line 84 reads `?? TN_PAYMENT_TARJETA`, import at line 16 | confirmed by file read | PASS |
| R10 bare slug literals in app logic | grep for comparison/default slug literals excluding tests/migrations/constants/DTOs | zero matches | PASS |
| R10 types.ts PAYMENT_METHOD_LABELS | `'tarjeta_debito_credito'` at `types.ts:53` | display label dictionary key — not a comparison/default | EXEMPT |
| R10 @ApiProperty examples in DTOs | slug strings in `@ApiProperty({ example: '...' })` in calc and scenario DTOs | Swagger documentation examples — not branching logic | EXEMPT |
| R10 comments mentioning slugs | `calculadora.service.ts:115` mentions `pago_nube` in a comment | comment, not code — exempt | EXEMPT |
| R10 migration SQL | slug literals in `1772800000000-CreateTiendanubeConfigTables.ts` | immutable historical migration — explicitly exempt per SPEC constraints | EXEMPT |
| R10 test fixtures | slug strings in spec files | mock data, not application logic | EXEMPT |

---

### Probe Execution

No probe scripts declared or discovered for this phase.

---

### Requirements Coverage

| Req ID | Description | Plans | Status | Evidence |
|--------|-------------|-------|--------|----------|
| R1 | Backend demo-login endpoint, env-gated | 12.5-01 | SATISFIED | POST /auth/demo-login exists; env-gated; pinned to DEMO_USER_EMAIL; 153/153 tests pass including 6 validateDemoLogin cases |
| R2 | Frontend Credentials provider + demo entry | 12.5-05 | CODE COMPLETE / HUMAN NEEDED | Credentials provider wired; DemoLoginButton exists; login page env-gated; end-to-end round-trip requires live environment (deferred per user decision) |
| R3 | Seed demo user migration | 12.5-06 | CODE COMPLETE / HUMAN NEEDED | Migration file exists with correct idempotent SQL; live-DB apply deferred per user decision |
| R4 | Backend service specs for untested business logic | 12.5-02 | SATISFIED | 3 spec files created; 31 new behavior tests pass; domain logic (SKU, BOM, rate parsing) covered |
| R5 | Frontend client-logic tests (CalculadoraClient, ScenarioEditorClient) | 12.5-03 | SATISFIED | Both test suites exist; 7 tests total; assert visible behavior (fetch calls, renders) |
| R6 | Home page test replaced | 12.5-03 | SATISFIED | `page.test.tsx` uses `render(await Home())` + `jest.mock('@/auth')`; 3 cases; no stale assertions |
| R7 | Full test suite green in both repos | 12.5-02/03 | SATISFIED | 153/153 backend, 24/24 frontend; both exit 0 (confirmed live in this re-verification) |
| R8 | Error and loading boundaries in (app)/ | 12.5-04 | SATISFIED | `error.tsx` and `loading.tsx` exist and are substantive |
| R9 | Remove process comments (prod + tests) | 12.5-04 | SATISFIED | Exact acceptance criterion grep (`grep -rE 'WR-[0-9]\|IN-A[0-9]\|CRITICAL: review fix'`) returns zero matches in both repos. Note: `scenarios.service.ts` retains explanatory `CRITICAL:` comments that describe runtime invariants — these do NOT match the `CRITICAL: review fix` pattern and are not tracker markers |
| R10 | Centralize Tiendanube slug constants (front + back, migration untouched) | 12.5-04 | SATISFIED | Constants modules exist in both repos. All application-logic comparison/default sites use named constants. Final gap (GatewayPlanSelector.tsx:83) closed in commit 8bc624a. Comprehensive grep for bare slug literals in app logic: zero matches |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `nemea-front/src/auth.ts` | 23 | Credentials provider always registered regardless of env flag | Info | Defense-in-depth gap: backend gate is authoritative; frontend exposes `/api/auth/callback/credentials` unconditionally. Non-blocking — backend env-gate is the security boundary |
| `nemea-front/src/auth.ts` | 89-98 | Unsafe `as` cast on `user` in credentials jwt branch — no runtime guard | Info | If authorize() shape drifts, silent `undefined` in token fields. Current authorize() always returns null or correct shape, so no immediate defect |
| `nemea-back/src/scenarios/scenarios.service.ts` | 225, 269, 292, 313 | `CRITICAL:` prefix on explanatory comments | Info | These comments describe runtime invariants and implementation rationale (transaction requirement, string-coercion note) — NOT tracker markers. They do NOT match `CRITICAL: review fix` and pass the R9 acceptance criterion grep. Retain as-is; they are genuinely explanatory |

No `TBD`, `FIXME`, `XXX`, `WR-`, or `IN-A` markers found in any phase-modified files. No debt markers per the acceptance criterion.

---

### Human Verification Required

#### 1. Demo Login End-to-End Round-Trip (R2)

**Test:** Start nemea-back with `DEMO_LOGIN_ENABLED=true` and nemea-front with `NEXT_PUBLIC_DEMO_LOGIN_ENABLED=true`. Navigate to `/login`. Confirm "Entrar como demo" button is visible. Click it. Confirm session established and landing on `/` with admin permissions populated. Verify Google login still works. Confirm button is hidden when both flags are off.

**Expected:** Authenticated session with `session.user.permissions` populated from the demo@nemea.app ADMIN role. All protected routes accessible. Google path unchanged.

**Why human:** Requires both running servers and a live Postgres instance with seed migration applied. Deferred per user decision.

#### 2. Seed Migration Applied to Live DB (R3)

**Test:** With `DATABASE_URL` pointing at a reachable Postgres: `cd nemea-back && npm run migration:run`. Then: `SELECT u.email, u.is_active, r.name FROM users u JOIN roles r ON r.id = u.role_id WHERE u.email = 'demo@nemea.app';` Re-run `migration:run` (idempotency check).

**Expected:** One row: `demo@nemea.app | t | ADMIN`. Second run produces no error. One row remains (not duplicated).

**Why human:** No reachable Postgres available at verification time. User explicitly deferred this.

---

### Gaps Summary

**No code gaps remain.** All 10 requirements are code-complete.

The last code gap (R10: `GatewayPlanSelector.tsx:83` bare `'tarjeta_debito_credito'` default) was closed in commit `8bc624a`. The fix adds `import { TN_PAYMENT_TARJETA } from '@/constants/tiendanube'` at line 16 and changes line 84 from `?? 'tarjeta_debito_credito'` to `?? TN_PAYMENT_TARJETA`. A comprehensive grep for bare slug literals in comparison/default positions across all application code now returns zero matches. Both test suites remain 100% green (no regressions).

The two open items (R2 end-to-end demo login, R3 live seed migration) are live-runtime verifications deliberately deferred by user decision — the code that implements them is complete and correct. These are tracked as human verification items, not code gaps.

---

_Verified: 2026-05-26T21:00:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification 3: All code gaps closed. Final gap (GatewayPlanSelector.tsx:83) confirmed fixed in commit 8bc624a. Score: 10/10._

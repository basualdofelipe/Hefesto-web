---
phase: 12.5-demo-login-and-code-readiness-for-portfolio
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - nemea-front/src/app/(app)/calculadora/__tests__/CalculadoraClient.test.tsx
  - nemea-front/src/app/(app)/escenarios/[id]/__tests__/ScenarioEditorClient.test.tsx
  - nemea-front/src/app/(app)/__tests__/page.test.tsx
autonomous: true
requirements: [R5, R6, R7]
must_haves:
  truths:
    - "CalculadoraClient test asserts forward and inverse modes render results and that debounced input triggers a single fetch"
    - "ScenarioEditorClient test asserts override entry updates displayed margin and non-owner cannot save"
    - "Home test (replaced) renders cards per mocked permissions and the empty-state, with @/auth mocked"
    - "npm test in nemea-front exits 0 (the previously-failing page.test no longer breaks parse)"
    - "D-08: front tests are RTL behavior-focused, use render(await Home()) for the async RSC, and jest.mock('@/auth')"
  artifacts:
    - path: "nemea-front/src/app/(app)/calculadora/__tests__/CalculadoraClient.test.tsx"
      provides: "RTL behavior test for CalculadoraClient"
      contains: "CalculadoraClient"
    - path: "nemea-front/src/app/(app)/escenarios/[id]/__tests__/ScenarioEditorClient.test.tsx"
      provides: "RTL behavior test for ScenarioEditorClient"
      contains: "ScenarioEditorClient"
    - path: "nemea-front/src/app/(app)/__tests__/page.test.tsx"
      provides: "Rewritten async-RSC home test"
      contains: "render(await Home())"
  key_links:
    - from: "page.test.tsx"
      to: "@/auth"
      via: "jest.mock('@/auth') short-circuits NextAuth v5 ESM"
      pattern: "jest.mock\\('@/auth'"
    - from: "CalculadoraClient.test.tsx"
      to: "global.fetch"
      via: "mocked apiClientFetch with content-length header"
      pattern: "global.fetch"
---

<objective>
Add RTL behavior tests for the flagship client components (`CalculadoraClient`, `ScenarioEditorClient`) and rewrite the broken home test using `render(await Home())` + `jest.mock('@/auth')`. Covers R5, R6 and contributes to R7 (frontend suite green). Follows the existing front RTL pattern per D-08.

Purpose: Cover the flagship client logic (forward/inverse calculation, cascading gateways, debounce, override-driven margin, ownership guard, permission-card rendering) and unblock the frontend suite, which currently fails to parse on the stale home test.
Output: two new RTL test files + one replaced home test, all green under `npm test` in nemea-front.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-SPEC.md
@.planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-RESEARCH.md
@.planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-PATTERNS.md

<interfaces>
<!-- From RESEARCH.md Findings 4 & 6 + PATTERNS.md. Mock blocks + fixtures are spelled out verbatim there. -->

Standard front RTL mock block (from EditUserDialog.test.tsx lines 6–28): mock 'next-auth/react' useSession, 'next/navigation' useRouter, 'sonner' toast.
global.fetch mock MUST include `headers: new Headers({ 'content-length': '100' })` — apiClientFetch reads content-length without optional chaining.

CalculadoraClient: props { products, config }; renders GatewaySelectors, ProductSelector, ModeToggle, DesglosePanel (mock all four to expose onConfigChange/onSelect/onModeChange callbacks); DEBOUNCE_MS=300 (CalculadoraClient.tsx:33) → use jest.useFakeTimers()/runAllTimers(); calls /api/calculadora/forward and /api/calculadora/inverse.

ScenarioEditorClient: props { scenario, products, config }; mock ProductOverrideTable, GatewayPlanSelector, MarginSummary, next/link; ownership = scenario.user.id === session.user.id; gatewayPlanConfig init reads scenario.gatewaySlug ?? 'pago_nube', plan.slug ?? 'esencial'.

Home (page.test.tsx REPLACE): Home is `async function Home(): Promise<ReactElement>` (page.tsx:15); jest.mock('@/auth', () => ({ auth: jest.fn() })) BEFORE importing ../page; also jest.mock('next/link'); then `render(await Home())`. Cards 'Productos'/'Calculadora'/'Insumos'/'Gastos' gated by permissions; empty-state text /No tenés acceso a ninguna sección/.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CalculadoraClient + ScenarioEditorClient RTL tests</name>
  <files>nemea-front/src/app/(app)/calculadora/__tests__/CalculadoraClient.test.tsx, nemea-front/src/app/(app)/escenarios/[id]/__tests__/ScenarioEditorClient.test.tsx</files>
  <read_first>
    - nemea-front/src/app/(app)/calculadora/CalculadoraClient.tsx (SUT — mode/state, debounce DEBOUNCE_MS, forward/inverse fetch)
    - nemea-front/src/app/(app)/escenarios/[id]/ScenarioEditorClient.tsx (SUT — overrides state, ownership check lines 54–62)
    - nemea-front/src/app/(app)/usuarios/__tests__/EditUserDialog.test.tsx (canonical RTL pattern — mock block + global.fetch)
    - nemea-front/src/app/(app)/usuarios/__tests__/DeleteUserAlertDialog.test.tsx (simpler analog for ScenarioEditorClient)
    - nemea-front/jest.setup.ts (Radix/ResizeObserver polyfills already present — reuse)
    - .planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-PATTERNS.md (sections: CalculadoraClient.test.tsx + ScenarioEditorClient.test.tsx — full mock blocks + fixtures)
  </read_first>
  <behavior>
    - CalculadoraClient forward mode: after selecting a product + gateway config and entering a price, calls /api/calculadora/forward and renders the returned result (via DesglosePanel receiving results).
    - CalculadoraClient inverse mode: toggling to inverse + entering ganancia calls /api/calculadora/inverse and renders the result.
    - CalculadoraClient debounce: rapid input changes followed by runAllTimers trigger exactly one fetch.
    - ScenarioEditorClient override entry: triggering an override change updates the displayed margin/results (MarginSummary reflects new state).
    - ScenarioEditorClient ownership: when session user id !== scenario.user.id, the Guardar button is absent/disabled.
  </behavior>
  <action>
    Create CalculadoraClient.test.tsx: copy the EditUserDialog mock block (useSession authenticated with canUseCalculator, useRouter, sonner); mock the four calculadora child components (GatewaySelectors→onConfigChange, ProductSelector→onSelect, ModeToggle→onModeChange, DesglosePanel→data-testid) so the test drives state via their callbacks; set up global.fetch with the content-length header returning a calc result fixture. Use jest.useFakeTimers() in beforeEach and jest.useRealTimers() in afterEach for the debounce case; assert a single fetch after runAllTimers. Cover forward, inverse, and debounce per the behavior block.
    Create ScenarioEditorClient.test.tsx at nemea-front/src/app/(app)/escenarios/[id]/__tests__/: copy the same mock block; mock ProductOverrideTable→onOverrideChange, GatewayPlanSelector, MarginSummary→renders results.length, and next/link; build the OWNER_ID fixture scenario. Cover override-entry-updates-margin and non-owner-cannot-save. Assert user-visible behavior (rendered results / button presence), not implementation details. Valid-v4 UUID fixtures.
  </action>
  <verify>
    <automated>cd nemea-front && npm test -- --testPathPattern "CalculadoraClient|ScenarioEditorClient"</automated>
  </verify>
  <acceptance_criteria>
    - CalculadoraClient.test.tsx asserts a result renders in forward mode and in inverse mode, and that debounced input yields exactly one fetch (`toHaveBeenCalledTimes(1)`).
    - ScenarioEditorClient.test.tsx asserts override entry changes the displayed margin/results and that the Guardar button is not shown for a non-owner.
    - Both suites assert visible behavior (rendered text/results/button), not spies on internal handlers.
    - `npm test -- --testPathPattern "CalculadoraClient|ScenarioEditorClient"` exits 0.
  </acceptance_criteria>
  <done>Both RTL suites exist, assert behavior, green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace broken home test (async RSC + mocked @/auth)</name>
  <files>nemea-front/src/app/(app)/__tests__/page.test.tsx</files>
  <read_first>
    - nemea-front/src/app/(app)/__tests__/page.test.tsx (current broken file — to be REPLACED, not deleted)
    - nemea-front/src/app/(app)/page.tsx (SUT — Home async RSC lines 15–76: card list + empty-state)
    - .planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-PATTERNS.md (section: page.test.tsx replace — full replacement pattern)
    - .planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-RESEARCH.md (Finding 4 — why current test fails, ESM fix)
  </read_first>
  <behavior>
    - With all four view permissions true → renders 'Productos', 'Calculadora', 'Insumos', 'Gastos'.
    - With all permissions false → renders the empty-state /No tenés acceso a ninguna sección/ and no cards.
    - With partial permissions (only canViewProducts) → renders 'Productos' and not 'Calculadora'.
  </behavior>
  <action>
    Overwrite page.test.tsx. Declare `jest.mock('@/auth', () => ({ auth: jest.fn() }))` and `jest.mock('next/link', ...)` BEFORE importing `../page`. Import { render, screen }, { auth }, and Home; type `const mockAuth = auth as jest.MockedFunction<typeof auth>`. In each case set `mockAuth.mockResolvedValue({ user: { permissions: {...} } } as Awaited<ReturnType<typeof auth>>)` then `render(await Home())` and assert per the behavior block. Remove all `getByText('NEMEA')`/subtitle assertions. Keep it a REPLACEMENT (file must continue to exist).
  </action>
  <verify>
    <automated>cd nemea-front && npm test -- --testPathPattern page.test</automated>
  </verify>
  <acceptance_criteria>
    - page.test.tsx contains `jest.mock('@/auth'` and `render(await Home())`; contains NO `'NEMEA'` or old-subtitle assertions.
    - Three cases present: all-permissions (cards), no-permissions (empty-state), partial (only permitted cards).
    - `npm test -- --testPathPattern page.test` exits 0.
  </acceptance_criteria>
  <done>Home test rewritten, green, asserts cards + empty-state; no stale text assertions remain.</done>
</task>

<task type="auto">
  <name>Task 3: frontend suite + coverage green gate (R7)</name>
  <files>nemea-front/src/app/(app)/__tests__/page.test.tsx</files>
  <read_first>
    - .planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-VALIDATION.md (R7 rows + sampling rate)
  </read_first>
  <action>
    Run the full frontend suite and coverage to confirm the two new tests + rewritten home test integrate green and a coverage report is produced without errors. Do NOT weaken `transformIgnorePatterns` project-wide (ESM is handled via the `@/auth` module mock per constraint). Do NOT add a coverage threshold gate. Fix only genuine spec defects surfaced by the full run.
  </action>
  <verify>
    <automated>cd nemea-front && npm test && npm test -- --coverage</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 in nemea-front (previously failed on page.test parse).
    - `npm test -- --coverage` produces a report and exits 0.
    - jest.config.js `transformIgnorePatterns` unchanged; no coverage threshold added.
  </acceptance_criteria>
  <done>Full frontend suite green; coverage report clean; R7 (frontend half) satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test files only — no runtime trust boundary change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.5-ESM | Tampering | jest config weakened to pass ESM | mitigate | Fix the NextAuth v5 ESM parse via `jest.mock('@/auth')` only; do NOT broaden transformIgnorePatterns (constraint) |
| T-12.5-TST | Tampering | test mocks leaking to prod | accept | jsdom test files only; no production source modified |
| T-12.5-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies (SPEC constraint); RTL/jest already installed; no install tasks |
</threat_model>

<verification>
- `npm test` exits 0 in nemea-front; `npm test -- --coverage` produces a report.
- page.test.tsx no longer contains `'NEMEA'`/subtitle assertions; uses `render(await Home())`.
- No production source modified; jest config not weakened.
</verification>

<success_criteria>
- R5 satisfied: CalculadoraClient + ScenarioEditorClient behavior tests pass.
- R6 satisfied: home test rewritten (not deleted), asserts cards + empty-state.
- R7 (frontend) satisfied: full frontend suite green, coverage clean.
</success_criteria>

<output>
Create `.planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-03-SUMMARY.md` when done.
</output>
</output>

---
phase: 12.5-demo-login-and-code-readiness-for-portfolio
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - nemea-front/src/app/(app)/error.tsx
  - nemea-front/src/app/(app)/loading.tsx
  - nemea-back/src/constants/tiendanube.ts
  - nemea-back/src/calculadora/calculadora.service.ts
  - nemea-back/src/scenarios/scenarios.service.ts
  - nemea-back/src/scenarios/scenarios.service.spec.ts
  - nemea-back/src/users/users.service.ts
  - nemea-back/src/users/users.service.spec.ts
  - nemea-back/src/users/dto/update-user.dto.ts
  - nemea-front/src/constants/tiendanube.ts
  - nemea-front/src/components/calculadora/GatewaySelectors.tsx
  - nemea-front/src/app/(app)/configuracion/tiendanube/TiendanubeConfigClient.tsx
  - nemea-front/src/app/(app)/escenarios/[id]/ScenarioEditorClient.tsx
  - nemea-front/src/app/(app)/usuarios/UsersClient.tsx
  - nemea-front/src/app/(app)/usuarios/EditUserDialog.tsx
  - nemea-front/src/app/(app)/usuarios/__tests__/EditUserDialog.test.tsx
  - nemea-front/src/app/(app)/usuarios/__tests__/DeleteUserAlertDialog.test.tsx
  - nemea-front/src/lib/api-client.ts
autonomous: true
requirements: [R8, R9, R10]
must_haves:
  truths:
    - "A failed (app)/ SSR/fetch renders a branded Spanish error UI with a Reintentar button instead of crashing Next"
    - "An (app)/ route shows a centered spinner while loading"
    - "Slug branching/default sites import named constants from one module per repo; no bare slug literals at comparison/default positions in app code"
    - "grep for WR-/IN-A/CRITICAL: review fix over both src trees returns zero matches"
    - "lint, tsc, and full suites stay green in both repos after cleanup"
  artifacts:
    - path: "nemea-front/src/app/(app)/error.tsx"
      provides: "Client error boundary with reset()"
      contains: "'use client'"
    - path: "nemea-front/src/app/(app)/loading.tsx"
      provides: "Centered spinner loading boundary"
      contains: "animate-spin"
    - path: "nemea-back/src/constants/tiendanube.ts"
      provides: "Backend slug constants module"
      contains: "TN_GATEWAY_PAGO_NUBE"
    - path: "nemea-front/src/constants/tiendanube.ts"
      provides: "Frontend slug constants module"
      contains: "TN_PLAN_ESENCIAL"
  key_links:
    - from: "calculadora.service.ts / scenarios.service.ts"
      to: "nemea-back/src/constants/tiendanube.ts"
      via: "import named slug constants"
      pattern: "TN_GATEWAY_PAGO_NUBE|TN_PLAN_ESENCIAL|TN_PAYMENT_TARJETA"
    - from: "GatewaySelectors.tsx / ScenarioEditorClient.tsx / TiendanubeConfigClient.tsx"
      to: "nemea-front/src/constants/tiendanube.ts"
      via: "import named slug constants"
      pattern: "TN_PLAN_ESENCIAL|TN_GATEWAY_PAGO_NUBE"
    - from: "error.tsx reset button"
      to: "App Router reset()"
      via: "onClick={reset}"
      pattern: "onClick=\\{reset\\}"
---

<objective>
Add the App Router error/loading boundaries in `(app)/` (R8), centralize Tiendanube slug literals into one constants module per repo and replace inline comparison/default literals (R10), and remove all `WR-`/`IN-A`/`CRITICAL: review fix` process comments from production source AND test files in both repos (R9). Per D-09 (branded Spanish error + "Reintentar") and D-10 (centered spinner).

Purpose: Make the app degrade gracefully on SSR failure, give the codebase a clean professional surface (no internal tracker noise), and remove slug magic-strings from branching logic — all portfolio-bar polish.
Output: error.tsx, loading.tsx, two constants modules, slug-literal replacements at the enumerated sites, and a process-comment-free source tree (grep returns zero).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-SPEC.md
@.planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-RESEARCH.md
@.planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-PATTERNS.md

<interfaces>
<!-- From RESEARCH.md Findings 7,8,9 + PATTERNS.md. Exact file:line inventory below. -->

error.tsx: MUST have 'use client'; signature ({ error: Error & { digest?: string }, reset: () => void }); render branded Spanish message + Shadcn Button onClick={reset} labeled "Reintentar".
loading.tsx: plain Server Component (no 'use client'); centered Loader2 animate-spin (lucide-react).

Backend constants module nemea-back/src/constants/tiendanube.ts:
  TN_GATEWAY_PAGO_NUBE='pago_nube', TN_PLAN_ESENCIAL='esencial', TN_PAYMENT_TARJETA='tarjeta_debito_credito' (all `as const`).
  Replace sites: calculadora.service.ts:116 (=== 'pago_nube'); scenarios.service.ts:276 (?? 'pago_nube'), :277 (?? 'tarjeta_debito_credito'), :280 (?? 'esencial').
  EXCLUDED: DTO @ApiProperty({ example: ... }) Swagger metadata — NOT branching logic, do NOT touch.

Frontend constants module nemea-front/src/constants/tiendanube.ts (same three constants).
  Replace sites: GatewaySelectors.tsx:30 (DEFAULT_PLAN_SLUG='esencial'); TiendanubeConfigClient.tsx:38 (=== 'esencial'); ScenarioEditorClient.tsx:54 (?? 'pago_nube'), :60 (=== 'esencial').

Process-comment markers (remove WR-*, IN-A*, CRITICAL: review fix only; keep genuinely explanatory text):
  Backend: users.service.ts, users.service.spec.ts, scenarios.service.ts, scenarios.service.spec.ts, users/dto/update-user.dto.ts.
  Frontend: usuarios/UsersClient.tsx, usuarios/EditUserDialog.tsx, usuarios/__tests__/EditUserDialog.test.tsx, usuarios/__tests__/DeleteUserAlertDialog.test.tsx, lib/api-client.ts.
  Rule: if a line is solely a tracker note → delete the line; if marker prefixes genuine explanation → trim only the prefix.
  Migrations EXCLUDED from R10; for R9 the grep covers src/ (migrations under src are unaffected — markers are in services/components/tests).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: error.tsx + loading.tsx boundaries in (app)/ (R8)</name>
  <files>nemea-front/src/app/(app)/error.tsx, nemea-front/src/app/(app)/loading.tsx</files>
  <read_first>
    - nemea-front/src/components/ui/button.tsx (Shadcn Button — variant/size props)
    - .planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-PATTERNS.md (sections: error.tsx, loading.tsx — full file content)
    - .planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-RESEARCH.md (Finding 7 — 'use client' requirement, route-group scoping)
  </read_first>
  <action>
    Create error.tsx with a `'use client'` directive at the top, default export `AppError({ error, reset }: { error: Error & { digest?: string }; reset: () => void }): ReactElement`, rendering a centered branded Spanish message ("Algo salió mal" + optional digest code) and a Shadcn `<Button variant='outline' onClick={reset}>Reintentar</Button>` (D-09). Create loading.tsx as a plain Server Component default export `AppLoading(): ReactElement` rendering a centered `Loader2` with `animate-spin` (D-10). Both files live directly under `nemea-front/src/app/(app)/` so they scope to the (app) route group only (the (auth) group is unaffected). Explicit return types, single quotes.
  </action>
  <verify>
    <automated>cd nemea-front && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - error.tsx first line is `'use client';`, default export receives `{ error, reset }`, has a button with `onClick={reset}` labeled "Reintentar".
    - loading.tsx has NO `'use client'` and renders a `Loader2` with `animate-spin`.
    - `npx tsc --noEmit` and `npm run lint` pass in nemea-front.
  </acceptance_criteria>
  <done>Both boundary files exist under (app)/; tsc + lint green. (Manual SSR-failure render check happens at phase gate.)</done>
</task>

<task type="auto">
  <name>Task 2: Tiendanube slug constants modules + replace inline literals (R10)</name>
  <files>nemea-back/src/constants/tiendanube.ts, nemea-back/src/calculadora/calculadora.service.ts, nemea-back/src/scenarios/scenarios.service.ts, nemea-front/src/constants/tiendanube.ts, nemea-front/src/components/calculadora/GatewaySelectors.tsx, nemea-front/src/app/(app)/configuracion/tiendanube/TiendanubeConfigClient.tsx, nemea-front/src/app/(app)/escenarios/[id]/ScenarioEditorClient.tsx</files>
  <read_first>
    - nemea-back/src/calculadora/calculadora.service.ts (line 116 — `=== 'pago_nube'`; keep the explanatory CPT comment)
    - nemea-back/src/scenarios/scenarios.service.ts (lines 276–280 — default fallbacks)
    - nemea-front/src/components/calculadora/GatewaySelectors.tsx (line 30 — DEFAULT_PLAN_SLUG)
    - nemea-front/src/app/(app)/configuracion/tiendanube/TiendanubeConfigClient.tsx (line 38 — find by slug)
    - nemea-front/src/app/(app)/escenarios/[id]/ScenarioEditorClient.tsx (lines 54, 60 — defaults)
    - .planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-PATTERNS.md (sections: nemea-back/src/constants/tiendanube.ts + nemea-front/src/constants/tiendanube.ts — module contents + per-site replacements)
    - .planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-RESEARCH.md (Finding 8 — full slug inventory + DTO exclusion)
  </read_first>
  <action>
    Create nemea-back/src/constants/tiendanube.ts exporting `TN_GATEWAY_PAGO_NUBE = 'pago_nube' as const`, `TN_PLAN_ESENCIAL = 'esencial' as const`, `TN_PAYMENT_TARJETA = 'tarjeta_debito_credito' as const`. Replace: calculadora.service.ts:116 `=== 'pago_nube'` → `=== TN_GATEWAY_PAGO_NUBE`; scenarios.service.ts:276 `?? 'pago_nube'` → `?? TN_GATEWAY_PAGO_NUBE`, :277 `?? 'tarjeta_debito_credito'` → `?? TN_PAYMENT_TARJETA`, :280 `?? 'esencial'` → `?? TN_PLAN_ESENCIAL`. Add imports. Do NOT touch any DTO `@ApiProperty({ example: ... })` — Swagger metadata is excluded.
    Create nemea-front/src/constants/tiendanube.ts with the same three `as const` exports. Replace: GatewaySelectors.tsx:30 `const DEFAULT_PLAN_SLUG = 'esencial'` → `= TN_PLAN_ESENCIAL`; TiendanubeConfigClient.tsx:38 `=== 'esencial'` → `=== TN_PLAN_ESENCIAL`; ScenarioEditorClient.tsx:54 `?? 'pago_nube'` → `?? TN_GATEWAY_PAGO_NUBE`, :60 `=== 'esencial'` → `=== TN_PLAN_ESENCIAL`. Add imports. Migration files NOT touched (immutable).
  </action>
  <verify>
    <automated>cd nemea-back && npx tsc --noEmit && cd ../nemea-front && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Both constants modules exist and export the three named constants.
    - The four backend sites and four frontend sites import and use named constants (no bare slug literal at those comparison/default positions).
    - No DTO `@ApiProperty` example string changed.
    - `npx tsc --noEmit` passes in both repos.
  </acceptance_criteria>
  <done>Slug constants centralized per repo; inline branching/default literals replaced; tsc green both repos; calculadora round-trip behavior preserved (verified by suite in Task 4).</done>
</task>

<task type="auto">
  <name>Task 3: Remove process comments from source + tests, both repos (R9)</name>
  <files>nemea-back/src/users/users.service.ts, nemea-back/src/users/users.service.spec.ts, nemea-back/src/scenarios/scenarios.service.ts, nemea-back/src/scenarios/scenarios.service.spec.ts, nemea-back/src/users/dto/update-user.dto.ts, nemea-front/src/app/(app)/usuarios/UsersClient.tsx, nemea-front/src/app/(app)/usuarios/EditUserDialog.tsx, nemea-front/src/app/(app)/usuarios/__tests__/EditUserDialog.test.tsx, nemea-front/src/app/(app)/usuarios/__tests__/DeleteUserAlertDialog.test.tsx, nemea-front/src/lib/api-client.ts</files>
  <read_first>
    - .planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-PATTERNS.md (section: Process-Comment Cleanup Map — exact files + markers)
    - .planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-RESEARCH.md (Finding 9 + Pitfall 7 — preservation rule, inline-vs-standalone handling)
  </read_first>
  <action>
    Remove every `WR-<digit>`, `IN-A<digit>`, and `CRITICAL: review fix` marker from the listed files. For a line that is solely a tracker note (e.g. `// WR-A4: ...`) delete the whole line. For an inline trailing comment (`code; // WR-A4: ...`) remove from `//` to end of line, preserving the code. If a marker prefixes a genuinely explanatory comment, trim only the marker prefix and keep the explanation (Pitfall 7). Preserve all non-tracker explanatory comments. Note: PATTERNS.md also lists `CR-A1/CR-A2` in users.service.spec.ts — those are NOT in the R9 grep set; leave them unless they are part of a WR/IN-A line. After edits, the grep gate over both `src` trees must return zero matches for the R9 patterns.
  </action>
  <verify>
    <automated>cd "C:/Users/Feli/Documents/programación/Nemea-web" && (grep -rnE 'WR-[0-9]|IN-A[0-9]|CRITICAL: review fix' nemea-front/src nemea-back/src && echo "FAIL: markers remain" && exit 1) || echo "PASS: zero markers"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE 'WR-[0-9]|IN-A[0-9]|CRITICAL: review fix' nemea-front/src nemea-back/src` returns zero matches.
    - No production logic altered (only comments removed/trimmed); genuinely explanatory comments preserved.
  </acceptance_criteria>
  <done>Zero process-comment markers across both src trees including tests; explanatory comments intact.</done>
</task>

<task type="auto">
  <name>Task 4: Cross-repo green gate after cleanup (R7 support)</name>
  <files>nemea-back/src/constants/tiendanube.ts, nemea-front/src/constants/tiendanube.ts</files>
  <read_first>
    - .planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-VALIDATION.md (per-wave merge: npm test + tsc + lint both repos)
  </read_first>
  <action>
    After Tasks 1–3, run lint + tsc + full test suite in both repos to confirm the boundaries, slug-constant replacements, and comment removals did not break anything (in particular the calculadora round-trip and scenarios defaults still behave). Fix only genuine defects introduced by this plan.
  </action>
  <verify>
    <automated>cd nemea-back && npm run lint && npx tsc --noEmit && npm test && cd ../nemea-front && npm run lint && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - lint + `tsc --noEmit` + `npm test` all exit 0 in nemea-back AND nemea-front.
    - calculadora round-trip / scenarios default behavior unchanged (covered by existing + new specs staying green).
  </acceptance_criteria>
  <done>Both repos lint/tsc/test green after R8/R9/R10 changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (app)/ SSR → error boundary | A thrown server error crosses into the client error boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.5-ER | Information Disclosure | error.tsx leaking stack/internal details | mitigate | Render a generic Spanish message + optional `error.digest` only; never render raw `error.message`/stack to the user |
| T-12.5-CMT | Information Disclosure | removing comments alters logic / leaves secrets | mitigate | Comment-only edits; run tsc+lint+tests (Task 4); preserve explanatory comments, strip only tracker markers |
| T-12.5-SLUG | Tampering | slug refactor changes branching behavior | mitigate | Constants hold identical literal values (`as const`); round-trip + scenarios specs stay green (Task 4) |
| T-12.5-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies (SPEC constraint); no install tasks |
</threat_model>

<verification>
- `npm run lint` + `npx tsc --noEmit` + `npm test` green in both repos.
- grep for R9 markers returns zero across both src trees.
- error.tsx renders generic Spanish + Reintentar (no raw stack); loading.tsx centered spinner.
- Manual (phase gate): stop backend, load an (app)/ page → branded error boundary renders instead of a Next crash.
</verification>

<success_criteria>
- R8 satisfied: error.tsx + loading.tsx exist under (app)/ and degrade gracefully.
- R9 satisfied: zero process-comment markers in both src trees.
- R10 satisfied: slug branching/defaults import named constants per repo; no bare literals at those sites (migrations/DTO examples excluded).
</success_criteria>

<output>
Create `.planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-04-SUMMARY.md` when done.
</output>

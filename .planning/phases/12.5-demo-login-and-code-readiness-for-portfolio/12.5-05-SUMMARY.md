---
phase: 12.5-demo-login-and-code-readiness-for-portfolio
plan: "05"
subsystem: frontend-auth
tags: [auth, demo-login, credentials-provider, nextauth, env-gate]
dependency_graph:
  requires: [POST /api/auth/demo-login (Plan 01)]
  provides: [Credentials provider in auth.ts, DemoLoginButton Server Action, env-gated /login render]
  affects: [nemea-front/src/auth.ts, nemea-front/src/app/(auth)/login/]
tech_stack:
  added: []
  patterns: [NextAuth Credentials provider, Server Action for form submission, env-gate pattern]
key_files:
  created:
    - nemea-front/src/app/(auth)/login/DemoLoginButton.tsx
  modified:
    - nemea-front/src/auth.ts
    - nemea-front/src/app/(auth)/login/page.tsx
decisions:
  - "Credentials provider authorize() returns null on every failure path (never throws) — maps to standard sign-in error, no stack leak (T-12.5-AUTH)"
  - "DemoLoginButton uses Server Action with signIn from @/auth — (auth)/layout.tsx has no SessionProvider so next-auth/react signIn cannot be used"
  - "jwt callback credentials branch reads from user arg (what authorize() returned) — no provider field stored in token (T-12.5-03)"
  - "NEXT_PUBLIC_DEMO_LOGIN_ENABLED gates the button on the frontend; backend DEMO_LOGIN_ENABLED is the authoritative gate (D-04, T-12.5-01 defense in depth)"
requirements-completed: [R2]
metrics:
  duration: "~8 minutes"
  completed: "2026-05-26"
  tasks_completed: 2
  files_changed: 3
---

# Phase 12.5 Plan 05: Demo Login Frontend — Credentials Provider + Button Summary

**NextAuth Credentials provider added alongside Google, with a one-click "Entrar como demo" Server Action button on /login gated by NEXT_PUBLIC_DEMO_LOGIN_ENABLED — code-complete; manual round-trip smoke deferred per user decision.**

## Performance

- **Duration:** ~8 minutes
- **Started:** 2026-05-26T22:50:00Z
- **Completed:** 2026-05-26T23:02:00Z
- **Tasks:** 2 of 3 (Task 3 deferred — see below)
- **Files modified:** 3

## Accomplishments

- Credentials provider wired into `auth.ts` alongside Google (additive, Google path untouched)
- `authorize()` fetches Plan 01 backend endpoint, returns null on every failure path (never throws — T-12.5-AUTH)
- `DemoLoginButton.tsx` created as Server Component with `'use server'` Server Action; label exactly "Entrar como demo" (D-02)
- `/login` page conditionally renders the demo button only when `NEXT_PUBLIC_DEMO_LOGIN_ENABLED === 'true'` (D-04)
- Build passes 17/17 pages, lint reports 0 errors, `npm run build` green

## Task Commits

Each task was committed atomically:

1. **Task 1: Credentials provider + signIn/jwt callback branches in auth.ts** — `108b46a` (feat)
2. **Task 2: DemoLoginButton Server Action + env-gated render on /login** — `c949b3b` (feat)
3. **Task 3: Manual demo-login round-trip smoke** — DEFERRED (see below)

## Files Created/Modified

- `nemea-front/src/auth.ts` — Added `import Credentials from 'next-auth/providers/credentials'`; Credentials provider in providers array; `signIn` callback returns true for `provider === 'credentials'`; `jwt` callback credentials branch sets `token.backendToken/permissions/userId` from user arg
- `nemea-front/src/app/(auth)/login/DemoLoginButton.tsx` — New Server Component; inner `'use server'` Server Action calling `signIn('credentials', { email: 'demo@nemea.app', redirectTo: '/' })` from `@/auth`; no `next-auth/react` import
- `nemea-front/src/app/(auth)/login/page.tsx` — Imports `DemoLoginButton`; conditional render behind `process.env.NEXT_PUBLIC_DEMO_LOGIN_ENABLED === 'true'` with "o" divider; Google form unchanged

## What Was Built

- **Credentials provider** in `nemea-front/src/auth.ts`: added `import Credentials from 'next-auth/providers/credentials'`; registered alongside Google (additive, Google untouched). `authorize()` fetches `POST /api/auth/demo-login` using the Plan 01 backend contract, returns `{ backendToken, permissions, userId, email }` on success, `null` on any failure (never throws — T-12.5-AUTH).
- **signIn callback** updated: `provider === 'credentials'` returns true.
- **jwt callback** updated: added `user` param; credentials branch reads `user.backendToken/permissions/userId` into token. Google branch unchanged. No provider field added to token (T-12.5-03).
- **session callback**: unchanged — already reads from `token.backendToken`, `token.permissions`, `token.userId`.
- **DemoLoginButton.tsx**: Server Component with inner `'use server'` Server Action `handleDemoLogin` calling `signIn('credentials', { email: 'demo@nemea.app', redirectTo: '/' })` from `@/auth`. No `next-auth/react` import (no SessionProvider in (auth) layout). Label exactly "Entrar como demo" (D-02).
- **login/page.tsx**: imports `DemoLoginButton`; renders it conditionally behind `process.env.NEXT_PUBLIC_DEMO_LOGIN_ENABLED === 'true'` with a divider "o" above. Google form untouched; demo button placed secondary (D-02).

## Decisions Made

- `authorize()` returns null on all failure paths (never throws) — standard NextAuth convention, prevents stack leak via sign-in error page (T-12.5-AUTH)
- DemoLoginButton uses Server Action with `signIn` from `@/auth` (not `next-auth/react`) because `(auth)/layout.tsx` has no SessionProvider — client-side `signIn` would fail at runtime
- `jwt` credentials branch reads from `user` arg (what `authorize()` returned) — no `provider` field stored in token (T-12.5-03 — session confusion prevention)
- `NEXT_PUBLIC_DEMO_LOGIN_ENABLED` gates the frontend button; backend `DEMO_LOGIN_ENABLED` is the authoritative gate (D-04, T-12.5-01 defense in depth)

## Deviations from Plan

None — plan executed exactly as written.

## Deferred Items

### Task 3: Manual Demo-Login Round-Trip Smoke (DEFERRED — user decision)

**Status:** DEFERRED per explicit user decision on 2026-05-26. The user chose to skip the live smoke test for now and treat the plan as code-complete.

**Why deferred:** Requires both servers running locally + the demo user seeded (Plan 06 migration) + both env flags enabled (`NEXT_PUBLIC_DEMO_LOGIN_ENABLED=true` / `DEMO_LOGIN_ENABLED=true`). This was impractical at plan execution time without a live environment.

**What the smoke test covers (per plan Task 3):**
1. "Entrar como demo" button visible on /login when flag is on
2. Click authenticates and lands user on / with admin permissions populated
3. Google login still works after demo login feature added
4. Button hidden when `NEXT_PUBLIC_DEMO_LOGIN_ENABLED=false`
5. Backend rejects with 4xx when `DEMO_LOGIN_ENABLED=false` (defense in depth)
6. `git diff` confirms no `*.guard.ts` or `@RequirePermission` changes

**Required to close R2:** R2 (one-click demo entry via real auth path with populated permissions) is code-complete but NOT verified end-to-end until this smoke is run.

**How to verify when ready:** See Plan 05 Task 3 `<how-to-verify>` block in `12.5-05-PLAN.md` for the exact 8-step smoke procedure.

### Open Item: tsc --noEmit Pre-existing Type Errors in Test Files

**Status:** OPEN — flagged for verifier/follow-up, NOT introduced by this plan.

**Description:** Running `npx tsc --noEmit` in `nemea-front/` surfaces approximately 4 type errors in test files (`page.test.tsx`, `ScenarioEditorClient.test.tsx`). These were confirmed as pre-existing (present before this plan's commits via stash round-trip in Plan 03). This plan's commits do not introduce or worsen them.

**Impact:** TypeScript compilation errors in test files only — does not affect the Next.js build (which excludes test files). `npm run build` passes 17/17 pages green.

**Action required:** A future cleanup pass should fix the test file type errors to bring `tsc --noEmit` to fully clean. Recommend fixing in a follow-up task or Phase 12.5 gap closure if one is created.

## Verification Results

- `npx tsc --noEmit`: 4 pre-existing errors in test files (`page.test.tsx`, `ScenarioEditorClient.test.tsx`) from prior plans — NOT introduced by this plan. Confirmed via stash round-trip.
- `npm run lint`: 4 pre-existing warnings (react-hooks/incompatible-library in EditUserDialog, UsersClient, ExpenseFormDialog, ProductEditDialog) — 0 errors, same as baseline.
- `npm run build`: PASS — 17/17 pages generated, /login route dynamic, no new build errors.
- `git diff --name-only HEAD~2 HEAD -- '*.guard.ts'`: empty — no guard files touched.
- Manual round-trip smoke: DEFERRED (see Deferred Items above).

## Known Stubs

None. The Credentials provider and DemoLoginButton are fully wired. Manual round-trip smoke (Task 3) is the only remaining gate, deferred to human verification.

## Threat Surface Scan

No new threat surface beyond the plan's threat model:
- T-12.5-01 mitigated: button only renders when `NEXT_PUBLIC_DEMO_LOGIN_ENABLED === 'true'`; backend is authoritative gate.
- T-12.5-03 mitigated: credentials branch sets same token fields as Google; no provider field in JWT.
- T-12.5-AUTH mitigated: authorize() returns null on every failure path.
- T-12.5-SC accepted: zero new npm dependencies.

## Next Phase Readiness

- Plan 06 (demo-user seed migration) can proceed — Credentials provider contract (`email: 'demo@nemea.app'`) is established.
- R2 code is in place; smoke test (Task 3) needs to run against a live environment to formally close R2.
- Pre-existing `tsc --noEmit` test-file errors should be addressed in a cleanup pass.

## Self-Check: PASSED

Files exist:
- nemea-front/src/auth.ts (modified): FOUND
- nemea-front/src/app/(auth)/login/DemoLoginButton.tsx (created): FOUND
- nemea-front/src/app/(auth)/login/page.tsx (modified): FOUND

Commits exist:
- 108b46a: feat(12.5-05): add Credentials provider + demo-login callbacks in auth.ts — FOUND
- c949b3b: feat(12.5-05): add DemoLoginButton Server Action + env-gated render on /login — FOUND

---
*Phase: 12.5-demo-login-and-code-readiness-for-portfolio*
*Completed: 2026-05-26*

---
phase: 12.5-demo-login-and-code-readiness-for-portfolio
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - nemea-back/src/database/migrations/1773200000000-SeedDemoUser.ts
autonomous: false
requirements: [R3]
user_setup:
  - service: postgres
    why: "Applying and verifying the demo-user seed requires a live local Postgres"
    dashboard_config:
      - task: "Run migrations against a local/Railway Postgres"
        location: "nemea-back — npm run migration:run (DATABASE_URL must point at a reachable DB)"
must_haves:
  truths:
    - "A fresh migration:run creates an active demo user (demo@nemea.app) with the admin role"
    - "demo-login with that email succeeds once the demo user exists"
    - "The migration is idempotent (re-run does not error or duplicate)"
    - "No historical migration file is modified"
  artifacts:
    - path: "nemea-back/src/database/migrations/1773200000000-SeedDemoUser.ts"
      provides: "New seed migration inserting the demo user with the ADMIN role"
      contains: "demo@nemea.app"
  key_links:
    - from: "1773200000000-SeedDemoUser.ts up()"
      to: "roles table (ADMIN) + users table"
      via: "SELECT ADMIN role id, INSERT user with role_id, ON CONFLICT DO NOTHING"
      pattern: "INSERT INTO \"users\""
---

<objective>
Add a new TypeORM migration that seeds an active demo user (`demo@nemea.app`) with the ADMIN role, so a fresh clone can log in via the demo path immediately. Covers R3. New migration only — the Phase 10 / historical migrations are immutable (never edited). Seed mechanism is a new migration per Claude's discretion (matches the existing admin-seed pattern).

Purpose: Provide the account the demo-login path (Plans 01/05) authenticates against, so the repo is genuinely clone-and-run.
Output: `1773200000000-SeedDemoUser.ts` with idempotent up()/down().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-SPEC.md
@.planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-RESEARCH.md
@.planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-PATTERNS.md

<interfaces>
<!-- From RESEARCH.md Finding 3 + PATTERNS.md migration section. -->

Last existing migration: 1773100000000-AddSortOrderToProductSizes.ts → new timestamp MUST be > that. Use 1773200000000 (Pitfall 6).
Roles structure (Phase 12.1, 1773000000000-CreateRolesAndMigrateUsers.ts): users.role_id FK → roles; ADMIN role id is a generated uuid (NOT fixed) — query it by name: `SELECT id FROM "roles" WHERE "name" = 'ADMIN' LIMIT 1`.
users columns relevant: email, name, is_active, role_id. User.role is eager-loaded so demo-login resolves permissions from the role.
Idempotency: `INSERT INTO "users" (...) VALUES ('demo@nemea.app', 'Demo Nemea', true, $1) ON CONFLICT ("email") DO NOTHING`.
down(): `DELETE FROM "users" WHERE "email" = 'demo@nemea.app'`.
Class convention: class SeedDemoUser1773200000000 implements MigrationInterface; `name = 'SeedDemoUser1773200000000'`; both up and down present.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write SeedDemoUser migration</name>
  <files>nemea-back/src/database/migrations/1773200000000-SeedDemoUser.ts</files>
  <read_first>
    - nemea-back/src/database/migrations/1773000000000-CreateRolesAndMigrateUsers.ts (analog — queryRunner.query, RETURNING/role lookup pattern, lines 1–60)
    - nemea-back/src/database/migrations/1772369233961-CreateUserTable.ts (original admin seed pattern)
    - .planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-PATTERNS.md (section: 1773200000000-SeedDemoUser.ts — full file pattern)
    - .planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-RESEARCH.md (Finding 3 + Pitfalls 6, A6)
  </read_first>
  <action>
    Create nemea-back/src/database/migrations/1773200000000-SeedDemoUser.ts. Class `SeedDemoUser1773200000000 implements MigrationInterface` with `name = 'SeedDemoUser1773200000000'`. In `up(queryRunner)`: query `SELECT id FROM "roles" WHERE "name" = 'ADMIN' LIMIT 1`, read `(result[0] as { id: string } | undefined)?.id`, and if absent throw an Error explaining CreateRolesAndMigrateUsers must run first. Then run a parameterized `INSERT INTO "users" ("email", "name", "is_active", "role_id") VALUES ('demo@nemea.app', 'Demo Nemea', true, $1) ON CONFLICT ("email") DO NOTHING` with `[adminRoleId]`. In `down(queryRunner)`: `DELETE FROM "users" WHERE "email" = 'demo@nemea.app'`. Do NOT edit any existing migration. If the running Postgres version lacks a unique constraint on `email` for ON CONFLICT to target, fall back to a guarded conditional INSERT (insert only if not exists) — see RESEARCH A6.
  </action>
  <verify>
    <automated>cd nemea-back && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - File 1773200000000-SeedDemoUser.ts exists; timestamp > 1773100000000; class + `name` field match the convention.
    - up() looks up the ADMIN role id by name (not a hardcoded uuid) and inserts the demo user idempotently; down() deletes it.
    - `npx tsc --noEmit` + `npm run lint` pass in nemea-back.
  </acceptance_criteria>
  <done>Migration file written, compiles, lints; no historical migration touched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Apply + verify demo-user seed against live Postgres (R3)</name>
  <what-built>A new TypeORM migration seeding demo@nemea.app as an active user with the ADMIN role.</what-built>
  <read_first>
    - .planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-VALIDATION.md (Manual-Only Verifications — demo user seeded row)
  </read_first>
  <how-to-verify>
    Requires a reachable Postgres (DATABASE_URL set). The migration is a data seed (INSERT), not auto-verifiable headlessly.
    1. From nemea-back run `npm run migration:run`.
    2. Verify: `psql "$DATABASE_URL" -c "SELECT u.email, u.is_active, r.name FROM users u JOIN roles r ON r.id = u.role_id WHERE u.email = 'demo@nemea.app';"` → expect one row, is_active = t (true), role name = ADMIN.
    3. Re-run `npm run migration:run` once more (or run the migration twice) → confirm no error and still exactly one demo row (idempotency).
    4. (Optional, ties to Plan 05) With DEMO_LOGIN_ENABLED=true, `curl -X POST $API/api/auth/demo-login -d '{"email":"demo@nemea.app"}' -H 'Content-Type: application/json'` → expect 200 + token with populated permissions.
  </how-to-verify>
  <resume-signal>Type "approved" if the demo user exists as active with the ADMIN role and the migration is idempotent — or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → users/roles tables | Seed inserts a privileged (admin-role) account |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.5-04 | Elevation of Privilege | seeded admin demo account usable in prod | mitigate | The account is inert without the demo-login flag; demo-login is OFF by default in prod (Plan 01 gate). Each cloner runs their own local DB, so admin scope is intentional and local-only |
| T-12.5-MIG | Tampering | editing a historical migration | mitigate | New migration file only (timestamp 1773200000000); immutability constraint — no existing migration touched |
| T-12.5-IDEM | Denial of Service | non-idempotent seed breaks migration:run | mitigate | `ON CONFLICT ("email") DO NOTHING` (or guarded conditional insert) makes re-runs safe |
| T-12.5-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies (SPEC constraint); no install tasks |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npm run lint` green in nemea-back.
- Manual (Task 2): migration:run creates demo@nemea.app as active with ADMIN role; re-run is idempotent.
- No existing migration file modified (`git status` shows only the new file under migrations).
</verification>

<success_criteria>
- R3 satisfied: fresh migration:run + seed creates an active demo user with the admin role; demo-login with that email succeeds.
</success_criteria>

<output>
Create `.planning/phases/12.5-demo-login-and-code-readiness-for-portfolio/12.5-06-SUMMARY.md` when done.
</output>

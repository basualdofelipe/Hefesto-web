---
phase: 06-cost-calculation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - nemea-front/src/components/products/types.ts
  - nemea-front/src/components/products/ProductTypeGroup.tsx
  - nemea-front/src/components/products/ProductExpandedRow.tsx
  - nemea-front/src/components/products/ProductTable.tsx
  - nemea-front/src/app/(app)/productos/[id]/page.tsx
  - nemea-front/src/components/products/ProductDetailClient.tsx
autonomous: false
requirements:
  - COST-02
  - COST-03
  - COST-04

must_haves:
  truths:
    - "Product list table shows Costo, Precio Venta, and Margen columns"
    - "Column order is: SKU | Nombre | Terminacion | Color | Talle | Costo | Precio Venta | Margen"
    - "Margen shows $ amount and % markup (Margen/Costo x 100)"
    - "Product without BOM shows em-dash in Costo and Margen with tooltip 'Sin materiales definidos'"
    - "Expanded row BOM table has Precio Unitario and Costo Linea columns with total row and margin summary"
    - "Group header shows count + average cost excluding products without BOM"
    - "Product detail page at /productos/:id shows header + BOM with cost breakdown + margin summary"
    - "Navigating from /insumos (after price update) to /productos shows updated costs"
  artifacts:
    - path: "nemea-front/src/components/products/types.ts"
      provides: "ProductWithCost type, formatCost, formatMargin helpers"
      contains: "ProductWithCost"
    - path: "nemea-front/src/components/products/ProductTypeGroup.tsx"
      provides: "Cost and Margin columns in table, group header average cost"
      contains: "Costo prom"
    - path: "nemea-front/src/components/products/ProductExpandedRow.tsx"
      provides: "Enriched BOM table with Precio Unitario and Costo Linea columns"
      contains: "Precio Unit"
    - path: "nemea-front/src/app/(app)/productos/[id]/page.tsx"
      provides: "Product detail page with cost breakdown"
      contains: "ProductDetailClient"
  key_links:
    - from: "nemea-front/src/app/(app)/productos/page.tsx"
      to: "/api/products"
      via: "apiFetch in server component"
      pattern: "apiFetch.*products"
    - from: "nemea-front/src/app/(app)/productos/[id]/page.tsx"
      to: "/api/products/:id"
      via: "apiFetch in server component"
      pattern: "apiFetch.*products.*id"
    - from: "nemea-front/src/components/products/ProductTypeGroup.tsx"
      to: "nemea-front/src/components/products/types.ts"
      via: "formatCost, formatMargin imports"
      pattern: "formatCost|formatMargin"
---

<objective>
Add cost and margin columns to the product list, enrich the BOM expanded row with price data, create the product detail page at /productos/:id, and add group header cost aggregation.

Purpose: Users need to see the calculated cost alongside the selling price to understand their margins at a glance. The detail page provides a full cost breakdown per material. This is the visual payoff of the core value proposition.

Output: Product list with Costo/Margen columns, enriched BOM table, product detail page, group header averages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cost-calculation/06-CONTEXT.md
@.planning/phases/06-cost-calculation/06-RESEARCH.md
@.planning/phases/06-cost-calculation/06-01-SUMMARY.md

<interfaces>
<!-- Types from plan 06-01 that this plan consumes -->

From nemea-back/src/costs/dto/product-with-cost.dto.ts (created in 06-01):
```typescript
export interface CostBreakdownItem {
  supplyId: string;
  supplyName: string;
  supplyType: string;
  quantity: number;
  unitType: string;
  unitPrice: number | null;
  lineCost: number | null;
  isSupplyActive: boolean;
}

export interface ProductCostData {
  cost: number;
  costBreakdown: CostBreakdownItem[];
  costWarnings: string[];
}

export interface ProductWithCost extends ProductWithPrice {
  cost: number | null;
  costBreakdown: CostBreakdownItem[] | null;
  costWarnings: string[];
}
```

From nemea-front/src/components/products/types.ts (existing):
```typescript
export interface Product {
  id: string; skuCode: string;
  type: ProductDimension; name: ProductDimension;
  finish: ProductDimension; color: ProductDimension; size: ProductDimension;
  isActive: boolean; currentPrice: number | null;
  lastPriceUpdate: string | null; createdAt: string;
}

export interface BomItem {
  id: string;
  supply: { id: string; name: string; unitType: string; isActive: boolean; type: { name: string } };
  quantity: string; isActive: boolean;
}

export function formatSellingPrice(price: number | null): string;
export function getProductDisplayName(product: Product): string;
```

From nemea-front/src/components/products/ProductTypeGroup.tsx:
- Current colSpan = 6 (needs to become 8 after adding Costo + Margen)
- Current columns: SKU | Nombre | Terminacion | Color | Talle | Precio Venta
- ProductExpandedRow receives colSpan as prop

From nemea-front/src/components/products/ProductExpandedRow.tsx:
- BOM table columns: Insumo | Tipo | Cantidad | Unidad (needs + Precio Unit. | Costo Linea)
- Fetches BOM lazily via apiClientFetch on expand
- Already has UNIT_LABELS dict and inactive supply warning

From nemea-front/src/app/(app)/productos/page.tsx:
- Server component fetching apiFetch<{ data: Product[] }>('/api/products?includeInactive=true')
- Passes initialProducts to ProductTable
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend types + cost/margin columns in product list + group header aggregation</name>
  <files>
    nemea-front/src/components/products/types.ts,
    nemea-front/src/components/products/ProductTypeGroup.tsx,
    nemea-front/src/components/products/ProductTable.tsx,
    nemea-front/src/components/products/ProductExpandedRow.tsx
  </files>
  <action>
    1. Update `nemea-front/src/components/products/types.ts`:
       - Add `CostBreakdownItem` interface matching backend DTO: supplyId, supplyName, supplyType, quantity (number), unitType, unitPrice (number|null), lineCost (number|null), isSupplyActive
       - Extend `Product` interface to add: cost (number|null), costBreakdown (CostBreakdownItem[]|null), costWarnings (string[])
       - Add `formatCost(cost: number | null): string` -- returns em-dash for null, otherwise `$${cost.toLocaleString('es-AR', { minimumFractionDigits: 0, maximumFractionDigits: 2 })}`
       - Add `formatMargin(cost: number | null, price: number | null): { amount: string; percent: string }` -- returns em-dash for either null. Margin = price - cost. Percent = Math.round((margin / cost) * 100) when cost > 0.

    2. Update `nemea-front/src/components/products/ProductTypeGroup.tsx`:
       - Change `const colSpan = 8` (was 6 -- adding Costo and Margen)
       - Add column headers after Talle: `<TableHead>Costo</TableHead>`, then keep `<TableHead>Precio Venta</TableHead>`, then add `<TableHead>Margen</TableHead>`
       - Column order: SKU | Nombre | Terminacion | Color | Talle | Costo | Precio Venta | Margen
       - In each product row, add Costo cell between Talle and Precio Venta:
         ```
         <TableCell>{formatCost(product.cost)}</TableCell>
         ```
         If product.cost is null AND product has no costWarnings, add a Tooltip wrapping the cell content with "Sin materiales definidos"
       - Add Margen cell after Precio Venta:
         ```
         const margin = formatMargin(product.cost, product.currentPrice);
         <TableCell>
           <span>{margin.amount}</span>
           {margin.percent !== '\u2014' && <span className="text-muted-foreground ml-1 text-xs">({margin.percent})</span>}
         </TableCell>
         ```
       - If product has costWarnings (length > 0), show a small AlertTriangle icon next to the cost value with Tooltip listing warnings
       - Update group header to show average cost:
         - Calculate: filter products where cost is not null, average their costs
         - Format: `Costo prom: ${formatCost(avgCost)}` next to the count badge
         - Only show if at least 1 product has cost

    3. Update `nemea-front/src/components/products/ProductExpandedRow.tsx`:
       - The BOM table now needs cost data. Since the backend enriches GET /products/:id/bom response or the cost data comes from the product itself:
         - Option: Use product.costBreakdown (from findAll response -- will be null in list). For the expanded row, fetch BOM as before BUT also use cost data.
         - Better approach: The expanded row already fetches BOM items. Add 2 new columns to the BOM table: "Precio Unit." and "Costo Linea".
         - The BOM items from the API don't have price data. However, the product's `costBreakdown` from the list endpoint also lacks full breakdown (null in list per research recommendation).
         - Solution: Modify the expanded row to ALSO fetch the product's cost breakdown from `GET /api/products/${product.id}` when expanding, OR add the cost breakdown to the BOM fetch response.
         - Simplest: Make a second fetch to `GET /api/products/${product.id}` to get the full ProductWithCost including costBreakdown. Build a map from supplyId -> { unitPrice, lineCost } and display alongside BOM items.
         - Alternative (cleaner): Just use the costBreakdown directly since it already contains all BOM data. Replace the BOM table with costBreakdown data when available.
       - Implementation: Fetch `GET /api/products/${product.id}` alongside the BOM fetch. Parse costBreakdown from the response. Build a Map<supplyId, { unitPrice, lineCost, isSupplyActive }>.
       - BOM table columns become: Insumo | Tipo | Cantidad | Unidad | Precio Unit. | Costo Linea
       - Add total row at the bottom: `<TableRow>` with "Total" spanning first 4 columns and the sum of all lineCosts in the last column
       - Add margin summary below the table:
         - Costo total: formatCost(totalCost)
         - Precio venta: formatSellingPrice(product.currentPrice)
         - Margen: formatMargin amount and percent
       - Supply without price: show em-dash in Precio Unit. and Costo Linea cells, with AlertTriangle + tooltip
       - Inactive supply: row gets `opacity-50` (already has this) plus dimmed styling

    4. Ensure Shadcn Tooltip component is installed. If not, install via `npx shadcn@latest add tooltip` in nemea-front.

    5. Update `ProductTable.tsx`: No changes needed to props since Product interface is extended in-place. Verify it compiles.

    6. Product name cell in ProductTypeGroup: Make product name a link to `/productos/${product.id}`. Use Next.js `<Link>` with `e.stopPropagation()` on the wrapping element to prevent row click from firing when clicking the link. The name should look like a link (underline on hover, text-primary color).
  </action>
  <verify>
    <automated>cd nemea-front && npx tsc --noEmit</automated>
  </verify>
  <done>Product list table shows 8 columns (SKU, Nombre, Terminacion, Color, Talle, Costo, Precio Venta, Margen). Expanded row BOM table shows Precio Unit. and Costo Linea with total and margin summary. Group headers show average cost. Product name links to /productos/:id. Frontend compiles without type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Product detail page at /productos/:id</name>
  <files>
    nemea-front/src/app/(app)/productos/[id]/page.tsx,
    nemea-front/src/components/products/ProductDetailClient.tsx
  </files>
  <action>
    1. Create `nemea-front/src/app/(app)/productos/[id]/page.tsx` as a server component:
       - Fetch session via `auth()` for isAdmin check
       - Fetch product data: `apiFetch<{ data: ProductWithCost }>(`/api/products/${id}`)`
       - Fetch BOM data: `apiFetch<{ data: BomItem[] }>(`/api/products/${id}/bom`)`
       - Use `Promise.all` for parallel fetching
       - Pass data to `ProductDetailClient` component
       - Handle params: `params: Promise<{ id: string }>` (Next.js 16 pattern, await params)

    2. Create `nemea-front/src/components/products/ProductDetailClient.tsx` as a client component:
       - Props: product (ProductWithCost), bomItems (BomItem[]), isAdmin (boolean)
       - Also needs catalogs (types, names, finishes, colors, sizes) and supplies for admin action modals
       - Layout: single column, stacked sections with spacing

       **Header section:**
       - "Volver a productos" link at top using `<Link href="/productos">` with ArrowLeft icon
       - Product full name (getProductDisplayName) as h1
       - SKU in mono font below
       - Status badge (Activo/Inactivo)

       **BOM + Cost Breakdown section:**
       - Same enriched BOM table as expanded row: Insumo | Tipo | Cantidad | Unidad | Precio Unit. | Costo Linea
       - Use product.costBreakdown to populate price columns
       - Build Map<supplyId, CostBreakdownItem> from costBreakdown for lookup
       - Total row at bottom
       - Warning indicators for supplies without price or inactive supplies

       **Margin Summary section:**
       - Card with: Costo total, Precio de venta, Margen ($ and %)
       - If cost is null: "Sin materiales definidos"
       - If price is null: "Sin precio de venta definido"
       - Warnings list if any costWarnings exist

       **Admin Actions section (if isAdmin):**
       - Buttons row: Editar producto, Editar BOM, Agregar precio, Historial precios, Toggle status
       - Reuse existing dialog components: ProductEditDialog, BomEditorDialog, AddSellingPriceInline, PriceHistoryDialog
       - For these to work, the page also needs catalog data (types, names, finishes, colors, sizes) and supplies -- fetch them in the server component or pass from page

    3. Update the server component to also fetch catalogs and supplies (same as productos/page.tsx does) for the admin action modals to work.

    4. Verify compilation: `cd nemea-front && npx tsc --noEmit`
  </action>
  <verify>
    <automated>cd nemea-front && npx tsc --noEmit</automated>
  </verify>
  <done>Product detail page renders at /productos/:id with product header, BOM cost breakdown table, margin summary, and admin actions. Back link returns to /productos. All reused modal components work. Frontend compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of cost calculation flow</name>
  <files>none</files>
  <action>
    Human verifies the full cost calculation flow end-to-end:
    - Product list with cost and margin columns
    - Cost breakdown in expanded rows
    - Product detail page at /productos/:id
    - Cost auto-refresh after supply price update
  </action>
  <verify>Human visual inspection</verify>
  <done>User confirms all cost display, margins, detail page, and auto-refresh behavior work correctly</done>
  <what-built>
    Full cost calculation flow: backend calculates costs from latest supply prices, product list shows Costo and Margen columns, expanded row shows enriched BOM with line costs and margin summary, product detail page at /productos/:id shows full breakdown, group headers show average cost.
  </what-built>
  <how-to-verify>
    1. Start backend and frontend (npm run start:dev in nemea-back, npm run dev in nemea-front)
    2. Navigate to /productos -- verify columns: SKU | Nombre | Terminacion | Color | Talle | Costo | Precio Venta | Margen
    3. Verify products WITH BOM show calculated cost and margin ($ and %)
    4. Verify products WITHOUT BOM show em-dash in Costo and Margen columns
    5. Verify group headers show "Costo prom: $X.XXX" next to product count
    6. Click a product name -- verify it navigates to /productos/:id detail page
    7. On detail page: verify BOM table with Precio Unit. and Costo Linea columns, total row, margin summary
    8. On detail page: verify "Volver a productos" link works
    9. Go to /insumos, update a supply price, then navigate back to /productos -- verify cost reflects the new price
    10. Expand a product row -- verify BOM shows cost data inline with margin summary
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `cd nemea-front && npx tsc --noEmit` -- no type errors
2. `cd nemea-back && npx jest --no-coverage` -- all backend tests pass
3. Product list shows 8 columns with correct order
4. Cost updates immediately when supply price changes (navigate to verify)
5. Product detail page accessible via product name link
6. Group headers show count + average cost
</verification>

<success_criteria>
- Product list table has Costo and Margen columns between Talle and after Precio Venta
- Margin shows $ amount and % markup
- Products without BOM show em-dash with tooltip
- Expanded row BOM table enriched with price columns, total, and margin summary
- Group headers show average cost excluding products without BOM
- Product detail page at /productos/:id with header, BOM breakdown, margin summary, admin actions
- Cost reflects latest supply prices on navigation (no cache, no manual refresh)
</success_criteria>

<output>
After completion, create `.planning/phases/06-cost-calculation/06-02-SUMMARY.md`
</output>

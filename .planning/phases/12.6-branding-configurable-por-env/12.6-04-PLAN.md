---
phase: 12.6-branding-configurable-por-env
plan: 04
type: execute
wave: 3
depends_on: [12.6-01, 12.6-02, 12.6-03]
files_modified:
  - nemea-front/.env.example
  - nemea-back/.env.example
  - nemea-back/src/auth/dto/demo-login.dto.ts
  - nemea-back/docs/ARCHITECTURE.md
  - nemea-back/README.md
autonomous: true
requirements: [R8]
must_haves:
  truths:
    - "El README tiene una seccion 'Rebranding' que lista APP_NAME, DEMO_EMAIL, ADMIN_EMAIL, ADMIN_NAME y sus contrapartes NEXT_PUBLIC_* + el swap del logo + el requisito de rebuild + la regla de que DEMO_EMAIL y NEXT_PUBLIC_DEMO_EMAIL deben coincidir"
    - "Los .env.example (front+back) traen los defaults Hefesto / demo@hefesto.com / admin@hefesto.com / Admin"
    - "grep -i 'nemea' y 'basualdofelipe' / 'demo@nemea.app' sobre source+docs de front y back EXCLUYENDO tests (R1: 'excluyendo tests') e historial git y los internos out-of-scope no devuelve la marca"
  artifacts:
    - path: "nemea-back/README.md"
      provides: "Seccion Rebranding minima con env vars + swap de logo + rebuild + regla de paridad demo email"
      contains: "Rebranding"
    - path: "nemea-front/.env.example"
      provides: "Defaults Hefesto + nuevas NEXT_PUBLIC_* de branding"
      contains: "NEXT_PUBLIC_APP_NAME=Hefesto"
  key_links: []
---

<objective>
Cerrar la fase con docs y limpieza de marca (R8): actualizar `.env.example` de ambos repos con los defaults neutros y las nuevas env vars, limpiar las refs cosmeticas (`example` del DTO, `ARCHITECTURE.md`), agregar una seccion "Rebranding" minima al README (D-04), y correr los greps finales de marca para confirmar que no queda "Nemea"/`demo@nemea.app`/`basualdofelipe` en source+docs VISIBLES/SHIPPED (excluyendo tests por R1, historial git, y los internos explicitamente out-of-scope).

Purpose: sin la seccion Rebranding el comprador no sabria que las `NEXT_PUBLIC_*` se hornean en build (necesita rebuild) ni que el logo se reemplaza a mano — la guia enganaria (D-04: las 2 lineas son obligatorias, no adorno). Wave 3: documenta el estado final ya implementado por los planes 01-03 y corre la verificacion negativa de cierre.
Output: `.env.example` ×2 + DTO + ARCHITECTURE.md + README con seccion Rebranding; greps de marca vacios sobre source visible/shipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.6-branding-configurable-por-env/12.6-SPEC.md
@.planning/phases/12.6-branding-configurable-por-env/12.6-CONTEXT.md
@.planning/phases/12.6-branding-configurable-por-env/12.6-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Actualizar .env.example (front+back) + DTO example + ARCHITECTURE.md</name>
  <files>nemea-front/.env.example, nemea-back/.env.example, nemea-back/src/auth/dto/demo-login.dto.ts, nemea-back/docs/ARCHITECTURE.md</files>
  <read_first>
    - nemea-front/.env.example (linea 1 NEXT_PUBLIC_APP_NAME=Nemea; bloque NEXT_PUBLIC_DEMO_LOGIN_ENABLED ~linea 12)
    - nemea-back/.env.example (lineas 18-20; comment con demo@nemea.app ~linea 19)
    - nemea-back/src/auth/dto/demo-login.dto.ts (linea 6: @ApiProperty example 'demo@nemea.app'; @IsEmail linea 9 — NO tocar)
    - nemea-back/docs/ARCHITECTURE.md (linea 116: mencion demo@nemea.app)
    - .planning/phases/12.6-branding-configurable-por-env/12.6-PATTERNS.md (Docs lineas 332-344)
  </read_first>
  <action>
    En `nemea-front/.env.example`: cambiar `NEXT_PUBLIC_APP_NAME=Nemea` → `NEXT_PUBLIC_APP_NAME=Hefesto`; agregar `NEXT_PUBLIC_DEMO_EMAIL=demo@hefesto.com` y `NEXT_PUBLIC_CONTACT_EMAIL=admin@hefesto.com` cerca del bloque `NEXT_PUBLIC_DEMO_LOGIN_ENABLED` existente. En `nemea-back/.env.example`: agregar `APP_NAME=Hefesto`, `DEMO_EMAIL=demo@hefesto.com`, `ADMIN_EMAIL=admin@hefesto.com`, `ADMIN_NAME=Admin`; actualizar el comment de la linea ~19 que menciona `demo@nemea.app` → `demo@hefesto.com`. Agregar un comment corto junto a `ADMIN_EMAIL` aclarando que en deploy debe ser una cuenta Google REAL del deployer para poder loguear (el default `admin@hefesto.com` es solo un seed valido, no logueable — constraint SPEC). En `demo-login.dto.ts`: el `@ApiProperty({ example: 'demo@nemea.app' })` (linea 6) → `example: 'demo@hefesto.com'`; el `@IsEmail()` (linea 9) queda intacto. En `ARCHITECTURE.md:116`: reemplazar la mencion `demo@nemea.app` por el default env-driven `demo@hefesto.com`.
  </action>
  <verify>
    <automated>cd nemea-back && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `nemea-front/.env.example` contiene `NEXT_PUBLIC_APP_NAME=Hefesto`, `NEXT_PUBLIC_DEMO_EMAIL=demo@hefesto.com`, `NEXT_PUBLIC_CONTACT_EMAIL=admin@hefesto.com`; ya no contiene `=Nemea`.
    - `nemea-back/.env.example` contiene `APP_NAME=Hefesto`, `DEMO_EMAIL=demo@hefesto.com`, `ADMIN_EMAIL=admin@hefesto.com`, `ADMIN_NAME=Admin` + comment de cuenta Google real; sin `demo@nemea.app`.
    - `demo-login.dto.ts` example = `'demo@hefesto.com'`; `@IsEmail()` presente.
    - `ARCHITECTURE.md` ya no menciona `demo@nemea.app`.
    - `npx tsc --noEmit` (back) pasa.
  </acceptance_criteria>
  <done>Los .env.example, el DTO example y ARCHITECTURE reflejan los defaults Hefesto / demo@hefesto.com.</done>
</task>

<task type="auto">
  <name>Task 2: Seccion "Rebranding" en README + greps finales de marca (excluyendo tests)</name>
  <files>nemea-back/README.md</files>
  <read_first>
    - nemea-back/README.md (secciones existentes ## Local setup ~linea 80, ## Deployment ~linea 243 — ubicar la seccion cerca)
    - .planning/phases/12.6-branding-configurable-por-env/12.6-CONTEXT.md (D-04 linea 46: seccion minima, 2 lineas obligatorias, sin marketing)
    - .planning/phases/12.6-branding-configurable-por-env/12.6-PATTERNS.md (README lineas 344)
    - .planning/phases/12.6-branding-configurable-por-env/12.6-SPEC.md (R8 lineas 61-64, R1 acceptance linea 29 "excluyendo tests", Acceptance Criteria lineas 108-116)
  </read_first>
  <action>
    Agregar una seccion `## Rebranding` al README (D-04: MINIMA, sin parrafo de marketing/pitch). Debe contener: (1) la lista de TODAS las env vars de branding — back: `APP_NAME`, `DEMO_EMAIL`, `ADMIN_EMAIL`, `ADMIN_NAME`; front: `NEXT_PUBLIC_APP_NAME`, `NEXT_PUBLIC_DEMO_EMAIL`, `NEXT_PUBLIC_CONTACT_EMAIL` — con sus defaults; (2) las DOS lineas obligatorias (no son adorno): "reemplazá el logo en `public/brand/`" y "rebuild — las `NEXT_PUBLIC_*` se hornean en build time"; (3) una nota de que `ADMIN_EMAIL` debe ser una cuenta Google real del deployer para poder loguear como admin (el default neutro es solo un seed valido); (4) UNA linea funcional (estilo minimo D-04, sin marketing) advirtiendo el acople demo-login cross-repo: "`DEMO_EMAIL` (back) y `NEXT_PUBLIC_DEMO_EMAIL` (front) deben tener el MISMO valor o el demo-login devuelve 401." — el front postea `NEXT_PUBLIC_DEMO_EMAIL` y el back pinea `validateDemoLogin` a `DEMO_EMAIL`; si difieren (o se setea uno solo), el 401 es silencioso (Senior PM: los defaults ya coinciden, el footgun solo dispara al cambiarlos a valores distintos). Es una sola linea funcional, no un parrafo. Ubicar cerca de Local setup / Deployment (orden a discrecion).

    Tras editar, correr los greps de cierre EXCLUYENDO test files (R1 acceptance dice explicitamente "excluyendo tests"; las fixtures de test no son texto visible ni shippeado, y limpiar emails de fixtures NO esta en scope del SPEC — out of scope, no se reescriben). Usar exclusiones de test en ambos comandos: con ripgrep `rg -i nemea nemea-front/src nemea-back/src --glob '!**/*.spec.ts' --glob '!**/*.test.ts' --glob '!**/*.test.tsx' --glob '!**/__tests__/**'`; o con grep `grep -ri nemea nemea-front/src nemea-back/src --exclude='*.spec.ts' --exclude='*.test.ts' --exclude='*.test.tsx' --exclude-dir=__tests__`. Idem para `grep -ri "demo@nemea.app\|basualdofelipe"` + sobre las docs editadas.

    Tras excluir tests, los greps deben quedar vacios EXCEPTO los internos out-of-scope conocidos (carpeta `nemea-front/`, `package.json name`, `Logger('Nemea')` en logging.interceptor.ts, `app:'nemea-back'` en app.controller.ts, `nemea_db` en docker-compose) que NO se tocan; y excepto el viejo default `demo@nemea.app` si quedo como caso de rechazo en el spec del plan 01 (que tambien es un test file, ya excluido). El `grep -i basualdofelipe` (con la misma exclusion de tests) sobre `nemea-front/src` DEBE quedar VACIO (R10) — el email del autor solo vivia en `acceso-denegado/page.tsx` (fix del plan 03, source no-test); confirmar que no quedo en ningun otro source no-test. Si `basualdofelipe` apareciera en una fixture de test, queda cubierto por la misma exclusion de tests (consistente con R1).

    Las fixtures de test que SI contienen la marca y son out-of-scope esperados (NO se tocan, R1 excluye tests): `nemea-front` — `usuarios/__tests__/*.test.tsx`, `escenarios/.../__tests__/*.test.tsx`, `calculadora/__tests__/*.test.tsx` (literales `demo@nemea.com`, `admin@nemea.com`, `user@nemea.com`, `other@nemea.com`, `Admin Nemea`); `nemea-back` — `users.controller.spec.ts`, `users.service.spec.ts`, `permissions.guard.spec.ts`, `auth.service.spec.ts`, `auth.controller.spec.ts`. Documentar en el SUMMARY: (a) que la exclusion de tests aplica por R1 ("excluyendo tests") + porque las fixtures son no-visibles/no-shippeadas y su rebranding esta fuera del SPEC; (b) la lista de fixtures con marca como matches conocidos esperados; (c) los internos out-of-scope que tambien quedaron.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -ril "basualdofelipe" nemea-front/src nemea-back/src --exclude='*.spec.ts' --exclude='*.test.ts' --exclude='*.test.tsx' --exclude-dir=__tests__; if [ $? -eq 1 ]; then echo "OK: sin basualdofelipe en source no-test"; fi</automated>
  </verify>
  <acceptance_criteria>
    - El README tiene una seccion `## Rebranding` que lista las 7 env vars (4 back + 3 front) con defaults.
    - La seccion incluye las 2 lineas obligatorias (swap de logo en `public/brand/` + rebuild de NEXT_PUBLIC_*).
    - La seccion nota que `ADMIN_EMAIL` debe ser cuenta Google real para loguear.
    - La seccion incluye la linea de paridad: `DEMO_EMAIL` (back) y `NEXT_PUBLIC_DEMO_EMAIL` (front) deben tener el MISMO valor o el demo-login devuelve 401.
    - `grep -ri basualdofelipe nemea-front/src nemea-back/src` con exclusion de tests (`*.spec.ts`/`*.test.ts`/`*.test.tsx`/`__tests__/`) vacio.
    - `grep -ri nemea` sobre source de ambos repos con exclusion de tests solo devuelve los internos out-of-scope conocidos (Logger, app id, nombres de carpeta/package/docker) — documentados en el SUMMARY; cero matches de marca visible/shippeada.
    - Las fixtures de test con marca (`*.spec.ts` back + `__tests__/*.test.tsx` front) quedan documentadas en el SUMMARY como matches esperados out-of-scope por R1 (NO se tocan).
  </acceptance_criteria>
  <done>El README explica como rebrandear (env vars + logo + rebuild + paridad demo email) y la marca no aparece en texto visible/docs/source no-test (las fixtures de test quedan excluidas por R1 y documentadas).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/.env.example → deployer | Lo que el comprador lee para rebrandear; una guia incompleta lleva a un deploy con la marca vieja o a un demo-login roto por vars desalineadas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.6-07 | Information Disclosure | README / .env.example / ARCHITECTURE.md | mitigate | R8: defaults neutros (Hefesto / hefesto.com) en docs + greps de cierre `grep -i nemea`/`grep -i basualdofelipe` (excluyendo tests por R1) vacios sobre source visible/shippeado — no se filtra la marca de un tercero ni el correo del autor en codigo que se despliega |
| T-12.6-08 | (operacional / guia incompleta) | seccion Rebranding del README | mitigate | D-04: las 2 lineas (logo + rebuild) son obligatorias + la linea de paridad `DEMO_EMAIL`/`NEXT_PUBLIC_DEMO_EMAIL` — sin ellas el rebrander no sabria del horneado build-time, del swap de logo, ni del 401 silencioso si desalinea el par de demo emails |
| T-12.6-SC | Tampering | npm/pip/cargo installs | accept | Cero deps npm nuevas (constraint v1.1) — no se instala ningun paquete |
</threat_model>

<verification>
- `cd nemea-back && npx tsc --noEmit` sin errores.
- README tiene `## Rebranding` con las 7 env vars + 2 lineas obligatorias + nota cuenta Google + linea de paridad demo email.
- `grep -ri basualdofelipe nemea-front/src nemea-back/src` con exclusion de tests vacio.
- `grep -ri nemea` sobre source visible (excluyendo tests) solo devuelve los internos out-of-scope (documentados). Las fixtures de test con marca quedan excluidas por R1 ("excluyendo tests") y documentadas en el SUMMARY.
</verification>

<success_criteria>
- .env.example (ambos), DTO example y ARCHITECTURE con defaults Hefesto / demo@hefesto.com.
- README con seccion Rebranding minima (env vars + logo + rebuild + nota cuenta Google + paridad demo email).
- Sin marca visible ni email del autor en source/docs no-test (greps con exclusion de tests vacios salvo internos out-of-scope); fixtures de test documentadas como out-of-scope por R1.
</success_criteria>

<output>
Create `.planning/phases/12.6-branding-configurable-por-env/12.6-04-SUMMARY.md` when done
</output>

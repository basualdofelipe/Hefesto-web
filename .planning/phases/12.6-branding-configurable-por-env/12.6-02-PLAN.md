---
phase: 12.6-branding-configurable-por-env
plan: 02
type: execute
wave: 2
depends_on: [12.6-01]
files_modified:
  - nemea-back/src/database/migrations/1772369233961-CreateUserTable.ts
  - nemea-back/src/database/migrations/1773200000000-SeedDemoUser.ts
autonomous: false
requirements: [R5, R9]
must_haves:
  truths:
    - "En DB fresca sin envs, la fila admin seedeada es admin@hefesto.com / 'Admin'"
    - "En DB fresca sin envs, la fila demo seedeada es demo@hefesto.com / 'Demo Hefesto'"
    - "Con APP_NAME=Foo + DEMO_EMAIL=demo@foo.com + ADMIN_EMAIL/ADMIN_NAME seteados, las filas reflejan esos valores ('Demo Foo', etc.)"
    - "Los INSERT de ambas migraciones usan query parametrizada ($1/$2), no interpolacion de string"
  artifacts:
    - path: "nemea-back/src/database/migrations/1772369233961-CreateUserTable.ts"
      provides: "Seed admin desde ADMIN_EMAIL/ADMIN_NAME con defaults neutros, parametrizado"
      contains: "process.env.ADMIN_EMAIL"
    - path: "nemea-back/src/database/migrations/1773200000000-SeedDemoUser.ts"
      provides: "Seed demo desde DEMO_EMAIL + name 'Demo ${APP_NAME}', parametrizado, down simetrico"
      contains: "process.env.DEMO_EMAIL"
  key_links:
    - from: "nemea-back/src/database/migrations/1773200000000-SeedDemoUser.ts"
      to: "process.env via dotenv/config (data-source.ts:1)"
      via: "lectura de DEMO_EMAIL/APP_NAME en up() y down()"
      pattern: "process.env.DEMO_EMAIL"
---

<objective>
Editar las dos migraciones de seed para que sembren la identidad desde env en DB fresca (seed-from-env): `CreateUserTable` siembra el admin desde `ADMIN_EMAIL`/`ADMIN_NAME` con defaults neutros y query parametrizada (R9), y `SeedDemoUser` siembra el demo desde `DEMO_EMAIL` con name derivado `Demo ${APP_NAME}` y un `down()` simetrico que borra el mismo valor del env (R5, Pitfall 4). Incluye una verificacion manual BLOQUEANTE: correr las migraciones contra una DB fresca e inspeccionar las filas seedeadas.

Purpose: las DB actuales son descartables (se wipean, sin reconciliacion — out of scope); estas ediciones garantizan que una DB fresca arranque con la identidad neutra/white-label. Sin correr las migraciones la verificacion es un falso positivo (los seeds son datos de runtime, no tipos; el build pasa igual).
Output: 2 migraciones seed-from-env + evidencia de filas seedeadas correctas en DB fresca.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.6-branding-configurable-por-env/12.6-SPEC.md
@.planning/phases/12.6-branding-configurable-por-env/12.6-RESEARCH.md
@.planning/phases/12.6-branding-configurable-por-env/12.6-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SeedDemoUser → seed-from-env (demo email + name derivado + down simetrico)</name>
  <files>nemea-back/src/database/migrations/1773200000000-SeedDemoUser.ts</files>
  <read_first>
    - nemea-back/src/database/migrations/1773200000000-SeedDemoUser.ts (self — ya esta parametrizado; lineas 18-29 up INSERT + down DELETE; bloque lookup adminRoleId lineas 8-16)
    - nemea-back/src/database/data-source.ts (linea 1 `import 'dotenv/config'` — NO tocar; puebla process.env en migraciones)
    - .planning/phases/12.6-branding-configurable-por-env/12.6-PATTERNS.md (SeedDemoUser lineas 156-185)
    - .planning/phases/12.6-branding-configurable-por-env/12.6-RESEARCH.md (Pattern 3 lineas 125-139, Pitfall 4 lineas 204-208)
  </read_first>
  <action>
    En `up()`: leer `const demoEmail = process.env.DEMO_EMAIL ?? 'demo@hefesto.com'` y `const demoName = \`Demo ${process.env.APP_NAME ?? 'Hefesto'}\`` (name derivado de APP_NAME, sin env propio — R5). Reemplazar los literales `'demo@nemea.app'` / `'Demo Nemea'` del INSERT por placeholders parametrizados: `VALUES ($1, $2, true, $3) ON CONFLICT ("email") DO NOTHING` con el array `[demoEmail, demoName, adminRoleId]`. En `down()`: borrar el MISMO valor del env — `DELETE FROM "users" WHERE "email" = $1` con `[process.env.DEMO_EMAIL ?? 'demo@hefesto.com']` (Pitfall 4: el down debe usar la misma expresion, no el viejo string hardcodeado). MANTENER intacto el bloque de lookup del `adminRoleId` (lineas 8-16) que ya evita hardcodear el uuid. NO importar `dotenv` (ya lo hace data-source.ts). Explicit return types en up/down (`Promise<void>`), single quotes, sin `any`.
  </action>
  <verify>
    <automated>cd nemea-back && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `up()` define `demoEmail` desde `process.env.DEMO_EMAIL ?? 'demo@hefesto.com'` y `demoName` como `Demo ${process.env.APP_NAME ?? 'Hefesto'}`.
    - El INSERT usa placeholders `$1`/`$2`/`$3` y un values array; ya no contiene los literales `'demo@nemea.app'` ni `'Demo Nemea'`.
    - `down()` borra `WHERE "email" = $1` con el mismo valor del env (no el viejo literal).
    - El bloque de lookup de `adminRoleId` queda sin cambios.
    - `npx tsc --noEmit` (back) pasa.
  </acceptance_criteria>
  <done>SeedDemoUser siembra el demo desde env (name = Demo ${APP_NAME}) y su down es simetrico.</done>
</task>

<task type="auto">
  <name>Task 2: CreateUserTable → seed admin desde env, parametrizado (R9)</name>
  <files>nemea-back/src/database/migrations/1772369233961-CreateUserTable.ts</files>
  <read_first>
    - nemea-back/src/database/migrations/1772369233961-CreateUserTable.ts (self — lineas 13-15 INSERT admin con string interpolado; lineas 7-12 CREATE TYPE/CREATE TABLE — NO tocar)
    - nemea-back/src/database/migrations/1773200000000-SeedDemoUser.ts (sibling — patron parametrizado $1 a copiar)
    - .planning/phases/12.6-branding-configurable-por-env/12.6-PATTERNS.md (CreateUserTable lineas 189-209)
    - .planning/phases/12.6-branding-configurable-por-env/12.6-RESEARCH.md (Pattern 3 nota linea 139 — parametrizar admin INSERT)
  </read_first>
  <action>
    En `up()`: leer `const adminEmail = process.env.ADMIN_EMAIL ?? 'admin@hefesto.com'` y `const adminName = process.env.ADMIN_NAME ?? 'Admin'` (defaults neutros white-label — R9). Reemplazar el INSERT con string interpolado actual (`VALUES ('basualdofelipe@gmail.com', 'admin', 'Felipe Basualdo', true)`) por la version parametrizada: `INSERT INTO "users" ("email", "role", "name", "is_active") VALUES ($1, 'admin', $2, true)` con el array `[adminEmail, adminName]` (R9 exige query parametrizada — guard de SQL injection, nunca interpolar el env en el string SQL). MANTENER intactos los `CREATE TYPE`/`CREATE TABLE` (lineas 7-12) — esta migracion usa la columna enum `role` (era pre-tabla-roles), solo cambia el INSERT. Explicit return types, single quotes, sin `any`.
  </action>
  <verify>
    <automated>cd nemea-back && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `up()` define `adminEmail` desde `process.env.ADMIN_EMAIL ?? 'admin@hefesto.com'` y `adminName` desde `process.env.ADMIN_NAME ?? 'Admin'`.
    - El INSERT del admin usa placeholders `$1`/`$2` + values array; ya no contiene los literales `'basualdofelipe@gmail.com'` ni `'Felipe Basualdo'`.
    - Los `CREATE TYPE`/`CREATE TABLE` quedan sin cambios.
    - `npx tsc --noEmit` (back) pasa.
  </acceptance_criteria>
  <done>El admin se seedea desde ADMIN_EMAIL/ADMIN_NAME con defaults neutros y query parametrizada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Correr migraciones en DB fresca + inspeccionar filas seedeadas</name>
  <files>nemea-back/src/database/migrations/1772369233961-CreateUserTable.ts, nemea-back/src/database/migrations/1773200000000-SeedDemoUser.ts</files>
  <read_first>
    - nemea-back/package.json (linea 27: script migration:run; linea 25: passthrough `typeorm` que tambien acepta `migration:show`)
    - nemea-back/src/database/data-source.ts (config del DataSource + import dotenv)
  </read_first>
  <what-built>
    Las migraciones `CreateUserTable` (admin) y `SeedDemoUser` (demo) ahora siembran desde env con defaults neutros. Esto requiere correrlas contra una DB FRESCA y verificar las filas — el build/tsc pasa sin correrlas (los seeds son datos de runtime, no tipos), asi que sin este paso la verificacion es un falso positivo. Las DB actuales son descartables (se wipean, sin reconciliacion — out of scope).

    CAUTION (operacional, Senior PM + Gemini MEDIUM): el flujo es "wipe-and-run" sobre una DB fresca. La app es clone-and-run-local (cada dev corre contra su propia DB), asi que wipear-y-correr ES el flujo previsto — PERO antes de dropear/recrear, CONFIRMAR que `DATABASE_URL` (del `.env` del back) apunta a una DB LOCAL/descartable, NUNCA a una DB que contenga datos que el operador quiera conservar. El dato se pierde solo si se apunta a una DB poblada; contra una DB local fresca el resultado son filas seed limpias.
  </what-built>
  <action>
    Verificacion manual BLOQUEANTE (no hay harness automatizado de migraciones en el repo). Pasos exactos:
    0. SEGURIDAD: abrir el `.env` del back y CONFIRMAR que `DATABASE_URL` apunta a una DB LOCAL/descartable (no a una DB de prod ni a una con datos que importen). El paso 1 wipea esa DB.
    1. Asegurar una DB FRESCA (vacia): dropear/recrear la base de `DATABASE_URL` del back (o usar una DB de prueba limpia). NO reconciliar una DB ya seedeada.
    2. (Pre-check barato — sugerido por Gemini) confirmar que solo las migraciones esperadas estan pendientes ANTES de correrlas: `cd nemea-back && npm run typeorm -- migration:show -d src/database/data-source`. Revisar que `CreateUserTable` y `SeedDemoUser` figuren como pendientes (sin `[X]`) y que no haya migraciones inesperadas.
    3. SIN envs de branding seteados, correr: `cd nemea-back && npm run migration:run` (script en nemea-back/package.json:27 → `npm run typeorm -- migration:run -d src/database/data-source`).
    4. Inspeccionar: `SELECT email, name FROM users ORDER BY email;` → esperado admin `admin@hefesto.com`/`Admin` y demo `demo@hefesto.com`/`Demo Hefesto`.
    5. (Override) Dropear/recrear la DB (confirmar de nuevo que `DATABASE_URL` es local/descartable), exportar `APP_NAME=Foo`, `DEMO_EMAIL=demo@foo.com`, `ADMIN_EMAIL=x@y.com`, `ADMIN_NAME=Bar`, repetir `npm run migration:run` e inspeccionar → admin `x@y.com`/`Bar`, demo `demo@foo.com`/`Demo Foo`.
    6. Confirmar que NO aparecen `basualdofelipe@gmail.com`, `Felipe Basualdo`, `demo@nemea.app` ni `Demo Nemea` en ninguna corrida.
  </action>
  <how-to-verify>
    1. CONFIRMAR `DATABASE_URL` apunta a DB local/descartable (paso 0 — el run wipea esa DB).
    2. DB fresca vacia → `cd nemea-back && npm run typeorm -- migration:show -d src/database/data-source` (confirmar solo migraciones esperadas pendientes) → `npm run migration:run` sin envs → `SELECT email, name FROM users ORDER BY email;`.
    3. Repetir con `APP_NAME=Foo DEMO_EMAIL=demo@foo.com ADMIN_EMAIL=x@y.com ADMIN_NAME=Bar` en DB fresca.
  </how-to-verify>
  <verify>
    <automated>MISSING — verificacion manual de migraciones (no hay harness automatizado en el repo); ver pasos en <action></automated>
  </verify>
  <acceptance_criteria>
    - Antes del wipe, se confirmo que `DATABASE_URL` apunta a una DB local/descartable (no prod, no datos que importen).
    - `migration:show` confirmo que solo las migraciones esperadas (incluidas CreateUserTable + SeedDemoUser) estaban pendientes antes del run.
    - DB fresca + sin envs → users tiene exactamente `admin@hefesto.com`/`Admin` y `demo@hefesto.com`/`Demo Hefesto`.
    - DB fresca + envs override → users refleja `x@y.com`/`Bar` y `demo@foo.com`/`Demo Foo`.
    - Ninguna corrida produce las filas viejas (basualdofelipe / Felipe Basualdo / demo@nemea.app / Demo Nemea).
    - `migration:run` completa sin error en ambas corridas.
  </acceptance_criteria>
  <done>En DB fresca (DATABASE_URL local confirmado), las migraciones siembran el admin y demo desde env (defaults Hefesto sin envs; override reflejado con envs), sin filas viejas; migration:show confirmo el set de migraciones pendiente antes del run.</done>
  <resume-signal>Escribi "approved" si las filas seedeadas coinciden con los defaults/override (y confirmaste DATABASE_URL local + migration:show), o describi la discrepancia.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy env → migracion (runtime) | `DEMO_EMAIL`/`ADMIN_EMAIL`/`ADMIN_NAME`/`APP_NAME` se leen en up()/down() de las migraciones |
| migracion → PostgreSQL | El valor del env termina en un INSERT contra la tabla users |
| operador → DATABASE_URL | El wipe-and-run del checkpoint destruye lo que apunte DATABASE_URL — debe ser local/descartable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.6-03 | Tampering (SQL injection) | INSERT seed admin/demo en migraciones | mitigate | Query parametrizada `$1`/`$2`/`$3` + values array (R9); NUNCA interpolar el env en el string SQL — `SeedDemoUser` ya tiene el patron a copiar |
| T-12.6-04 | Information Disclosure | seed admin (CreateUserTable) | mitigate | Default neutro `admin@hefesto.com` / `Admin` reemplaza el email/nombre personal del autor; coherencia white-label |
| T-12.6-09 | Denial (data loss) | checkpoint wipe-and-run (Task 3) | mitigate | Paso 0 obligatorio: confirmar `DATABASE_URL` apunta a DB local/descartable antes del drop; `migration:show` previo confirma el set pendiente; perdida de datos solo si se apunta a una DB poblada (operacional) |
| T-12.6-SC | Tampering | npm/pip/cargo installs | accept | Cero deps npm nuevas (constraint v1.1) — no se instala ningun paquete |
</threat_model>

<verification>
- `cd nemea-back && npx tsc --noEmit` sin errores.
- Task 3 (manual, bloqueante): `DATABASE_URL` local confirmado + `migration:show` previo + filas seedeadas correctas en DB fresca con defaults y con override.
- `grep -i "demo@nemea.app\|basualdofelipe\|Felipe Basualdo\|Demo Nemea" nemea-back/src/database/migrations` vacio.
</verification>

<success_criteria>
- DB fresca sin envs → admin `admin@hefesto.com`/`Admin` + demo `demo@hefesto.com`/`Demo Hefesto`.
- DB fresca con override → filas reflejan los envs (`Demo Foo`, etc.).
- Ambos INSERT parametrizados; down de SeedDemoUser simetrico al env.
- El checkpoint confirma DATABASE_URL local/descartable y corre migration:show antes del run.
</success_criteria>

<output>
Create `.planning/phases/12.6-branding-configurable-por-env/12.6-02-SUMMARY.md` when done
</output>

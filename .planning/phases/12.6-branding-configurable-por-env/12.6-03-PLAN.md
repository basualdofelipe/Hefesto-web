---
phase: 12.6-branding-configurable-por-env
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - nemea-front/src/constants/branding.ts
  - nemea-front/src/app/layout.tsx
  - nemea-front/src/app/(auth)/login/page.tsx
  - nemea-front/src/components/layout/AppSidebar.tsx
  - nemea-front/src/app/(auth)/acceso-denegado/page.tsx
  - nemea-front/src/app/(auth)/login/DemoLoginButton.tsx
autonomous: true
requirements: [R1, R4, R10]
must_haves:
  truths:
    - "Sin env, el title de la pestaña y los 3 alt del logo dicen 'Hefesto'; con NEXT_PUBLIC_APP_NAME=Foo (rebuild) dicen 'Foo'"
    - "Sin env, acceso-denegado muestra el heading 'HEFESTO', el texto '...acceder a Hefesto.' y el contacto admin@hefesto.com"
    - "El boton demo postea demo@hefesto.com sin env (= default del back); con NEXT_PUBLIC_DEMO_EMAIL=demo@foo.com (rebuild) postea demo@foo.com"
    - "grep -i basualdofelipe sobre nemea-front/src no devuelve nada"
  artifacts:
    - path: "nemea-front/src/constants/branding.ts"
      provides: "Fuente unica de branding del front (APP_NAME, DEMO_EMAIL, CONTACT_EMAIL) con literales NEXT_PUBLIC_*"
      contains: "process.env.NEXT_PUBLIC_APP_NAME"
  key_links:
    - from: "nemea-front/src/app/(auth)/login/DemoLoginButton.tsx"
      to: "nemea-front/src/constants/branding.ts"
      via: "import DEMO_EMAIL"
      pattern: "from '@/constants/branding'"
    - from: "nemea-front/src/app/(auth)/acceso-denegado/page.tsx"
      to: "nemea-front/src/constants/branding.ts"
      via: "import APP_NAME + CONTACT_EMAIL"
      pattern: "CONTACT_EMAIL"
---

<objective>
Crear la fuente unica de branding del front (`src/constants/branding.ts`, D-03) leyendo las `NEXT_PUBLIC_*` con literales completos (inlining de Next) + fallback, y migrar todos los puntos de uso a importar de ahi: `layout.tsx` (title, R1), los 3 `alt='NEMEA'` (login + 2 en sidebar, R1), `acceso-denegado` (heading + texto + contacto, R1 + R10) y `DemoLoginButton` (demo email, R4). Elimina la marca "Nemea" y el email personal del autor de todo el texto visible del front.

Purpose: white-label por env del lado cliente; el contacto en acceso-denegado dejaba expuesto el correo personal del autor a cualquier usuario bloqueado (incluido el demo publico) — R10 lo neutraliza. Repo independiente del back (sin overlap de archivos) → Wave 1 en paralelo con plan 01.
Output: `constants/branding.ts` (front) + 5 archivos consumidores; sin "Nemea"/`basualdofelipe` en `nemea-front/src`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.6-branding-configurable-por-env/12.6-SPEC.md
@.planning/phases/12.6-branding-configurable-por-env/12.6-CONTEXT.md
@.planning/phases/12.6-branding-configurable-por-env/12.6-RESEARCH.md
@.planning/phases/12.6-branding-configurable-por-env/12.6-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear branding.ts (front) con literales NEXT_PUBLIC_*</name>
  <files>nemea-front/src/constants/branding.ts</files>
  <read_first>
    - nemea-front/src/constants/tiendanube.ts (analog exacto: modulo flat de `export const`, sin default)
    - .planning/phases/12.6-branding-configurable-por-env/12.6-RESEARCH.md (Pattern 1 lineas 92-105, nota inlining linea 105, Pitfall 2 lineas 190-196)
    - .planning/phases/12.6-branding-configurable-por-env/12.6-PATTERNS.md (front branding.ts lineas 35-56, CRITICAL inlining rule linea 54)
  </read_first>
  <action>
    Crear `nemea-front/src/constants/branding.ts` como modulo flat de named exports (mismo shape que `tiendanube.ts`): `export const APP_NAME = process.env.NEXT_PUBLIC_APP_NAME ?? 'Hefesto'`, `export const DEMO_EMAIL = process.env.NEXT_PUBLIC_DEMO_EMAIL ?? 'demo@hefesto.com'`, `export const CONTACT_EMAIL = process.env.NEXT_PUBLIC_CONTACT_EMAIL ?? 'admin@hefesto.com'`. CRITICO (inlining de Next): escribir el nombre literal COMPLETO `process.env.NEXT_PUBLIC_X` en cada export — NO destructurar (`const { NEXT_PUBLIC_X } = process.env`) ni indexar dinamicamente (`process.env['NEXT_PUBLIC_' + x]`), porque el compilador solo reemplaza la forma literal y el cliente recibiria `undefined`. El default `'demo@hefesto.com'` DEBE coincidir con el default del back (R4, constraint "defaults front == back"). Agregar un comentario de una linea arriba: las `NEXT_PUBLIC_*` se inlinean en build time → rebuild requerido. Single quotes, sin `any`.
  </action>
  <verify>
    <automated>cd nemea-front && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `nemea-front/src/constants/branding.ts` existe y exporta `APP_NAME`, `DEMO_EMAIL`, `CONTACT_EMAIL`.
    - Cada export usa el literal completo `process.env.NEXT_PUBLIC_APP_NAME` / `_DEMO_EMAIL` / `_CONTACT_EMAIL` con `??` y los fallbacks exactos `'Hefesto'` / `'demo@hefesto.com'` / `'admin@hefesto.com'`.
    - NO hay destructuring ni indexado dinamico de `process.env`.
    - `npx tsc --noEmit` (front) pasa.
  </acceptance_criteria>
  <done>El branding del front vive en un modulo unico con literales NEXT_PUBLIC_* inlineables.</done>
</task>

<task type="auto">
  <name>Task 2: Migrar layout, login, sidebar y DemoLoginButton a branding.ts</name>
  <files>nemea-front/src/app/layout.tsx, nemea-front/src/app/(auth)/login/page.tsx, nemea-front/src/components/layout/AppSidebar.tsx, nemea-front/src/app/(auth)/login/DemoLoginButton.tsx</files>
  <read_first>
    - nemea-front/src/app/layout.tsx (lineas 36-39: metadata estatico, title: 'Nemea')
    - nemea-front/src/app/(auth)/login/page.tsx (linea 58: alt='NEMEA' en next/image)
    - nemea-front/src/components/layout/AppSidebar.tsx (lineas 83 y 90: dos alt='NEMEA'; 'use client' en linea 1)
    - nemea-front/src/app/(auth)/login/DemoLoginButton.tsx (linea 8: const DEMO_EMAIL = 'demo@nemea.app'; uso en signIn linea 21-22)
    - nemea-front/src/constants/branding.ts (creado en Task 1)
    - .planning/phases/12.6-branding-configurable-por-env/12.6-PATTERNS.md (layout lineas 213-224, login page 228-242, AppSidebar 246-255, DemoLoginButton 281-291; import convention lineas 358-363)
  </read_first>
  <action>
    Todos importan via alias `@/`: `import { APP_NAME } from '@/constants/branding'` (y `DEMO_EMAIL` donde aplique). En `layout.tsx`: `title: APP_NAME` (era `'Nemea'`); `metadata` es objeto estatico server/build, el import resuelve en build time. En `login/page.tsx`: el `alt='NEMEA'` del `<Image>` → `alt={APP_NAME}`; el `src='/brand/...'` del logo NO se toca (logo out of scope, asset swappable). En `AppSidebar.tsx`: ambos `alt='NEMEA'` (lineas 83 y 90) → `alt={APP_NAME}`; es `'use client'` pero el literal `NEXT_PUBLIC_APP_NAME` dentro de branding.ts igual se inlinea en build, asi que el import funciona client-side; los `src` de logo sin cambios. En `DemoLoginButton.tsx`: eliminar la const local `DEMO_EMAIL = 'demo@nemea.app'` e importar `DEMO_EMAIL` de `'@/constants/branding'` (= `NEXT_PUBLIC_DEMO_EMAIL ?? 'demo@hefesto.com'`); el `signIn('credentials', { email: DEMO_EMAIL, ... })` queda igual usando el import. Respetar el orden de imports de CONVENTIONS (grupo `@/`). Single quotes, sin `any`.
  </action>
  <verify>
    <automated>cd nemea-front && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `layout.tsx` importa `APP_NAME` de `@/constants/branding` y `title: APP_NAME`; ya no contiene `'Nemea'`.
    - `login/page.tsx` y `AppSidebar.tsx` (ambos alt) usan `alt={APP_NAME}`; ningun `alt='NEMEA'` queda; los `src` de logo intactos.
    - `DemoLoginButton.tsx` ya no define la const local ni contiene `'demo@nemea.app'`; importa `DEMO_EMAIL` de `@/constants/branding`.
    - `npx tsc --noEmit` (front) pasa.
  </acceptance_criteria>
  <done>Title, los 3 alt del logo y el demo email del boton salen de branding.ts.</done>
</task>

<task type="auto">
  <name>Task 3: acceso-denegado → APP_NAME + CONTACT_EMAIL (R1 + R10)</name>
  <files>nemea-front/src/app/(auth)/acceso-denegado/page.tsx</files>
  <read_first>
    - nemea-front/src/app/(auth)/acceso-denegado/page.tsx (lineas 16-24: heading 'NEMEA' :17, texto '...acceder a Nemea...' :23, contacto 'basualdofelipe@gmail.com' :24)
    - nemea-front/src/constants/branding.ts (creado en Task 1 — APP_NAME, CONTACT_EMAIL)
    - .planning/phases/12.6-branding-configurable-por-env/12.6-PATTERNS.md (acceso-denegado lineas 259-277)
    - .planning/phases/12.6-branding-configurable-por-env/12.6-SPEC.md (R10 lineas 71-74: acceptance + grep basualdofelipe)
  </read_first>
  <action>
    Importar `{ APP_NAME, CONTACT_EMAIL }` de `'@/constants/branding'`. Tres reemplazos: (1) heading :17 `NEMEA` → `{APP_NAME}` — el uppercase es CSS (`tracking-widest`/`engraving-title`), NO el string, asi que NO tocar las clases; (2) texto :23 `...acceder a Nemea.` → `...acceder a {APP_NAME}.`; (3) contacto :24 `...en basualdofelipe@gmail.com.` → `...en {CONTACT_EMAIL}.` (R10, default neutro `admin@hefesto.com`). Esto saca el correo personal del autor de una pagina visible a cualquier usuario bloqueado (incluido el demo publico). Single quotes, sin `any`.
  </action>
  <verify>
    <automated>cd nemea-front && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `acceso-denegado/page.tsx` importa `APP_NAME` y `CONTACT_EMAIL` de `@/constants/branding`.
    - El heading usa `{APP_NAME}`; el texto usa `{APP_NAME}`; el contacto usa `{CONTACT_EMAIL}`.
    - Las clases CSS del heading (`engraving-title`/`tracking-widest`) quedan sin cambios.
    - `grep -i basualdofelipe nemea-front/src/app/(auth)/acceso-denegado/page.tsx` no devuelve nada.
    - `npx tsc --noEmit` (front) pasa.
  </acceptance_criteria>
  <done>El heading, texto y contacto de acceso-denegado salen de env; el email personal del autor ya no aparece.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build env → bundle cliente | Las `NEXT_PUBLIC_*` se inlinean en build time al bundle servido al browser |
| pagina acceso-denegado → usuario bloqueado | Texto visible a cualquier usuario sin acceso (incluido el demo publico) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.6-05 | Information Disclosure | acceso-denegado/page.tsx (contacto :24) | mitigate | R10: mover el contacto a `NEXT_PUBLIC_CONTACT_EMAIL ?? 'admin@hefesto.com'`; `grep -i basualdofelipe` sobre `nemea-front/src` debe quedar vacio — saca el correo personal del autor de una pagina publica |
| T-12.6-06 | (build correctness) | constants/branding.ts (front) | mitigate | Escribir los `NEXT_PUBLIC_*` como literales completos (sin destructuring/indexado) para que Next los inline y el cliente no reciba `undefined` |
| T-12.6-SC | Tampering | npm/pip/cargo installs | accept | Cero deps npm nuevas (constraint v1.1) — no se instala ningun paquete |
</threat_model>

<verification>
- `cd nemea-front && npx tsc --noEmit` sin errores.
- `grep -i basualdofelipe nemea-front/src` vacio (R10).
- `grep -i nemea nemea-front/src` (excluyendo el logo `src` de carpetas/rutas internas que son out of scope) no devuelve la marca en texto visible.
</verification>

<success_criteria>
- branding.ts (front) es la fuente unica con literales NEXT_PUBLIC_* + fallback; default demo == back.
- title, 3 alt, heading/texto/contacto de acceso-denegado y demo email salen del env con fallback.
- Sin `basualdofelipe` en `nemea-front/src`; sin "Nemea" en texto visible.
</success_criteria>

<output>
Create `.planning/phases/12.6-branding-configurable-por-env/12.6-03-SUMMARY.md` when done
</output>

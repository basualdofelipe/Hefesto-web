---
phase: 12.6-branding-configurable-por-env
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nemea-back/src/constants/branding.ts
  - nemea-back/src/config/env.validation.ts
  - nemea-back/src/auth/auth.service.ts
  - nemea-back/src/main.ts
  - nemea-back/src/auth/auth.service.spec.ts
autonomous: true
requirements: [R2, R3, R6, R7]
must_haves:
  truths:
    - "Con DEMO_EMAIL sin setear, demo-login acepta demo@hefesto.com y rechaza otro email (401)"
    - "Con DEMO_EMAIL=demo@foo.com, demo-login acepta demo@foo.com y rechaza demo@hefesto.com"
    - "El titulo de Swagger es 'Hefesto API' sin env; '<APP_NAME> API' con APP_NAME seteado"
    - "Un DEMO_EMAIL/ADMIN_EMAIL con formato invalido (demo@demo) hace fallar la validacion Joi al bootear"
    - "El test de override falla si el codigo hardcodea el demo email en vez de leer el env"
  artifacts:
    - path: "nemea-back/src/constants/branding.ts"
      provides: "Fuente unica de branding del back (APP_NAME, ADMIN_EMAIL, ADMIN_NAME, demo email getter)"
      contains: "process.env.APP_NAME"
    - path: "nemea-back/src/config/env.validation.ts"
      provides: "Validacion Joi de DEMO_EMAIL y ADMIN_EMAIL al boot"
      contains: "DEMO_EMAIL"
    - path: "nemea-back/src/auth/auth.service.spec.ts"
      provides: "7 asserts retargeteados + test de override"
      contains: "DEMO_EMAIL"
  key_links:
    - from: "nemea-back/src/auth/auth.service.ts"
      to: "nemea-back/src/constants/branding.ts"
      via: "import del demo email (getter)"
      pattern: "from '\\.\\./constants/branding'"
    - from: "nemea-back/src/main.ts"
      to: "nemea-back/src/constants/branding.ts"
      via: "import APP_NAME para Swagger"
      pattern: "APP_NAME"
---

<objective>
Crear la fuente unica de branding del back (`src/constants/branding.ts`) leyendo `process.env` con fallbacks neutros (D-01/D-02), agregar la validacion Joi de `DEMO_EMAIL`/`ADMIN_EMAIL` al boot (R6), migrar `auth.service.ts` (demo email, R3) y `main.ts` (Swagger title/description, R2) a importar de ese modulo, y actualizar el spec del auth (7 asserts a la fuente unica + test de override que prueba R7 de verdad).

Purpose: centraliza la identidad del back en un solo modulo que tanto services como migraciones (plan 02) importan, evitando dos fuentes que diverjan. El test de override es el unico punto de riesgo tecnico de la fase (const congelada da falso verde).
Output: `constants/branding.ts` (back) + 4 archivos consumidores/validacion actualizados + suite back verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.6-branding-configurable-por-env/12.6-SPEC.md
@.planning/phases/12.6-branding-configurable-por-env/12.6-CONTEXT.md
@.planning/phases/12.6-branding-configurable-por-env/12.6-RESEARCH.md
@.planning/phases/12.6-branding-configurable-por-env/12.6-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear branding.ts (back) + Joi de DEMO_EMAIL/ADMIN_EMAIL</name>
  <files>nemea-back/src/constants/branding.ts, nemea-back/src/config/env.validation.ts</files>
  <read_first>
    - nemea-back/src/constants/tiendanube.ts (analog exacto: modulo flat de `export const`, sin default, sin clase)
    - nemea-back/src/config/env.validation.ts (forma de las entradas Joi existentes: `Joi.string()...default(...)`)
    - nemea-back/src/database/data-source.ts (linea 1 `import 'dotenv/config'` — NO tocar; es lo que puebla process.env en migraciones)
    - .planning/phases/12.6-branding-configurable-por-env/12.6-RESEARCH.md (Pattern 2 lineas 112-121, Open Question lineas 265-268, Joi TLD caveat linea 243)
    - .planning/phases/12.6-branding-configurable-por-env/12.6-PATTERNS.md (seccion back branding.ts lineas 60-78)
  </read_first>
  <action>
    Crear `nemea-back/src/constants/branding.ts` como modulo flat de named exports (mismo shape que `tiendanube.ts`, sin `ConfigService` por D-02 porque las migraciones corren fuera del DI de Nest). Exports literales con fallback en codigo: `APP_NAME = process.env.APP_NAME ?? 'Hefesto'`, `ADMIN_EMAIL = process.env.ADMIN_EMAIL ?? 'admin@hefesto.com'`, `ADMIN_NAME = process.env.ADMIN_NAME ?? 'Admin'`. Para el demo email usar la Opcion A (recomendada por RESEARCH para que el test de override funcione sin `jest.resetModules()`): exportar un getter `getDemoEmail(): string` con explicit return type que lea `process.env.DEMO_EMAIL ?? 'demo@hefesto.com'` en cada llamada — NO una const de modulo congelada (Pitfall 1). Todas las funciones con explicit return type, single quotes, sin `any` (CLAUDE.md). En `env.validation.ts` agregar dos keys siguiendo la forma existente: `DEMO_EMAIL: Joi.string().email().default('demo@hefesto.com')` y `ADMIN_EMAIL: Joi.string().email().default('admin@hefesto.com')`. NO pasar `{ tlds: { allow: false } }` — el acceptance de R6 exige que `demo@demo` falle el boot, lo que el `.email()` por default ya garantiza. `ADMIN_NAME` es texto libre, NO se valida en Joi.
  </action>
  <verify>
    <automated>cd nemea-back && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `nemea-back/src/constants/branding.ts` existe y exporta `APP_NAME`, `ADMIN_EMAIL`, `ADMIN_NAME` (const) y `getDemoEmail` (funcion con return type `string`).
    - El RHS de cada export usa `process.env.X ?? '<fallback>'` con los fallbacks exactos `'Hefesto'` / `'admin@hefesto.com'` / `'Admin'` / `'demo@hefesto.com'`.
    - `env.validation.ts` contiene las lineas `DEMO_EMAIL: Joi.string().email().default('demo@hefesto.com')` y `ADMIN_EMAIL: Joi.string().email().default('admin@hefesto.com')` sin `tlds`.
    - `npx tsc --noEmit` (back) pasa sin errores nuevos.
    - El modulo NO importa ni usa `ConfigService` (D-02).
  </acceptance_criteria>
  <done>El branding del back vive en un unico modulo leyendo env con fallback; Joi valida DEMO_EMAIL/ADMIN_EMAIL al boot.</done>
</task>

<task type="auto">
  <name>Task 2: Migrar auth.service.ts (demo email) y main.ts (Swagger) a branding.ts</name>
  <files>nemea-back/src/auth/auth.service.ts, nemea-back/src/main.ts</files>
  <read_first>
    - nemea-back/src/auth/auth.service.ts (linea 15 const DEMO_USER_EMAIL; usos en validateDemoLogin: gate DEMO_LOGIN_ENABLED ~76, pin check ~81, lookup ~86)
    - nemea-back/src/main.ts (lineas 45-48: guard NODE_ENV !== production + DocumentBuilder .setTitle/.setDescription)
    - nemea-back/src/constants/branding.ts (creado en Task 1 — `getDemoEmail`, `APP_NAME`)
    - .planning/phases/12.6-branding-configurable-por-env/12.6-PATTERNS.md (auth.service lineas 82-105, main.ts lineas 109-127)
  </read_first>
  <action>
    En `auth.service.ts`: reemplazar la const de modulo `DEMO_USER_EMAIL = 'demo@nemea.app'` por el consumo de `getDemoEmail()` importado de `'../constants/branding'`. En `validateDemoLogin`, llamar `getDemoEmail()` en el pin check (`if (email !== getDemoEmail())`) y en el lookup (`findActiveByEmail(getDemoEmail())`) — leer el env en call-time (R3/R7, Pitfall 1). MANTENER intacto el gate `DEMO_LOGIN_ENABLED` (Security V2/spoofing: el env solo cambia *cual* es el unico email permitido, NUNCA abre el endpoint a email arbitrario). Si el simbolo exportado `DEMO_USER_EMAIL` es referenciado en otro lado, conservarlo derivado de la misma fuente (`getDemoEmail()`) para una sola fuente de verdad; de lo contrario eliminarlo. En `main.ts`: importar `APP_NAME` de `'./constants/branding'` y usar `.setTitle(\`${APP_NAME} API\`)` y `.setDescription(\`${APP_NAME} leather goods cost management API\`)` (R2) — NO tocar el guard `if (process.env.NODE_ENV !== 'production')` ni el resto del DocumentBuilder. Explicit return types, single quotes, sin `any`.
  </action>
  <verify>
    <automated>cd nemea-back && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `auth.service.ts` ya no contiene el literal `'demo@nemea.app'`.
    - `validateDemoLogin` lee el demo email via `getDemoEmail()` (call-time) en el pin check y en el lookup; el gate `DEMO_LOGIN_ENABLED` sigue presente y previo al pin check.
    - `main.ts` importa `APP_NAME` de `'./constants/branding'`; `.setTitle` y `.setDescription` lo templatean; ya no contienen el literal `'Nemea'`.
    - El guard `NODE_ENV !== 'production'` de Swagger queda sin cambios.
    - `npx tsc --noEmit` (back) pasa.
  </acceptance_criteria>
  <done>El demo email y el titulo de Swagger salen de branding.ts; el endpoint demo-login sigue pineado a un unico email.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Actualizar 7 asserts del spec + test de override (R7)</name>
  <files>nemea-back/src/auth/auth.service.spec.ts</files>
  <behavior>
    - Con DEMO_EMAIL sin override (default): `validateDemoLogin(getDemoEmail())` resuelve; un email distinto lanza UnauthorizedException (los 7 asserts existentes pasan apuntando a la fuente unica).
    - Override: con `process.env.DEMO_EMAIL = 'demo@foo.com'` + gate `DEMO_LOGIN_ENABLED` mockeado a `'true'`, `validateDemoLogin('demo@foo.com')` resuelve y `validateDemoLogin('demo@nemea.app')` (o el viejo default) lanza UnauthorizedException.
    - Contrato de fallo (R7): si el codigo hardcodea el email en vez de leer el env, el test de override DEBE fallar.
  </behavior>
  <read_first>
    - nemea-back/src/auth/auth.service.spec.ts (describe validateDemoLogin lineas 214-307; mockConfigService ~55, mockUsersService ~45, mockJwtService ~51, mockDemoUser ~217)
    - nemea-back/src/constants/branding.ts (getDemoEmail — la fuente unica a referenciar en los asserts)
    - .planning/phases/12.6-branding-configurable-por-env/12.6-PATTERNS.md (spec lineas 295-322: lista exacta de los 7 asserts en lineas 219/246/261/270/279/301/304 + el `it()` de override)
    - .planning/phases/12.6-branding-configurable-por-env/12.6-RESEARCH.md (Pitfall 1 lineas 173-188, Pitfall 3 lineas 197-202)
  </read_first>
  <action>
    Retargetear los 7 asserts que hoy usan el literal `'demo@nemea.app'` (lineas ~219, 246, 261, 270, 279, 301, 304) para que referencien `getDemoEmail()` importado de `'../constants/branding'` en vez del string literal; alinear tambien `mockDemoUser.name` (linea ~220) con el nombre derivado. Agregar un `it('respeta DEMO_EMAIL del env (override)')` nuevo: dentro setear `process.env.DEMO_EMAIL = 'demo@foo.com'`, llamar `mockConfigService.get.mockReturnValue('true')` (Pitfall 3 — sin esto `validateDemoLogin` corta en el gate antes de comparar el email), mockear `mockUsersService.findActiveByEmail` y `mockJwtService.sign`, luego `await expect(service.validateDemoLogin('demo@foo.com')).resolves.toBeDefined()` y `await expect(service.validateDemoLogin('demo@nemea.app')).rejects.toThrow(UnauthorizedException)`. Como branding.ts usa el getter (Opcion A, call-time read), NO se necesita `jest.resetModules()` ni re-import dinamico. Restaurar `process.env.DEMO_EMAIL` en un `afterEach`/`finally` para no contaminar otros tests. Single quotes, sin `any`.
  </action>
  <verify>
    <automated>cd nemea-back && npm test -- auth.service</automated>
  </verify>
  <acceptance_criteria>
    - `auth.service.spec.ts` ya no contiene el literal `'demo@nemea.app'` en los asserts de happy-path/pin (referencian `getDemoEmail()`).
    - Existe un `it()` cuyo nombre menciona override/env que setea `process.env.DEMO_EMAIL = 'demo@foo.com'` y mockea el gate a `'true'`.
    - `npm test -- auth.service` (back) verde.
    - Contrato de fallo verificable: revertir momentaneamente `auth.service.ts` a un email hardcodeado hace fallar el test de override (no se commitea — solo confirma que el test prueba R7).
    - `process.env.DEMO_EMAIL` queda restaurado tras el test (no contamina la suite).
  </acceptance_criteria>
  <done>Los asserts apuntan a la fuente unica y el test de override prueba R7 de verdad (falla si se hardcodea).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (demo-login) | El body `{ email }` del POST /auth/demo-login es input no confiable |
| deploy env → proceso back | Las env vars `DEMO_EMAIL`/`ADMIN_EMAIL`/`APP_NAME` cruzan al runtime/boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.6-01 | Spoofing/Elevation | validateDemoLogin (auth.service.ts) | mitigate | Mantener el gate `DEMO_LOGIN_ENABLED` + el pin check `email === getDemoEmail()`; el env solo cambia *cual* email unico se acepta, NUNCA abre el endpoint a email arbitrario (V2 Authentication) |
| T-12.6-02 | Input Validation (V5) | env.validation.ts | mitigate | Joi `.email()` sobre `DEMO_EMAIL`/`ADMIN_EMAIL` al boot rechaza formato invalido (`demo@demo` falla); el DTO conserva `@IsEmail()` |
| T-12.6-SC | Tampering | npm/pip/cargo installs | accept | Cero deps npm nuevas (constraint v1.1) — no se instala ningun paquete; auditoria de legitimidad no aplica |
</threat_model>

<verification>
- `cd nemea-back && npx tsc --noEmit` sin errores nuevos.
- `cd nemea-back && npm test -- auth.service` verde (7 asserts retargeteados + override).
- `grep -i "demo@nemea.app" nemea-back/src/auth/auth.service.ts` vacio (el spec puede conservar el viejo default solo como caso de rechazo en el override test).
</verification>

<success_criteria>
- branding.ts (back) es la fuente unica de APP_NAME/ADMIN_EMAIL/ADMIN_NAME/demo email, sin ConfigService.
- demo-login acepta el email del env y rechaza cualquier otro (401); con override acepta el nuevo.
- Swagger title = `<APP_NAME> API`; Joi valida DEMO_EMAIL/ADMIN_EMAIL al boot.
- Suite back verde; el test de override falla si el email se hardcodea.
</success_criteria>

<output>
Create `.planning/phases/12.6-branding-configurable-por-env/12.6-01-SUMMARY.md` when done
</output>

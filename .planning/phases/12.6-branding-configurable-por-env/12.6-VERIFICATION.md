---
phase: 12.6-branding-configurable-por-env
verified: 2026-06-04T00:00:00Z
status: passed
score: 10/10 must-haves verified
overrides_applied: 0
human_verification: []
resolution:
  - item: "R8 gap — front README showed NEXT_PUBLIC_APP_NAME=Nemea + 'web app for Nemea'"
    decision: "fixed"
    detail: "User chose fix. nemea-front/README.md lines 3 + 88 changed Nemea -> Hefesto (commit e037623, nemea-front feature branch). Repo-name refs (nemea-front/nemea-back) left per CONTEXT out-of-scope. Grep confirms no in-scope Nemea remains in the front README. R8 now fully satisfied."
gaps: []
---

# Phase 12.6: Branding configurable por env — Verification Report

**Phase Goal:** El nombre de la app y la identidad del demo/admin/contacto dejan de estar hardcodeados ("Nemea") y se leen de env con fallback en codigo (default Hefesto / demo@hefesto.com / admin@hefesto.com), de modo que rebrandear = setear envs + reemplazar el logo + rebuild (white-label ready).
**Verified:** 2026-06-04
**Status:** human_needed — 9/10 truths verified; one documentation gap requires human decision
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| R1 | Sin env, front muestra "Hefesto" en title/alt/heading/texto; con APP_NAME seteado muestra ese valor; grep -i nemea sobre src/*.ts*.tsx (non-test) solo devuelve internos out-of-scope | VERIFIED | `layout.tsx:38 title: APP_NAME`, `login/page.tsx:59 alt={APP_NAME}`, `AppSidebar.tsx:85,92 alt={APP_NAME}`, `acceso-denegado/page.tsx:18,24 {APP_NAME}`. All import from `@/constants/branding`. `nemea-front/src/constants/branding.ts:2 APP_NAME = process.env.NEXT_PUBLIC_APP_NAME ?? 'Hefesto'`. Grep of `.ts/.tsx` non-test under src returns zero brand-visible matches. |
| R2 | Swagger title/description usan APP_NAME del env; sin env = "Hefesto API" | VERIFIED | `main.ts:10 import APP_NAME from './constants/branding'`, `main.ts:48-49 .setTitle(\`${APP_NAME} API\`)` and `.setDescription(\`${APP_NAME} leather goods cost management API\`)`. `nemea-back/src/constants/branding.ts:12 APP_NAME = process.env.APP_NAME ?? 'Hefesto'`. Guard `NODE_ENV !== 'production'` intact at line 46. |
| R3 | demo-login acepta el email del env y rechaza cualquier otro; con override acepta el nuevo email | VERIFIED | `auth.service.ts:8 import { getDemoEmail } from '../constants/branding'`, `auth.service.ts:75 if (email !== getDemoEmail())`, `auth.service.ts:80 findActiveByEmail(getDemoEmail())`. Getter reads `process.env.DEMO_EMAIL ?? 'demo@hefesto.com'` at call-time (not frozen at module load). Old literal `demo@nemea.app` absent from auth.service.ts. |
| R4 | DemoLoginButton postea el email del env (default demo@hefesto.com); defaults front == back | VERIFIED | `DemoLoginButton.tsx:8 import { DEMO_EMAIL } from '@/constants/branding'`, `DemoLoginButton.tsx:22 email: DEMO_EMAIL`. `nemea-front/src/constants/branding.ts:3-4 DEMO_EMAIL = process.env.NEXT_PUBLIC_DEMO_EMAIL ?? 'demo@hefesto.com'`. Matches back default exactly. |
| R5 | DB fresca sin envs: demo seeded as demo@hefesto.com / "Demo Hefesto"; con APP_NAME=Foo + DEMO_EMAIL=demo@foo.com: "Demo Foo" / demo@foo.com | VERIFIED | `SeedDemoUser.ts:7-8`: `const demoEmail = process.env.DEMO_EMAIL ?? 'demo@hefesto.com'` and `const demoName = \`Demo ${process.env.APP_NAME ?? 'Hefesto'}\``. Parametrized INSERT with `[$1, $2, $3]`. `down()` deletes using same env-read email. UAT 12.6-02 empirically verified both default and override runs against fresh DB. |
| R6 | DEMO_EMAIL/ADMIN_EMAIL con formato invalido (demo@demo) hace fallar la validacion Joi al bootear | VERIFIED | `env.validation.ts:13 DEMO_EMAIL: Joi.string().email().default('demo@hefesto.com')` and `env.validation.ts:14 ADMIN_EMAIL: Joi.string().email().default('admin@hefesto.com')`. No `tlds: { allow: false }` override — standard `.email()` rejects `demo@demo` (no TLD). |
| R7 | Tests actualizados a fuente unica; test de override falla si email esta hardcodeado | VERIFIED | `auth.service.spec.ts:7 import { getDemoEmail }`. All 7 asserts use `getDemoEmail()` (lines 263, 278, 287, 296, 318, 321, matching pattern). Override test at line 325: sets `process.env.DEMO_EMAIL = 'demo@foo.com'`, mocks gate `'true'`, asserts `demo@foo.com` resolves and `demo@hefesto.com` rejects. `beforeEach` (line 221) saves `originalDemoEmail`; `afterEach` (lines 224-229) restores or deletes — correct isolation. |
| R8 | .env.example (front+back) defaults Hefesto/hefesto.com; back README has seccion "Rebranding"; ARCHITECTURE.md y DTO sin demo@nemea.app; sin marca en source/docs | PARTIAL | `.env.example` (back) correct: `APP_NAME=Hefesto`, `DEMO_EMAIL=demo@hefesto.com`, `ADMIN_EMAIL=admin@hefesto.com`, `ADMIN_NAME=Admin`. `.env.example` (front) correct: `NEXT_PUBLIC_APP_NAME=Hefesto`, `NEXT_PUBLIC_DEMO_EMAIL=demo@hefesto.com`, `NEXT_PUBLIC_CONTACT_EMAIL=admin@hefesto.com`. `demo-login.dto.ts:6 example: 'demo@hefesto.com'`. `ARCHITECTURE.md` clean (grep returns nothing). Back `README.md` has `## Rebranding` at line 244 with all 7 env vars, logo swap note, rebuild note, ADMIN_EMAIL Google account note, and DEMO_EMAIL/NEXT_PUBLIC_DEMO_EMAIL parity warning. **GAP**: `nemea-front/README.md:88` still has `NEXT_PUBLIC_APP_NAME=Nemea` in Quick Start snippet (see Human Verification). |
| R9 | DB fresca sin envs: admin seeded as admin@hefesto.com / "Admin"; con ADMIN_EMAIL/ADMIN_NAME seteados, seedea esos valores | VERIFIED | `CreateUserTable.ts:7-8`: `const adminEmail = process.env.ADMIN_EMAIL ?? 'admin@hefesto.com'` and `const adminName = process.env.ADMIN_NAME ?? 'Admin'`. Parametrized INSERT `[$1, $2]`. UAT Test 1 (Cold Start) confirmed admin@hefesto.com / "Admin" seeded. |
| R10 | acceso-denegado muestra NEXT_PUBLIC_CONTACT_EMAIL (default admin@hefesto.com); grep -i basualdofelipe sobre src non-test VACIO | VERIFIED | `acceso-denegado/page.tsx:7 import { APP_NAME, CONTACT_EMAIL }`, `page.tsx:25 contacta al administrador en {CONTACT_EMAIL}`. `nemea-front/src/constants/branding.ts:5-6 CONTACT_EMAIL = process.env.NEXT_PUBLIC_CONTACT_EMAIL ?? 'admin@hefesto.com'`. Grep of `basualdofelipe` over non-test src returns nothing. |

**Score:** 9/10 truths verified (R8 PARTIAL — front README Quick Start snippet)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `nemea-back/src/constants/branding.ts` | Single source for APP_NAME, ADMIN_EMAIL, ADMIN_NAME, getDemoEmail() with security rationale | VERIFIED | All 4 exports present. `getDemoEmail` is a getter (call-time). Security rationale comment preserved at lines 17-28. No ConfigService dependency. |
| `nemea-back/src/config/env.validation.ts` | Joi DEMO_EMAIL and ADMIN_EMAIL with .email() validation | VERIFIED | Lines 13-14 present. `.email()` without tlds override. Defaults hefesto.com. |
| `nemea-back/src/auth/auth.service.ts` | Imports getDemoEmail(), no literal demo@nemea.app | VERIFIED | Line 8 import, lines 75/80 call-time reads. No old literal. |
| `nemea-back/src/main.ts` | Imports APP_NAME, Swagger title = `${APP_NAME} API` | VERIFIED | Line 10 import, lines 48-49 template. |
| `nemea-back/src/auth/auth.service.spec.ts` | 7 asserts retargeted + override test + afterEach cleanup | VERIFIED | All present. beforeEach saves env, afterEach restores. |
| `nemea-back/src/database/migrations/1772369233961-CreateUserTable.ts` | Seed admin from ADMIN_EMAIL/ADMIN_NAME with parametrized INSERT | VERIFIED | Lines 7-8 env reads, parametrized at line 17. |
| `nemea-back/src/database/migrations/1773200000000-SeedDemoUser.ts` | Seed demo from DEMO_EMAIL/APP_NAME with parametrized INSERT | VERIFIED | Lines 7-8 env reads, parametrized at line 21. |
| `nemea-front/src/constants/branding.ts` | APP_NAME, DEMO_EMAIL, CONTACT_EMAIL from NEXT_PUBLIC_* with Hefesto fallbacks | VERIFIED | Lines 2-6 present. Correct variable names and fallbacks. |
| `nemea-front/src/app/layout.tsx` | title: APP_NAME (not 'Nemea') | VERIFIED | Line 38. Imported from branding.ts. |
| `nemea-front/src/app/(auth)/login/page.tsx` | alt={APP_NAME} (not 'NEMEA') | VERIFIED | Line 59. Imported from branding.ts. |
| `nemea-front/src/app/(auth)/login/DemoLoginButton.tsx` | DEMO_EMAIL from branding (not hardcoded) | VERIFIED | Line 8 import, line 22 use. |
| `nemea-front/src/components/layout/AppSidebar.tsx` | 2x alt={APP_NAME} (not 'NEMEA') | VERIFIED | Lines 85 and 92. |
| `nemea-front/src/app/(auth)/acceso-denegado/page.tsx` | APP_NAME + CONTACT_EMAIL from branding (no basualdofelipe) | VERIFIED | Lines 7 import, 18/25 use. |
| `nemea-back/README.md` (Rebranding section) | Section with 7 env vars, logo swap, rebuild, ADMIN_EMAIL Google note, DEMO_EMAIL parity | VERIFIED | Line 244 section heading. Table covers all 7 vars. All 4 mandatory notes present. |
| `nemea-back/.env.example` | Hefesto defaults + DEMO_EMAIL + ADMIN_EMAIL + ADMIN_NAME | VERIFIED | All 4 branding entries present with correct defaults and comments. |
| `nemea-front/.env.example` | Hefesto defaults + NEXT_PUBLIC_DEMO_EMAIL + NEXT_PUBLIC_CONTACT_EMAIL | VERIFIED | All 3 entries present. |
| `nemea-back/src/auth/dto/demo-login.dto.ts` | example: 'demo@hefesto.com' (not demo@nemea.app) | VERIFIED | Line 6. |
| `nemea-back/docs/ARCHITECTURE.md` | No demo@nemea.app | VERIFIED | Grep returns empty. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `auth.service.ts` | `constants/branding.ts` | `import { getDemoEmail }` at line 8 | WIRED | Used at lines 75, 80 (call-time reads) |
| `main.ts` | `constants/branding.ts` | `import { APP_NAME }` at line 10 | WIRED | Used at lines 48, 49 in Swagger DocumentBuilder |
| `auth.service.spec.ts` | `constants/branding.ts` | `import { getDemoEmail }` at line 7 | WIRED | Used in 7 assertions + override test |
| `layout.tsx` | `@/constants/branding` | `import { APP_NAME }` at line 8 | WIRED | Used at line 38 in metadata.title |
| `login/page.tsx` | `@/constants/branding` | `import { APP_NAME }` at line 7 | WIRED | Used at line 59 in Image alt |
| `DemoLoginButton.tsx` | `@/constants/branding` | `import { DEMO_EMAIL }` at line 8 | WIRED | Used at line 22 in signIn payload |
| `AppSidebar.tsx` | `@/constants/branding` | `import { APP_NAME }` at line 32 | WIRED | Used at lines 85, 92 in Image alt |
| `acceso-denegado/page.tsx` | `@/constants/branding` | `import { APP_NAME, CONTACT_EMAIL }` at line 7 | WIRED | Used at lines 18, 25 |

All 8 key links WIRED.

---

### Data-Flow Trace (Level 4)

Branding constants are env-read-at-module-load (front: build-time inlined by Next.js `NEXT_PUBLIC_*`) and call-time for the getter. No database involved. Data source is `process.env`, which is the correct source by design. The `getDemoEmail()` getter reads env on each call — no frozen value.

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `nemea-back/src/constants/branding.ts` | `APP_NAME`, `ADMIN_EMAIL`, `ADMIN_NAME` | `process.env.*` with fallback | Yes — reads from process.env at module load | FLOWING |
| `nemea-back/src/constants/branding.ts` | `getDemoEmail()` | `process.env.DEMO_EMAIL` with fallback | Yes — reads process.env at call time | FLOWING |
| `nemea-front/src/constants/branding.ts` | `APP_NAME`, `DEMO_EMAIL`, `CONTACT_EMAIL` | `process.env.NEXT_PUBLIC_*` with fallback | Yes — inlined at Next.js build time | FLOWING |

---

### Behavioral Spot-Checks

Step 7b: Branding is a constants/env phase — no new API endpoints or CLI entry points were introduced. The relevant behaviors are covered by the existing test suite (auth.service.spec.ts 154 back tests) and the empirical UAT (7/7 passed). Spot-checks via curl/node would require a running server.

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Back constants export correct defaults | `node -e "process.env.DEMO_EMAIL=undefined; const b=require('./nemea-back/src/constants/branding'); console.log(b.APP_NAME, b.getDemoEmail())"` | Requires TS compilation — not runnable directly | SKIP (needs compiled output) |
| Test suite green | Documented: back 154/154, front 24/24 per context_notes | Pre-verified per context | SKIP (pre-verified, no new test targets) |

---

### Probe Execution

No probes declared in PLAN files. No `scripts/*/tests/probe-*.sh` files in this phase. Step 7c: SKIPPED — no probes.

---

### Requirements Coverage

All requirements for this phase are R1-R10, locked in `12.6-SPEC.md` (not in global REQUIREMENTS.md — this is a phase-local spec). Coverage:

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| R1 | 12.6-03 | App name por env (front) — title, 3 alt, heading, texto | SATISFIED | branding.ts front + all 5 consumer files verified |
| R2 | 12.6-01 | App name por env (back Swagger) | SATISFIED | main.ts imports APP_NAME, uses in setTitle/setDescription |
| R3 | 12.6-01 | Demo email por env (back) | SATISFIED | auth.service.ts imports getDemoEmail(), call-time usage |
| R4 | 12.6-03 | Demo email por env (front) | SATISFIED | DemoLoginButton imports DEMO_EMAIL from branding.ts |
| R5 | 12.6-02 | Nombre del demo derivado de APP_NAME (SeedDemoUser) | SATISFIED | SeedDemoUser.ts lines 7-8, parametrized INSERT |
| R6 | 12.6-01 | DEMO_EMAIL/ADMIN_EMAIL validacion Joi al boot | SATISFIED | env.validation.ts lines 13-14 |
| R7 | 12.6-01 | Tests actualizados + test de override | SATISFIED | auth.service.spec.ts — 7 asserts use getDemoEmail(), override test present with env isolation |
| R8 | 12.6-04 | Docs rebranding (README, .env.example x2, ARCHITECTURE, DTO) | PARTIAL | Back README Rebranding section complete, all .env.example correct, ARCHITECTURE/DTO clean. **nemea-front/README.md:88** still has `NEXT_PUBLIC_APP_NAME=Nemea` in Quick Start snippet — R8 acceptance says "sin Nemea en source/docs" |
| R9 | 12.6-02 | App admin por env (CreateUserTable) | SATISFIED | CreateUserTable.ts lines 7-8, parametrized INSERT |
| R10 | 12.6-03 | Contact email por env (front) | SATISFIED | acceso-denegado/page.tsx imports CONTACT_EMAIL from branding.ts |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `nemea-back/src/common/interceptors/logging.interceptor.ts` | 13 | `Logger('Nemea')` | Info | Internal server logger, not visible to users. Explicitly excluded by SPEC (interview round 4: "Logger('Nemea')… fuera (no visibles)") |
| `nemea-back/src/app.controller.ts` | 25 | `app: 'nemea-back'` | Info | Internal process identifier. Explicitly excluded by SPEC. |
| `nemea-front/README.md` | 88 | `NEXT_PUBLIC_APP_NAME=Nemea` in Quick Start snippet | Warning | Cosmetic doc inconsistency — the actual .env.example is correctly set to Hefesto. No runtime impact. Violates R8 acceptance literal "sin Nemea en source/docs". Plan 04 grep was scoped to `src/*.ts/.tsx` only, so this was not caught. |

No TBD/FIXME/XXX markers found in any modified source file.

---

### Human Verification Required

#### 1. Front README Quick Start snippet — accept or fix

**Test:** Open `nemea-front/README.md` and check line 88.
**Current state:** The Quick Start `.env.local` snippet shows `NEXT_PUBLIC_APP_NAME=Nemea` (the old hardcoded value). The actual `.env.example` file correctly shows `NEXT_PUBLIC_APP_NAME=Hefesto`.
**Expected (per R8 acceptance):** All docs should show the new default "Hefesto" — no "Nemea" in source/docs.
**Why human:** This is a scope decision: the plan 04 explicitly narrowed the docs grep to `src/*.ts/.tsx` only and did not list `nemea-front/README.md` in `files_modified`. The actual functional default (`.env.example`) is correct. The owner must decide whether to:
  - **Fix it** (update `nemea-front/README.md:88` to `NEXT_PUBLIC_APP_NAME=Hefesto`) — satisfies R8 acceptance literally
  - **Accept it as-is** (add an override: the Quick Start is a developer setup example, the canonical defaults live in `.env.example`, and README headings referencing "Nemea" as the project identity are in-scope exclusions like folder names)

If accepting, add to VERIFICATION.md frontmatter:
```yaml
overrides:
  - must_have: "sin Nemea en source/docs (R8 acceptance)"
    reason: "nemea-front/README.md Quick Start snippet (line 88) showing NEXT_PUBLIC_APP_NAME=Nemea is a cosmetic doc issue — the canonical defaults file (.env.example) is correctly set to Hefesto; no runtime impact; plan 04 explicitly scoped docs grep to src/*.ts/.tsx only"
    accepted_by: "fbasualdo"
    accepted_at: "2026-06-04T00:00:00Z"
```

---

### Gaps Summary

No runtime blockers found. The only gap is a cosmetic documentation inconsistency:

**R8 partial — nemea-front/README.md Quick Start snippet:** `nemea-front/README.md:88` has `NEXT_PUBLIC_APP_NAME=Nemea` in a developer Quick Start example. This is the only "Nemea" brand string remaining in non-test, non-internal source/docs. The functional `.env.example` (the actual defaults file used by developers) is correctly set to `Hefesto`. This violates the R8 acceptance criterion literally but has no runtime or white-label impact.

All 9 other requirements are fully satisfied with substantive, wired implementation. The phase goal — "rebrandear = setear envs + reemplazar el logo + rebuild" — is operationally achieved.

---

_Verified: 2026-06-04_
_Verifier: Claude (gsd-verifier)_

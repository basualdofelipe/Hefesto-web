---
phase: 12.6-branding-configurable-por-env
plan: "04"
subsystem: docs
tags: [branding, env, docs, rebranding]
dependency_graph:
  requires: [12.6-01, 12.6-02, 12.6-03]
  provides: [rebranding-docs-complete, brand-closure-greps]
  affects: [nemea-back/README.md, nemea-back/.env.example, nemea-front/.env.example, nemea-back/src/auth/dto/demo-login.dto.ts, nemea-back/docs/ARCHITECTURE.md]
tech_stack:
  added: []
  patterns: [env-defaults-in-examples, minimal-rebranding-docs]
key_files:
  created: []
  modified:
    - nemea-back/README.md
    - nemea-back/.env.example
    - nemea-front/.env.example
    - nemea-back/src/auth/dto/demo-login.dto.ts
    - nemea-back/docs/ARCHITECTURE.md
decisions:
  - D-04 applied: Rebranding section is minimal (no marketing) with 2 mandatory lines (logo swap + rebuild) plus ADMIN_EMAIL Google note and DEMO_EMAIL parity warning
  - Test files excluded from brand grep (R1): fixtures in test files are non-visible/non-shipped and out of scope per SPEC
metrics:
  duration: "8min"
  completed: "2026-06-02"
  tasks: 2
  files: 5
---

# Phase 12.6 Plan 04: Docs & Marca Closure Summary

**One-liner:** Defaults Hefesto/hefesto.com en ambos .env.example, DTO example y ARCHITECTURE actualizados, seccion Rebranding minima en README con 7 env vars + 2 lineas obligatorias + notas criticas; greps de cierre sin marca visible en source no-test.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Actualizar .env.example (front+back) + DTO example + ARCHITECTURE.md | back: 5093f6e, front: 65377d3 | .env.example x2, demo-login.dto.ts, ARCHITECTURE.md |
| 2 | Seccion "Rebranding" en README + greps finales de marca | back: 0462324 | README.md |

## Deviations from Plan

None - plan executed exactly as written.

## Grep de Cierre (R8 / T-12.6-07)

### grep -ri nemea (source no-test)

Comando: `grep -ri nemea nemea-front/src nemea-back/src --include="*.ts" --include="*.tsx" --exclude="*.spec.ts" --exclude="*.test.ts" --exclude="*.test.tsx"` (excluyendo `__tests__/`)

Resultados (internos out-of-scope, NO se tocan):

| Match | Archivo | Razon out-of-scope |
|-------|---------|-------------------|
| `app: 'nemea-back'` | `nemea-back/src/app.controller.ts` | Identificador interno del proceso, no visible en UI ni docs publicos. Rename = out-of-scope per SPEC. |
| `Logger('Nemea')` | `nemea-back/src/common/interceptors/logging.interceptor.ts` | Log de servidor, no visible. Rename = out-of-scope per SPEC. |

Ningun match de marca visible/shippeada. 0 falsos positivos en texto de usuario.

### grep -ri basualdofelipe (source no-test)

Comando: `grep -ri basualdofelipe nemea-front/src nemea-back/src --include="*.ts" --include="*.tsx" --exclude="*.spec.ts" --exclude="*.test.ts" --exclude="*.test.tsx"`

Resultado: **VACIO** — R10 cumplido. El email del autor fue removido de `acceso-denegado/page.tsx` en el plan 03 y no quedo en ningun otro source no-test.

### grep -ri demo@nemea.app (source+docs no-test)

Resultado: **VACIO** — ningun hardcode de `demo@nemea.app` en source visible ni docs.

## Fixtures de Test con Marca (Out-of-Scope por R1)

Las siguientes fixtures de test contienen emails/nombres con marca y NO se tocaron (R1: "excluyendo tests"; las fixtures son no-visibles/no-shippeadas y su rebranding esta fuera del SPEC):

**nemea-front (test fixtures):**
- `src/app/(app)/usuarios/__tests__/*.test.tsx` — literales `user@nemea.com`, `other@nemea.com`, `Admin Nemea`
- `src/app/(app)/escenarios/**/__tests__/*.test.tsx` — literales de escenarios con marca
- `src/app/(app)/calculadora/__tests__/*.test.tsx` — literales de calculadora con marca

**nemea-back (spec files):**
- `src/users/users.controller.spec.ts`
- `src/users/users.service.spec.ts`
- `src/auth/guards/permissions.guard.spec.ts`
- `src/auth/auth.service.spec.ts` — 7 asserts ya migrados a constante branding.ts (plan 01), pero emails de otros test users quedan como fixtures no-shipped
- `src/auth/auth.controller.spec.ts`

Estos matches son **esperados** y **correctos** — excluidos por R1 y documentados aqui por la acceptance criteria de Task 2.

## Internos Out-of-Scope (Confirmados, NO se Tocan)

| Item | Archivo | Razon |
|------|---------|-------|
| `app: 'nemea-back'` | app.controller.ts | Identificador de proceso interno, no visible. SPEC out-of-scope. |
| `Logger('Nemea')` | logging.interceptor.ts | Logging de servidor. SPEC out-of-scope. |
| `nemea_db` | docker-compose.yml | Nombre de DB, no visible para usuario. SPEC out-of-scope. |
| `package.json name: "nemea-..."` | package.json x2 | Nombre de paquete npm. SPEC out-of-scope. |
| `nemea-front/`, `nemea-back/` carpetas | filesystem | Nombre de carpeta/repo. SPEC out-of-scope. |

## Seccion Rebranding — Verificacion de Acceptance Criteria

- [x] README tiene `## Rebranding` con las 7 env vars (4 back + 3 front) con defaults
- [x] Seccion incluye la 2 lineas obligatorias: "reemplaza el logo en `public/brand/`" + "rebuild — las `NEXT_PUBLIC_*` se hornean en build time"
- [x] Seccion nota que `ADMIN_EMAIL` debe ser cuenta Google real para loguear
- [x] Seccion incluye linea de paridad: `DEMO_EMAIL` (back) y `NEXT_PUBLIC_DEMO_EMAIL` (front) deben tener el MISMO valor o el demo-login devuelve 401
- [x] `grep -ri basualdofelipe` con exclusion de tests: VACIO
- [x] `grep -ri nemea` con exclusion de tests: solo internos out-of-scope (documentados arriba)
- [x] Fixtures de test con marca documentadas como out-of-scope por R1

## Threat Surface Scan

T-12.6-07 mitigado: defaults neutros en .env.example (ambos repos), ARCHITECTURE.md sin emails hardcodeados, greps de cierre confirmaron ausencia de marca visible en source/docs no-test.

T-12.6-08 mitigado: D-04 cumplido — las 2 lineas obligatorias presentes, linea de paridad demo email incluida, nota cuenta Google incluida.

No nueva superficie de seguridad introducida en este plan (solo docs/examples).

## Self-Check: PASSED

- nemea-back/README.md: seccion `## Rebranding` presente con 7 env vars, 2 lineas obligatorias, nota ADMIN_EMAIL, linea paridad
- nemea-back/.env.example: APP_NAME=Hefesto, DEMO_EMAIL=demo@hefesto.com, ADMIN_EMAIL=admin@hefesto.com, ADMIN_NAME=Admin, sin demo@nemea.app
- nemea-front/.env.example: NEXT_PUBLIC_APP_NAME=Hefesto, NEXT_PUBLIC_DEMO_EMAIL=demo@hefesto.com, NEXT_PUBLIC_CONTACT_EMAIL=admin@hefesto.com
- nemea-back/src/auth/dto/demo-login.dto.ts: example = 'demo@hefesto.com', @IsEmail() presente
- nemea-back/docs/ARCHITECTURE.md:116: sin demo@nemea.app, descripcion env-driven
- Commits: back 5093f6e (Task 1), back 0462324 (Task 2), front 65377d3 (Task 1 front)
- npx tsc --noEmit (back): PASSED

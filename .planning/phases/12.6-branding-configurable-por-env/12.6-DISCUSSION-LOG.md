# Phase 12.6: Branding configurable por env - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-01
**Phase:** 12.6-branding-configurable-por-env
**Areas discussed:** README depth, Back env access, Front config location

---

## Guía de rebranding (README)

| Option | Description | Selected |
|--------|-------------|----------|
| Walkthrough corto y completo | Lista de envs + paso de logo + nota de rebuild (~15 líneas) | |
| Lista mínima | Solo enumerar las env vars | ✓ (reconciliado) |
| Walkthrough + pitch de venta | Lo anterior + párrafo "white-label ready" | |

**User's choice:** "Lista mínima" — reconciliado a **lista + 2 líneas** (logo + rebuild) tras flag del senior: el acceptance del SPEC (req 8) ya exige logo + rebuild, y esas 2 líneas son load-bearing (sin ellas la guía engaña al rebrander).
**Notes:** Sin marketing/pitch. Mínimo funcional, no marketing.

---

## Acceso a env en el back

| Option | Description | Selected |
|--------|-------------|----------|
| constants/branding.ts compartido | `process.env` directo, importado por services Y migraciones (una fuente) | ✓ |
| ConfigService + process.env separados | Services via ConfigService, migraciones via process.env directo | |

**User's choice:** constants/branding.ts compartido.
**Notes:** Las migraciones no pueden usar ConfigService; `import 'dotenv/config'` en data-source garantiza process.env en migraciones. Una sola fuente evita divergencia.

---

## Front: dónde vive el config

| Option | Description | Selected |
|--------|-------------|----------|
| constants/branding.ts | Simétrico con back + matchea tiendanube.ts | ✓ |
| config/ nuevo | Carpeta config/ dedicada | |

**User's choice:** constants/branding.ts.
**Notes:** El user pidió aclaración ("¿esto es para meterle la constante que lee del env?") → confirmado que es solo dónde vive el archivo con la constante. Elegido por simetría con el back y consistencia con el patrón existente.

---

## Claude's Discretion

- Cómo setear env por-test en el test de override (jest).
- Forma exacta de los exports de `branding.ts` (named consts vs objeto).
- Ubicación de la sección "Rebranding" dentro del README.

## Deferred Ideas

- Branding interno no visible (`Logger('Nemea')`, `app:'nemea-back'`, rename de package/db/carpetas/repo) — fuera de scope (ya en SPEC out-of-scope).
- Todos revisados y NO foldeados (falsos positivos por keyword "phase"): "Drag & drop ordering for all catalog dimensions" (ui), "Include CalculadoraService tests in Phase 11 plan" (testing).

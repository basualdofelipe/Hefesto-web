---
phase: 12.6
slug: branding-configurable-por-env
status: secured
threats_total: 10
threats_closed: 10
threats_open: 0
accepted_risks: 1
asvs_level: 1
created: 2026-06-04
---

# SECURITY.md — Phase 12.6 (branding-configurable-por-env)

**Audited:** 2026-06-04
**Result:** SECURED — all declared mitigations verified in implemented code.
**Threats Closed:** 10/10 (9 mitigate verified in code, 1 accept confirmed)
**ASVS Level:** default

This audit verifies each threat in the plan-time register by its declared disposition.
Documentation/intent was not accepted as evidence — every `mitigate` threat was confirmed by
a direct match in the cited implementation file.

---

## Threat Verification

| Threat ID | Category | Disposition | Status | Evidence |
|-----------|----------|-------------|--------|----------|
| T-12.6-01 | Spoofing/Elevation | mitigate | CLOSED | `auth.service.ts:70-77` — gate `DEMO_LOGIN_ENABLED !== 'true'` rejects (401) BEFORE pin check `email !== getDemoEmail()` (also 401); lookup at `:80` uses `getDemoEmail()`, never the client address. Pin rationale preserved as JSDoc in `constants/branding.ts:22-27`. Env changes only WHICH single email is accepted; arbitrary email impossible even with gate on. |
| T-12.6-02 | Input Validation (V5) | mitigate | CLOSED | `env.validation.ts:13-14` — `DEMO_EMAIL`/`ADMIN_EMAIL` use `Joi.string().email()` (no `tlds` override → `demo@demo` fails boot). DTO `demo-login.dto.ts:10` retains `@IsEmail()`. |
| T-12.6-03 | Tampering (SQLi) | mitigate | CLOSED | `SeedDemoUser...up()` (`:21-26`) parametrized `$1/$2/$3` + values array; `down()` (`:30-32`) `$1`. `CreateUserTable...up()` (`:16-19`) parametrized `$1/$2`. Env values are never interpolated into SQL strings. |
| T-12.6-04 | Information Disclosure | mitigate | CLOSED | `CreateUserTable...up():7-8` neutral defaults `admin@hefesto.com` / `Admin` replace author's personal email/name. Grep `Felipe Basualdo`/`demo@nemea.app` over `nemea-back/src` (non-test) → empty. |
| T-12.6-05 | Information Disclosure | mitigate | CLOSED | `acceso-denegado/page.tsx:7,25` uses `{CONTACT_EMAIL}` (= `NEXT_PUBLIC_CONTACT_EMAIL ?? 'admin@hefesto.com'`, `front/constants/branding.ts:5-6`). Independent grep `basualdofelipe` over `nemea-front/src` → empty. |
| T-12.6-06 | Build correctness | mitigate | CLOSED | `front/constants/branding.ts:2-6` — all three reads use the full literal `process.env.NEXT_PUBLIC_*` form; no destructuring, no dynamic indexing → Next inlines them at build. |
| T-12.6-07 | Information Disclosure | mitigate | CLOSED | Neutral defaults in `nemea-back/.env.example:6-11`, `nemea-front/.env.example:1,13-14`, `ARCHITECTURE.md` (grep `demo@nemea.app` → empty). Closure greps `basualdofelipe`/`Felipe Basualdo`/`demo@nemea.app` over shipped non-test source → empty. Remaining `nemea` matches are documented internal out-of-scope (`Logger('Nemea')`, `app: 'nemea-back'`, db/package/folder names). |
| T-12.6-08 | Operational | mitigate | CLOSED | `README.md:244-270` `## Rebranding` — 7 env vars + defaults table, mandatory logo-swap + rebuild lines (`:267-268`), ADMIN_EMAIL real-Google-account note (`:269`), and DEMO_EMAIL/NEXT_PUBLIC_DEMO_EMAIL parity line (`:270`). |
| T-12.6-09 | Denial (data loss) | mitigate | CLOSED | Operational mitigation, not code. `12.6-02-PLAN.md` Task 3 Step-0 documents DATABASE_URL local/disposable confirmation + `migration:show` pre-check; `12.6-02-SUMMARY.md` checkpoint evidence shows it was performed against throwaway DB `nemea_rebrand_verify` (dev `nemea_db` intact). |
| T-12.6-SC | Tampering (npm) | accept | CLOSED (accepted) | `git diff` of both `nemea-back/package.json` and `nemea-front/package.json` across the full phase span (kickoff `df2f872` → HEAD) is empty in both repos → zero new npm dependencies. Accepted risk confirmed (v1.1 zero-dep constraint). |

---

## Unregistered Flags

None. Threat Flags / Threat Surface Scan sections in all four SUMMARY files map to existing
register IDs (T-12.6-01..09); no new attack surface appeared during implementation without a
threat mapping.

---

## Accepted Risks Log

- **T-12.6-SC** — Zero new npm dependencies (v1.1 constraint). Verified by empty `package.json`
  diff in both sub-repos across the phase. No dependency-legitimacy audit required.

## Adjudicated Design Decisions (not threats)

- Demo account seeded with ADMIN role + `DEMO_LOGIN_ENABLED` defaulting to `true` is a
  user-confirmed design decision for the clone-and-run-LOCAL distribution model (no anonymous
  public attacker). The real security boundary for T-12.6-01 — the email pin/gate that prevents
  the endpoint from accepting an arbitrary email — is verified present and intact. Not an open threat.

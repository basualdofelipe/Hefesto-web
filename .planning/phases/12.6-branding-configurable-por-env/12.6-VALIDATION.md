---
phase: 12.6
slug: branding-configurable-por-env
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-06-01
---

# Phase 12.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest `^30.0.0` + ts-jest `^29.2.5` (back) |
| **Config file** | inline en `nemea-back/package.json` (`"jest"` key), `rootDir: src`, `testRegex: .*\.spec\.ts$` |
| **Quick run command** | `cd nemea-back && npm test -- auth.service` |
| **Full suite command** | `cd nemea-back && npm test` |
| **Estimated runtime** | ~15 seconds (quick) / ~60 seconds (full) |

> Front (`nemea-front`) no tiene suite de tests para los puntos de branding — su verificación es por `grep` + build (manual). El override de texto visible se valida con greps de marca vacíos (excluyendo tests, R1) + rebuild.

---

## Sampling Rate

- **After every task commit:** Run `cd nemea-back && npm test -- auth.service`
- **After every plan wave:** Run `cd nemea-back && npm test`
- **Before `/gsd-verify-work`:** Full back suite green + greps de marca vacíos excluyendo tests (`grep -i nemea`, `grep -i basualdofelipe` sobre `*/src` con `--exclude='*.spec.ts'`/`--exclude-dir=__tests__`) + boot OK con defaults
- **Max feedback latency:** ~15 seconds

---

## Per-Task Verification Map

> Populated by the planner from the real PLAN.md task IDs.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01-T1 | 12.6-01 | 1 | R6 — Joi `.email()` valida DEMO_EMAIL/ADMIN_EMAIL al boot; branding.ts fuente única | T-12.6-02 (V5) | `demo@demo` (sin TLD) falla el boot; sin `tlds:{allow:false}` | unit/manual | `npx tsc --noEmit` + boot con env inválido | ✅ `branding.ts` + `env.validation.ts` | ⬜ pending |
| 01-T2 | 12.6-01 | 1 | R2/R3 — Swagger `${APP_NAME} API`; demo email call-time desde branding | T-12.6-01 (V2) | gate `DEMO_LOGIN_ENABLED` intacto; el env solo cambia *cuál* email único | unit | `npm test -- auth.service` | ✅ `auth.service.ts` / `main.ts` | ⬜ pending |
| 01-T3 | 12.6-01 | 1 | R3/R7 — 7 asserts retargeteados + test de override (env distinto → acepta) | T-12.6-01 | el test falla si el email está hardcodeado | unit | `npm test -- auth.service` | ❌ W0 (nuevo `it()`) | ⬜ pending |
| 02-T1 | 12.6-02 | 2 | R5 — SeedDemoUser seed-from-env (`Demo ${APP_NAME}`), down simétrico | T-12.6-03 (SQLi) | INSERT/DELETE parametrizados `$1`; sin interpolar env | manual | DB fresca + `migration:run` + inspección | ❌ manual (02-T3) | ⬜ pending |
| 02-T2 | 12.6-02 | 2 | R9 — CreateUserTable seed admin desde ADMIN_EMAIL/ADMIN_NAME, parametrizado | T-12.6-03 (SQLi) / T-12.6-04 | INSERT parametrizado `$1`/`$2`; default neutro reemplaza email/nombre del autor | manual | DB fresca + `migration:run` + inspección | ❌ manual (02-T3) | ⬜ pending |
| 02-T3 | 12.6-02 | 2 | R5/R9 — BLOCKING: `migration:run` en DB fresca + inspección filas seedeadas | T-12.6-03 / T-12.6-04 | filas = defaults Hefesto sin envs; override reflejado con envs; sin filas viejas | manual (blocking) | `cd nemea-back && npm run migration:run` + `SELECT email,name FROM users` | ❌ manual (sin harness) | ⬜ pending |
| 03-T1 | 12.6-03 | 1 | R1/R4/R10 — front branding.ts con literales NEXT_PUBLIC_* + fallback | T-12.6-06 (build) | literales completos (sin destructuring/indexado) para inlining de Next | manual/build | `cd nemea-front && npx tsc --noEmit` | ✅ `branding.ts` (front) | ⬜ pending |
| 03-T2 | 12.6-03 | 1 | R1/R4 — layout title + 3 alt + DemoLoginButton desde branding | T-12.6 (info disclosure) | demo email del front == default back | manual/build | `npx tsc --noEmit` + rebuild + `grep -i nemea` (excl. tests) | ✅ layout/login/sidebar/DemoLoginButton | ⬜ pending |
| 03-T3 | 12.6-03 | 1 | R1/R10 — acceso-denegado heading/texto + CONTACT_EMAIL | T-12.6-05 (info disclosure) | `grep -i basualdofelipe` vacío en `nemea-front/src` (excl. tests) | manual/build | `grep -i basualdofelipe nemea-front/src` (excl. tests) | ✅ `acceso-denegado/page.tsx` | ⬜ pending |
| 04-T1 | 12.6-04 | 3 | R8 — `.env.example` ×2 + DTO example + ARCHITECTURE con defaults Hefesto | T-12.6-07 (info disclosure) | docs sin `demo@nemea.app`/`Nemea`; defaults neutros | build | `cd nemea-back && npx tsc --noEmit` | ✅ `.env.example` ×2 / DTO / ARCHITECTURE | ⬜ pending |
| 04-T2 | 12.6-04 | 3 | R8/R1/R10 — README `## Rebranding` + greps de cierre (excluyendo tests) | T-12.6-07 / T-12.6-08 | grep de marca vacío sobre source no-test salvo internos out-of-scope; fixtures de test documentadas (R1 "excluyendo tests") | manual/build | `grep -ri basualdofelipe nemea-front/src nemea-back/src --exclude='*.spec.ts' --exclude-dir=__tests__` | ✅ `README.md` | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Actualizar los 7 asserts de `auth.service.spec.ts` (líneas ~219, 246, 261, 270, 279, 289, 301, 304) para referenciar la fuente única (`getDemoEmail()`) en vez del literal `'demo@nemea.app'` (01-T3).
- [ ] Agregar `it('respeta DEMO_EMAIL del env')` con el patrón getter/call-time (Pitfall 1, Opción A — sin `jest.resetModules()`) + `mockConfigService.get` devolviendo `'true'` para `DEMO_LOGIN_ENABLED` (Pitfall 3) (01-T3).
- [ ] (Opcional) test del schema Joi para `DEMO_EMAIL` inválido (R6) — alternativamente verificación manual de boot (01-T1).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Seed demo/admin desde env en DB fresca | R5 / R9 (02-T3) | Las migraciones corren contra una DB real; no hay harness automatizado de migraciones en el repo | DB fresca → `cd nemea-back && npm run migration:run` con defaults → inspeccionar fila admin (`admin@hefesto.com`/`Admin`) y demo (`demo@hefesto.com`/`Demo Hefesto`); repetir con `ADMIN_EMAIL`/`APP_NAME`/`DEMO_EMAIL` seteados |
| Texto visible del front refleja el env | R1 / R4 / R10 (03-T2, 03-T3) | `NEXT_PUBLIC_*` se inlinea en build time; requiere rebuild + inspección visual | `NEXT_PUBLIC_APP_NAME=Foo NEXT_PUBLIC_DEMO_EMAIL=demo@foo.com NEXT_PUBLIC_CONTACT_EMAIL=x@y.com` → rebuild front → verificar title, alts, acceso-denegado |
| Ausencia de marca en source/docs | R1 / R8 / R10 (04-T2) | Verificación negativa por grep | `grep -i nemea` y `grep -i basualdofelipe` sobre `nemea-front/src` y `nemea-back/src` EXCLUYENDO tests (`*.spec.ts`/`*.test.tsx`/`__tests__/`) según acceptance de R1 ("excluyendo tests") → vacío salvo internos out-of-scope |
| Boot con defaults (sin envs) | constraint SPEC | Verifica que ningún `?? fallback` falte | arrancar back+front sin envs de branding → bootea, renderiza defaults Hefesto, sin `undefined` ni crash |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 15s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-06-02

---
phase: 12.6
slug: branding-configurable-por-env
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-01
---

# Phase 12.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest `^30.0.0` + ts-jest `^29.2.5` (back) |
| **Config file** | inline en `nemea-back/package.json` (`"jest"` key), `rootDir: src`, `testRegex: .*\.spec\.ts$` |
| **Quick run command** | `cd nemea-back && npm test -- auth.service` |
| **Full suite command** | `cd nemea-back && npm test` |
| **Estimated runtime** | ~15 seconds (quick) / ~60 seconds (full) |

> Front (`nemea-front`) no tiene suite de tests para los puntos de branding — su verificación es por `grep` + build (manual). El override de texto visible se valida con greps de marca vacíos + rebuild.

---

## Sampling Rate

- **After every task commit:** Run `cd nemea-back && npm test -- auth.service`
- **After every plan wave:** Run `cd nemea-back && npm test`
- **Before `/gsd-verify-work`:** Full back suite green + greps de marca vacíos (`grep -i nemea`, `grep -i basualdofelipe` sobre `*/src`) + boot OK con defaults
- **Max feedback latency:** ~15 seconds

---

## Per-Task Verification Map

> Populated by the planner once PLAN.md task IDs exist. Rows below are the requirement→test anchors derived from RESEARCH.md that the per-task map must satisfy.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | R3 — demo-login acepta email del env, rechaza otro | — | `email !== DEMO_USER_EMAIL → 401`; el env solo cambia *cuál* es el único permitido | unit | `npm test -- auth.service` | ✅ `auth.service.spec.ts` | ⬜ pending |
| TBD | TBD | TBD | R7 — override: env distinto → acepta ese email, rechaza otro | — | test falla si el email está hardcodeado | unit | `npm test -- auth.service` | ❌ W0 (nuevo `it()`) | ⬜ pending |
| TBD | TBD | TBD | R6 — `DEMO_EMAIL`/`ADMIN_EMAIL` inválido falla boot | T-12.6 V5 | Joi `.email()` rechaza formato inválido al boot | unit/manual | test del schema Joi (opcional) o boot con env inválido | ❌ W0 (opcional) | ⬜ pending |
| TBD | TBD | TBD | R5/R9 — seed-from-env (demo/admin), query parametrizada `$1` | T-12.6 (SQLi) | sin interpolar env en SQL string | manual | DB fresca + `migration:run` + inspección | ❌ manual | ⬜ pending |
| TBD | TBD | TBD | R1/R2/R4/R10 — texto visible refleja env | T-12.6 (info disclosure) | `grep -i basualdofelipe` vacío en front/src | manual/build | `grep -i nemea` / `grep -i basualdofelipe` sobre `*/src` + build | ❌ manual | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Actualizar los 7 asserts de `auth.service.spec.ts` (líneas ~219, 246, 261, 270, 279, 289, 301, 304) para referenciar la fuente única en vez del literal `'demo@nemea.app'`.
- [ ] Agregar `it('respeta DEMO_EMAIL del env')` con el patrón resetModules/getter (Pitfall 1 de RESEARCH.md) + `mockConfigService.get` devolviendo `'true'` para `DEMO_LOGIN_ENABLED` (Pitfall 3).
- [ ] (Opcional) test del schema Joi para `DEMO_EMAIL` inválido (R6) — alternativamente verificación manual de boot.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Seed demo/admin desde env en DB fresca | R5 / R9 | Las migraciones corren contra una DB real; no hay harness automatizado de migraciones en el repo | DB fresca → `cd nemea-back && npm run migration:run` con defaults → inspeccionar fila admin (`admin@hefesto.com`/`Admin`) y demo (`demo@hefesto.com`/`Demo Hefesto`); repetir con `ADMIN_EMAIL`/`APP_NAME`/`DEMO_EMAIL` seteados |
| Texto visible del front refleja el env | R1 / R4 / R10 | `NEXT_PUBLIC_*` se inlinea en build time; requiere rebuild + inspección visual | `NEXT_PUBLIC_APP_NAME=Foo NEXT_PUBLIC_DEMO_EMAIL=demo@foo.com NEXT_PUBLIC_CONTACT_EMAIL=x@y.com` → rebuild front → verificar title, alts, acceso-denegado |
| Ausencia de marca en source/docs | R1 / R8 / R10 | Verificación negativa por grep | `grep -i nemea` y `grep -i basualdofelipe` sobre `nemea-front/src` y `nemea-back/src` (excluyendo tests para la marca según acceptance de R1) → vacío |
| Boot con defaults (sin envs) | constraint SPEC | Verifica que ningún `?? fallback` falte | arrancar back+front sin envs de branding → bootea, renderiza defaults Hefesto, sin `undefined` ni crash |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending

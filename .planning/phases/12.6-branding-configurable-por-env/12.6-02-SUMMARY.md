---
phase: 12.6-branding-configurable-por-env
plan: "02"
subsystem: backend-migrations
tags: [migrations, seed, branding, env, parametrized-query, security]
dependency_graph:
  requires: [12.6-01]
  provides: [seed-from-env-migrations]
  affects: [nemea-back/src/database/migrations]
tech_stack:
  added: []
  patterns: [parametrized-query, seed-from-env, symmetric-down]
key_files:
  modified:
    - nemea-back/src/database/migrations/1773200000000-SeedDemoUser.ts
    - nemea-back/src/database/migrations/1772369233961-CreateUserTable.ts
decisions:
  - "SeedDemoUser.down() usa process.env.DEMO_EMAIL ?? 'demo@hefesto.com' — mismo valor que up() para simetria (Pitfall 4)"
  - "CreateUserTable INSERT parametrizado con $1/$2 — elimina interpolacion de string, cumple R9 SQL injection guard"
  - "demoName derivado de APP_NAME ('Demo Hefesto' por defecto) — sin env propio DEMO_NAME"
metrics:
  duration: "~15min (incl. blocking human-verify checkpoint)"
  completed_date: "2026-06-02"
  tasks: 3
  files: 2
---

# Phase 12.6 Plan 02: Seed-from-env migrations (admin + demo) Summary

**One-liner:** Ambas migraciones de seed parametrizadas con env vars y defaults neutros white-label (admin@hefesto.com/Admin, demo@hefesto.com/Demo Hefesto), eliminando datos personales del autor y literales hardcodeados — verificado contra DB throwaway con runs sin y con override.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | SeedDemoUser seed-from-env + down simetrico | 85bb585 | migrations/1773200000000-SeedDemoUser.ts |
| 2 | CreateUserTable seed admin desde env, parametrizado | de677f3 | migrations/1772369233961-CreateUserTable.ts |
| 3 | [BLOCKING] Correr migraciones en DB fresca | APPROVED | checkpoint — verificacion manual completada y aprobada |

## What Was Built

### Task 1 — SeedDemoUser (85bb585)

`up()` ahora lee:
- `const demoEmail = process.env.DEMO_EMAIL ?? 'demo@hefesto.com'`
- `const demoName = \`Demo ${process.env.APP_NAME ?? 'Hefesto'}\``

INSERT cambiado de literales hardcodeados a `VALUES ($1, $2, true, $3)` con array `[demoEmail, demoName, adminRoleId]`.

`down()` simétrico: `DELETE FROM "users" WHERE "email" = $1` con `[process.env.DEMO_EMAIL ?? 'demo@hefesto.com']` — mismo valor del env, no el viejo literal `demo@nemea.app` (Pitfall 4).

### Task 2 — CreateUserTable (de677f3)

`up()` ahora lee:
- `const adminEmail = process.env.ADMIN_EMAIL ?? 'admin@hefesto.com'`
- `const adminName = process.env.ADMIN_NAME ?? 'Admin'`

INSERT cambiado de `VALUES ('basualdofelipe@gmail.com', 'admin', 'Felipe Basualdo', true)` a `VALUES ($1, 'admin', $2, true)` con array `[adminEmail, adminName]`.

`CREATE TYPE` y `CREATE TABLE` intactos — solo el INSERT cambió.

## Verification

- `cd nemea-back && npx tsc --noEmit` — PASS (sin errores)
- `grep` check de literales viejos (`demo@nemea.app`, `basualdofelipe`, `Felipe Basualdo`, `Demo Nemea`) — CLEAN
- Task 3 (manual BLOQUEANTE): COMPLETADA Y APROBADA — ver evidencia abajo

### Task 3 Verification Evidence (checkpoint approved)

| Check | Resultado |
|-------|-----------|
| DATABASE_URL confirmado local/descartable | DB `nemea_rebrand_verify` (throwaway) — dev `nemea_db` intacto |
| migration:show pre-check | Solo migraciones esperadas pendientes antes del run |
| Run sin envs (defaults) | `admin@hefesto.com` / `Admin`, `demo@hefesto.com` / `Demo Hefesto` |
| Run con override (APP_NAME=Foo, DEMO_EMAIL=demo@foo.com, ADMIN_EMAIL=x@y.com, ADMIN_NAME=Bar) | `x@y.com` / `Bar`, `demo@foo.com` / `Demo Foo` |
| Brand-leak check | Sin filas con `nemea`, `basualdo`, ni `felipe` en ninguna corrida |
| Cleanup | DB `nemea_rebrand_verify` dropeada tras la verificacion |

Todos los 6 criterios de aceptacion del plan cumplidos.

## Deviations from Plan

None — plan ejecutado exactamente como estaba escrito.

## Known Stubs

None — los datos seed son valores reales derivados del env, no placeholders.

## Threat Flags

None — las mitigaciones del threat model están implementadas:
- T-12.6-03 (SQL injection): queries parametrizadas en ambas migraciones
- T-12.6-04 (Information Disclosure): literales personales eliminados, defaults neutros
- T-12.6-09 (Data loss): checkpoint Task 3 incluye paso 0 de seguridad DATABASE_URL

## Self-Check: PASSED

- [x] Task 1 commit exists: `85bb585`
- [x] Task 2 commit exists: `de677f3`
- [x] No old literals in migration files
- [x] tsc --noEmit passes
- [x] Task 3 (manual verification): APPROVED — DB fresca, ambas corridas verificadas, brand-leak check limpio

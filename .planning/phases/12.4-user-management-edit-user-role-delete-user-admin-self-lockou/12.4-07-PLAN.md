---
phase: 12.4
plan: 07
type: execute
wave: 3
depends_on:
  - 12.4-05
  - 12.4-06
files_modified: []
autonomous: false
requirements:
  - REQ-12.4-1
  - REQ-12.4-2
  - REQ-12.4-3
  - REQ-12.4-4
  - REQ-12.4-5
  - REQ-12.4-6
  - REQ-12.4-7
tags:
  - verification
  - phase-gate

must_haves:
  truths:
    - "Backend tsc --noEmit exits 0 con cero errores"
    - "Frontend tsc --noEmit exits 0 con cero errores"
    - "Backend lint exits 0"
    - "Frontend lint exits 0"
    - "Backend full jest suite exits 0 (incluye 12.4 nuevos + sin regresiones)"
    - "Frontend full jest suite exits 0 (incluye EditUserDialog + DeleteUserAlertDialog + sin regresiones)"
    - "Manual smoke pasa todos los AC del SPEC con dos cuentas admin reales"
  artifacts: []
  key_links: []
---

<objective>
Phase gate de Plan 12.4: validar que el conjunto completo de cambios (Plans 01-06) compila, lint pasa, todos los tests verdes en ambas apps, y un smoke manual del happy path + cada uno de los 3 self-lockout guards funciona contra una base real con dos usuarios admin.

Purpose: Cerrar el phase con confianza antes de `/gsd:verify-work`. Si algo falla, NO crear gap closure aquí — devolver al phase con plan-checker para diagnóstico y plan de cierre.

Output: ninguno (no files_modified). Solo el SUMMARY del plan que documenta el resultado de cada gate.

Este plan tiene un checkpoint humano al final (smoke manual) — no es totalmente autonomous.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-SPEC.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-VALIDATION.md
@CLAUDE.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-01-SUMMARY.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-02-SUMMARY.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-03-SUMMARY.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-04-SUMMARY.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-05-SUMMARY.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend type-check + lint + full test suite</name>
  <files></files>
  <read_first>
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-VALIDATION.md (sección Test Infrastructure y Sampling Rate)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-05-SUMMARY.md (estado final del backend post-refactor)
    - CLAUDE.md (Code Style — npm run lint y npm run prettier:fix antes de commits)
  </read_first>
  <action>
    Ejecutar las tres verificaciones del backend secuencialmente. Capturar el exit code de cada una y los snippets relevantes si fallan.

    (a) Type check:
    ```
    cd nemea-back && npx tsc --noEmit
    ```
    Expected: exit 0, no output.
    If errors aparecen: NO arreglar inline — escribir el output a `12.4-07-SUMMARY.md` y marcar el gate como FAILED. Diagnosticar en SUMMARY: ¿qué Plan introdujo el error? ¿es un contract mismatch?

    (b) Lint:
    ```
    cd nemea-back && npm run lint
    ```
    Expected: exit 0. Warnings sobre archivos pre-existentes NO bloquean; errores sobre archivos modificados en 12.4 SÍ bloquean. Si hay errores en archivos de 12.4, reportar en SUMMARY.

    (c) Test suite completo:
    ```
    cd nemea-back && npm test
    ```
    Expected: exit 0. TODOS los specs pasando (no solo los de 12.4 — verificar que no haya regresiones en specs viejos como scenarios, roles, etc.).

    Capturar:
    - Total test count (de la línea final de jest).
    - Pass/fail count.
    - Lista de fail names si hay.

    Si TODO pasa: anotarlo en SUMMARY como "Backend gate: PASS".
    Si algo falla: SUMMARY refleja "Backend gate: FAILED — <razón>" y NO proseguir a Task 2 hasta que se resuelva.

    Style note: estos comandos son verificación pura — NO modificar código en este task. Si hay un fail real, el SUMMARY documenta la naturaleza y el ciclo plan-checker/replan se invoca por el orchestrator.
  </action>
  <verify>
    <automated>cd nemea-back && npx tsc --noEmit &amp;&amp; cd nemea-back && npm run lint &amp;&amp; cd nemea-back && npm test 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cd nemea-back && npx tsc --noEmit` exits 0 con cero líneas de error en stdout/stderr.
    - `cd nemea-back && npm run lint` exits 0 (warnings pre-existentes aceptables solo si NO tocan archivos modificados en este phase).
    - `cd nemea-back && npm test` exits 0 con todos los suites en green (incluyendo `users.service.spec.ts`, `users.controller.spec.ts`, `scenarios.service.spec.ts` modificados en este phase, plus todos los specs pre-existentes intactos).
    - El SUMMARY de este plan registra el total de tests y el resultado de cada uno de los 3 gates.
  </acceptance_criteria>
  <done>Backend three-gate verification PASS o documented FAILURE.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend type-check + lint + full test suite</name>
  <files></files>
  <read_first>
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-VALIDATION.md
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-03-SUMMARY.md
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-04-SUMMARY.md
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-06-SUMMARY.md
  </read_first>
  <action>
    Mismo patrón que Task 1 pero para el frontend:

    (a) Type check:
    ```
    cd nemea-front && npx tsc --noEmit
    ```
    Expected: exit 0.

    (b) Lint:
    ```
    cd nemea-front && npm run lint
    ```
    Expected: exit 0.

    (c) Test suite completo:
    ```
    cd nemea-front && npm test
    ```
    Expected: exit 0. Los nuevos suites de `EditUserDialog.test.tsx` y `DeleteUserAlertDialog.test.tsx` deben aparecer en la lista de specs ejecutados (PRIMERA suite interactiva del repo per RESEARCH Finding 11).

    Capturar:
    - Total test count del frontend.
    - Pass/fail count.
    - Si el `page.test.tsx` (smoke pre-existente) sigue verde — no regresión.

    SUMMARY refleja "Frontend gate: PASS" o el diagnóstico de FAIL.
  </action>
  <verify>
    <automated>cd nemea-front && npx tsc --noEmit &amp;&amp; cd nemea-front && npm run lint &amp;&amp; cd nemea-front && npm test 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cd nemea-front && npx tsc --noEmit` exits 0.
    - `cd nemea-front && npm run lint` exits 0.
    - `cd nemea-front && npm test` exits 0.
    - Salida de jest muestra al menos 3 test files ejecutados: `page.test.tsx` (pre-existente), `EditUserDialog.test.tsx` (Plan 03), `DeleteUserAlertDialog.test.tsx` (Plan 04). Conteo total de specs `>=` los previos + los 4-5 nuevos de cada dialog.
    - SUMMARY registra cada gate y total de tests.
  </acceptance_criteria>
  <done>Frontend three-gate verification PASS o documented FAILURE.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual smoke — 2-admin scenario end-to-end</name>
  <what-built>
    Plans 01-06 levantaron: PATCH /users/:id, DELETE /users/:id, EditUserDialog, DeleteUserAlertDialog, UsersClient refactor, 3 self-lockout guards, transactional scenario transfer. Esta verificación humana cubre lo que los unit tests no pueden: comportamiento end-to-end con dos cuentas admin reales contra la base de datos en dev.
  </what-built>
  <how-to-verify>
    Setup pre-condición (UNA VEZ):
    1. Levantar dev servers: `cd nemea-back && npm run start:dev` (puerto 4000) y `cd nemea-front && npm run dev` (puerto 3000).
    2. Asegurarse de tener AL MENOS DOS usuarios con role `can_manage_users = true` y `isActive = true` en la base. Si solo hay uno (el admin seed `basualdofelipe@gmail.com`), crear otro admin: ir a `/usuarios` → "Nuevo usuario" → email del admin secundario + role ADMIN. Si no tenés un segundo google account para login, podés usar UPDATE manual en DB para activar un user existente con role ADMIN.
    3. Crear AL MENOS UN scenario para el "victim user" (el segundo admin) — sign in como ese user, ir a `/escenarios`, crear un scenario llamado por ejemplo "Promo Test" con algunos overrides.
    4. Volver a sign in como el admin primary.

    Test cases (cada uno mapea a un AC del SPEC):

    **AC1 — EditUserDialog renderiza prefilled y guarda nombre** (REQ-12.4-1, REQ-12.4-2):
    - En `/usuarios`, click "Editar" sobre el admin secundario.
    - Verificar: Email read-only con su email, Name input prefilled, Role Select prefilled con su role, Switch reflejando isActive.
    - Cambiar Name a "Test Edited" → click Guardar.
    - Esperado: toast "Usuario actualizado", dialog cierra, tabla refresca con el nombre nuevo.

    **AC2 — Cambio de role** (REQ-12.4-1, REQ-12.4-2):
    - Editar el mismo admin secundario, cambiar Role a uno NO admin (ej. USER) → Guardar.
    - Esperado: 200 + badge de role en la tabla cambia.

    **AC3 — Guard self-role-edit** (REQ-12.4-5):
    - Click "Editar" sobre TU PROPIA fila.
    - Verificar: Role Select aparece disabled, Switch aparece disabled (con tooltip al hover). Name editable.
    - Si por algún motivo el UI permite (ej. devtools), intentar disparar PATCH manual con un roleId distinto al actual:
      ```
      fetch('/api/users/<tu-id>', { method: 'PATCH', headers: {...}, body: JSON.stringify({ roleId: '<other>' }) })
      ```
      Esperado: 400 con `{ message: 'No puedes cambiar tu propio rol' }`.

    **AC4 — Guard self-deactivate** (REQ-12.4-5 acceptance):
    - Mismo PATCH manual: `{ isActive: false }` sobre tu propio user.
    - Esperado: 400 con `'No puedes desactivar tu propia cuenta'`.

    **AC5 — Botón Borrar oculto en propia fila** (REQ-12.4-6):
    - Verificar visualmente: en tu propia fila NO hay botón Borrar. En su lugar aparece "(tu cuenta)".

    **AC6 — DeleteUserAlertDialog preview + delete happy path** (REQ-12.4-4, REQ-12.4-3):
    - Click "Borrar" sobre el admin secundario.
    - Verificar dialog title "Eliminar usuario", body con su email en bold + sufijo `' - <nombre>'` mostrado, action button "Borrar usuario" en rojo.
    - Click "Borrar usuario".
    - Esperado: toast "Usuario eliminado", fila desaparece de la tabla, scenario "Promo Test" (que era de él) ahora aparece en `/escenarios` bajo TU cuenta con nombre `"Promo Test - <su-nombre>"`.

    **AC7 — Guard self-delete** (REQ-12.4-6):
    - Si por algún motivo el UI permite (devtools), DELETE manual sobre tu propio user:
      ```
      fetch('/api/users/<tu-id>', { method: 'DELETE', headers: {...} })
      ```
      Esperado: 400 con `'No puedes borrarte a vos mismo'`.

    **AC8 — Guard last-admin** (REQ-12.4-7):
    - Pre-setup: asegurarse que ahora SOLO TÚ sos admin activo (al admin secundario lo borraste en AC6, o si te lo querés saltar — desactivá manualmente en DB todos los demás admins menos tú).
    - PATCH manual: `{ isActive: false }` sobre cualquier OTRO admin que aún exista — NO debería poder fire este guard contra TI mismo (AC4 lo cubre).
    - DELETE manual sobre TU PROPIO id → AC7 lo bloquea con self-delete primero, pero para testear last-admin podés:
      - Crear un tercer admin temporal.
      - Borrar el otro admin (no tú) → debería pasar.
      - Intentar borrar al ÚNICO otro admin que queda activo → debería decir "No se puede dejar el sistema sin administradores activos" si dejarlo sin él dejaría 0 (puede ser que tú aún contás como admin activo, en cuyo caso no fire; documentar el comportamiento observado).
    - Alternativa cleaner: crear DOS test admins (A y B) + el seed tuyo. Sign in como tú. PATCH A para desactivarlo → 200. PATCH B para desactivarlo → si queda solo TÚ, debería tirar 400 con el mensaje del último-admin si la lógica considera "TÚ" como remaining; si la lógica permite (porque tú no estás en el path del cambio), 200. **Documentar el comportamiento observado en SUMMARY** — esto valida el contract real del guard.

    **AC9 — Transacción atómica (defensa observable)** (REQ-12.4-3 acceptance — rollback):
    - Difícil de provocar manualmente sin DB tooling. SKIP del manual smoke, cubierto por el unit test de rollback en Plan 05.

    Reportar en el resume-signal: para cada AC1-AC8, marcar PASS / FAIL / SKIP + descripción del comportamiento real observado. Si TODO pasa, type "approved". Si algo falla, type "FAIL: AC<N> — <descripción>".
  </how-to-verify>
  <files>(none — manual checkpoint, no file mutations)</files>
  <action>Human-driven verification. See <how-to-verify> block above for the 8 AC sequence. Executor (Claude) runs the dev servers and presents the checklist; human tester executes each AC against the running app and reports the result via resume-signal.</action>
  <verify>
    <human-check>Tester completes AC1-AC8 sequence. Each AC has explicit expected behavior in the <how-to-verify> block. Pass criteria: AC1, AC2, AC6 (happy paths) must PASS. AC3, AC4, AC5, AC7, AC8 (guards) must either PASS or surface a clearly-documented FAIL with the response payload observed.</human-check>
  </verify>
  <done>Tester types "approved" (all 8 ACs pass) OR types "FAIL: AC&lt;N&gt; — &lt;description&gt;" (specific failure documented). Either outcome closes the task; FAIL triggers gap-closure workflow externally.</done>
  <resume-signal>Type "approved" para confirmar todos los AC manuales pasan, o detallar fallos con "FAIL: AC&lt;N&gt; — ..." para que el orchestrator dispare gap-closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev environment → smoke tests | Test data is non-PII (admin seed email is the user's own). Dev DB is local Docker — isolated. |
| Manual verification → final phase approval | Human-in-the-loop gate; no automated bypass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.4-29 | Data Integrity | Manual deletion of real admin in dev DB | accept | Dev env only. Productive admin seed (basualdofelipe@gmail.com) should NOT be deleted during smoke. If accidentally deleted, re-run migration seeds. |
| T-12.4-30 | Spoofing | Forged JWT during devtools manual PATCH | accept | Auth is enforced by JwtAuthGuard upstream; devtools cannot forge JWT without `NEXTAUTH_SECRET`. The manual tests assume the tester is signed in as a real admin. |
| T-12.4-31 | Information Disclosure | smoke test logs containing real emails | accept | Smoke is local; logs are not committed. Recommend rotating any logs before commit. |
| T-12.4-SC | Tampering | npm/pip/cargo installs | n/a | No installs in this plan — pure verification. |
</threat_model>

<verification>
- Backend three-gate sequence exits 0 sequentially.
- Frontend three-gate sequence exits 0 sequentially.
- Manual checkpoint: human confirms each AC PASSes or documents failures.
- Phase considered ready for `/gsd:verify-work` once all three sections green.
</verification>

<success_criteria>
- Backend: tsc 0, lint 0, jest 0 (all suites green).
- Frontend: tsc 0, lint 0, jest 0 (all suites green, including 2 new interactive ones).
- Manual smoke: 8 ACs (1-6, 7, 8) reportados con resultado; al menos los happy paths (1, 2, 6) deben PASS para considerar el phase shippable.
- SUMMARY documenta: resultado de cada gate + lista de ACs + cualquier discrepancia entre comportamiento esperado y real.
</success_criteria>

<output>
Create `.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-07-SUMMARY.md` when done. Include:
- Backend gate result (3 sub-gates) con números reales
- Frontend gate result (3 sub-gates) con números reales
- Manual smoke result table: AC1-AC8 → PASS/FAIL/SKIP + nota observacional
- Final go/no-go recommendation: "Phase 12.4 ready for /gsd:verify-work" o "Phase 12.4 needs gap closure — see ACx failures"
</output>

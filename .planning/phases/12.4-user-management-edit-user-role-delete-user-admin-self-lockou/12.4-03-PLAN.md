---
phase: 12.4
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - nemea-front/src/app/(app)/usuarios/EditUserDialog.tsx
  - nemea-front/src/app/(app)/usuarios/__tests__/EditUserDialog.test.tsx
autonomous: true
requirements:
  - REQ-12.4-2
  - REQ-12.4-5
tags:
  - frontend
  - dialog
  - form
  - test

must_haves:
  truths:
    - "EditUserDialog renderiza con valores prefilled: Email (read-only) + Name (editable) + Role (Select) + Active (Switch)"
    - "En propia fila (isOwnRow=true) Role Select y Active Switch están disabled con Tooltip; Name sigue editable"
    - "Submit envía PATCH /api/users/:id solo con campos cambiados; en éxito llama onSuccess()"
    - "En error muestra toast.error con el mensaje del backend (Spanish verbatim)"
    - "Test suite cubre: prefill, own-row disabled, submit happy path, error toast"
  artifacts:
    - path: "nemea-front/src/app/(app)/usuarios/EditUserDialog.tsx"
      provides: "Standalone Dialog component con props EditUserDialogProps"
      exports: ["EditUserDialog"]
    - path: "nemea-front/src/app/(app)/usuarios/__tests__/EditUserDialog.test.tsx"
      provides: "Jest+RTL test suite con ≥4 it cases"
      contains: "describe('EditUserDialog'"
  key_links:
    - from: "EditUserDialog onSubmit"
      to: "apiClientFetch /api/users/:id PATCH"
      via: "fetch wrapper from @/lib/api-client"
      pattern: "apiClientFetch.*PATCH"
    - from: "EditUserDialog isOwnRow=true"
      to: "Role Select disabled + Switch disabled + Tooltip"
      via: "prop drilling"
      pattern: "disabled=\\{isOwnRow"
---

<objective>
Crear el componente `EditUserDialog` (nuevo archivo standalone, co-located con UsersClient) y su test suite. Es un Dialog con 4 campos (Email read-only, Name, Role, Active) que persiste cambios via PATCH `/api/users/:id`.

Purpose: Reemplazar el botón inline de toggle-status con un flujo completo de edit. Cumple REQ-12.4-2 (UI de edit) + REQ-12.4-5 (reflejo UI del guard de self-role-edit via disabled Role/Switch en own row).

Output:
- `nemea-front/src/app/(app)/usuarios/EditUserDialog.tsx` (NEW)
- `nemea-front/src/app/(app)/usuarios/__tests__/EditUserDialog.test.tsx` (NEW — primer test interactivo del repo)

Independiente de Plans 01/02/05: no necesita el backend running para compilar/testear. Plan 06 lo wire-ea en UsersClient.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-SPEC.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-CONTEXT.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-RESEARCH.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-UI-SPEC.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-PATTERNS.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-VALIDATION.md
@CLAUDE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md

<interfaces>
<!-- Contratos clave del codebase que el executor necesita SIN explorar -->

From nemea-front/src/types/role.ts:
```ts
export interface RoleOption {
  id: string;
  name: string;
  // ... posibles flags adicionales
}
```

UserRow type (definido en UsersClient.tsx — re-declarar en EditUserDialog.tsx o exportar desde shared):
```ts
interface UserRow {
  id: string;
  email: string;
  name: string | null;
  role: { id: string; name: string };
  isActive: boolean;
  createdAt: string;
}
```
Recomendación: declarar/exportar `UserRow` desde un archivo compartido `nemea-front/src/app/(app)/usuarios/types.ts` (NUEVO MINI-FILE) y que tanto EditUserDialog, DeleteUserAlertDialog (Plan 04) y UsersClient (Plan 06) lo importen. ALTERNATIVA: declarar `UserRow` interface localmente en EditUserDialog (Plan 04 hará su propia declaración) — más simple, sin file extra. ELEGIR: declaración local en cada file (más simple, sin coordinación con Plans 04/06).

From nemea-front/src/components/ui/dialog.tsx — exports:
- `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle`, `DialogDescription`, `DialogFooter`

From nemea-front/src/components/ui/switch.tsx — exports `Switch`. API: `<Switch checked={...} onCheckedChange={(v) => ...} disabled={...} />`.

From nemea-front/src/components/ui/tooltip.tsx — exports `TooltipProvider`, `Tooltip`, `TooltipTrigger`, `TooltipContent`.

From nemea-front/src/lib/api-client.ts — `apiClientFetch(path, token, init)`: handles 401 redirect + extrae `error.message` del body JSON y propaga via `throw new Error(message)`.

From nemea-front/src/app/(app)/usuarios/UsersClient.tsx (PATRÓN ESPEJO — Create dialog form):
- Patrón react-hook-form + zod resolver.
- `<form onSubmit={(e) => void handleSubmit(onSubmit)(e)} className='space-y-4'>` wrapping all field groups.
- Field group: `<div className='space-y-2'><Label htmlFor=... /><Input id=... {...register('field')} /></div>`.
- Select wiring: `<Select value={watch('roleId')} onValueChange={(v) => setValue('roleId', v, { shouldValidate: true })} disabled={isSubmitting}>`.
- Footer: `<DialogFooter><Button variant='outline' type='button'>Cancelar</Button><Button type='submit'>{isSubmitting && <Loader2 .../>}Guardar</Button></DialogFooter>`.

From nemea-front/src/app/(app)/roles/RolesClient.tsx (PATRÓN ESPEJO — reset on edit target change + Switch wiring):
```tsx
function openEditDialog(role: RoleRow): void {
  reset({ name, description, canViewProducts, ... });
  setDialogOpen(true);
}

<Switch
  id={`perm-${key}`}
  checked={watch(key)}
  onCheckedChange={(value) => setValue(key, value, { shouldDirty: true })}
  disabled={isSubmitting}
/>
```

Zod schema (per UI-SPEC):
```ts
const editUserSchema = z.object({
  name: z.string().max(255).optional().or(z.literal('')),
  roleId: z.string().uuid('Selecciona un rol'),
  isActive: z.boolean(),
});
```

Tooltip wrapping disabled control (per UI-SPEC + RESEARCH P11):
```tsx
<TooltipProvider>
  <Tooltip>
    <TooltipTrigger asChild>
      <span tabIndex={0}>
        <Switch ... disabled />
      </span>
    </TooltipTrigger>
    <TooltipContent>...</TooltipContent>
  </Tooltip>
</TooltipProvider>
```
Razón del wrapper span: browsers don't emit pointer events on disabled controls — el `<span tabIndex={0}>` recibe el focus/hover y dispara el tooltip.

Jest config (verified per RESEARCH Finding 11):
- `nemea-front/jest.config.js` ya existe con `testEnvironment: jsdom` y `moduleNameMapper '@/(.*)' -> src/$1`.
- `nemea-front/jest.setup.ts` carga `@testing-library/jest-dom`.
- Mocks (`next-auth/react`, `next/navigation`, `sonner`, `global.fetch`) van per-test, NO global.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear EditUserDialog component</name>
  <files>nemea-front/src/app/(app)/usuarios/EditUserDialog.tsx</files>
  <read_first>
    - nemea-front/src/app/(app)/usuarios/UsersClient.tsx (analog A: Create dialog inline)
    - nemea-front/src/app/(app)/roles/RolesClient.tsx (analog B: reset on edit target + Switch wiring)
    - nemea-front/src/components/scenarios/CreateScenarioDialog.tsx (analog C: standalone Dialog props contract)
    - nemea-front/src/components/ui/dialog.tsx (Dialog primitive)
    - nemea-front/src/components/ui/switch.tsx (Switch primitive)
    - nemea-front/src/components/ui/tooltip.tsx (Tooltip primitive)
    - nemea-front/src/lib/api-client.ts (apiClientFetch wrapper)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-UI-SPEC.md (sección "EditUserDialog" + State Matrix + Copy Inventory + Implementation Notes)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-CONTEXT.md (D-08, D-17, D-18)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-PATTERNS.md (sección "NEW: nemea-front/src/app/(app)/usuarios/EditUserDialog.tsx")
  </read_first>
  <behavior>
    - Test 1: Cuando se renderiza con un user válido y open=true, muestra Email (en input read-only) + Name input prefilled con user.name + Role Select prefilled con user.role.id + Switch reflejando user.isActive.
    - Test 2: Con isOwnRow=true: Role Select disabled + Switch disabled + Name input NO disabled. Wrappers con TooltipProvider/Tooltip presentes para Role y Switch.
    - Test 3: Al cambiar Name y clickear "Guardar": fetch PATCH /api/users/:id se llama con body JSON que contiene SOLO los campos cambiados; on success llama onSuccess(); muestra toast.success('Usuario actualizado').
    - Test 4: Si fetch retorna 400 con body { message: 'No puedes cambiar tu propio rol' }: muestra toast.error con ese mensaje verbatim; dialog STAYS OPEN; isSubmitting vuelve a false.
    - Test 5: Si user prop cambia (re-open con otro target), form re-resetea con los nuevos valores (useEffect on user).
  </behavior>
  <action>
    Crear `nemea-front/src/app/(app)/usuarios/EditUserDialog.tsx` desde cero. File `'use client'` directive obligatoria al top.

    (a) Imports: `useEffect` de react, `useSession` de next-auth/react, `useForm` de react-hook-form, `z` de zod, `zodResolver` de @hookform/resolvers/zod, `Loader2` de lucide-react, `toast` de sonner, plus Shadcn primitives: `Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter` (dialog.tsx), `Input` (input.tsx), `Label` (label.tsx), `Button` (button.tsx), `Select, SelectContent, SelectItem, SelectTrigger, SelectValue` (select.tsx), `Switch` (switch.tsx), `Tooltip, TooltipContent, TooltipProvider, TooltipTrigger` (tooltip.tsx). Plus `apiClientFetch` de @/lib/api-client y `RoleOption` de @/types/role.

    (b) Declarar localmente la interface `UserRow` con los campos: `id, email, name (nullable), role: { id, name }, isActive, createdAt`. NO crear file de types compartido.

    (c) Declarar el zod schema `editUserSchema` con los tres campos (name optional/empty allowed, roleId uuid, isActive boolean) per UI-SPEC Implementation Notes.

    (d) Definir `type EditUserFormData = z.infer<typeof editUserSchema>;`.

    (e) Declarar `interface EditUserDialogProps` con: `user: UserRow | null; open: boolean; onOpenChange: (open: boolean) => void; roles: RoleOption[]; isOwnRow: boolean; onSuccess: () => void;`.

    (f) Export named function `EditUserDialog(props: EditUserDialogProps): ReactElement | null`:
       - Si `user === null`: return `null` (defensive — dialog should also be `open=false` in that case).
       - Obtener `token` desde `useSession()`: `const { data: session } = useSession(); const token = session?.accessToken ?? '';`.
       - useForm con zodResolver(editUserSchema). defaultValues: name `''`, roleId `''`, isActive `true`.
       - `useEffect(() => { if (user) reset({ name: user.name ?? '', roleId: user.role.id, isActive: user.isActive }); }, [user, reset]);` — re-resetea cuando el target cambia.

    (g) Definir `async function onSubmit(data: EditUserFormData): Promise<void>`:
       - Construir un `dto: Record<string, unknown> = {};` y agregar solo campos cambiados respecto al `user` actual:
         * `if (data.name !== (user.name ?? '')) dto.name = data.name;`
         * `if (data.roleId !== user.role.id) dto.roleId = data.roleId;`
         * `if (data.isActive !== user.isActive) dto.isActive = data.isActive;`
       - Llamar `await apiClientFetch(\`/api/users/\${user.id}\`, token, { method: 'PATCH', body: JSON.stringify(dto) });`. Si `dto` está vacío, igual llamar al PATCH (backend devuelve 200 sin cambios — per UI-SPEC recommendation, evita branching).
       - On success: `toast.success('Usuario actualizado');` + `onSuccess();`.
       - try/catch: en catch llamar `toast.error(error instanceof Error ? error.message : 'No se pudo actualizar el usuario');` y NO cerrar dialog (el setState de RHF `isSubmitting` se desactiva solo).

    (h) JSX:
       - `<Dialog open={open} onOpenChange={onOpenChange}>`
         - `<DialogContent className='sm:max-w-md'>`
           - `<DialogHeader><DialogTitle>Editar usuario</DialogTitle><DialogDescription className='sr-only'>Editar nombre, rol y estado del usuario.</DialogDescription></DialogHeader>`
           - `<form onSubmit={(e) => void handleSubmit(onSubmit)(e)} className='space-y-4'>`
             - Field group "Email" (NO editable): `<div className='space-y-2'><Label htmlFor='edit-user-email'>Email</Label><Input id='edit-user-email' type='email' value={user.email} readOnly className='bg-muted text-muted-foreground cursor-not-allowed' /></div>` (NO usar `disabled`).
             - Field group "Nombre": Label + Input register('name') placeholder='Nombre del usuario' disabled={isSubmitting} + errors.name si existe.
             - Field group "Rol": Label + Select. Si `isOwnRow`: envolver el Select en TooltipProvider/Tooltip/TooltipTrigger(asChild)/span(tabIndex={0}) + TooltipContent con copy "No puedes cambiar tu propio rol". El Select tiene `disabled={isOwnRow || isSubmitting}` siempre. Mostrar errors.roleId si existe.
             - Field group "Activo": Label + Switch (con id 'edit-user-active'). El Switch usa `<div className='flex items-center justify-between'>` (per UI-SPEC layout) wrapping Label + Switch. Si `isOwnRow`: envolver el Switch igual que el Select (Tooltip wrapper + span tabIndex). Switch tiene `checked={watch('isActive')} onCheckedChange={(v) => setValue('isActive', v, { shouldDirty: true })} disabled={isOwnRow || isSubmitting}`. Copy del tooltip: "No puedes cambiar tu propio estado".
           - `<DialogFooter><Button type='button' variant='outline' onClick={() => onOpenChange(false)} disabled={isSubmitting}>Cancelar</Button><Button type='submit' disabled={isSubmitting}>{isSubmitting && <Loader2 className='mr-2 size-4 animate-spin' />}Guardar</Button></DialogFooter>`

    Style: TS strict, no `any` (usar `unknown` para el dto Record value type). Single quotes. Explicit `ReactElement | null` return.
  </action>
  <verify>
    <automated>cd nemea-front && npx tsc --noEmit 2>&amp;1 | grep -E "EditUserDialog" | wc -l ; cd nemea-front && npm run lint -- src/app/\\(app\\)/usuarios/EditUserDialog.tsx 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `nemea-front/src/app/(app)/usuarios/EditUserDialog.tsx` exists with `'use client'` directive at the top.
    - `grep -cE "export function EditUserDialog" nemea-front/src/app/\(app\)/usuarios/EditUserDialog.tsx` returns `1`.
    - `grep -cE "interface EditUserDialogProps" nemea-front/src/app/\(app\)/usuarios/EditUserDialog.tsx` returns `1` with `user`, `open`, `onOpenChange`, `roles`, `isOwnRow`, `onSuccess` all present (verify by grep on each name).
    - `grep -cE "readOnly" nemea-front/src/app/\(app\)/usuarios/EditUserDialog.tsx` returns ≥1 (Email field).
    - `grep -cE "disabled=\\{isOwnRow" nemea-front/src/app/\(app\)/usuarios/EditUserDialog.tsx` returns ≥2 (Role Select + Switch).
    - `grep -cE "apiClientFetch.*PATCH|method: 'PATCH'" nemea-front/src/app/\(app\)/usuarios/EditUserDialog.tsx` returns ≥1.
    - `grep -cE "TooltipProvider|Tooltip|TooltipTrigger|TooltipContent" nemea-front/src/app/\(app\)/usuarios/EditUserDialog.tsx` returns ≥4 (all four imports + ≥2 usages).
    - `grep -cE "sm:max-w-md" nemea-front/src/app/\(app\)/usuarios/EditUserDialog.tsx` returns `1`.
    - `grep -cE "useEffect.*\\[user" nemea-front/src/app/\(app\)/usuarios/EditUserDialog.tsx` returns ≥1 (reset on user change).
    - `cd nemea-front && npx tsc --noEmit 2>&amp;1 | grep -E "EditUserDialog\\.tsx" | wc -l` returns `0` (no type errors in this file).
    - `cd nemea-front && npm run lint -- src/app/\\(app\\)/usuarios/EditUserDialog.tsx` exits 0.
  </acceptance_criteria>
  <done>Component compila, lint pasa, props contract correcto, own-row disables Role+Switch con tooltip, submit usa apiClientFetch PATCH.</done>
</task>

<task type="auto">
  <name>Task 2: Crear test suite EditUserDialog.test.tsx</name>
  <files>nemea-front/src/app/(app)/usuarios/__tests__/EditUserDialog.test.tsx</files>
  <read_first>
    - nemea-front/src/app/(app)/usuarios/EditUserDialog.tsx (component recién creado en Task 1)
    - nemea-front/src/app/(app)/__tests__/page.test.tsx (único analog FE — render-only smoke; este test será el primero interactivo)
    - nemea-front/jest.config.js + jest.setup.ts (verificar paths + setup)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-CONTEXT.md (D-11 — test scope)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-RESEARCH.md (Finding 7 — mock useSession; Finding 11 — setup; P3 + P4 pitfalls)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-PATTERNS.md (sección "NEW: ...EditUserDialog.test.tsx" — full skeleton)
  </read_first>
  <action>
    Crear `nemea-front/src/app/(app)/usuarios/__tests__/EditUserDialog.test.tsx`. Co-located dentro de `__tests__/` directorio next al component.

    (a) Top del file, ANTES de cualquier import del componente bajo test, declarar TODOS los jest.mock calls (Jest hoist-ea `jest.mock`, NO los imports):
       - `jest.mock('next-auth/react', () => ({ useSession: jest.fn(() => ({ data: { user: { id: 'caller-uuid', email: 'admin@nemea.com', permissions: { canManageUsers: true } }, accessToken: 'fake-token' }, status: 'authenticated' })) }));`
       - `jest.mock('next/navigation', () => ({ useRouter: () => ({ refresh: jest.fn() }) }));`
       - `jest.mock('sonner', () => ({ toast: { success: jest.fn(), error: jest.fn() } }));`

    (b) Imports de testing: `render, screen, waitFor` de `@testing-library/react`, `userEvent` (default export) de `@testing-library/user-event`, y el componente `EditUserDialog` desde `'../EditUserDialog'`. Tip: importar `toast` de `'sonner'` también para poder assertear las llamadas mockeadas (TS-friendly: `import { toast } from 'sonner';`).

    (c) Declarar constantes de fixture al top-level:
       ```
       const mockUser = {
         id: 'victim-uuid',
         email: 'user@nemea.com',
         name: 'Juan',
         role: { id: 'role-uuid', name: 'EDITOR' },
         isActive: true,
         createdAt: '2026-01-01T00:00:00.000Z',
       };
       const mockRoles = [
         { id: 'role-uuid', name: 'EDITOR' },
         { id: 'admin-role-uuid', name: 'ADMIN' },
       ];
       ```

    (d) `describe('EditUserDialog', () => { ... })` con `beforeEach`:
       ```
       beforeEach(() => {
         global.fetch = jest.fn(() => Promise.resolve({
           ok: true,
           status: 200,
           json: () => Promise.resolve({ data: { ...mockUser, name: 'Nuevo' } }),
         })) as jest.Mock;
         jest.clearAllMocks();
       });
       ```

    (e) Tests (mínimo cuatro, idealmente cinco):

    1. `it('renders prefilled with current values', () => { ... })`:
       - render `<EditUserDialog user={mockUser} open={true} onOpenChange={jest.fn()} roles={mockRoles} isOwnRow={false} onSuccess={jest.fn()} />`.
       - `expect(screen.getByDisplayValue('user@nemea.com')).toBeInTheDocument();` (Email).
       - `expect(screen.getByDisplayValue('Juan')).toBeInTheDocument();` (Name).

    2. `it('disables Role and Switch when isOwnRow is true', () => { ... })`:
       - render con isOwnRow={true}.
       - Name editable: `expect(screen.getByLabelText(/Nombre/i)).not.toBeDisabled();`.
       - Switch disabled: `expect(screen.getByRole('switch')).toBeDisabled();`.
       - Para el Select, asegurar que existe (Radix Select renderiza un button trigger): `expect(screen.getByRole('combobox')).toBeDisabled();` (o `getByText('EDITOR')` aria) — si Radix Select API en tu versión usa otro role, fallback a `getByText('EDITOR')` y assertear visualmente que el componente padre tiene atributo `data-state` o `aria-disabled`. Documentar lo que sea que pase.

    3. `it('submits PATCH and triggers onSuccess on success', async () => { ... })`:
       - `const onSuccess = jest.fn();`
       - render con isOwnRow={false} y la onSuccess.
       - `const user = userEvent.setup();`
       - `await user.clear(screen.getByLabelText(/Nombre/i)); await user.type(screen.getByLabelText(/Nombre/i), 'Nuevo');`
       - `await user.click(screen.getByRole('button', { name: /Guardar/i }));`
       - `await waitFor(() => { expect(global.fetch).toHaveBeenCalledWith(expect.stringContaining('/api/users/victim-uuid'), expect.objectContaining({ method: 'PATCH' })); expect(onSuccess).toHaveBeenCalled(); });`

    4. `it('shows backend error message via toast.error on failure', async () => { ... })`:
       - `(global.fetch as jest.Mock).mockResolvedValueOnce({ ok: false, status: 400, json: () => Promise.resolve({ message: 'No puedes cambiar tu propio rol' }) });`
       - render con isOwnRow={false}, onSuccess mock.
       - userEvent.click Guardar.
       - `await waitFor(() => { expect(toast.error).toHaveBeenCalledWith('No puedes cambiar tu propio rol'); });`
       - Importar toast del mock: `import { toast } from 'sonner';` y typear `(toast.error as jest.Mock).mock.calls[0][0]` si la assertion arriba es flaky.

    5. (Opcional) `it('resets form when user prop changes', () => { ... })`:
       - Rerender con un user distinto y assertear que los display values cambian.

    Coverage targets (per D-11 + VALIDATION.md):
    - form renders with prefilled values
    - Name editable / Role+Active disabled on own row
    - submit calls PATCH and triggers onSuccess
    - error handling shows toast

    NO testear el contenido del `<TooltipContent>` (Radix Portal — jsdom flaky per RESEARCH P7). Assertear solo que `disabled` está aplicado.

    Style: single quotes. Explicit return types donde aplique. Usar `as jest.Mock` para cast del `global.fetch`. Usar `userEvent.setup()` (v14 API).
  </action>
  <verify>
    <automated>cd nemea-front && npm test -- --testPathPattern=EditUserDialog 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `nemea-front/src/app/(app)/usuarios/__tests__/EditUserDialog.test.tsx` exists.
    - `grep -cE "describe\\('EditUserDialog'" nemea-front/src/app/\(app\)/usuarios/__tests__/EditUserDialog.test.tsx` returns `1`.
    - `grep -cE "jest\\.mock\\('next-auth/react'" nemea-front/src/app/\(app\)/usuarios/__tests__/EditUserDialog.test.tsx` returns `1`.
    - `grep -cE "jest\\.mock\\('sonner'" nemea-front/src/app/\(app\)/usuarios/__tests__/EditUserDialog.test.tsx` returns `1`.
    - `grep -cE "^[[:space:]]*it\\(" nemea-front/src/app/\(app\)/usuarios/__tests__/EditUserDialog.test.tsx` returns ≥4.
    - `cd nemea-front && npm test -- --testPathPattern=EditUserDialog` exits 0; output reports ≥4 passing tests.
    - No "Cannot find module" errors: the test file imports `'../EditUserDialog'` which resolves to the Task-1 file.
    - Sampling rate satisfied per VALIDATION.md (rows 12.4-04-XX REQ-12.4-2 and REQ-12.4-5).
  </acceptance_criteria>
  <done>≥4 tests pass. Mocks at top. fetch/sonner/next-auth/next-navigation all mocked per-test. No real network calls. Tooltip content not asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM → React state | All form input handled via react-hook-form (typed) + zod validation before serialization |
| React state → fetch body | JSON.stringify of validated dto (only changed fields) |
| fetch response → toast UI | error.message from backend Spanish strings surfaced verbatim (no HTML rendering, no eval) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.4-14 | Spoofing | session.user.id misuse to forge own-row | mitigate (defense-in-depth) | `isOwnRow` is computed in the parent (UsersClient Plan 06) from `session.user.id === user.id`. The dialog itself trusts the prop. Backend re-checks self via callerId from JWT in Plan 05 — UI bypass would still hit a 400. |
| T-12.4-03 | Elevation of Privilege | own-row Role/Switch UI bypass | mitigate (UI layer) | Role Select + Switch have `disabled={isOwnRow || isSubmitting}`. Backend guard (Plan 05) is the authoritative check; the UI disable is a UX courtesy. |
| T-12.4-15 | Information Disclosure | error toast surfacing internal info | accept | Backend BadRequestException messages are short Spanish ("No puedes cambiar tu propio rol") — informational, not infrastructure-leaking. Generic 500 errors fall back to "No se pudo actualizar el usuario" message. |
| T-12.4-16 | Tampering | fetch body construction in dto | accept | dto is built field-by-field from validated `data` (zod-checked). No `Object.assign` from raw user input. JSON.stringify is safe. |
| T-12.4-17 | XSS | rendering user.name / user.email in JSX | mitigate | React escapes text children by default. No `dangerouslySetInnerHTML` used. The `user.email` rendered in the Email Input value is a string attribute, also escaped. |
| T-12.4-SC | Tampering | npm install for testing libraries | n/a | No new packages — @testing-library/react, jest-environment-jsdom, @testing-library/user-event all already in package.json (verified per RESEARCH stack confirmation table). |
</threat_model>

<verification>
- `cd nemea-front && npx tsc --noEmit` produces no errors referencing EditUserDialog.tsx or its test.
- `cd nemea-front && npm run lint -- src/app/(app)/usuarios/EditUserDialog.tsx src/app/(app)/usuarios/__tests__/EditUserDialog.test.tsx` exits 0.
- `cd nemea-front && npm test -- --testPathPattern=EditUserDialog` exits 0, ≥4 tests pass.
- Grep gates per task acceptance_criteria.
- Manual: Plan 06 will wire this dialog into UsersClient — at that point can do a visual smoke. Not required at this plan's gate.
</verification>

<success_criteria>
- EditUserDialog.tsx exists, exports `EditUserDialog`, props contract matches UI-SPEC.
- Email read-only, Name editable always, Role/Switch disabled on own row with Tooltip wrappers.
- onSubmit builds partial dto (only changed fields), calls apiClientFetch PATCH, propagates backend message via toast.error on failure.
- Test suite has ≥4 it cases passing: prefill, own-row disabled, submit happy path, error toast.
- Mocks (next-auth/react, next/navigation, sonner, global.fetch) all per-test, no global jest.setup changes.
</success_criteria>

<output>
Create `.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-03-SUMMARY.md` when done. Include:
- The exact EditUserDialogProps contract (interface signature)
- List of `it()` cases that pass
- Confirmation that Plan 06 will integrate this component
- Note: First interactive FE test in the repo
</output>

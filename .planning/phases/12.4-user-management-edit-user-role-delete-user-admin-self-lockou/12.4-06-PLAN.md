---
phase: 12.4
plan: 06
type: execute
wave: 2
depends_on:
  - 12.4-01
  - 12.4-03
  - 12.4-04
files_modified:
  - nemea-front/src/app/(app)/usuarios/UsersClient.tsx
  - nemea-back/src/users/users.controller.spec.ts
autonomous: true
requirements:
  - REQ-12.4-2
  - REQ-12.4-4
  - REQ-12.4-6
tags:
  - frontend
  - backend
  - integration
  - controller-test

must_haves:
  truths:
    - "UsersClient ya NO usa toggle-status — `togglingId` state y `handleToggleStatus` removidos"
    - "Cada fila tiene un botón Editar + un botón Borrar (Borrar oculto en propia fila, reemplazado por '(tu cuenta)')"
    - "Clicks abren EditUserDialog / DeleteUserAlertDialog con el target correcto"
    - "isOwnRow se pasa al EditUserDialog para deshabilitar Role/Switch en propio user"
    - "users.controller.spec.ts ya NO testea toggle-status; testea PATCH y DELETE con (id, dto/_, callerId)"
  artifacts:
    - path: "nemea-front/src/app/(app)/usuarios/UsersClient.tsx"
      provides: "UsersClient refactored con Editar+Borrar buttons + dialog mounts"
      contains: "EditUserDialog"
    - path: "nemea-back/src/users/users.controller.spec.ts"
      provides: "Controller spec con casos PATCH y DELETE; sin toggle-status"
      contains: "describe('PATCH /users/:id'"
  key_links:
    - from: "UsersClient row action buttons"
      to: "EditUserDialog + DeleteUserAlertDialog"
      via: "editTarget/deleteTarget state"
      pattern: "setEditTarget|setDeleteTarget"
    - from: "users.controller.spec PATCH cases"
      to: "mockUsersService.update"
      via: "Test.createTestingModule provider mock"
      pattern: "mockUsersService\\.update"
---

<objective>
Integrar `EditUserDialog` (Plan 03) y `DeleteUserAlertDialog` (Plan 04) en `UsersClient.tsx`, reemplazando el botón inline de toggle-status con dos botones (Editar/Borrar) per row. Borrar oculto en propia fila per D-09. Paralelamente actualizar `users.controller.spec.ts` para reflejar los nuevos endpoints (Plan 01) y eliminar los tests de toggle-status (D-14).

Purpose: Cierra el integration layer del UI (REQ-12.4-2 Edit + REQ-12.4-4 Delete preview + REQ-12.4-6 UX reflection del self-delete guard via hiding the button) y el controller test coverage (D-14 cleanup + 2 nuevos describes).

Output:
- `nemea-front/src/app/(app)/usuarios/UsersClient.tsx` (MODIFIED) — remove toggle-status, add Editar/Borrar buttons, mount dialogs.
- `nemea-back/src/users/users.controller.spec.ts` (MODIFIED) — borrar toggle-status describe, agregar PATCH y DELETE describes.

Depende de Plan 01 (controller endpoints existen para que el spec compile), Plan 03 (EditUserDialog) y Plan 04 (DeleteUserAlertDialog).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-SPEC.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-CONTEXT.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-UI-SPEC.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-PATTERNS.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-VALIDATION.md
@CLAUDE.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-01-SUMMARY.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-03-SUMMARY.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-04-SUMMARY.md

<interfaces>
From Plan 03 (EditUserDialog):
```ts
interface EditUserDialogProps {
  user: UserRow | null;
  open: boolean;
  onOpenChange: (open: boolean) => void;
  roles: RoleOption[];
  isOwnRow: boolean;
  onSuccess: () => void;
}
```
Import: `import { EditUserDialog } from './EditUserDialog';`

From Plan 04 (DeleteUserAlertDialog):
```ts
interface DeleteUserAlertDialogProps {
  user: UserRow | null;
  open: boolean;
  onOpenChange: (open: boolean) => void;
  onSuccess: () => void;
}
```
Import: `import { DeleteUserAlertDialog } from './DeleteUserAlertDialog';`

From nemea-back/src/users/users.controller.ts (post Plan 01):
- `async update(@Param() id, @Body() dto: UpdateUserDto, @CurrentUser('id') callerId): Promise<User>` — delegates to `usersService.update(id, dto, callerId)`.
- `async remove(@Param() id, @CurrentUser('id') callerId): Promise<void>` — delegates to `usersService.remove(id, callerId)`.

From nemea-front/src/app/(app)/usuarios/UsersClient.tsx (current — actionable lines):
- Line 71: `const [togglingId, setTogglingId] = useState<string | null>(null);` — DELETE.
- Lines 110-125: `async function handleToggleStatus(user: UserRow)` — DELETE.
- Lines 183-201: actions cell with single button — REPLACE.
- Line 68: `const currentUserId = session?.user?.id;` — KEEP (used by both dialogs).
- Line 65: `const router = useRouter();` — KEEP (used by router.refresh in onSuccess).

UI-SPEC Actions Column tokens:
- Container: `<div className='flex items-center justify-end gap-2'>`.
- Editar button: `<Button size='sm' variant='outline' onClick={() => setEditTarget(user)}><Pencil className='mr-1 size-3' />Editar</Button>`.
- Borrar button (other rows): `<Button size='sm' variant='outline' className='text-destructive hover:text-destructive' onClick={() => setDeleteTarget(user)}><Trash2 className='mr-1 size-3' />Borrar</Button>`.
- Own row marker: `<span className='text-muted-foreground text-xs'>(tu cuenta)</span>` (matches existing line 185-187).

From nemea-back/src/users/users.controller.spec.ts (current):
- Has `mockUsersService = { findAll, findByEmail, findById, create, deactivate, activate }`.
- Has `describe('PATCH /users/:id/toggle-status', ...)` (lines ~95-159) — DELETE.
- Has `describe('POST /users', ...)` — KEEP.
- Has `describe('GET /users', ...)` — KEEP.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor UsersClient.tsx — remove toggle-status, integrate dialogs</name>
  <files>nemea-front/src/app/(app)/usuarios/UsersClient.tsx</files>
  <read_first>
    - nemea-front/src/app/(app)/usuarios/UsersClient.tsx (current state)
    - nemea-front/src/app/(app)/usuarios/EditUserDialog.tsx (Plan 03 output — props contract)
    - nemea-front/src/app/(app)/usuarios/DeleteUserAlertDialog.tsx (Plan 04 output — props contract)
    - nemea-front/src/app/(app)/roles/RolesClient.tsx (lines 363-383 — actions column analog)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-UI-SPEC.md (Components Visual Tokens "UsersClient — Actions Column")
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-CONTEXT.md (D-08, D-09, D-13)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-PATTERNS.md (sección "MODIFIED: ...UsersClient.tsx")
  </read_first>
  <behavior>
    - Renderiza la tabla con cada fila mostrando Editar + Borrar para otros users; Editar + "(tu cuenta)" text para propia fila.
    - Click en Editar abre EditUserDialog con `user={target}` y `isOwnRow={target.id === currentUserId}`.
    - Click en Borrar (otra fila) abre DeleteUserAlertDialog con `user={target}`.
    - El dialog Create del usuario actual sigue funcionando idéntico (no se toca).
    - Tras success de Edit o Delete, dialog se cierra y `router.refresh()` se llama.
    - `togglingId` state y `handleToggleStatus` ya NO existen.
  </behavior>
  <action>
    Editar `nemea-front/src/app/(app)/usuarios/UsersClient.tsx`:

    (a) Imports — agregar:
    - `import { EditUserDialog } from './EditUserDialog';`
    - `import { DeleteUserAlertDialog } from './DeleteUserAlertDialog';`
    - `import { Pencil, Trash2 } from 'lucide-react';` (verificar: si ya hay imports de lucide-react `{ Loader2, Plus }`, agregar `Pencil` y `Trash2` al mismo statement).
    - Mantener `useState` import.

    (b) State del componente:
    - BORRAR `const [togglingId, setTogglingId] = useState<string | null>(null);`.
    - AGREGAR `const [editTarget, setEditTarget] = useState<UserRow | null>(null);`.
    - AGREGAR `const [deleteTarget, setDeleteTarget] = useState<UserRow | null>(null);`.

    (c) BORRAR la función `handleToggleStatus` entera (líneas 110-125 aprox). Borrar también la importación de cualquier helper que solo se usaba en `handleToggleStatus` (verificar lint warnings tras la edición).

    (d) Reemplazar la celda de acciones (líneas ~183-201 — el ternario `user.id === currentUserId ? <span> : <Button>`) por la nueva estructura:

    ```tsx
    <TableCell>
      <div className='flex items-center justify-end gap-2'>
        <Button
          size='sm'
          variant='outline'
          onClick={() => setEditTarget(user)}
        >
          <Pencil className='mr-1 size-3' />
          Editar
        </Button>
        {user.id === currentUserId ? (
          <span className='text-muted-foreground text-xs'>(tu cuenta)</span>
        ) : (
          <Button
            size='sm'
            variant='outline'
            className='text-destructive hover:text-destructive'
            onClick={() => setDeleteTarget(user)}
          >
            <Trash2 className='mr-1 size-3' />
            Borrar
          </Button>
        )}
      </div>
    </TableCell>
    ```

    Observación: el botón Editar siempre se muestra (D-08), incluso en propia fila. El control disabled se delega al EditUserDialog via `isOwnRow` prop.

    (e) Al FINAL del JSX del componente, DESPUÉS del `<Dialog>` existente de create-user, AGREGAR los dos nuevos mounts:

    ```tsx
    <EditUserDialog
      user={editTarget}
      open={editTarget !== null}
      onOpenChange={(open) => { if (!open) setEditTarget(null); }}
      roles={roles}
      isOwnRow={editTarget?.id === currentUserId}
      onSuccess={() => {
        setEditTarget(null);
        router.refresh();
      }}
    />
    <DeleteUserAlertDialog
      user={deleteTarget}
      open={deleteTarget !== null}
      onOpenChange={(open) => { if (!open) setDeleteTarget(null); }}
      onSuccess={() => {
        setDeleteTarget(null);
        router.refresh();
      }}
    />
    ```

    Nota: `isOwnRow={editTarget?.id === currentUserId}` evalúa a `false` cuando `editTarget` es null (boolean coercion); cuando se abre con el usuario propio, evalúa a `true`. NO usar `!!` extra — `===` ya retorna boolean.

    (f) Verificar imports unused tras la edición — `Loader2` puede quedar unused si NO se usa más en este file (era solo para el spinner del toggle button). Si lint warning aparece, borrar el import de `Loader2`. Mantener `Plus` (todavía se usa en el botón "Nuevo usuario").

    (g) NO modificar el form de Create (líneas ~209-294) — el flujo de creación sigue intacto.

    Style: TS strict, single quotes, no `any`. Mantener explicit return types existentes.
  </action>
  <verify>
    <automated>grep -cE "handleToggleStatus|togglingId|toggle-status" nemea-front/src/app/\\(app\\)/usuarios/UsersClient.tsx; grep -cE "EditUserDialog|DeleteUserAlertDialog" nemea-front/src/app/\\(app\\)/usuarios/UsersClient.tsx; grep -cE "Pencil|Trash2" nemea-front/src/app/\\(app\\)/usuarios/UsersClient.tsx; cd nemea-front && npx tsc --noEmit 2>&amp;1 | grep -E "UsersClient\\.tsx" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "handleToggleStatus" nemea-front/src/app/\\(app\\)/usuarios/UsersClient.tsx` returns `0`.
    - `grep -cE "togglingId" nemea-front/src/app/\\(app\\)/usuarios/UsersClient.tsx` returns `0`.
    - `grep -cE "toggle-status" nemea-front/src/app/\\(app\\)/usuarios/UsersClient.tsx` returns `0`.
    - `grep -cE "import.*EditUserDialog.*from './EditUserDialog'" nemea-front/src/app/\\(app\\)/usuarios/UsersClient.tsx` returns `1`.
    - `grep -cE "import.*DeleteUserAlertDialog.*from './DeleteUserAlertDialog'" nemea-front/src/app/\\(app\\)/usuarios/UsersClient.tsx` returns `1`.
    - `grep -cE "setEditTarget\\(user\\)" nemea-front/src/app/\\(app\\)/usuarios/UsersClient.tsx` returns ≥1.
    - `grep -cE "setDeleteTarget\\(user\\)" nemea-front/src/app/\\(app\\)/usuarios/UsersClient.tsx` returns ≥1.
    - `grep -cE "<Pencil " nemea-front/src/app/\\(app\\)/usuarios/UsersClient.tsx` returns `1`.
    - `grep -cE "<Trash2 " nemea-front/src/app/\\(app\\)/usuarios/UsersClient.tsx` returns `1`.
    - `grep -cE "isOwnRow=\\{editTarget\\?\\.id === currentUserId\\}" nemea-front/src/app/\\(app\\)/usuarios/UsersClient.tsx` returns `1`.
    - `grep -cE "\\(tu cuenta\\)" nemea-front/src/app/\\(app\\)/usuarios/UsersClient.tsx` returns `1` (kept exactly as existing copy).
    - `grep -cE "router\\.refresh\\(\\)" nemea-front/src/app/\\(app\\)/usuarios/UsersClient.tsx` returns ≥2 (one in each dialog onSuccess) — current create-user flow may also have it, recount allowed.
    - `cd nemea-front && npx tsc --noEmit 2>&amp;1 | grep -E "UsersClient\\.tsx" | wc -l` returns `0`.
    - `cd nemea-front && npm run lint -- src/app/\\(app\\)/usuarios/UsersClient.tsx` exits 0 (no unused imports, no warnings).
  </acceptance_criteria>
  <done>UsersClient sin toggle-status, con Editar+Borrar buttons + own-row marker + dialog mounts wired al state. tsc + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor users.controller.spec.ts — delete toggle-status, add PATCH/DELETE describes</name>
  <files>nemea-back/src/users/users.controller.spec.ts</files>
  <read_first>
    - nemea-back/src/users/users.controller.spec.ts (current — leer entero)
    - nemea-back/src/users/users.controller.ts (post Plan 01 — para conocer signatures)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-CONTEXT.md (D-10, D-14)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-PATTERNS.md (sección "MODIFIED: ...users.controller.spec.ts")
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-VALIDATION.md (rows 12.4-01-XX)
  </read_first>
  <action>
    Editar `nemea-back/src/users/users.controller.spec.ts`:

    (a) BORRAR `describe('PATCH /users/:id/toggle-status', ...)` entero (líneas ~95-159 — el bloque completo del toggle status, incluyendo todos sus `it` cases).

    (b) Actualizar `mockUsersService`: REMOVER `deactivate: jest.fn(),` y `activate: jest.fn(),`. AGREGAR `update: jest.fn(),` y `remove: jest.fn(),`. Resto del objeto intacto.

    (c) AGREGAR `describe('PATCH /users/:id', () => { ... })` con casos mínimos (per D-04 — el controller solo delega, los guards viven en service; los tests del controller chequean shape de call + propagación de excepciones):

    1. `it('forwards id, dto, callerId to service.update and returns updated user', async () => { ... })`:
       - `const dto = { name: 'Nuevo' };`
       - `const updatedUser = { ...mockUser, name: 'Nuevo' };`
       - `mockUsersService.update.mockResolvedValue(updatedUser);`
       - `const result = await controller.update('victim-uuid', dto, 'caller-uuid');`
       - `expect(result).toEqual(updatedUser);`
       - `expect(usersService.update).toHaveBeenCalledWith('victim-uuid', dto, 'caller-uuid');`

    2. `it('propagates BadRequestException from service', async () => { ... })`:
       - `mockUsersService.update.mockRejectedValue(new BadRequestException('No puedes cambiar tu propio rol'));`
       - `await expect(controller.update('own-id', { roleId: 'role-2' }, 'own-id')).rejects.toThrow(BadRequestException);`

    3. `it('propagates NotFoundException from service', async () => { ... })`:
       - `mockUsersService.update.mockRejectedValue(new NotFoundException('Usuario no encontrado'));`
       - `await expect(...).rejects.toThrow(NotFoundException);`

    (d) AGREGAR `describe('DELETE /users/:id', () => { ... })`:

    1. `it('forwards id and callerId to service.remove and returns void', async () => { ... })`:
       - `mockUsersService.remove.mockResolvedValue(undefined);`
       - `const result = await controller.remove('victim-uuid', 'caller-uuid');`
       - `expect(result).toBeUndefined();`
       - `expect(usersService.remove).toHaveBeenCalledWith('victim-uuid', 'caller-uuid');`

    2. `it('propagates BadRequestException (self-delete) from service', async () => { ... })`:
       - `mockUsersService.remove.mockRejectedValue(new BadRequestException('No puedes borrarte a vos mismo'));`
       - `await expect(controller.remove('own-id', 'own-id')).rejects.toThrow(BadRequestException);`

    3. `it('propagates last-admin BadRequestException from service', async () => { ... })`:
       - Similar shape con mensaje del último-admin.

    4. `it('propagates NotFoundException from service', async () => { ... })`:
       - `mockUsersService.remove.mockRejectedValue(new NotFoundException('Usuario no encontrado'));`
       - `await expect(...).rejects.toThrow(NotFoundException);`

    (e) Asegurar imports en el spec file:
    - `BadRequestException`, `NotFoundException` desde `@nestjs/common`. Si ya estaban (porque toggle-status testeaba self-deactivate), mantenerlos.
    - NO importar `JwtUser` si no se usa en los tests nuevos (los nuevos endpoints reciben `callerId: string` directo, no `JwtUser`).

    Notas:
    - NO testear guards aquí — los guards viven en service (D-04); su cobertura está en Plan 05 / users.service.spec.ts.
    - Los `it` del controller solo verifican: (a) shape de la llamada al service, (b) propagación de excepciones, (c) return value.
    - Mantener describes intactos: `describe('GET /users')`, `describe('POST /users')`.

    Style: single quotes, no `any`. Pasar UUID literals como strings (`'victim-uuid-1'`, etc.) — no necesita ser UUID válido en unit test (ParseUUIDPipe corre en runtime, no en el unit del controller).
  </action>
  <verify>
    <automated>grep -cE "toggle-status" nemea-back/src/users/users.controller.spec.ts; grep -cE "describe\\('PATCH /users/:id'" nemea-back/src/users/users.controller.spec.ts; grep -cE "describe\\('DELETE /users/:id'" nemea-back/src/users/users.controller.spec.ts; cd nemea-back && npm test -- --testPathPattern=users.controller.spec 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "toggle-status" nemea-back/src/users/users.controller.spec.ts` returns `0` (suite fully removed).
    - `grep -cE "describe\\('PATCH /users/:id'" nemea-back/src/users/users.controller.spec.ts` returns `1`.
    - `grep -cE "describe\\('DELETE /users/:id'" nemea-back/src/users/users.controller.spec.ts` returns `1`.
    - `grep -cE "(activate|deactivate): jest\\.fn" nemea-back/src/users/users.controller.spec.ts` returns `0` (removed from mock).
    - `grep -cE "(update|remove): jest\\.fn" nemea-back/src/users/users.controller.spec.ts` returns ≥2 (added to mock).
    - `grep -cE "controller\\.update\\(" nemea-back/src/users/users.controller.spec.ts` returns ≥3 (PATCH tests).
    - `grep -cE "controller\\.remove\\(" nemea-back/src/users/users.controller.spec.ts` returns ≥4 (DELETE tests).
    - `cd nemea-back && npm test -- --testPathPattern=users.controller.spec` exits 0; reports ≥7 passing tests across PATCH and DELETE describes (in addition to existing GET/POST).
    - Existing `describe('POST /users')` and `describe('GET /users')` tests still pass (no regression).
  </acceptance_criteria>
  <done>Controller spec sin toggle-status; 7+ tests nuevos pasando entre PATCH y DELETE; sin regresiones en GET/POST.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM → React state (UsersClient) | `editTarget` / `deleteTarget` mutated only via row button click handlers; no user-controlled mutation beyond click |
| React state → Dialog props | UserRow object pass-through with `isOwnRow` computed from session userId (JWT-derived) |
| Controller test → Service mock | Test boundary; mocks isolate the controller layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.4-04 | Elevation of Privilege | Borrar button on own row | mitigate (UI layer) | Borrar button is conditionally rendered: `user.id === currentUserId ? <span> : <Button>`. The button literally does not exist in DOM for own row. Defense-in-depth — backend guard in Plan 05 is authoritative. |
| T-12.4-03 | Elevation of Privilege | EditUserDialog own-row Role/Switch | mitigate (UI layer) | `isOwnRow={editTarget?.id === currentUserId}` prop passed to EditUserDialog, which disables Role+Switch (Plan 03). Backend guards (Plan 05) are authoritative. |
| T-12.4-26 | Spoofing | currentUserId from `session?.user?.id` could be `''` if backend unreachable | accept | UUIDs are never `''`, so `user.id === ''` is always false. Defensive fallback handled by UsersClient's existing `session?.user?.id` chain. No additional mitigation. |
| T-12.4-27 | Tampering | EditUserDialog/DeleteUserAlertDialog imported from `./EditUserDialog` / `./DeleteUserAlertDialog` | accept | Local file imports — no external supply chain. Plans 03/04 are the source of truth; their threat models cover the components. |
| T-12.4-28 | Information Disclosure | controller spec test data | accept | Test fixtures use literal strings (`'caller-uuid'`, etc.) — no PII, no real secrets. |
| T-12.4-SC | Tampering | npm/pip/cargo installs | n/a | No new packages — only adds imports of `Pencil` and `Trash2` from existing `lucide-react`. |
</threat_model>

<verification>
- `cd nemea-front && npx tsc --noEmit` exits 0 (no errors anywhere).
- `cd nemea-front && npm run lint` exits 0 (specifically for the modified file).
- `cd nemea-back && npm test -- --testPathPattern=users.controller.spec` exits 0 with all describes passing.
- `cd nemea-front && npm test -- --testPathPattern=usuarios` runs Plan 03 + Plan 04 tests AND any prior test in usuarios path — should still pass (UsersClient.tsx has no direct test in this phase; Plans 03/04 cover the dialogs).
- Manual visual smoke (deferred to Plan 07): open `/usuarios`, verify row layout = Editar + Borrar (or "(tu cuenta)" for self), open both dialogs, confirm behavior.
</verification>

<success_criteria>
- UsersClient.tsx: no more toggle-status; Editar + Borrar buttons per row; "(tu cuenta)" marker for self; dialogs mounted with correct props; tsc + lint clean.
- users.controller.spec.ts: no toggle-status describe; PATCH + DELETE describes with ≥3 + ≥4 it cases respectively; mockUsersService updated; existing GET/POST untouched.
- Both new dialogs receive `user` prop and onSuccess callback that triggers router.refresh().
- isOwnRow correctly computed and passed to EditUserDialog.
</success_criteria>

<output>
Create `.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-06-SUMMARY.md` when done. Include:
- The new row layout JSX (briefly described)
- Final mockUsersService field roster
- Test count delta in users.controller.spec.ts
- Confirmation that tsc/lint pass in both apps
</output>

---
status: diagnosed
phase: 12.4-user-management-edit-user-role-delete-user-admin-self-lockou
source: [12.4-01-SUMMARY.md, 12.4-02-SUMMARY.md, 12.4-03-SUMMARY.md, 12.4-04-SUMMARY.md, 12.4-05-SUMMARY.md, 12.4-06-SUMMARY.md, 12.4-07-SUMMARY.md]
started: 2026-05-26T05:10:36Z
updated: 2026-05-26T05:35:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Editar nombre de usuario (fila ajena)
expected: Click "Editar" en fila ajena abre diálogo prefilled (Email read-only, Nombre/Rol/Switch). Cambiar Nombre + Guardar → toast "Usuario actualizado" y la tabla refresca con el nombre nuevo. (REQ-12.4-1, REQ-12.4-2 / AC1)
result: pass

### 2. Editar rol de usuario (fila ajena)
expected: En el diálogo de un usuario ajeno, cambiás el Rol y hacés Guardar → 200 OK y el badge de rol cambia en la tabla. (REQ-12.4-1, REQ-12.4-2 / AC2)
result: issue
reported: "Al degradar a un admin (cambio de rol que dispara el guard de último admin), el backend tira 500: 'FOR UPDATE is not allowed with aggregate functions' y hace ROLLBACK. Logs del server muestran el error repetido en PATCH /api/users/:id (users.service.ts:198). Cambios de rol que NO degradan admin sí commitean OK."
severity: blocker

### 3. Fila propia — Rol y Switch deshabilitados + guard backend
expected: Al hacer "Editar" sobre tu propia fila, el Select de Rol y el Switch de Activo están deshabilitados (tooltip al pasar el mouse); el Nombre sigue editable. Un PATCH manual (devtools) con un roleId nuevo sobre tu propio id devuelve 400 "No puedes cambiar tu propio rol". (REQ-12.4-5 / AC3)
result: pass

### 4. Guard backend — no desactivar tu propia cuenta
expected: Un PATCH manual (devtools) con { isActive: false } sobre tu propio id devuelve 400 "No puedes desactivar tu propia cuenta". (REQ-12.4-5 / AC4)
result: pass

### 5. Fila propia — muestra "(tu cuenta)" en vez de Borrar
expected: En la columna de acciones, tu propia fila muestra el texto "(tu cuenta)" en lugar del botón "Borrar" (el botón no existe en el DOM para tu fila). (REQ-12.4-6 / AC5)
result: pass

### 6. Borrar usuario (happy path) + transferencia de scenarios
expected: Borrás a un usuario ajeno (no-self) que es dueño de un scenario, ej "Promo Test". Confirmás en el AlertDialog "Borrar usuario" → toast "Usuario eliminado", la fila desaparece, y el scenario reaparece bajo tu propiedad renombrado "Promo Test - <nombre del borrado>". (REQ-12.4-3, REQ-12.4-4 / AC6)
result: issue
reported: "DELETE /api/users/:id falla con 500 EntityPropertyNotFoundError: Property 'updated_at' was not found in 'Scenario' (transferOwnership en scenarios.service.ts:415, llamado desde users.service.ts:294). La transacción hace ROLLBACK y el usuario NO se borra. Probado borrando a un usuario no-admin con scenario."
severity: blocker

### 7. Guard backend — no borrarte a vos mismo
expected: Un DELETE manual (devtools) sobre tu propio id devuelve 400 "No puedes borrarte a vos mismo". (REQ-12.4-6 / AC7)
result: pass

### 8. Guard último admin activo
expected: Con varios admins, desactivar/borrar a todos menos uno → 200. La acción que dejaría 0 admins activos → 400 "No se puede dejar el sistema sin administradores activos". (REQ-12.4-7 / AC8)
result: issue
reported: "Mismo blocker que el Test 2: el guard de último admin invoca countOtherActiveAdmins (FOR UPDATE + COUNT) → 500 'FOR UPDATE is not allowed with aggregate functions'. La ruta de degradar/desactivar/borrar un admin nunca llega a devolver el 400 esperado. No se reconfirmó con 2 admins (decisión del usuario); el crash ya quedó probado en el Test 2."
severity: blocker

## Summary

total: 8
passed: 5
issues: 3
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Cambiar el rol de un usuario (incluso si degrada a un admin) devuelve 200 o el 400 'No se puede dejar el sistema sin administradores activos' — nunca un 500"
  status: failed
  reason: "User reported: 500 'FOR UPDATE is not allowed with aggregate functions' al degradar un admin; transacción hace ROLLBACK en PATCH /api/users/:id"
  severity: blocker
  test: 2
  root_cause: "countOtherActiveAdmins (users.service.ts:104-116) combina .setLock('pessimistic_write') (→ FOR UPDATE) con .getCount() (→ COUNT(DISTINCT)). PostgreSQL prohíbe FOR UPDATE con funciones de agregado. Introducido por el fix de code-review CR-A1 que envolvió update/remove en transacción SERIALIZABLE + locks pesimistas."
  artifacts:
    - path: "nemea-back/src/users/users.service.ts"
      issue: "countOtherActiveAdmins usa setLock('pessimistic_write') + getCount() (líneas 104-116); invocado en update():198 y remove():268"
  missing:
    - "Quitar .setLock('pessimistic_write') de countOtherActiveAdmins — la transacción SERIALIZABLE ya garantiza la protección anti-race sin row-lock sobre el agregado (alternativa: usar getMany().length con el lock si se quiere lock explícito de filas)"
    - "Verificar que el guard de último admin devuelve 400 correcto (no 500) tras el fix, en update y remove"
    - "Cubrir con test el escenario admin-demotion / last-admin contra DB real o integración (el unit test mockea getCount y no detecta el SQL inválido)"
  debug_session: ""

- truth: "Borrar un usuario que es dueño de scenarios devuelve 204, lo elimina y transfiere/renombra sus scenarios al caller — sin 500"
  status: failed
  reason: "User reported: 500 EntityPropertyNotFoundError 'Property updated_at was not found in Scenario' en DELETE /api/users/:id; rollback, usuario no borrado"
  severity: blocker
  test: 6
  root_cause: "transferOwnership (scenarios.service.ts:421) hace .set({ updated_at: () => 'NOW()' }) usando el nombre de COLUMNA DB 'updated_at'. TypeORM QueryBuilder .set() espera el nombre de PROPIEDAD de la entidad, que es 'updatedAt' (la @UpdateDateColumn mapea a la columna updated_at). Introducido por el fix WR-A8 (bump de updatedAt en el bulk UPDATE)."
  artifacts:
    - path: "nemea-back/src/scenarios/scenarios.service.ts"
      issue: ".set({ ... updated_at: () => 'NOW()' }) usa nombre de columna en vez de propiedad de entidad (línea 421)"
  missing:
    - "Cambiar 'updated_at' por 'updatedAt' en el .set() de transferOwnership"
    - "Test de integración (DB real) del borrado con transferencia — el unit test de transferOwnership mockea el QueryBuilder y no detecta el nombre de propiedad inválido (mismo gap de cobertura que el blocker del Test 2)"
  debug_session: ""

- truth: "El guard de último admin devuelve 400 'No se puede dejar el sistema sin administradores activos' cuando una acción dejaría 0 admins activos — y 200 en caso contrario"
  status: failed
  reason: "User reported: mismo blocker que Test 2 — countOtherActiveAdmins (FOR UPDATE + COUNT) → 500; la ruta nunca alcanza el 400 esperado"
  severity: blocker
  test: 8
  root_cause: "Mismo root cause que el gap del Test 2: countOtherActiveAdmins (users.service.ts:104-116) combina .setLock('pessimistic_write') con .getCount(). Un solo fix cierra Test 2 y Test 8."
  artifacts:
    - path: "nemea-back/src/users/users.service.ts"
      issue: "countOtherActiveAdmins setLock + getCount (líneas 104-116); afecta update():198 y remove():268"
  missing:
    - "Cubierto por el fix del gap del Test 2 (quitar el pessimistic_write del count)"
    - "Verificar el 400 de último admin con ≥2 admins reales tras el fix"
  debug_session: ""

---
phase: 12.4-user-management-edit-user-role-delete-user-admin-self-lockou
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - nemea-back/src/users/users.service.ts
  - nemea-back/src/users/users.service.spec.ts
  - nemea-back/src/scenarios/scenarios.service.ts
  - nemea-back/test/users-admin-guard.e2e-spec.ts
autonomous: true
gap_closure: true
requirements:
  - REQ-12.4-2
  - REQ-12.4-3
  - REQ-12.4-4
  - REQ-12.4-7

must_haves:
  truths:
    - "Degradar/desactivar/borrar a un admin (cuando hay >=2 admins activos) devuelve 200/204 — nunca 500"
    - "Degradar/desactivar/borrar al ULTIMO admin activo devuelve 400 'No se puede dejar el sistema sin administradores activos' — nunca 500"
    - "Borrar un usuario que es dueño de scenarios devuelve 204, lo elimina y transfiere+renombra sus scenarios al caller con updatedAt bumpeado — sin 500"
    - "Existe cobertura de test que corre contra una Postgres real y reproduce/previene los dos bugs SQL (FOR UPDATE+aggregate, propiedad de entidad invalida)"
  artifacts:
    - path: "nemea-back/src/users/users.service.ts"
      provides: "countOtherActiveAdmins sin setLock (COUNT valido bajo SERIALIZABLE)"
      contains: "countOtherActiveAdmins"
    - path: "nemea-back/src/scenarios/scenarios.service.ts"
      provides: "transferOwnership con .set({ updatedAt: () => 'NOW()' })"
      contains: "updatedAt: () =>"
    - path: "nemea-back/test/users-admin-guard.e2e-spec.ts"
      provides: "Integration test contra Postgres real (puerto 5433) ejercitando ambos paths SQL"
      min_lines: 80
  key_links:
    - from: "nemea-back/src/users/users.service.ts (update/remove)"
      to: "countOtherActiveAdmins"
      via: "llamada dentro de la transaccion SERIALIZABLE"
      pattern: "countOtherActiveAdmins\\("
    - from: "nemea-back/src/users/users.service.ts (remove)"
      to: "scenariosService.transferOwnership"
      via: "transferencia atomica antes del delete"
      pattern: "transferOwnership\\("
---

<objective>
Cerrar los 2 blockers de la UAT de la fase 12.4 (Tests 2, 6 y 8 → 2 root causes), ambos
introducidos por fixes de code-review previos (CR-A1 y WR-A8) y confirmados contra el codigo
vivo + trazas de runtime reales:

1. **BLOCKER 1 (Tests 2 + 8):** `countOtherActiveAdmins` combina `FOR UPDATE` (pessimistic_write)
   con `COUNT(DISTINCT)`, prohibido por PostgreSQL → 500 + ROLLBACK al degradar/desactivar/borrar
   un admin.
2. **BLOCKER 2 (Test 6):** `transferOwnership` usa el nombre de columna `updated_at` en el `.set()`
   de un QueryBuilder, pero TypeORM espera el nombre de PROPIEDAD `updatedAt` →
   EntityPropertyNotFoundError → 500 + ROLLBACK al borrar un usuario con scenarios.

Ambos bugs pasaron los unit tests porque estos MOCKEAN el QueryBuilder / `getCount`, asi que
el SQL invalido (FOR UPDATE+aggregate) y el nombre de propiedad invalido nunca se ejercitan
contra una DB real. Este plan agrega cobertura de integracion contra Postgres real que SI los
detecta.

Purpose: dejar el flujo de gestion de usuarios (editar rol, desactivar, borrar con transferencia
de scenarios, guard de ultimo admin) funcionando end-to-end sin 500, y blindarlo con regresion.
Output: 2 fixes quirurgicos de una linea cada uno + actualizacion de un assert unit obsoleto +
un nuevo e2e spec contra Postgres real.

Scope boundary: NO tocar los 5 comportamientos que pasaron (editar nombre, editar rol no-admin,
guard self-role, guard self-deactivate, guard self-delete, UI "(tu cuenta)"). Fuera de scope
(solo observaciones, NO planear): TypeORM `logging: true` filtrando SQL/emails en dev logs, y el
tamano del eager-join de scenarios — ambos preexistentes, no regresiones de 12.4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-UAT.md

<interfaces>
<!-- Contratos clave que el executor necesita. Extraidos del codebase. NO explorar. -->

BaseEntity (nemea-back/src/common/entities/base.entity.ts) — la @UpdateDateColumn mapea
PROPIEDAD updatedAt → COLUMNA updated_at:
```typescript
@UpdateDateColumn({ name: 'updated_at', type: 'timestamptz' })
updatedAt!: Date;
```

countOtherActiveAdmins ACTUAL (nemea-back/src/users/users.service.ts:104-116) — el .setLock es el bug:
```typescript
private async countOtherActiveAdmins(
  manager: EntityManager,
  excludeId: string,
): Promise<number> {
  return manager
    .createQueryBuilder(User, 'u')
    .innerJoin('u.role', 'r')
    .setLock('pessimistic_write')       // <-- BUG: FOR UPDATE + COUNT prohibido
    .where('u.isActive = :active', { active: true })
    .andWhere('r.canManageUsers = :flag', { flag: true })
    .andWhere('u.id != :id', { id: excludeId })
    .getCount();
}
```
La transaccion que la envuelve ya corre `startTransaction('SERIALIZABLE')` en update() (linea 139)
y remove() (linea 243), por lo que el row-lock sobre el agregado es REDUNDANTE — la garantia
anti-race que queria CR-A1 la da el aislamiento SERIALIZABLE.

transferOwnership ACTUAL (nemea-back/src/scenarios/scenarios.service.ts:415-425) — la key es el bug:
```typescript
await qr.manager
  .createQueryBuilder()
  .update(Scenario)
  .set({
    name: () => `LEFT(COALESCE(name, ''), ${baseMax}) || :suffix`,
    user: { id: newOwnerId },
    updated_at: () => 'NOW()',        // <-- BUG: nombre de columna, no de propiedad
  })
  .where('user_id = :victimId', { victimId })
  .setParameter('suffix', suffix)
  .execute();
```

Harness e2e existente (nemea-back/test/): jest-e2e.json corre `*.e2e-spec.ts` con setupFiles
`./setup-e2e.ts`, que apunta DATABASE_URL a Postgres real en puerto 5433
(`postgresql://nemea:nemea_test@localhost:5433/nemea_test`, levantado por `docker compose up -d`
servicio `postgres-test`). data-source corre `migrationsRun: true`. Script: `npm run test:e2e`.
El app.e2e-spec.ts bootea AppModule real y espeja la config de main.ts (globalPrefix 'api',
helmet, ValidationPipe whitelist+transform, HttpExceptionFilter, Response+LoggingInterceptor).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quitar el FOR UPDATE del COUNT de admins (BLOCKER 1, Tests 2+8)</name>
  <files>nemea-back/src/users/users.service.ts, nemea-back/src/users/users.service.spec.ts</files>
  <action>
    En `nemea-back/src/users/users.service.ts`, metodo `countOtherActiveAdmins` (lineas 104-116):
    eliminar la linea `.setLock('pessimistic_write')` de la cadena del QueryBuilder. Dejar el
    resto del query intacto (innerJoin, where isActive, andWhere canManageUsers, andWhere id !=,
    getCount). NO cambiar la firma del metodo ni los callers (update():198, remove():268).

    Actualizar el comentario CR-A1 arriba del metodo (lineas 100-103): la garantia anti-race ya
    NO depende de un pessimistic_write sobre las filas del COUNT — la da el aislamiento
    SERIALIZABLE de la transaccion que envuelve a update()/remove(). Reescribir el comentario para
    reflejar esto: PostgreSQL prohibe `FOR UPDATE` con funciones de agregado (COUNT), y el lock
    sobre el agregado seria redundante porque SERIALIZABLE ya serializa los demote/delete
    concurrentes del penultimo admin. Referenciar el gap de la UAT (Tests 2/8).

    En `nemea-back/src/users/users.service.spec.ts`, el test "CR-A1/CR-A2: la COUNT de admins
    corre sobre queryRunner.manager" (alrededor de linea 538-561) tiene un assert AHORA OBSOLETO:
    `expect(mockTxnQueryBuilder.setLock).toHaveBeenCalledWith('pessimistic_write')`. Ese assert
    correria igual de verde (porque la re-lectura del victim en update() SI usa setLock), pero ya
    no prueba lo que dice. Cambiarlo para afirmar especificamente que la COUNT NO agrega un lock
    sobre el agregado: assert que `mockTxnQueryBuilder.getCount` fue llamado (prueba que se tomo
    el path de last-admin) y eliminar/ajustar el assert de setLock para que NO afirme que el COUNT
    usa pessimistic_write. El victim re-fetch (que SI debe seguir usando setLock) ya esta cubierto
    por otros tests; aca el foco es el COUNT. Mantener verdes los asserts de
    `mockQueryRunner.manager.createQueryBuilder` y `commitTransaction`.

    NO inventar logica nueva: este es un fix de remocion de una sola linea + ajuste de comentario
    y de un assert.
  </action>
  <verify>
    <automated>cd nemea-back; npx jest src/users/users.service.spec.ts</automated>
  </verify>
  <done>countOtherActiveAdmins ya no contiene `.setLock`. El spec de users pasa entero. El assert obsoleto de setLock-sobre-COUNT fue reemplazado por uno que verifica el path del COUNT sin lock.</done>
</task>

<task type="auto">
  <name>Task 2: Corregir el nombre de propiedad en transferOwnership (BLOCKER 2, Test 6)</name>
  <files>nemea-back/src/scenarios/scenarios.service.ts</files>
  <action>
    En `nemea-back/src/scenarios/scenarios.service.ts`, metodo `transferOwnership`, en el `.set({...})`
    del QueryBuilder (alrededor de linea 418-422): cambiar la KEY `updated_at` por `updatedAt`.
    Mantener exactamente la expresion raw `() => 'NOW()'`. El resto del `.set()` (name con LEFT/COALESCE,
    user: { id: newOwnerId }), el `.where('user_id = :victimId')` y el `.setParameter('suffix', suffix)`
    quedan iguales — esos usan nombres de columna porque van crudos al SQL via arrow function / string,
    mientras que las KEYS de `.set()` son nombres de PROPIEDAD de entidad (de ahi que `name`, `user`,
    `updatedAt` sean propiedades validas y `updated_at` no).

    Ajustar el comentario WR-A8 (lineas 408-414) para mencionar que la key debe ser la PROPIEDAD de
    entidad `updatedAt` (no la columna `updated_at`), y referenciar el gap de la UAT (Test 6):
    TypeORM QueryBuilder `.set()` resuelve por nombre de propiedad y tira EntityPropertyNotFoundError
    si recibe el nombre de columna.

    NO cambiar la firma de transferOwnership ni el caller en users.service.ts:294.
  </action>
  <verify>
    <automated>cd nemea-back; npx jest src/scenarios/scenarios.service.spec.ts</automated>
  </verify>
  <done>El .set() de transferOwnership usa la key `updatedAt: () => 'NOW()'`. El spec de scenarios pasa entero.</done>
</task>

<task type="auto">
  <name>Task 3: Cobertura de regresion contra Postgres real (cierra el gap que los mocks no detectan)</name>
  <files>nemea-back/test/users-admin-guard.e2e-spec.ts</files>
  <action>
    Crear `nemea-back/test/users-admin-guard.e2e-spec.ts` — un test de integracion que corre contra
    la Postgres real del harness e2e (puerto 5433, via setup-e2e.ts). El proposito es ejercitar el
    SQL real de los dos bugs: el unit test NO los detecta porque mockea el QueryBuilder/getCount.

    Estrategia (a nivel SERVICIO, no HTTP — evita construir un harness de auth/JWT y seed completo):
    - En `beforeAll`: crear un `TestingModule` que importe `AppModule` (igual que app.e2e-spec.ts) y
      obtener el `DataSource` real via `moduleFixture.get(DataSource)` y los servicios reales via
      `moduleFixture.get(UsersService)` y `moduleFixture.get(ScenariosService)`. Las migraciones ya
      corren (migrationsRun: true). NO mockear repos ni QueryRunner — todo va contra la DB real.
    - Helper de seed usando el DataSource real: insertar (a) un rol admin con canManageUsers=true y
      un rol editor con canManageUsers=false (o reusar los roles seedeados por migration si existen —
      buscarlos por canManageUsers), (b) usuarios de prueba con esos roles, (c) un Scenario propiedad
      de un usuario victima. Usar repos del DataSource (`dataSource.getRepository(...)`).
    - En `afterAll`: cerrar la app/DataSource. En `afterEach` o por-test: limpiar las filas creadas
      (DELETE de scenarios y users de prueba) para no contaminar entre tests. Usar ids/emails con un
      prefijo unico de test (ej `e2e-admin-guard-...`) para aislar la limpieza.

    Tests minimos (cada uno DEBE fallar contra el codigo pre-fix y pasar post-fix):

    1. **last-admin guard NO crashea (BLOCKER 1):** con >=2 admins activos, llamar
       `usersService.update(otroAdminId, { roleId: editorRoleId }, callerId)` (degradar un admin que
       NO es el ultimo) → resuelve OK (no lanza). Esto ejercita `countOtherActiveAdmins` con SQL real:
       pre-fix tiraba `FOR UPDATE is not allowed with aggregate functions`.
    2. **last-admin guard devuelve 400 correcto:** con exactamente 1 admin activo, llamar
       `usersService.update(eseAdminId, { isActive: false }, otherCallerId)` → rechaza con
       BadRequestException mensaje 'No se puede dejar el sistema sin administradores activos'
       (NO un error de SQL). Verificar que es BadRequestException, no un 500/QueryFailedError.
    3. **delete con transferencia de scenarios NO crashea (BLOCKER 2):** seedear un usuario victima
       (no-admin, no-self) dueno de un Scenario, y un caller distinto. Llamar
       `usersService.remove(victimId, callerId)` → resuelve OK; verificar contra la DB que (a) el
       usuario victima ya no existe, (b) el scenario ahora pertenece al caller (user_id = callerId),
       (c) el name del scenario contiene el suffix ` - <nombre> #<shortId>`, (d) su `updated_at` fue
       bumpeado (>= el createdAt / posterior al valor previo). Esto ejercita el `.set({ updatedAt: ...})`
       real: pre-fix tiraba EntityPropertyNotFoundError 'Property updated_at was not found in Scenario'.

    Agregar un comentario de cabecera explicando POR QUE este spec existe: los unit tests
    (users.service.spec.ts, scenarios.service.spec.ts) mockean el QueryBuilder y getCount, por lo que
    el SQL invalido (FOR UPDATE+aggregate) y el nombre de propiedad invalido nunca se ejercitan; solo
    una DB real los detecta. Cualquier regresion futura de esta clase debe cubrirse aca, no en unit.

    REQUISITO de ejecucion: el harness necesita `docker compose up -d` (servicio postgres-test en
    5433) corriendo. Documentar esto en el comentario de cabecera y en el SUMMARY.

    Mantener TypeScript strict, sin `any`, return types explicitos, single quotes (CLAUDE.md).
  </action>
  <verify>
    <automated>cd nemea-back; docker compose up -d postgres-test; npx jest --config ./test/jest-e2e.json test/users-admin-guard.e2e-spec.ts --runInBand</automated>
  </verify>
  <done>El nuevo e2e spec corre contra Postgres real (5433) y pasa los 3 tests: (1) demote de admin no-ultimo OK sin error SQL, (2) accion sobre ultimo admin → 400 correcto, (3) delete con transferencia OK con scenario reasignado+renombrado+updatedAt bumpeado. Si se revierten los fixes de Task 1/2, estos tests fallan (prueban que detectan el bug).</done>
</task>

</tasks>

<verification>
- `cd nemea-back; npx tsc --noEmit` → 0 errores.
- `cd nemea-back; npm run lint` → 0 errores en los archivos tocados.
- `cd nemea-back; npx jest` → toda la suite unit verde (incluido el assert ajustado de users.service.spec.ts).
- `cd nemea-back; docker compose up -d postgres-test; npm run test:e2e` → app.e2e-spec.ts existente + users-admin-guard.e2e-spec.ts nuevo pasan.
- Grep de no-regresion: `countOtherActiveAdmins` ya NO contiene `setLock`; `transferOwnership` usa `updatedAt:` y NO `updated_at:` en el `.set()`.
</verification>

<success_criteria>
- BLOCKER 1 cerrado: degradar/desactivar/borrar un admin con >=2 admins activos devuelve 200/204; con el ultimo admin devuelve 400 'No se puede dejar el sistema sin administradores activos'. Nunca 500 'FOR UPDATE is not allowed with aggregate functions'.
- BLOCKER 2 cerrado: borrar un usuario con scenarios devuelve 204, lo elimina y transfiere+renombra sus scenarios al caller con updatedAt bumpeado. Nunca 500 EntityPropertyNotFoundError.
- Regresion: existe cobertura contra Postgres real que reproduce ambos bugs pre-fix y los previene post-fix. Queda documentado que los unit tests mockeados no detectan esta clase de bug.
- Los 5 comportamientos que pasaron la UAT siguen intactos (no se tocaron sus archivos de logica/UI).
</success_criteria>

<output>
Create `.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-08-SUMMARY.md` when done
</output>

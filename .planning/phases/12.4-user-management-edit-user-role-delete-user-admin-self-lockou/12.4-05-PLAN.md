---
phase: 12.4
plan: 05
type: execute
wave: 2
depends_on:
  - 12.4-02
files_modified:
  - nemea-back/src/users/users.module.ts
  - nemea-back/src/users/users.service.ts
  - nemea-back/src/users/users.service.spec.ts
autonomous: true
requirements:
  - REQ-12.4-1
  - REQ-12.4-3
  - REQ-12.4-5
  - REQ-12.4-6
  - REQ-12.4-7
tags:
  - backend
  - users
  - service
  - guards
  - transaction

must_haves:
  truths:
    - "UsersService.update(id, dto, callerId) persiste cambios y enforce los 3 guards de self-lockout"
    - "UsersService.remove(id, callerId) hace transferOwnership + delete dentro de transacción QueryRunner, con rollback en error"
    - "Guard self-role-edit throws BadRequestException 'No puedes cambiar tu propio rol'"
    - "Guard self-deactivate throws BadRequestException 'No puedes desactivar tu propia cuenta'"
    - "Guard self-delete throws BadRequestException 'No puedes borrarte a vos mismo'"
    - "Guard last-admin throws BadRequestException 'No se puede dejar el sistema sin administradores activos'"
    - "UsersService.activate() y UsersService.deactivate() ya no existen"
    - "UsersModule importa ScenariosModule"
    - "users.service.spec.ts cubre update happy paths + 3 guards + remove happy path + rollback + last-admin"
  artifacts:
    - path: "nemea-back/src/users/users.module.ts"
      provides: "Module config with ScenariosModule import"
      contains: "ScenariosModule"
    - path: "nemea-back/src/users/users.service.ts"
      provides: "Service with update(), refactored remove(), no activate/deactivate"
      contains: "async update"
    - path: "nemea-back/src/users/users.service.spec.ts"
      provides: "Test suite extended for update + refactored remove + guards"
      contains: "describe('update'"
  key_links:
    - from: "UsersService.remove"
      to: "ScenariosService.transferOwnership"
      via: "cross-module call inside QueryRunner transaction"
      pattern: "scenariosService\\.transferOwnership"
    - from: "UsersService.remove transaction"
      to: "commit/rollback/release"
      via: "try/catch/finally"
      pattern: "commitTransaction|rollbackTransaction"
    - from: "Last-admin guard"
      to: "createQueryBuilder count on canManageUsers"
      via: "innerJoin role"
      pattern: "r\\.canManageUsers"
---

<objective>
Refactor completo de `UsersService`: agregar `update(id, dto, callerId)` con 3 guards de self-lockout, refactor `remove(id, callerId)` para transferir scenarios + delete en transacción atómica, eliminar `activate()`/`deactivate()`. Wirear `ScenariosModule` en `UsersModule`. Extender `users.service.spec.ts` con la cobertura completa.

Purpose: Cumple REQ-12.4-1 (service-side de PATCH), REQ-12.4-3 (service-side de DELETE con transferencia), y REQ-12.4-5/6/7 (los tres guards). Cierra los compile errors que Plan 01 deja abiertos en el controller.

Output:
- `nemea-back/src/users/users.module.ts` (MODIFIED) — agrega ScenariosModule a imports.
- `nemea-back/src/users/users.service.ts` (MODIFIED) — agrega update, refactor remove, borra activate/deactivate, inyecta ScenariosService.
- `nemea-back/src/users/users.service.spec.ts` (MODIFIED) — extend con providers + QueryRunner mock + describes update/remove con 3 guards + rollback.

Depende de Plan 02: necesita `ScenariosService.transferOwnership(...)` existente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-SPEC.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-CONTEXT.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-RESEARCH.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-PATTERNS.md
@.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-VALIDATION.md
@CLAUDE.md

<interfaces>
From nemea-back/src/scenarios/scenarios.module.ts: exports ScenariosService. NO importa UsersModule (verified PATTERNS.md — no circular dep risk).

From nemea-back/src/scenarios/scenarios.service.ts (post Plan 02):
- `transferOwnership(victimId: string, newOwnerId: string, suffix: string, qr: QueryRunner): Promise<void>` — caller-controlled transaction.

From nemea-back/src/users/users.service.ts (current):
- Constructor: `@InjectRepository(User) usersRepository, @InjectRepository(Role) roleRepository`.
- Métodos current: findActiveByEmail, findByEmail, findAll, findById, create, updateGoogleProfile, deactivate, activate, remove(id).
- DEBE: borrar activate/deactivate, refactor remove(id) → remove(id, callerId), agregar update(id, dto, callerId).

From nemea-back/src/users/entities/user.entity.ts:
- `@ManyToOne(() => Role, { eager: true })` — role siempre cargado en findOne/save.

From nemea-back/src/users/dto/update-user.dto.ts (Plan 01):
- UpdateUserDto con name?/roleId?/isActive? todos optional.

QueryRunner pattern (RESEARCH Finding 1 + PATTERNS analog scenarios.service.ts:220-249):
- `this.usersRepository.manager.connection.createQueryRunner()` para obtener el qr.
- Try: connect → startTransaction → cross-service call → manager.delete(User, id) → commit.
- Catch: rollback → re-throw. Finally: release.

Last-admin count query (RESEARCH Finding 5):
```
this.usersRepository.createQueryBuilder('u')
  .innerJoin('u.role', 'r')
  .where('u.isActive = :active', { active: true })
  .andWhere('r.canManageUsers = :flag', { flag: true })
  .andWhere('u.id != :id', { id })
  .getCount();
```

Spanish errors locked (RESEARCH Finding 9):
- Self-role-edit: 'No puedes cambiar tu propio rol'
- Self-deactivate: 'No puedes desactivar tu propia cuenta'
- Self-delete: 'No puedes borrarte a vos mismo'
- Last-admin: 'No se puede dejar el sistema sin administradores activos'
- User 404: 'Usuario no encontrado'
- Role 404: 'Rol no encontrado'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ScenariosModule into UsersModule</name>
  <files>nemea-back/src/users/users.module.ts</files>
  <read_first>
    - nemea-back/src/users/users.module.ts (current)
    - nemea-back/src/scenarios/scenarios.module.ts (exports ScenariosService)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-CONTEXT.md (D-03)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-PATTERNS.md (sección users.module.ts)
  </read_first>
  <action>
    Agregar `import { ScenariosModule } from '../scenarios/scenarios.module';` y añadir `ScenariosModule` al array `imports` del `@Module({...})`. Shape final: `imports: [TypeOrmModule.forFeature([User, Role]), ScenariosModule]`. Resto del file intacto (controllers, providers, exports). NO usar forwardRef — ScenariosModule no importa UsersModule (verificar con `grep -E "UsersModule" nemea-back/src/scenarios/scenarios.module.ts` debe retornar 0). Style: single quotes, explicit.
  </action>
  <verify>
    <automated>grep -cE "ScenariosModule" nemea-back/src/users/users.module.ts; grep -cE "UsersModule" nemea-back/src/scenarios/scenarios.module.ts; cd nemea-back && npx tsc --noEmit 2>&amp;1 | grep -E "users\\.module\\.ts" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "import.*ScenariosModule" nemea-back/src/users/users.module.ts` returns `1`.
    - `grep -cE "ScenariosModule" nemea-back/src/users/users.module.ts` returns ≥2 (import + usage).
    - `grep -cE "UsersModule" nemea-back/src/scenarios/scenarios.module.ts` returns `0` (no circular dep).
    - `grep -cE "forwardRef" nemea-back/src/users/users.module.ts` returns `0`.
    - tsc errors referencing `users.module.ts`: `0`.
  </acceptance_criteria>
  <done>UsersModule importa ScenariosModule, sin circular dep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor UsersService — add update(), refactor remove(), delete activate/deactivate</name>
  <files>nemea-back/src/users/users.service.ts</files>
  <read_first>
    - nemea-back/src/users/users.service.ts (current — leer entero)
    - nemea-back/src/scenarios/scenarios.service.ts (rango líneas 220-249 para QueryRunner analog)
    - nemea-back/src/roles/roles.service.ts (Spanish errors + guard pattern)
    - nemea-back/src/users/dto/update-user.dto.ts (Plan 01 output)
    - nemea-back/src/users/entities/user.entity.ts (eager role)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-SPEC.md (REQ-1/3/5/6/7 + acceptance criteria)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-CONTEXT.md (D-01..D-06, D-12, D-16)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-RESEARCH.md (Finding 1, 5, 9, 10)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-PATTERNS.md (sección users.service.ts con skeleton completo)
  </read_first>
  <behavior>
    - update(victim_id, { name }, caller_id) where victim !== caller: persiste y retorna user con role eager.
    - update(victim, { roleId: valid_other }, caller) y rol existe: persiste con nuevo role.
    - update(victim, { roleId: 'invalid' }, caller): throws NotFoundException 'Rol no encontrado'.
    - update(unknown_id, dto, caller): throws NotFoundException 'Usuario no encontrado'.
    - update(own, { roleId: different_from_current }, own): throws BadRequest 'No puedes cambiar tu propio rol'.
    - update(own, { roleId: same_as_current }, own): success — no change → no guard fire.
    - update(own, { isActive: false }, own): throws BadRequest 'No puedes desactivar tu propia cuenta'.
    - update(own, { name: 'X' }, own): success — name siempre editable propio.
    - update(victim, { isActive: false }, caller) cuando victim = último admin activo: throws BadRequest 'No se puede dejar el sistema sin administradores activos'.
    - update(victim, { roleId: no-admin-role }, caller) cuando victim = último admin: throws último-admin.
    - remove(own, own): throws BadRequest 'No puedes borrarte a vos mismo'.
    - remove(unknown_id, caller): throws NotFoundException 'Usuario no encontrado'.
    - remove(victim último admin, caller): throws último-admin.
    - remove(victim, caller) happy: transferOwnership called → manager.delete(User, id) called → commit, returns void.
    - remove(victim, caller) cuando transferOwnership throws: rollback called, commit NOT called, manager.delete NOT called, release CALLED en finally, error re-thrown.
  </behavior>
  <action>
    Editar `nemea-back/src/users/users.service.ts`:

    (a) Imports: agregar `BadRequestException` desde `@nestjs/common` (junto al NotFoundException existente). Agregar `UpdateUserDto` desde `'./dto/update-user.dto'` y `ScenariosService` desde `'../scenarios/scenarios.service'`. NO importar QueryRunner explícito — TypeScript infiere del `createQueryRunner()` return.

    (b) Modificar constructor: añadir como tercer parámetro `private readonly scenariosService: ScenariosService`. NO usar `@Inject(forwardRef(...))`.

    (c) BORRAR los métodos `activate(id)` y `deactivate(id)` completos (~6 líneas).

    (d) BORRAR la implementación actual de `remove(id)` (`async remove(id: string): Promise<void> { await this.usersRepository.delete(id); }`).

    (e) AGREGAR `async update(id: string, dto: UpdateUserDto, callerId: string): Promise<User>`:
    - Buscar victim con `findOne({ where: { id } })` (role llega eager). Si null → throw NotFoundException('Usuario no encontrado').
    - Guard 1 (self-role-edit): `if (id === callerId && dto.roleId !== undefined && dto.roleId !== victim.role.id) throw new BadRequestException('No puedes cambiar tu propio rol');`
    - Guard 2 (self-deactivate): `if (id === callerId && dto.isActive === false) throw new BadRequestException('No puedes desactivar tu propia cuenta');`
    - Resolver new role si cambia: `let newRole = victim.role; if (dto.roleId !== undefined && dto.roleId !== victim.role.id) { const found = await this.roleRepository.findOne({ where: { id: dto.roleId } }); if (!found) throw new NotFoundException('Rol no encontrado'); newRole = found; }`
    - Guard 3 (last-admin): computar `willBeActive = dto.isActive ?? victim.isActive`, `willBeAdmin = newRole.canManageUsers`, `wasAdminActive = victim.isActive && victim.role.canManageUsers`, `willNoLongerBeAdminActive = wasAdminActive && !(willBeActive && willBeAdmin)`. Si `willNoLongerBeAdminActive`, query con `createQueryBuilder('u').innerJoin('u.role', 'r').where('u.isActive = :active', { active: true }).andWhere('r.canManageUsers = :flag', { flag: true }).andWhere('u.id != :id', { id }).getCount()`. Si count === 0, throw BadRequestException('No se puede dejar el sistema sin administradores activos').
    - Apply changes field-by-field (NO repo.merge per RESEARCH P9): `if (dto.name !== undefined) victim.name = dto.name; victim.role = newRole; if (dto.isActive !== undefined) victim.isActive = dto.isActive;`
    - Return `this.usersRepository.save(victim)`.

    (f) AGREGAR `async remove(id: string, callerId: string): Promise<void>`:
    - Guard 1 (self-delete): `if (id === callerId) throw new BadRequestException('No puedes borrarte a vos mismo');`
    - Buscar victim con `findOne({ where: { id } })`. Si null → throw NotFoundException('Usuario no encontrado').
    - Guard 2 (last-admin): if `victim.isActive && victim.role.canManageUsers`, ejecutar el mismo count query del update y throw si count === 0.
    - Abrir QueryRunner: `const queryRunner = this.usersRepository.manager.connection.createQueryRunner(); await queryRunner.connect(); await queryRunner.startTransaction();`
    - Try block:
      * `const suffix = ` - ${victim.name ?? 'usuario borrado'}`;` (SPACE-DASH-SPACE leading).
      * `await this.scenariosService.transferOwnership(id, callerId, suffix, queryRunner);`
      * `await queryRunner.manager.delete(User, id);`
      * `await queryRunner.commitTransaction();`
    - Catch block: `await queryRunner.rollbackTransaction(); throw error;`
    - Finally block: `await queryRunner.release();`

    Notas críticas:
    - Order matters: transferOwnership ANTES de manager.delete (per RESEARCH Finding 10 — cascade FK no se dispara porque scenarios ya están movidos).
    - Fast-fail guards (self-delete, user-404, last-admin) ANTES de abrir QueryRunner — evita orphan transactions.
    - `victim.role` accesible por eager loading — NO usar `relations: ['role']` (redundante).

    Métodos a MANTENER intactos: findActiveByEmail, findByEmail, findAll, findById, create, updateGoogleProfile.

    Style: TS strict, single quotes, explicit return types (`Promise<User>`, `Promise<void>`). No `any`.
  </action>
  <verify>
    <automated>grep -cE "async update\\(id: string" nemea-back/src/users/users.service.ts; grep -cE "async remove\\(id: string, callerId: string" nemea-back/src/users/users.service.ts; grep -cE "scenariosService\\.transferOwnership" nemea-back/src/users/users.service.ts; grep -cE "commitTransaction|rollbackTransaction" nemea-back/src/users/users.service.ts; cd nemea-back && npx tsc --noEmit 2>&amp;1 | grep -E "users\\.service\\.ts" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "async update\\(id: string, dto: UpdateUserDto, callerId: string\\)" nemea-back/src/users/users.service.ts` returns `1`.
    - `grep -cE "async remove\\(id: string, callerId: string\\)" nemea-back/src/users/users.service.ts` returns `1`.
    - Body-level (not comment-level) search: `grep -v '^[[:space:]]*//' nemea-back/src/users/users.service.ts | grep -cE "async (activate|deactivate)\\(" ` returns `0` (methods deleted).
    - `grep -cE "scenariosService\\.transferOwnership" nemea-back/src/users/users.service.ts` returns `1`.
    - `grep -cE "commitTransaction" nemea-back/src/users/users.service.ts` returns `1`.
    - `grep -cE "rollbackTransaction" nemea-back/src/users/users.service.ts` returns `1`.
    - `grep -cE "\\.release\\(\\)" nemea-back/src/users/users.service.ts` returns `1`.
    - `grep -cE "BadRequestException\\('No puedes cambiar tu propio rol'\\)" nemea-back/src/users/users.service.ts` returns `1`.
    - `grep -cE "BadRequestException\\('No puedes desactivar tu propia cuenta'\\)" nemea-back/src/users/users.service.ts` returns `1`.
    - `grep -cE "BadRequestException\\('No puedes borrarte a vos mismo'\\)" nemea-back/src/users/users.service.ts` returns `1`.
    - `grep -cE "BadRequestException\\('No se puede dejar el sistema sin administradores activos'\\)" nemea-back/src/users/users.service.ts` returns `2` (used in BOTH update and remove).
    - `grep -cE "private readonly scenariosService" nemea-back/src/users/users.service.ts` returns `1` (constructor injection).
    - `grep -cE "repo\\.merge|this\\.usersRepository\\.merge" nemea-back/src/users/users.service.ts` returns `0` (defense-in-depth — no merge with raw dto).
    - `cd nemea-back && npx tsc --noEmit 2>&amp;1 | grep -E "users\\.service\\.ts" | wc -l` returns `0`.
    - `cd nemea-back && npx tsc --noEmit 2>&amp;1 | grep -E "users\\.controller\\.ts" | wc -l` returns `0` (Plan 01's controller signature now closes against service shape).
  </acceptance_criteria>
  <done>Service refactorizado, 3 guards en place, transacción atómica, sin activate/deactivate. tsc clean para users.service.ts Y users.controller.ts (cierra el contrato abierto por Plan 01).</done>
</task>

<task type="auto">
  <name>Task 3: Extender users.service.spec.ts con tests de update + refactored remove</name>
  <files>nemea-back/src/users/users.service.spec.ts</files>
  <read_first>
    - nemea-back/src/users/users.service.spec.ts (current — leer entero para conocer mockRepository, mockRoleRepository, beforeEach)
    - nemea-back/src/users/users.service.ts (post Task 2 — para conocer signature exactas)
    - nemea-back/src/scenarios/scenarios.service.spec.ts:17-24 (QueryRunner mock analog)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-CONTEXT.md (D-10 — test scope)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-RESEARCH.md (Finding 8 — mock setup + rollback test)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-PATTERNS.md (sección users.service.spec.ts con skeleton)
    - .planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-VALIDATION.md (rows 12.4-02-XX y 12.4-03-XX)
  </read_first>
  <action>
    Editar `nemea-back/src/users/users.service.spec.ts`:

    (a) BORRAR los `describe('activate', ...)` y `describe('deactivate', ...)` enteros (las suites que testean los métodos eliminados en Task 2).

    (b) BORRAR el `describe('remove', ...)` existente (testea la versión single-arg pre-refactor).

    (c) Extender el setup de mocks:

    - Agregar `mockQueryRunner`:
      ```
      const mockQueryRunner = {
        connect: jest.fn(),
        startTransaction: jest.fn(),
        commitTransaction: jest.fn(),
        rollbackTransaction: jest.fn(),
        release: jest.fn(),
        manager: { delete: jest.fn() },
      };
      ```

    - Extender `mockRepository` (la del User repo): añadir
      ```
      createQueryBuilder: jest.fn(() => ({
        innerJoin: jest.fn().mockReturnThis(),
        where: jest.fn().mockReturnThis(),
        andWhere: jest.fn().mockReturnThis(),
        getCount: jest.fn().mockResolvedValue(1), // default: hay otros admins activos → no fire del guard
      })),
      manager: { connection: { createQueryRunner: jest.fn(() => mockQueryRunner) } },
      ```

    - Agregar `mockScenariosService`:
      ```
      const mockScenariosService = { transferOwnership: jest.fn() };
      ```

    - Extender providers de Test.createTestingModule añadiendo `{ provide: ScenariosService, useValue: mockScenariosService }`. Importar `ScenariosService` al top del file.

    (d) AGREGAR `describe('update', () => { ... })` con casos mínimos (uno `it` por behavior):

    1. `it('actualiza name cuando victim !== caller')` — happy path, sin role change → NO debe llamar createQueryBuilder. mockRepository.findOne resolve user válido, mockRepository.save resolve user actualizado.
    2. `it('actualiza role cuando roleId nuevo y válido')` — happy path con role change. mockRoleRepository.findOne resolve role nuevo.
    3. `it('actualiza isActive cuando victim !== caller')`.
    4. `it('throws NotFoundException cuando user no existe')` — mockRepository.findOne resolves null → expect rejects.toThrow(NotFoundException).
    5. `it('throws NotFoundException cuando roleId no existe')` — mockRoleRepository.findOne resolves null.
    6. `it('throws BadRequestException con No puedes cambiar tu propio rol cuando victim === caller y roleId distinto')`.
    7. `it('NO throws cuando victim === caller y roleId igual al actual')` — guard NO fire porque dto.roleId === victim.role.id.
    8. `it('throws BadRequestException con No puedes desactivar tu propia cuenta cuando victim === caller y isActive false')`.
    9. `it('throws último-admin cuando demote del último admin via roleId no-admin')` — setup: victim is active admin, mock getCount resolve 0. Verificar mensaje exacto.
    10. `it('throws último-admin cuando desactivar último admin')` — similar, vía isActive: false en otro user (no propio).

    (e) AGREGAR `describe('remove', () => { ... })`:

    1. `it('happy path: transferOwnership + manager.delete + commit')` — mockRepository.findOne resolve victim activo no-admin (no fire last-admin). Expect transferOwnership.toHaveBeenCalledWith(id, callerId, expect.any(String), mockQueryRunner). Expect mockQueryRunner.manager.delete.toHaveBeenCalledWith(User, id). Expect commitTransaction called. Expect rollback NOT called. Expect release called.
    2. `it('throws self-delete cuando id === callerId')` — fast-fail antes de cualquier DB call.
    3. `it('throws NotFoundException cuando user no existe')`.
    4. `it('throws último-admin cuando borrando único admin activo')` — getCount resolve 0.
    5. `it('rollbackea cuando transferOwnership falla')` — el caso clave (RESEARCH Finding 8):
       ```
       mockRepository.findOne.mockResolvedValue({ id: 'victim', name: 'V', isActive: false, role: { canManageUsers: false } });
       mockScenariosService.transferOwnership.mockRejectedValue(new Error('overflow'));
       await expect(service.remove('victim', 'caller')).rejects.toThrow('overflow');
       expect(mockQueryRunner.rollbackTransaction).toHaveBeenCalled();
       expect(mockQueryRunner.commitTransaction).not.toHaveBeenCalled();
       expect(mockQueryRunner.manager.delete).not.toHaveBeenCalled();
       expect(mockQueryRunner.release).toHaveBeenCalled();
       ```

    Tips:
    - En cada test, ANTES del act, asegurar que mockUsersRepository.findOne.mockResolvedValue(...) retorna un objeto con la estructura completa: `{ id, email, name, role: { id, canManageUsers }, isActive }`. Faltar `role.canManageUsers` produce undefined → guard 3 puede comportarse mal.
    - Para el caso "no fire del guard last-admin", asegurar que `getCount.mockResolvedValue(1)` (al menos 1 otro admin existe).
    - Para el caso "fire del guard last-admin", `getCount.mockResolvedValue(0)` para esa it specifically (usar `.mockResolvedValueOnce(0)`).
    - NO testear el contenido del suffix string en transferOwnership call — solo `expect.any(String)` o `expect.stringContaining(' - ')`. El test del suffix exacto vive en Plan 02 (scenarios.service.spec).
    - `jest.clearAllMocks()` en beforeEach.

    Style: single quotes, no `any`. Importar `BadRequestException`, `NotFoundException` desde `@nestjs/common` y `User` desde `'./entities/user.entity'`.
  </action>
  <verify>
    <automated>cd nemea-back && npm test -- --testPathPattern=users.service.spec 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "describe\\('update'" nemea-back/src/users/users.service.spec.ts` returns `1`.
    - `grep -cE "describe\\('remove'" nemea-back/src/users/users.service.spec.ts` returns `1`.
    - `grep -cE "describe\\('(activate|deactivate)'" nemea-back/src/users/users.service.spec.ts` returns `0` (suites deleted).
    - `grep -cE "rollbackTransaction" nemea-back/src/users/users.service.spec.ts` returns ≥1 (rollback test exists).
    - `grep -cE "mockScenariosService|ScenariosService" nemea-back/src/users/users.service.spec.ts` returns ≥2 (import + provider mock).
    - `grep -cE "No puedes cambiar tu propio rol" nemea-back/src/users/users.service.spec.ts` returns ≥1.
    - `grep -cE "No puedes borrarte a vos mismo" nemea-back/src/users/users.service.spec.ts` returns ≥1.
    - `grep -cE "No se puede dejar el sistema sin administradores activos" nemea-back/src/users/users.service.spec.ts` returns ≥1.
    - `cd nemea-back && npm test -- --testPathPattern=users.service.spec` exits 0 with ≥10 passing `it` cases between update and remove describes.
    - No existing test that wasn't deleted regresses.
  </acceptance_criteria>
  <done>Test suite cubre los 3 guards + happy paths + rollback + último-admin para ambos métodos. ≥10 tests nuevos pasando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller → Service | callerId is JWT-derived (Plan 01 uses `@CurrentUser('id')`) — trusted. dto is ValidationPipe-cleaned — trusted shape. |
| Service → DB via Repository / QueryRunner | TypeORM parametrized queries (createQueryBuilder + named params); save() uses entity-level field assignment, not raw SQL |
| Service guards → BadRequestException → HTTP 400 | Spanish messages surfaced to client via NestJS exception filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.4-03 | Elevation of Privilege | self-role-edit guard | mitigate | Guard 1 in `update`: `id === callerId && dto.roleId !== victim.role.id` throws BadRequestException. callerId is JWT-derived, not from request body — cannot be spoofed without forging JWT (guarded by JwtAuthGuard upstream). VERIFY: test "throws No puedes cambiar tu propio rol" passes. |
| T-12.4-04 | Elevation of Privilege | self-delete guard | mitigate | Guard 1 in `remove`: `id === callerId` throws fast-fail before any DB I/O. Same callerId trust chain as T-12.4-03. VERIFY: test "throws self-delete" passes. |
| T-12.4-05 | Elevation of Privilege | last-active-admin guard | mitigate | Guard 3 in both `update` and `remove`: count of `users.isActive AND role.canManageUsers AND id != victim`. Throws if count === 0. Computed via parametrized createQueryBuilder (no raw concat). VERIFY: 2 tests (one per method) pass. |
| T-12.4-07 | Data Integrity | transferOwnership + delete atomicity | mitigate | Wrapped in QueryRunner transaction. If transferOwnership throws (e.g., varchar(200) overflow, FK constraint, network blip), rollback fires; user is NOT deleted. VERIFY: rollback test asserts commit NOT called + manager.delete NOT called + release CALLED in finally. |
| T-12.4-22 | Tampering | repo.merge(victim, dto) leaking unintended fields | mitigate (defense-in-depth) | Service uses explicit field assignment (`if (dto.name !== undefined) victim.name = dto.name;` etc.), NOT `merge(victim, dto)`. Even if ValidationPipe whitelist bypassed, no extra fields persist. VERIFY: grep returns 0 for `merge\\(.*dto\\)`. |
| T-12.4-23 | Tampering | role re-assignment to invalid role | mitigate | `roleRepository.findOne({ where: { id: dto.roleId } })` validates existence; throws NotFoundException('Rol no encontrado') if absent. Cannot assign non-existent or arbitrary role string. |
| T-12.4-24 | Spoofing | another user passing forged victim.id matching admin's id | mitigate (upstream) | `@RequirePermission('can_manage_users')` from Plan 01 guards both endpoints. Only `can_manage_users` permission holders reach this service. If a non-admin somehow got here, the guards in this service still enforce self-rules per JWT callerId. |
| T-12.4-25 | Race Condition | two admins deactivating each other simultaneously | accept | RESEARCH Finding 5 acknowledges theoretical race window. Mitigation cost (SERIALIZABLE / SELECT FOR UPDATE) exceeds benefit at single-digit user count. Documented as "considered, not implemented". |
| T-12.4-SC | Tampering | npm/pip/cargo installs | n/a | No new packages — uses existing @nestjs/common, typeorm, @nestjs/testing. |
</threat_model>

<verification>
- `cd nemea-back && npm test -- --testPathPattern=users.service.spec` exits 0.
- `cd nemea-back && npx tsc --noEmit` exits 0 for the entire backend (this plan closes the contract opened by Plan 01).
- `cd nemea-back && npm run lint` exits 0 (or only warns on pre-existing, not new issues).
- Grep gates per task acceptance_criteria.
- Cross-check: `grep -E "this\\.scenariosService" nemea-back/src/users/users.service.ts` returns ≥1 — injection working.
</verification>

<success_criteria>
- `UsersModule` imports `ScenariosModule` (no circular dep, no forwardRef).
- `UsersService.update(id, dto, callerId)` exists with 3 guards (self-role-edit, self-deactivate, last-admin).
- `UsersService.remove(id, callerId)` exists with 2 fast-fail guards (self-delete, last-admin) + atomic transaction (transferOwnership + delete) + rollback on error.
- `UsersService.activate()` and `UsersService.deactivate()` removed.
- Spanish error messages match RESEARCH Finding 9 verbatim.
- Field-by-field assignment in `update` (no `repo.merge`).
- ≥10 passing test cases between `describe('update')` and `describe('remove')`, including rollback test.
- Full backend `npx tsc --noEmit` clean (closes Plan 01 controller contract errors).
</success_criteria>

<output>
Create `.planning/phases/12.4-user-management-edit-user-role-delete-user-admin-self-lockou/12.4-05-SUMMARY.md` when done. Include:
- Final signatures of `update` and `remove`
- Confirmation of constructor injection of ScenariosService
- Spanish error message inventory (4 BadRequestException strings + 2 NotFoundException strings)
- Test count delta (≥10 new tests, X removed)
- Note: tsc now passes globally; Plan 01's open contract is closed
</output>

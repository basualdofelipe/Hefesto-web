---
status: diagnosed
phase: 12.1-dynamic-roles-with-configurable-permissions
source:
  - 12.1-01-SUMMARY.md
  - 12.1-02-SUMMARY.md
  - 12.1-03-SUMMARY.md
  - 12.1-04-SUMMARY.md
started: 2026-04-22T00:00:00Z
updated: 2026-04-22T00:10:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running backend. Run `npm run start:dev` in nemea-back/. Backend boots without ReferenceError, migration 1773000000000 applies cleanly (roles table created, ADMIN/USER seeded, users.role_id FK populated), and GET /api/roles returns both system roles.
result: pass

### 2. Login contains permissions in session
expected: Log in with admin Google account. In browser devtools (Application → Cookies) or by calling a protected endpoint, the NextAuth session contains `session.user.permissions` (11 boolean flags), NOT `session.user.role`. Stale JWTs from before migration force a re-login instead of silently breaking.
result: pass

### 3. /roles page loads for admin
expected: Navigate to /roles as ADMIN. Page loads with table of roles. Non-admin users (no `canManageUsers` permission) visiting /roles are redirected away (middleware blocks the route).
result: pass

### 4. System roles display with Sistema badge and permission counts
expected: Table shows ADMIN with "Sistema" badge and "11/11" permission count badge. USER shows "Sistema" badge and a partial count (e.g., "3/11" or "4/11" — whatever the seed granted). Delete button is disabled/hidden for system roles.
result: pass

### 5. Create role dialog — 11 toggles in 5 groups
expected: Click "Crear rol". Dialog opens with name + description fields and 11 permission toggles grouped into 5 sections (Productos, Insumos, Gastos, Herramientas, Administracion) separated by dividers. "Ver dashboard (proximamente)" label visible on the dashboard toggle.
result: pass

### 6. Permission dependency — edit auto-enables view
expected: In create role dialog, toggle "Editar productos" ON. "Ver productos" auto-checks (cannot edit without view). Same for supplies/expenses edit→view pairs.
result: pass

### 7. Permission dependency — unchecking view auto-disables edit
expected: Enable both "Ver productos" and "Editar productos". Uncheck "Ver productos". "Editar productos" auto-unchecks (cannot retain edit without view).
result: pass

### 8. Create custom role end-to-end
expected: Fill name "Vendedor", pick permissions, submit. Dialog closes, table refreshes showing the new role. GET /api/roles returns the new role. Edit works (reopen dialog, change perms, save → count badge updates). Delete works with AlertDialog confirmation showing accurate backend-aligned warning text.
result: issue
reported: "no puedo borrarlo, le asigné un usuario, y no se puede ni cambiarle el rol al usuario, ni eliminarlo. Sale error de 'no se puede borrar roles con usuarios asignados'"
severity: major

### 9. Admin lockout prevention
expected: Edit ADMIN role and try to uncheck "canManageUsers". Backend returns 400 (or validation blocks submit) — the last admin cannot lose user-management permission. Lockout guard in RolesModule prevents locking everyone out.
result: pass

### 10. /usuarios — dynamic role select from API
expected: Navigate to /usuarios. "Crear usuario" dialog has a role dropdown populated from GET /api/roles (not a hardcoded admin/user enum). Any role created on /roles appears as an option. Existing users show `role.name` (not `role: 'admin'` string).
result: pass

### 11. Sidebar permission-based nav group visibility
expected: Log in as a user WITHOUT `canManageUsers` or `canManageConfig`. Sidebar does NOT show the "Administracion" group. Log in as admin — Administracion group visible with Usuarios, Roles, Configuracion links.
result: pass

### 12. Backend enforces permissions on mutations
expected: As a USER (no `canEditProducts`), call POST /api/products. Backend returns 403. GET /api/products still works (USER has `canViewProducts`). GET /api/tiendanube-config/all returns 200 without any specific permission (authenticated-only by design).
result: pass
note: "User verified UI-level enforcement (edit button hidden for role without canEditProducts). Backend @RequirePermission wiring confirmed via code audit (52 usages across 9 controllers per 12.1-VERIFICATION.md)."

## Summary

total: 12
passed: 11
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Admin can create, edit, and delete custom roles end-to-end via /roles page"
  status: failed
  reason: "User reported: no puedo borrarlo, le asigné un usuario, y no se puede ni cambiarle el rol al usuario, ni eliminarlo. Sale error de 'no se puede borrar roles con usuarios asignados'"
  severity: major
  test: 8
  root_cause: |
    Backend correctly refuses to delete a role with users assigned (400 'no se puede borrar roles con usuarios asignados' — working as designed per RolesModule guard).
    The actual gap is that the /usuarios page has no flow to (a) edit an existing user's role or (b) delete a user. Without those, there is no way to reassign users off a custom role, so custom roles become un-deletable the moment they are assigned.
    Matches two existing pending todos: .planning/todos/pending/edit-user-role.md and .planning/todos/pending/delete-user.md.
  artifacts:
    - path: "nemea-front/src/app/(app)/usuarios/UsersClient.tsx"
      issue: "No edit-role action on existing users (no row-level edit dialog or inline role Select for existing users). Create dialog works; edit does not exist."
    - path: "nemea-front/src/app/(app)/usuarios/UsersClient.tsx"
      issue: "No delete-user action (no row-level delete button + AlertDialog confirmation)."
    - path: "nemea-back/src/users/users.controller.ts"
      issue: "Verify PATCH /api/users/:id/role (or equivalent) endpoint exists and is guarded by @RequirePermission('canManageUsers'). DELETE /api/users/:id should already exist from Phase 2 but confirm it is still wired after 12.1 migration."
  missing:
    - "Frontend: add 'Editar rol' action to each row in UsersClient.tsx (dialog with role Select populated from GET /api/roles, PATCH on submit)."
    - "Frontend: add 'Eliminar usuario' action to each row (AlertDialog confirmation, DELETE on confirm, table refresh on success)."
    - "Backend: ensure PATCH /api/users/:id/role endpoint exists with @RequirePermission('canManageUsers') — if absent, add controller + service method."
    - "Guard: admin cannot delete/unassign themselves (prevent lockout — same pattern as role admin-lockout)."
  debug_session: ""

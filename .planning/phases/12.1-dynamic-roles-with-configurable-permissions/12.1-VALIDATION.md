---
phase: 12.1
slug: dynamic-roles-with-configurable-permissions
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-18
retroactive: true
---

# Phase 12.1 — Validation Strategy

> Per-phase validation contract for feedback sampling. Reconstructed retroactively from RESEARCH.md "Validation Architecture" + the 4 SUMMARY.md artifacts after the phase shipped without a VALIDATION.md.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest 29.x + ts-jest (NestJS default) |
| **Config file** | `nemea-back/package.json` (jest section) |
| **Quick run command** | `cd nemea-back && npx jest --testPathPatterns=<area> --no-coverage` |
| **Full suite command** | `cd nemea-back && npm test` |
| **Estimated runtime** | ~3 seconds for the 4 phase-12.1 specs in isolation; ~10 seconds full suite |

---

## Sampling Rate

- **After every task commit:** Run `cd nemea-back && npx jest --testPathPatterns=roles --no-coverage`
- **After every plan wave:** Run `cd nemea-back && npm test`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~10 seconds (full backend suite)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 12.1-01-01 | 01 | 1 | D-08 | — | PermissionsGuard.canActivate enforces all required permission flags; throws ForbiddenException on no-user / missing-perm / partial-perm; passes when metadata is empty or fully satisfied | unit | `cd nemea-back && npx jest --testPathPatterns=permissions.guard --no-coverage` | ✅ | ✅ green (10 tests) |
| 12.1-01-02 | 01 | 1 | D-09 | — | @RequirePermission(...perms) sets PERMISSIONS_KEY metadata; Reflector.get round-trips the exact array; empty call yields empty array; no metadata bleed between methods | unit | `cd nemea-back && npx jest --testPathPatterns=require-permission.decorator --no-coverage` | ✅ | ✅ green (6 tests) |
| 12.1-01-03 | 01 | 1 | D-04 | — | RolesService CRUD + system-role protection + admin-lockout (ADMIN cannot lose canManageUsers) + delete-with-users guard + rename-conflict guard | unit | `cd nemea-back && npx jest --testPathPatterns=roles.service --no-coverage` | ✅ | ✅ green (13 tests) |
| 12.1-01-04 | 01 | 1 | D-04 | — | RolesController delegates findAll / create / update / remove to RolesService with exact arguments | unit | `cd nemea-back && npx jest --testPathPatterns=roles.controller --no-coverage` | ✅ | ✅ green (5 tests) |
| 12.1-02 | 02 | 2 | D-04 (migration) | — | 40 @Roles() usages replaced with @RequirePermission(); old auth files deleted; scenarios admin bypass uses canManageUsers | static / human | covered by `12.1-VERIFICATION.md` (52 @RequirePermission usages across 9 controllers, audited) | n/a | ✅ human-verified |
| 12.1-03 | 03 | 3 | D-01 .. D-05 (frontend) | — | usePermissions() hook, NextAuth session extension, middleware ROUTE_PERMISSIONS, sidebar conditional rendering | human + e2e | covered by `12.1-UAT.md` tests 2, 3, 4, 10, 11 — all passed | n/a | ✅ human-verified |
| 12.1-04 | 04 | 4 | D-12 .. D-15 (Roles UI) | — | /roles page + /usuarios dynamic-role select + edit/create/delete dialogs + permission-dependency UX | human | covered by `12.1-UAT.md` tests 5, 6, 7, 8 — tests 5-7 passed; test 8 surfaced gap → Phase 12.4 | n/a | ⚠ gap → 12.4 |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Combined verification command:**
```
cd nemea-back && npx jest --testPathPatterns="permissions.guard|require-permission.decorator|roles.service|roles.controller" --no-coverage
```
→ 4 suites, 34 tests, 0 failures (verified 2026-05-18).

---

## Wave 0 Requirements

All four Wave 0 gaps from RESEARCH.md are now satisfied:

- [x] `nemea-back/src/auth/guards/permissions.guard.spec.ts` — covers D-08
- [x] `nemea-back/src/auth/decorators/require-permission.decorator.spec.ts` — covers D-09
- [x] `nemea-back/src/roles/roles.service.spec.ts` — covers D-04 (RolesService)
- [x] `nemea-back/src/roles/roles.controller.spec.ts` — covers D-04 (RolesController)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Migration `1773000000000-AddRolesAndPermissions` creates `roles` table, seeds ADMIN/USER, backfills `users.role_id` | D-06 | Requires a live PostgreSQL instance; cannot reliably integration-test against an in-memory fixture given the breaking schema change | Run `cd nemea-back && npm run migration:run` against a dev DB. Verify: `SELECT name, is_system FROM roles` returns ADMIN+USER, both `is_system=true`. `SELECT count(*) FROM users WHERE role_id IS NULL` returns 0. |
| Swagger `/api/docs` shows `Requires: can_XXX` in protected endpoint descriptions | D-04 | Requires running backend + browser to inspect rendered Swagger UI | Boot backend (`npm run start:dev`), open `http://localhost:4000/api/docs`, spot-check 3+ protected endpoints (e.g. POST /products, DELETE /supplies/:id, POST /roles) and confirm the description mentions the required permission. |
| Stale JWT (pre-12.1 token, role-only payload) forces re-auth instead of silently breaking | D-07 | Requires a real session with an old JWT; cannot synthesise the cookie state in a unit test | Log in pre-deploy → deploy 12.1 → reload `/`. Expect redirect to `/login` (NextAuth detects `token.role` present + `token.permissions` absent → clears `backendToken`). Listed under "human verification" in 12.1-VERIFICATION.md. |

---

## Validation Sign-Off

- [x] All tasks have automated verify or are recorded as manual-only with justification
- [x] Sampling continuity: 4 automated specs cover the 4 backend Wave 0 gaps; frontend tasks covered by UAT
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 10 s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-18 (retroactive via /gsd-validate-phase)

---

## Validation Audit 2026-05-18

| Metric | Count |
|--------|-------|
| Gaps found | 4 |
| Resolved | 4 |
| Escalated to manual-only | 0 (3 pre-existing manual items recorded above are from VERIFICATION.md, not new escalations) |
| Tests added | 4 spec files, 34 test cases |
| Test files | `permissions.guard.spec.ts`, `require-permission.decorator.spec.ts`, `roles.service.spec.ts`, `roles.controller.spec.ts` |

**Notable finding during gap fill:** `roles.service.spec.ts` initially leaked a `mockResolvedValueOnce(null)` from the update happy-path test into the subsequent remove test because `jest.clearAllMocks()` does not drain one-time mock queues. Fixed by using `mockResolvedValue(role)` (persistent) for the update happy path, where only one `findOne` call occurs. Recorded so future spec authors avoid the same pattern.

## Validation Audit 2026-05-18 (re-run)

| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |

Re-verification: ran `cd nemea-back && npx jest --testPathPatterns="permissions.guard|require-permission.decorator|roles.service|roles.controller" --no-coverage` — 4 suites, 34 tests, 0 failures. All per-task map entries remain green. No new gaps.

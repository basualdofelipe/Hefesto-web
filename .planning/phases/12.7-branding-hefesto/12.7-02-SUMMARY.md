---
phase: 12.7-branding-hefesto
plan: "02"
subsystem: infra
tags: [branding, rebrand, hefesto, package-identity, test-fixtures]

# Dependency graph
requires:
  - phase: 12.6-branding-configurable-por-env
    provides: in-app brand tokens already neutral (APP_NAME=Hefesto defaults)
provides:
  - nemea-front grep-clean of all "nemea" literals
  - package identity hefesto-front in package.json and package-lock.json
  - docs (README + ARCHITECTURE) reading public repo name hefesto-front/hefesto-back
  - 4 test fixtures using @hefesto.com emails; unit suite stays green
affects: [12.7-03, 12.7-planning-docs]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - nemea-front/package.json
    - nemea-front/package-lock.json
    - nemea-front/README.md
    - nemea-front/docs/ARCHITECTURE.md
    - nemea-front/src/app/(app)/calculadora/__tests__/CalculadoraClient.test.tsx
    - nemea-front/src/app/(app)/escenarios/[id]/__tests__/ScenarioEditorClient.test.tsx
    - nemea-front/src/app/(app)/usuarios/__tests__/DeleteUserAlertDialog.test.tsx
    - nemea-front/src/app/(app)/usuarios/__tests__/EditUserDialog.test.tsx

key-decisions:
  - "package-lock.json self-name edited directly (2 refs) rather than via npm install --package-lock-only — deterministic, no network, npm ci validates consistency"
  - "Email fixture domain @nemea.com → @hefesto.com across all 4 test files — fixture/string only, no assertion or behavior logic touched"

patterns-established:
  - "Rebrand mapping: nemea→hefesto, Nemea→Hefesto, NEMEA→HEFESTO (case-sensitive, 3 rules)"

requirements-completed: [2, 4, 5, 6]

# Metrics
duration: 4min
completed: 2026-06-17
---

# Phase 12.7 Plan 02: Sweep nemea-front package identity, docs, and test fixtures — Summary

**Mechanical nemea→hefesto rebrand of nemea-front: package name hefesto-front (package.json + lockfile), docs (README + ARCHITECTURE), and 4 test fixture files — repo grep-clean, npm ci exits 0, 24 tests pass.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-06-17T16:59:46Z
- **Completed:** 2026-06-17T17:03:56Z
- **Tasks:** 2 of 2
- **Files modified:** 8

## Accomplishments

- Package identity renamed from `nemea-front` to `hefesto-front` in both `package.json` and `package-lock.json` (2 self-name refs); `npm ci` exits 0 confirming consistency (SPEC req 2)
- User-facing docs (`README.md`, `docs/ARCHITECTURE.md`) updated: all `nemea-front`/`nemea-back` path tokens and prose references → `hefesto-front`/`hefesto-back` (SPEC req 4)
- 4 test fixtures updated: email domains `@nemea.com` → `@hefesto.com` across 14 occurrences; fixture/string data only — no assertion or behavior logic altered (SPEC req 5)
- `git grep -i nemea` returns empty (exit 1) across the entire nemea-front repo (SPEC req 6 / no-collateral guard passes)
- Unit suite: 5 test suites, 24 tests, 0 failures

## Task Commits

Each task was committed atomically:

1. **Task 1: Sweep package identity + docs** - `be602aa` (refactor)
2. **Task 2: Sweep 4 test fixtures + verify suite green** - `c847afb` (refactor)

**Plan metadata:** (see root repo final commit below)

## Files Created/Modified

- `nemea-front/package.json` - name: nemea-front → hefesto-front
- `nemea-front/package-lock.json` - 2 self-name refs updated to hefesto-front
- `nemea-front/README.md` - title, clone cmd, back link, inline comment, backend connection prose
- `nemea-front/docs/ARCHITECTURE.md` - title and opening sentence
- `nemea-front/src/app/(app)/calculadora/__tests__/CalculadoraClient.test.tsx` - demo@nemea.com → demo@hefesto.com (2 refs)
- `nemea-front/src/app/(app)/escenarios/[id]/__tests__/ScenarioEditorClient.test.tsx` - admin/other @nemea.com → @hefesto.com (3 refs)
- `nemea-front/src/app/(app)/usuarios/__tests__/DeleteUserAlertDialog.test.tsx` - admin/user @nemea.com → @hefesto.com (4 refs)
- `nemea-front/src/app/(app)/usuarios/__tests__/EditUserDialog.test.tsx` - admin/user/other @nemea.com → @hefesto.com (5 refs)

## Decisions Made

- **package-lock.json edited directly** (2 refs at lines 2 and 8) rather than via `npm install --package-lock-only`: deterministic, no network, and `npm ci` validates the lockfile↔package.json consistency as the acceptance gate. No risk of pulling new package versions.
- **Email domain only (not username)** changed in fixtures: `demo@nemea.com` → `demo@hefesto.com`, etc. Usernames (demo, admin, user, other) were not brand tokens and were left intact.

## Deviations from Plan

None — plan executed exactly as written. All 8 files listed in `files_modified` were the only files changed.

## Issues Encountered

None. Pre-existing `react-hooks/incompatible-library` ESLint warnings (4 occurrences, 0 errors) fired during the pre-commit hook on both commits as expected — these are pre-existing and unrelated to this plan.

Pre-existing `act()` console warnings in `CalculadoraClient.test.tsx` are also pre-existing and do not cause test failures; all 24 tests pass.

## Final Verification Results

Observed, not assumed:

```
git grep -i nemea (nemea-front)  → empty, exit 1  ✓
git status --porcelain           → clean (no unstaged changes)  ✓
npm ci                           → exit 0 (package.json ↔ lockfile consistent)  ✓
npm test                         → 5 suites, 24 tests, 0 failures, exit 0  ✓
package.json "name"              → "hefesto-front"  ✓
package-lock.json self-name refs → "hefesto-front" at lines 2 and 8  ✓
```

```
git log --oneline -6 (nemea-front, refactor/phase-12.7-branding-hefesto):
c847afb refactor(12.7-02): rebrand nemea email fixtures to hefesto in 4 test files
be602aa refactor(12.7-02): rename package identity and docs from nemea to hefesto
51e88d4 chore(phase-12.7): kickoff — branding hefesto
```

## Known Stubs

None — this plan performs in-place string token replacements only. No new UI, data wiring, or placeholder content introduced.

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or schema changes introduced. Mechanical string rename only.

## Next Phase Readiness

nemea-front is fully grep-clean of `nemea`. The remaining rebrand work is in nemea-back (plans 03+) and the orchestrator root `.planning/` docs.

---
*Phase: 12.7-branding-hefesto*
*Completed: 2026-06-17*

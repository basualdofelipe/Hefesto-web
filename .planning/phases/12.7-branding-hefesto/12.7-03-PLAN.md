---
phase: 12.7-branding-hefesto
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - README.md
  - README.es.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/STACK.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/TESTING.md
autonomous: true
requirements: [4, 7]

must_haves:
  truths:
    - "Root docs (CLAUDE.md, README.md, README.es.md) show zero Nemea"
    - "The swept .planning root docs + codebase docs show zero nemea"
    - "No file under .planning/phases/ is modified (D-03 — phases/* keep Nemea)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Root orchestrator doc rebranded to Hefesto / Hefesto-web"
      contains: "Hefesto"
    - path: ".planning/PROJECT.md"
      provides: "Principal planning root doc rebranded"
      contains: "Hefesto"
  key_links:
    - from: "root docs path tokens"
      to: "GitHub public repo names"
      via: "hefesto-front / hefesto-back / Hefesto-web referenced in prose"
      pattern: "hefesto-(front|back)"
---

<objective>
Sweep the literal `nemea` → `hefesto` (case-sensitive, 3 rules) across the orchestrator's PUBLIC surface: root docs (CLAUDE.md, README.md, README.es.md) and the principal `.planning` docs. Per 12.7-CONTEXT.md D-02 this is NARROWED from the SPEC's "full .planning (~60 files)" to the principal root docs only — `PROJECT.md`, `REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md` — PLUS `.planning/codebase/*.md` (planner's discretion under D-02, included below — see rationale). `.planning/phases/*` is EXCLUDED (D-03: those artifacts keep "Nemea") and the 12.7 phase dir is EXCLUDED (D-05).

Purpose: the orchestrator repo goes PUBLIC as a GSD-process showcase, so its "face" (root docs + principal planning docs + codebase reference docs) must not show the registered-mark `Nemea`. Deep per-phase process artifacts are a deliberately-accepted tradeoff and stay as-is.
Output: Grep-clean root docs and swept `.planning` docs, with the req-7 acceptance criterion re-scoped to exactly the swept set (NOT the whole `.planning`).

Implements SPEC reqs 4 (docs user-facing — root half) and 7 (orchestrator .planning rebrand, AS REFINED by D-02/D-03/D-05).
</objective>

<planning_decision_codebase_inclusion>
DECISION (planner, per D-02 which explicitly delegates the `codebase/*.md` call): `.planning/codebase/*.md` IS treated as "principal" and IS swept. Rationale: these 6 files (ARCHITECTURE, CONCERNS, INTEGRATIONS, STACK, STRUCTURE, TESTING) are the top-level codebase-map reference docs — they ARE the GSD-showcase "face" of how the project is built, not deep per-phase process detail. D-03 prioritizes cleaning the planning "cara"; codebase/* is squarely that. `.planning/phases/*` remains out (D-03). Verified footprint: codebase/* contains nemea in all 6 files (STRUCTURE.md and CONCERNS.md most heavily — file paths like `nemea-back/src/...` and prose).
</planning_decision_codebase_inclusion>

<artifacts_this_phase_produces>
This plan creates NO new files. Every change is an in-place case-sensitive token replacement on the 13 existing docs in `files_modified`. NO file under `.planning/phases/` is touched (D-03). The 12.7 phase dir is not swept (D-05). Local folder names and `.gitignore` are NOT changed (D-04 deferred — those tokens in prose are updated to the public names, but the actual filesystem rename + .gitignore edit is a follow-up).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.7-branding-hefesto/12.7-SPEC.md
@.planning/phases/12.7-branding-hefesto/12.7-CONTEXT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep root orchestrator docs</name>
  <files>CLAUDE.md, README.md, README.es.md</files>
  <read_first>
    - CLAUDE.md (brand refs: project title "Nemea", repo "Nemea-web", folder-path tokens `nemea-front`/`nemea-back`, sub-project table)
    - README.md and README.es.md (brand refs + repo/folder path tokens)
    - .planning/phases/12.7-branding-hefesto/12.7-SPEC.md (req 4 root acceptance: `git grep -n "Nemea" -- CLAUDE.md README.md README.es.md` returns zero)
    - .planning/phases/12.7-branding-hefesto/12.7-CONTEXT.md (D-04 — folder rename deferred; D-01 — real-identity OUT, do not enumerate)
  </read_first>
  <action>
    Apply the case-sensitive mapping (nemea→hefesto, Nemea→Hefesto, NEMEA→HEFESTO) to the 3 root docs with concrete targets:
    - Brand/repo refs: `Nemea` → `Hefesto`, `Nemea-web` → `Hefesto-web`.
    - Path/folder tokens in prose (`nemea-front`, `nemea-back`) → `hefesto-front`, `hefesto-back` so the public docs read the GitHub repo names. NOTE per D-04: this updates the DOC TEXT only; do NOT rename the actual local folders and do NOT edit `.gitignore` in this plan (that coupled step is deferred to the follow-up checklist — see Plan 04). The local working folders stay `nemea-front`/`nemea-back` for this session.
    - Do NOT add or touch anything about legal-entity / real-brand identity (D-01 — handled by the user off-plan; never enumerate).
    Diff-review every hunk for collateral.
  </action>
  <verify>
    <automated>git grep -n "Nemea" -- CLAUDE.md README.md README.es.md; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "Nemea" -- CLAUDE.md README.md README.es.md` returns empty (exit 1).
    - `git grep -i nemea -- CLAUDE.md README.md README.es.md` returns empty (exit 1) — covers lowercase path tokens too.
    - `.gitignore` is unchanged and the local folders `nemea-front`/`nemea-back` still exist (D-04 not executed here).
    - No real-identity / legal-entity content was added (D-01).
  </acceptance_criteria>
  <done>The 3 root docs are grep-clean of `nemea` (any case), referencing the public repo names, with `.gitignore` and local folders untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep principal .planning root docs + codebase docs (D-02 scope)</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/CONCERNS.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/STACK.md, .planning/codebase/STRUCTURE.md, .planning/codebase/TESTING.md</files>
  <read_first>
    - .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md (brand refs in titles/prose: "Nemea is...", "Roadmap: Nemea", folder-path tokens `nemea-front`/`nemea-back`)
    - .planning/codebase/STRUCTURE.md and CONCERNS.md (heaviest — file-path prose like `nemea-back/src/...`) plus ARCHITECTURE.md, INTEGRATIONS.md, STACK.md, TESTING.md
    - .planning/phases/12.7-branding-hefesto/12.7-CONTEXT.md (D-02 scope narrowing, D-03 phases/* excluded, D-05 12.7 excluded)
  </read_first>
  <action>
    Apply the case-sensitive mapping to exactly these 10 files (the D-02 principal set + planner-included codebase/*). Concrete handling:
    - Brand prose: `Nemea` → `Hefesto` (e.g. ROADMAP `# Roadmap: Nemea` → `# Roadmap: Hefesto`; PROJECT/STATE brand mentions).
    - Folder-path tokens in prose (`nemea-front`/`nemea-back`) → `hefesto-front`/`hefesto-back` (matches the public repo names; D-04 filesystem rename still deferred).
    Hard exclusions — do NOT touch:
    - Any file under `.planning/phases/` (D-03 — phases/* keep Nemea, accepted tradeoff).
    - The 12.7 phase dir (D-05 — excluded from public repo, no rephrase needed).
    - The Greek-god product seed names (Hefesto/Ares/etc. are NOT `nemea` and never matched anyway).
    Diff-review for collateral (these docs reference DB names, module names, etc. — confirm no real word broke; `nemea` is not a substring of any real word, but verify, e.g. that an existing `Hefesto` mention is not double-processed).
  </action>
  <verify>
    <automated>git grep -i nemea -- .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md '.planning/codebase/*.md'; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -i nemea -- .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md '.planning/codebase/*.md'` returns empty (exit 1). [This is the RE-SCOPED req-7 acceptance — the swept set only, NOT the whole `.planning`, per D-02/D-03.]
    - `git status` shows ZERO modified files under `.planning/phases/` (D-03 hard guard) — verify no phases/* file was swept.
    - The 12.7 phase dir is unmodified (D-05).
    - Diff-review confirms brand-token-only changes, no collateral.
  </acceptance_criteria>
  <done>The 10 principal/codebase planning docs are grep-clean of `nemea`, and `.planning/phases/*` (including 12.7) is provably untouched.</done>
</task>

</tasks>

<rescoped_acceptance_note>
SPEC req-7 originally said `git grep -i nemea -- .planning` returns zero. Under CONTEXT D-02 (scope narrowed to principal root docs) and D-03 (`phases/*` keep Nemea, accepted tradeoff), that whole-`.planning` check is NO LONGER the correct criterion — `.planning/phases/*` legitimately retains "Nemea". The corrected, in-scope acceptance is:

  git grep -i nemea -- .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md '.planning/codebase/*.md'

returns empty. The final phase verification (Plan 04) uses this re-scoped check, not the whole-`.planning` grep.
</rescoped_acceptance_note>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public orchestrator repo → reviewer | The GSD-process repo goes public as a showcase; only doc prose changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.7-R01 | Information Disclosure | Doc prose in public planning docs | accept | Docs contain no secrets or credentials; brand-token replacement only. Real-identity sanitization (D-01) is explicitly OUT and handled by the user off-plan, so this plan does not risk over- or under-redacting it. |
| T-12.7-R02 | Tampering | Accidental edit of `.planning/phases/*` or `.gitignore` | mitigate | Task 2 asserts `git status` shows zero modified files under `.planning/phases/`; Task 1 asserts `.gitignore` unchanged. Both are hard guards in acceptance_criteria. |
| T-12.7-SC | Tampering | package installs | n/a | No code, no installs — docs only. |

No high-severity threats — documentation-only string rename. The only risk surface is over-reach (sweeping excluded files), mitigated by the explicit `.planning/phases/` zero-diff guard.
</threat_model>

<verification>
- `git grep -n "Nemea" -- CLAUDE.md README.md README.es.md` → empty.
- `git grep -i nemea -- .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md '.planning/codebase/*.md'` → empty.
- `git status` → no modified files under `.planning/phases/`; `.gitignore` unchanged.
</verification>

<success_criteria>
Root docs and the D-02 principal planning set (root docs + codebase/*) are grep-clean of `nemea`; `.planning/phases/*` (incl. 12.7) and `.gitignore` are provably untouched; the req-7 acceptance is re-scoped to the swept set.
</success_criteria>

<output>
Create `.planning/phases/12.7-branding-hefesto/12.7-03-SUMMARY.md` when done.
</output>

---
phase: 12.7-branding-hefesto
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nemea-back/docker-compose.yml
  - nemea-back/.env.example
  - nemea-back/src/app.controller.ts
  - nemea-back/src/common/interceptors/logging.interceptor.ts
  - nemea-back/package.json
  - nemea-back/package-lock.json
  - nemea-back/README.md
  - nemea-back/docs/ARCHITECTURE.md
  - nemea-back/src/app.controller.spec.ts
  - nemea-back/src/auth/auth.controller.spec.ts
  - nemea-back/src/auth/auth.service.spec.ts
  - nemea-back/src/auth/guards/permissions.guard.spec.ts
  - nemea-back/src/users/users.controller.spec.ts
  - nemea-back/src/users/users.service.spec.ts
  - nemea-back/test/app.e2e-spec.ts
  - nemea-back/test/setup-e2e.ts
autonomous: true
requirements: [1, 2, 3, 5, 6]

must_haves:
  truths:
    - "git grep -i nemea returns zero across the entire nemea-back repo"
    - "docker compose up -d boots and the backend connects to hefesto_db without error"
    - "GET / returns app: \"hefesto-back\""
    - "npm test (unit) passes in nemea-back"
    - "npm ci runs (package.json/package-lock.json self-name consistent)"
    - "src/database/migrations/** shows zero diff"
  artifacts:
    - path: "nemea-back/docker-compose.yml"
      provides: "Infra naming on hefesto_* (containers, DB, user, pass, volume, healthcheck)"
      contains: "hefesto_db"
    - path: "nemea-back/.env.example"
      provides: "DATABASE_URL + DATABASE_URL_TEST on hefesto creds, consistent with compose"
      contains: "hefesto_db"
    - path: "nemea-back/src/app.controller.ts"
      provides: "Health response app identifier hefesto-back"
      contains: "hefesto-back"
  key_links:
    - from: "nemea-back/docker-compose.yml"
      to: "nemea-back/.env.example"
      via: "shared DB credentials (user/pass/db/port) must match exactly"
      pattern: "hefesto"
    - from: "nemea-back/src/app.controller.ts"
      to: "GET / health response"
      via: "app field"
      pattern: "app:\\s*'hefesto-back'"
---

<objective>
Sweep the literal `nemea` → `hefesto` (case-sensitive, 3 rules) across the entire `nemea-back` repo: infra (docker-compose + .env.example), non-test code (app.controller health response + logging interceptor logger context), package identity (package.json + package-lock.json self-name), docs (README + ARCHITECTURE), and 8 test specs/e2e fixtures. Recreate the Docker volume to re-seed on `hefesto_db`, then prove the suite is green and the migrations are untouched.

Purpose: nemea-back is one of the two clonable portfolio repos that must go public showing zero `nemea`. This is the heaviest sweep (16 files) and the only one with coupled infra (docker ↔ env) and a re-seed gotcha.
Output: A grep-clean nemea-back repo whose unit suite is green, whose docker boots on hefesto_db, and whose migrations show zero diff.

Implements SPEC reqs 1 (infra naming), 2 (package identity — back half), 3 (code references), 5 (test fixtures — back half), 6 (no-collateral guard). Scope and decisions follow 12.7-CONTEXT.md (D-01 real-identity OUT, D-04 folder/.gitignore DEFERRED — neither touched here).
</objective>

<artifacts_this_phase_produces>
This plan creates NO new files. Every change is an in-place case-sensitive token replacement (`nemea→hefesto`, `Nemea→Hefesto`, `NEMEA→HEFESTO`) on the 16 existing nemea-back files listed in `files_modified`. The Docker dev volume is destroyed and recreated (`hefesto_pgdata`), and migrations re-run to re-seed — but no source file is created.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.7-branding-hefesto/12.7-SPEC.md
@.planning/phases/12.7-branding-hefesto/12.7-CONTEXT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep nemea-back infra + non-test code + package identity</name>
  <files>nemea-back/docker-compose.yml, nemea-back/.env.example, nemea-back/src/app.controller.ts, nemea-back/src/common/interceptors/logging.interceptor.ts, nemea-back/package.json, nemea-back/package-lock.json</files>
  <read_first>
    - nemea-back/docker-compose.yml (current tokens: container_name nemea_postgres / nemea_postgres_test, POSTGRES_DB nemea_db / nemea_test, POSTGRES_USER nemea, POSTGRES_PASSWORD nemea_dev / nemea_test, volume nemea_pgdata, healthcheck `-U nemea -d nemea_db` / `-U nemea -d nemea_test`)
    - nemea-back/.env.example (lines 14 and 17: DATABASE_URL=postgresql://nemea:nemea_dev@localhost:5432/nemea_db ; DATABASE_URL_TEST=postgresql://nemea:nemea_test@localhost:5433/nemea_test)
    - nemea-back/src/app.controller.ts (line 25: `app: 'nemea-back'`)
    - nemea-back/src/common/interceptors/logging.interceptor.ts (line 13: `new Logger('Nemea')`)
    - nemea-back/package.json (line 2: `"name": "nemea-back"`)
    - nemea-back/package-lock.json (lines 2 and 8: 2 self-name refs `"name": "nemea-back"`)
    - .planning/phases/12.7-branding-hefesto/12.7-SPEC.md (reqs 1, 2, 3 — target values + acceptance)
  </read_first>
  <action>
    Apply the case-sensitive mapping (nemea→hefesto, Nemea→Hefesto, NEMEA→HEFESTO) to these 6 files with concrete targets:
    - docker-compose.yml: container_name nemea_postgres → hefesto_postgres ; nemea_postgres_test → hefesto_postgres_test ; POSTGRES_DB nemea_db → hefesto_db and nemea_test → hefesto_test ; POSTGRES_USER nemea → hefesto ; POSTGRES_PASSWORD nemea_dev → hefesto_dev and nemea_test → hefesto_test ; volume nemea_pgdata → hefesto_pgdata (both the service-level reference and the top-level `volumes:` key) ; healthcheck `pg_isready -U nemea -d nemea_db` → `-U hefesto -d hefesto_db` and `-U nemea -d nemea_test` → `-U hefesto -d hefesto_test`.
    - .env.example: DATABASE_URL → postgresql://hefesto:hefesto_dev@localhost:5432/hefesto_db ; DATABASE_URL_TEST → postgresql://hefesto:hefesto_test@localhost:5433/hefesto_test. These MUST be mutually consistent with docker-compose.yml above (same user/pass/db/port pairs) — this is the coupled-infra constraint.
    - src/app.controller.ts: `app: 'nemea-back'` → `app: 'hefesto-back'`.
    - src/common/interceptors/logging.interceptor.ts: `new Logger('Nemea')` → `new Logger('Hefesto')`.
    - package.json: `"name": "nemea-back"` → `"name": "hefesto-back"`.
    - package-lock.json: both self-name refs (lines 2 and 8) `"name": "nemea-back"` → `"name": "hefesto-back"`. Edit these 2 refs directly (per gotcha #4, direct edit is chosen over `npm install --package-lock-only` — deterministic, no network). Do NOT touch any dependency package names (none contain nemea).
    After editing, run `git diff` on these 6 files and review every hunk: confirm only brand tokens changed and no `nemea` substring inside a real word was mangled (there are none, but verify). Then run `npm ci` to prove package.json and package-lock.json stayed mutually consistent — `npm ci` aborts on any package.json↔lockfile mismatch, so a clean exit (0) confirms both self-name refs were edited in lockstep (SPEC req 2 acceptance).
  </action>
  <verify>
    <automated>cd nemea-back && git grep -i nemea -- docker-compose.yml .env.example src/app.controller.ts src/common/interceptors/logging.interceptor.ts package.json package-lock.json; test $? -eq 1 && npm ci</automated>
  </verify>
  <acceptance_criteria>
    - `cd nemea-back && git grep -i nemea -- docker-compose.yml .env.example src/app.controller.ts src/common/interceptors/logging.interceptor.ts package.json package-lock.json` returns empty (exit 1).
    - `npm ci` exits 0 (package.json/package-lock.json consistent after self-name edit — `npm ci` fails on any mismatch between the two files, proving the lockfile self-name was edited in lockstep with package.json; SPEC req 2 acceptance).
    - docker-compose.yml and .env.example use the SAME user (hefesto), password (hefesto_dev / hefesto_test), db (hefesto_db / hefesto_test), and ports — verified by reading both.
    - `git diff` hunks on these 6 files show only brand-token replacements, no logic or structural changes.
  </acceptance_criteria>
  <done>The 6 infra/code/package files are grep-clean of `nemea`, `npm ci` exits 0 (package.json↔package-lock.json consistent), and the docker↔env credentials are mutually consistent on `hefesto_*`.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep nemea-back docs + 8 test specs/e2e fixtures</name>
  <files>nemea-back/README.md, nemea-back/docs/ARCHITECTURE.md, nemea-back/src/app.controller.spec.ts, nemea-back/src/auth/auth.controller.spec.ts, nemea-back/src/auth/auth.service.spec.ts, nemea-back/src/auth/guards/permissions.guard.spec.ts, nemea-back/src/users/users.controller.spec.ts, nemea-back/src/users/users.service.spec.ts, nemea-back/test/app.e2e-spec.ts, nemea-back/test/setup-e2e.ts</files>
  <read_first>
    - nemea-back/README.md and nemea-back/docs/ARCHITECTURE.md (brand prose + any clone/db-name instructions referencing nemea)
    - nemea-back/src/auth/guards/permissions.guard.spec.ts (the `nemea` matches are email FIXTURES only: `user@nemea.com`, `admin@nemea.com`, `regular@nemea.com` — they are test data, NOT permission-check logic; the guard's permission flags carry no `nemea`)
    - the other 7 spec/e2e files (each contains `nemea` only as fixture/health-response/db-name strings that move with the source changed in Task 1)
    - .planning/phases/12.7-branding-hefesto/12.7-SPEC.md (req 5 acceptance: npm test green + grep-clean)
  </read_first>
  <action>
    Apply the same case-sensitive mapping to the 2 docs and 8 test files with concrete targets:
    - README.md, docs/ARCHITECTURE.md: replace brand refs `Nemea`/`nemea` → `Hefesto`/`hefesto`, including any DB-name / connection-string examples so docs match the docker-compose + .env.example values set in Task 1 (e.g. any `nemea_db` example → `hefesto_db`).
    - The 8 test files: replace fixture strings only — health-response assertion `'nemea-back'` → `'hefesto-back'` (app.controller.spec / app.e2e-spec), email fixtures `*@nemea.com` → `*@hefesto.com`, any `nemea_db`/`nemea_test` connection strings in setup-e2e.ts / app.e2e-spec.ts → `hefesto_db`/`hefesto_test`. These fixtures change together with the Task-1 source so the suite stays green.
    Diff-review every changed test file to confirm the changes are fixture/string-only and do NOT alter any permission flag, guard boolean, expected-status, or assertion structure (this is the security-relevant review — the permissions.guard.spec changes must be email fixtures only).
  </action>
  <verify>
    <automated>cd nemea-back && git grep -i nemea; test $? -eq 1 && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd nemea-back && git grep -i nemea` returns empty across the WHOLE repo (exit 1).
    - `npm test` exits 0 (all unit specs green).
    - `git diff --stat` shows ZERO changes under `src/database/migrations/` (req 6 hard guard).
    - Diff-review confirms permissions.guard.spec.ts changes are email-fixture replacements only, with no change to any permission flag or guard assertion.
  </acceptance_criteria>
  <done>The whole nemea-back repo is grep-clean of `nemea`, the unit suite is green, and migrations show zero diff.</done>
</task>

<task type="auto">
  <name>Task 3: Recreate Docker volume on hefesto_db, re-seed, verify boot + health</name>
  <files>(no file changes — runtime verification of Task 1/2 infra rename)</files>
  <read_first>
    - nemea-back/docker-compose.yml (post-Task-1: volume hefesto_pgdata, dbs hefesto_db / hefesto_test)
    - nemea-back/.env.example (post-Task-1 DATABASE_URL values)
    - 12.7-SPEC.md Constraints (volume re-seed gotcha) and req 1 acceptance
  </read_first>
  <action>
    The rename of volume `nemea_pgdata` → `hefesto_pgdata` orphans the old dev volume (gotcha #1). Recreate from scratch so the new `hefesto_db` exists and is seeded:
    - From nemea-back: `docker compose down -v` (removes old nemea_pgdata volume + containers), then `docker compose up -d` (creates hefesto_postgres + hefesto_postgres_test on the new hefesto_pgdata volume).
    - Run the project's migration command (e.g. `npm run migration:run`) against the fresh hefesto_db to recreate schema + re-seed (products = Greek gods, admin/demo users from env). Use the repo's actual migration script name — read package.json scripts to confirm.
    - Confirm the backend connects to hefesto_db: bring the app up (`npm run start:dev` or equivalent) and hit `GET /`. If running the full app server is impractical in the executor environment, at minimum confirm `docker compose ps` shows both containers healthy and that a `psql`/connection check against `hefesto_db` succeeds; and assert the health-response string via the unit/e2e test from Task 2 (app.controller.spec / app.e2e-spec already assert `app: "hefesto-back"`).
    Do NOT touch Feli's local gitignored `.env` (gotcha #3) — only `.env.example` is in scope.
  </action>
  <verify>
    <automated>cd nemea-back && docker compose down -v && docker compose up -d && docker compose ps --format '{{.Name}} {{.Status}}' | grep -i hefesto</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose up -d` succeeds; `docker compose ps` shows `hefesto_postgres` (and `hefesto_postgres_test`) running/healthy — no `nemea`-named container exists.
    - Migrations run against `hefesto_db` without error and re-seed (Greek-god products present).
    - `GET /` returns `app: "hefesto-back"` (directly, or asserted via the app.controller / e2e test which is now grep-clean and green).
  </acceptance_criteria>
  <done>Docker boots on the new hefesto_pgdata volume, the backend connects to hefesto_db, migrations re-seed cleanly, and the health response reads hefesto-back.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public repo → cloner | A reviewer clones the public repo; .env.example dev creds are visible by design (example file, not a real secret). |
| HTTP client → API | Existing auth/permission boundary — unchanged by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.7-01 | Information Disclosure | .env.example credentials (hefesto_dev / hefesto_test) | accept | These are dev-only example values in a file meant to be public; no real secret is introduced or leaked by renaming nemea_dev→hefesto_dev. Real secrets live in Feli's gitignored `.env` (untouched). |
| T-12.7-02 | Tampering | auth/guards permission logic (permissions.guard.spec.ts changes) | mitigate | Diff-review in Task 2 confirms the spec changes are email-FIXTURE replacements only (`*@nemea.com`→`*@hefesto.com`); no permission flag, guard boolean, or expected-status assertion is altered. The guard's permission-check logic contains zero `nemea` and is not edited. |
| T-12.7-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan — package-lock self-name refs are edited directly, not via `npm install`. `npm ci` is run only as a read-only consistency check against the committed lockfile (installs the existing pinned tree, adds no new dependency). Zero new dependencies (SPEC: rebrand is string-only). |

No high-severity threats — this is a mechanical rename. T-12.7-02 is the only one touching a security-relevant surface, and it is mitigated by the mandatory fixture-only diff-review gate (block-on severity: high → no blocking threat found).
</threat_model>

<verification>
- `cd nemea-back && git grep -i nemea` → empty (whole repo).
- `npm ci` → exits 0 (package.json ↔ package-lock.json self-name consistent after the rename; SPEC req 2).
- `npm test` → exits 0.
- `git diff --stat` → zero changes under `src/database/migrations/`.
- `docker compose up -d` → hefesto containers healthy; backend connects to hefesto_db.
- `GET /` (or its green test) → `app: "hefesto-back"`.
</verification>

<success_criteria>
nemea-back is grep-clean of `nemea`, `npm ci` runs (package.json ↔ lockfile self-name consistent), unit suite green, docker boots and seeds on `hefesto_db`, health response is `hefesto-back`, and migrations are untouched.
</success_criteria>

<output>
Create `.planning/phases/12.7-branding-hefesto/12.7-01-SUMMARY.md` when done.
</output>
</content>
</invoke>

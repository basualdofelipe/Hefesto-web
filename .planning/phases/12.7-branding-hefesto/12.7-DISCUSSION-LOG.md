# Phase 12.7: branding hefesto — Discussion Log

**Date:** 2026-06-16
*Human-reference only — not consumed by downstream agents.*

SPEC.md loaded (7 requirements). Discussion scoped to implementation decisions only.

## Areas discussed (3 selected, batched into one turn for brevity)

### 1. Alcance de sanitización
- **Options:** "Nemea + identidad real" / "Solo Nemea".
- **User:** SEMPERGY es grave pero lo borra el usuario a mano (ya empezado, commit pendiente). NO documentarlo como checklist en la fase. El rebrand del executor NO debe barrer todos los `phases/`, solo los docs principales de `.planning`.
- **Locked:** D-01 (sanitización real = manual, fuera de scope documentado), D-02 (.planning = solo docs principales, acota req 7), D-03 (tradeoff: phases/ públicos conservan Nemea).

### 2. Carpetas locales y paths
- **Options:** "Renombrar a hefesto-*" / "Mantener nemea-* local".
- **User:** Diferir al último paso; cambiar `.gitignore` + carpetas juntos; por ahora local sigue nemea para no romper la sesión.
- **Senior caveat dado:** `.gitignore` y nombre de carpeta están acoplados — el paso final hace ambos.
- **Locked:** D-04 (deferred al paso final, carpetas + gitignore juntos).

### 3. Meta-docs del rebrand
- **Options:** "Reescribir a neutro" / "Excluir 12.7 del repo público".
- **User:** Excluir 12.7 del repo público.
- **Locked:** D-05 (artefactos de 12.7 fuera del público; reemplaza el constraint de rephrase del SPEC).

## Todos
- Reviewed not folded: `drag-drop-catalog-ordering.md` (sin relación, → backlog).

## Deferred ideas
- (ninguna nueva; la discusión quedó dentro del scope)

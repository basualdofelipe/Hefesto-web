---
phase: 12.7-branding-hefesto
plan: 04
type: execute
wave: 2
depends_on: ["12.7-01", "12.7-02", "12.7-03"]
files_modified:
  - .planning/phases/12.7-branding-hefesto/12.7-FOLLOWUP-CHECKLIST.md
autonomous: true
requirements: [1, 4, 7]

must_haves:
  truths:
    - "A manual follow-up checklist doc exists covering the deferred/out-of-scope actions"
    - "The checklist covers the 3 GitHub repo renames + remote set-url + push + About"
    - "The checklist covers the D-04 coupled folder-rename + .gitignore step"
    - "The checklist covers the D-05 mechanism to exclude the 12.7 phase dir from the public repo"
    - "The checklist does NOT contain any real-identity / legal-entity sanitization item (D-01)"
    - "Final cross-repo verification confirms all in-scope surfaces are grep-clean"
  artifacts:
    - path: ".planning/phases/12.7-branding-hefesto/12.7-FOLLOWUP-CHECKLIST.md"
      provides: "Manual follow-up checklist for deferred/outward actions"
      min_lines: 20
      contains: "git remote set-url"
  key_links:
    - from: "12.7-FOLLOWUP-CHECKLIST.md"
      to: "the user's manual pre-publish workflow"
      via: "GitHub renames + D-04 coupled step + D-05 exclusion"
      pattern: "hefesto"
---

<objective>
Produce the phase's single NEW deliverable — the manual follow-up checklist (`12.7-FOLLOWUP-CHECKLIST.md`) capturing every out-of-scope / deferred action the executor must NOT do during this phase — and run the final cross-repo verification that proves the in-scope sweep landed clean.

Purpose: the phase rebrands what GSD can touch automatically; the rest (GitHub repo renames, the D-04 coupled folder+.gitignore rename, the D-05 exclusion of the 12.7 dir from the public repo) is outward/manual or deliberately deferred and must be handed to the user as an explicit checklist so nothing is dropped. This plan also closes the phase by aggregating the verification across all three sweeps.
Output: The follow-up checklist doc, plus a confirmed-green final verification of SPEC acceptance (re-scoped per D-02/D-03).

Implements the SPEC's follow-up-checklist requirement (boundary of reqs 1, 4, 7) and the phase-level acceptance roll-up. Runs in Wave 2 because the final aggregate grep only means something after Plans 01/02/03 have swept the three surfaces.
</objective>

<artifacts_this_phase_produces>
This plan creates exactly ONE new file: `.planning/phases/12.7-branding-hefesto/12.7-FOLLOWUP-CHECKLIST.md`. (This is the only NEW file in the entire phase — all other phase changes are in-place token replacements on existing files.) Task 2 modifies no files; it is pure verification.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.7-branding-hefesto/12.7-SPEC.md
@.planning/phases/12.7-branding-hefesto/12.7-CONTEXT.md
@.planning/phases/12.7-branding-hefesto/12.7-01-PLAN.md
@.planning/phases/12.7-branding-hefesto/12.7-02-PLAN.md
@.planning/phases/12.7-branding-hefesto/12.7-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the manual follow-up checklist deliverable</name>
  <files>.planning/phases/12.7-branding-hefesto/12.7-FOLLOWUP-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/12.7-branding-hefesto/12.7-SPEC.md (Out-of-scope section: GitHub renames + push + About are follow-up)
    - .planning/phases/12.7-branding-hefesto/12.7-CONTEXT.md (D-01 OUT/never-enumerate; D-04 coupled folder+.gitignore deferred; D-05 12.7-dir exclusion mechanism deferred)
    - ~/.claude/notes/Nemea-web/2026-06-15_rebrand-nemea-to-hefesto.md (manual steps section: gh repo rename, git remote set-url, About/description) — read if accessible
  </read_first>
  <action>
    Create `12.7-FOLLOWUP-CHECKLIST.md` as a checkbox checklist of the manual/deferred actions the user runs OUTSIDE GSD, just before publishing. Concrete items to include (each as a `- [ ]` line with the exact command/action):
    1. GitHub repo renames: `nemea-front`→`hefesto-front`, `nemea-back`→`hefesto-back`, `Nemea-web`→`Hefesto-web` (via `gh repo rename hefesto-front` or the GitHub UI; GitHub leaves a redirect from the old name).
    2. Post-rename `git remote set-url origin <new-url>` for each repo (note the redirect makes this optional but recommended).
    3. `/push` each repo through the user's normal push flow (do NOT auto-push — outward action requires the user's explicit OK).
    4. Fill each repo's GitHub About/description with what the app is (kills the name ambiguity).
    5. D-04 coupled step (run TOGETHER, at the final pre-publish moment): rename local folders `nemea-front`/`nemea-back` → `hefesto-front`/`hefesto-back` AND update root `.gitignore` entries `/nemea-front/` `/nemea-back/` → `/hefesto-front/` `/hefesto-back/` in the same step. State WHY coupled: renaming a folder without the matching `.gitignore` entry makes the orchestrator start tracking the sub-repo. Note this was deliberately deferred so the active session's paths don't break.
    6. D-05 exclusion mechanism: ensure the `.planning/phases/12.7-branding-hefesto/` directory is excluded from the PUBLIC orchestrator repo (e.g. gitignore-on-publish or remove-before-publish) so the rebrand-rationale artifacts (SPEC/CONTEXT/this checklist/SUMMARYs) stay private. Frame as "decide + apply the exclusion mechanism" — it is a deferred manual concern, not auto-executed here.
    HARD EXCLUSION: do NOT add any item about real-identity / legal-entity / registered-mark sanitization — D-01 keeps that off-plan and un-enumerated. The checklist mentions only the literal `nemea`→`hefesto` follow-ups above.
    Write user-facing copy in Spanish (project convention), with the literal commands in code spans.
  </action>
  <verify>
    <automated>test -f .planning/phases/12.7-branding-hefesto/12.7-FOLLOWUP-CHECKLIST.md && grep -q "git remote set-url" .planning/phases/12.7-branding-hefesto/12.7-FOLLOWUP-CHECKLIST.md && grep -q ".gitignore" .planning/phases/12.7-branding-hefesto/12.7-FOLLOWUP-CHECKLIST.md && grep -qi "12.7" .planning/phases/12.7-branding-hefesto/12.7-FOLLOWUP-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - The file `.planning/phases/12.7-branding-hefesto/12.7-FOLLOWUP-CHECKLIST.md` exists with at least 20 lines.
    - It contains all 6 item groups: (1) GitHub repo renames for all three repos, (2) `git remote set-url`, (3) push via user flow, (4) GitHub About/description, (5) D-04 coupled folder-rename + `.gitignore` step with the coupling rationale, (6) D-05 12.7-dir exclusion mechanism.
    - It contains ZERO references to real-identity / legal-entity / registered-mark sanitization (D-01) — verified by reading the file.
  </acceptance_criteria>
  <done>The follow-up checklist deliverable exists, covers GitHub renames + D-04 + D-05, and contains no D-01 real-identity item.</done>
</task>

<task type="auto">
  <name>Task 2: Final cross-repo + orchestrator verification (acceptance roll-up)</name>
  <files>(no file changes — pure verification of the completed phase)</files>
  <read_first>
    - .planning/phases/12.7-branding-hefesto/12.7-SPEC.md (Acceptance Criteria block)
    - .planning/phases/12.7-branding-hefesto/12.7-03-PLAN.md (the RE-SCOPED req-7 acceptance: swept root docs + codebase/* only, NOT whole .planning)
  </read_first>
  <action>
    Run the full phase acceptance roll-up and record results in the SUMMARY. Execute these checks (each must pass):
    - nemea-back: from nemea-back, `git grep -i nemea` returns empty; `npm test` exits 0; `git diff --stat` shows zero changes under `src/database/migrations/`; docker boots on hefesto_db (confirm via Plan 01 Task 3 outcome / `docker compose ps`).
    - nemea-front: from nemea-front, `git grep -i nemea` returns empty; `npm test` exits 0 (no new failures vs baseline).
    - root: from the orchestrator root, `git grep -n "Nemea" -- CLAUDE.md README.md README.es.md` returns empty.
    - orchestrator .planning (RE-SCOPED per D-02/D-03): `git grep -i nemea -- .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md '.planning/codebase/*.md'` returns empty. Do NOT run the whole-`.planning` grep as a pass/fail gate — `.planning/phases/*` legitimately retains "Nemea" (D-03), and the 12.7 dir is excluded (D-05).
    - Confirm `git status` in the orchestrator shows zero modified files under `.planning/phases/` (other than this phase's own NEW SUMMARY/checklist artifacts).
    If any check fails, report the failing surface and the offending file/line in the SUMMARY (do NOT hand-fix here — surface for gap closure). Note in the SUMMARY that the D-04 folder/.gitignore rename and the GitHub renames remain in the follow-up checklist (intentionally not executed).
  </action>
  <verify>
    <automated>cd nemea-back && git grep -i nemea; b=$?; cd ../nemea-front && git grep -i nemea; f=$?; cd .. && git grep -n "Nemea" -- CLAUDE.md README.md README.es.md; r=$?; git grep -i nemea -- .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md '.planning/codebase/*.md'; p=$?; test $b -eq 1 -a $f -eq 1 -a $r -eq 1 -a $p -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - nemea-back whole-repo `git grep -i nemea` empty; `npm test` exits 0; zero migration diff.
    - nemea-front whole-repo `git grep -i nemea` empty; `npm test` exits 0 (no new failures).
    - root `git grep -n "Nemea" -- CLAUDE.md README.md README.es.md` empty.
    - re-scoped `git grep -i nemea -- .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md '.planning/codebase/*.md'` empty.
    - `git status` shows no modified `.planning/phases/*` files beyond this phase's own new artifacts.
  </acceptance_criteria>
  <done>All in-scope surfaces (both sub-repos + root docs + the D-02 principal planning set) are verified grep-clean, suites green, migrations untouched, and the deferred work is recorded in the checklist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private 12.7 dir → public orchestrator repo | The rebrand-rationale artifacts (SPEC/CONTEXT/checklist/SUMMARYs) must NOT leak into the public repo (D-05). |
| user manual actions → GitHub/public | Outward renames + push happen by the user's hand, never auto-executed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.7-V01 | Information Disclosure | 12.7 phase dir leaking the brand/investor rationale into the public repo | mitigate | The follow-up checklist (Task 1, item 6) records the D-05 exclusion mechanism so the user applies it before publishing. This plan does NOT publish — it only documents the required exclusion. |
| T-12.7-V02 | Repudiation/Tampering | Auto-pushing or auto-renaming GitHub repos without the user's OK | mitigate | Checklist frames push + GitHub renames as user-run (item 3 explicitly says do-NOT-auto-push). No git push / no `gh repo rename` is executed in this plan. |
| T-12.7-SC | Tampering | package installs | n/a | No installs — doc creation + verification greps only. |

No high-severity threats — this plan writes a checklist and runs read-only verification greps/tests. The only sensitive surface (12.7-dir leak) is handled by documenting the D-05 exclusion for the user to apply pre-publish, not by acting outward here.
</threat_model>

<verification>
- nemea-back: `git grep -i nemea` empty; `npm test` 0; zero migration diff; docker on hefesto_db.
- nemea-front: `git grep -i nemea` empty; `npm test` 0.
- root: `git grep -n "Nemea" -- CLAUDE.md README.md README.es.md` empty.
- re-scoped `.planning`: `git grep -i nemea -- .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md '.planning/codebase/*.md'` empty.
- `12.7-FOLLOWUP-CHECKLIST.md` exists with GitHub renames + D-04 + D-05, no D-01 item.
</verification>

<success_criteria>
The follow-up checklist deliverable exists and is complete (GitHub renames + D-04 coupled step + D-05 exclusion, no D-01 item), and the full phase acceptance roll-up is verified green across both sub-repos, root docs, and the re-scoped `.planning` set, with migrations and `.planning/phases/*` provably untouched.
</success_criteria>

<output>
Create `.planning/phases/12.7-branding-hefesto/12.7-04-SUMMARY.md` when done.
</output>

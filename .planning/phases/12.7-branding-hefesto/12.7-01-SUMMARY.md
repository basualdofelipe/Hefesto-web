---
phase: 12.7-branding-hefesto
plan: 01
subsystem: nemea-back
tags: [rebrand, rename, infra, docker, tests]
dependency_graph:
  requires: []
  provides: [hefesto-back-grep-clean, npm-test-green, docker-hefesto]
  affects: [nemea-back]
tech_stack:
  added: []
  patterns: [case-sensitive-token-replace, fixture-only-test-edit]
key_files:
  created: []
  modified:
    - nemea-back/docker-compose.yml
    - nemea-back/.env.example
    - nemea-back/src/app.controller.ts
    - nemea-back/src/common/interceptors/logging.interceptor.ts
    - nemea-back/package.json
    - nemea-back/package-lock.json
    - nemea-back/README.md
    - nemea-back/docs/ARCHITECTURE.md
    - nemea-back/src/app.controller.spec.ts
    - nemea-back/src/auth/auth.controller.spec.ts
    - nemea-back/src/auth/auth.service.spec.ts
    - nemea-back/src/auth/guards/permissions.guard.spec.ts
    - nemea-back/src/users/users.controller.spec.ts
    - nemea-back/src/users/users.service.spec.ts
    - nemea-back/test/app.e2e-spec.ts
    - nemea-back/test/setup-e2e.ts
decisions:
  - "Direct edit of package-lock.json self-name refs (not npm install --package-lock-only) for determinism"
  - "Migration run deferred to Feli: local .env still points nemea_db (gotcha #3, not in scope)"
metrics:
  duration: ~30 minutes
  completed: 2026-06-17
---

# Phase 12.7 Plan 01: Branding Hefesto — nemea-back Sweep Summary

Mechanical case-sensitive rebrand of the entire `nemea-back` sub-repo: 16 files, 3 token rules (nemea→hefesto, Nemea→Hefesto), infra recreation on hefesto_pgdata, all 154 unit tests green.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Infra + non-test code + package identity | f195bb7 | docker-compose.yml, .env.example, app.controller.ts, logging.interceptor.ts, package.json, package-lock.json |
| 2 | Docs + 8 test specs/e2e fixtures | 85ae435 | README.md, docs/ARCHITECTURE.md, 8 spec/e2e files |
| 3 | Docker volume recreation + health verify | (no commit — runtime only) | — |

## Verification Results

**grep-clean:** `git grep -il nemea` → EXIT 1 (no matches). Full repo clean.

**npm ci:** EXIT 0. package.json ↔ package-lock.json consistent after self-name rename (both refs updated atomically).

**npm test:** 154 tests, 15 suites — all PASS.

**Migrations zero-diff:** `git diff development -- src/database/migrations/` → empty. No migration files touched.

**Docker:**
- `docker compose down -v` removed `nemea_postgres` and `nemea_postgres_test` containers.
- `docker compose up -d` created `hefesto_postgres` (healthy on port 5432) and `hefesto_postgres_test` (healthy on port 5433) with volume `nemea-back_hefesto_pgdata`.
- `psql -U hefesto -d hefesto_db` connects successfully (verified via `docker exec`).
- Migration run deferred: local `.env` (gitignored, not in scope) still points to `nemea_db`. Feli must update `.env` to `postgresql://hefesto:hefesto_dev@localhost:5432/hefesto_db` and run `npm run migration:run` to seed `hefesto_db`. This is gotcha #3 from the plan — explicitly out of scope.

**GET / health:** Asserted via green unit test in `app.controller.spec.ts` → `app: 'hefesto-back'`. E2E test assertion also updated to `'hefesto-back'`.

## Deviations from Plan

None — plan executed exactly as written.

The `commit-to-subrepo` SDK returned `no sub_repos configured` (planning.sub_repos in config.json is not read by the SDK at the top-level key). Fell back to direct `git commit` inside `nemea-back` — same outcome, correct branch (`refactor/phase-12.7-branding-hefesto`), same commit format.

## Security Review: permissions.guard.spec.ts

Diff-review confirmed: all 6 changes in `permissions.guard.spec.ts` are email fixture replacements (`user@nemea.com`→`user@hefesto.com`, `admin@nemea.com`→`admin@hefesto.com`, `regular@nemea.com`→`regular@hefesto.com`). No permission flag, guard boolean, or assertion structure was altered. Threat T-12.7-02 mitigated.

## Known Stubs

None. All source changes are string token replacements; no data-wiring gaps introduced.

## Threat Flags

No new security surface introduced. This plan is a string-only rename with no new endpoints, auth paths, or schema changes.

## Self-Check: PASSED

- SUMMARY.md: FOUND at .planning/phases/12.7-branding-hefesto/12.7-01-SUMMARY.md
- Commit f195bb7: FOUND (Task 1 — infra/code/package)
- Commit 85ae435: FOUND (Task 2 — docs/specs)
- grep-clean: confirmed EXIT 1 post-commit
- npm test: 154/154 PASS
- Migrations: zero-diff confirmed

---
phase: 12.7-branding-hefesto
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - nemea-front/package.json
  - nemea-front/package-lock.json
  - nemea-front/README.md
  - nemea-front/docs/ARCHITECTURE.md
  - nemea-front/src/app/(app)/calculadora/__tests__/CalculadoraClient.test.tsx
  - nemea-front/src/app/(app)/escenarios/[id]/__tests__/ScenarioEditorClient.test.tsx
  - nemea-front/src/app/(app)/usuarios/__tests__/DeleteUserAlertDialog.test.tsx
  - nemea-front/src/app/(app)/usuarios/__tests__/EditUserDialog.test.tsx
autonomous: true
requirements: [2, 4, 5, 6]

must_haves:
  truths:
    - "git grep -i nemea returns zero across the entire nemea-front repo"
    - "npm test (unit) passes in nemea-front"
    - "package.json name is hefesto-front and the lockfile self-name matches"
  artifacts:
    - path: "nemea-front/package.json"
      provides: "Package identity hefesto-front"
      contains: "hefesto-front"
    - path: "nemea-front/package-lock.json"
      provides: "Lockfile self-name hefesto-front (2 refs)"
      contains: "hefesto-front"
  key_links:
    - from: "nemea-front/package.json"
      to: "nemea-front/package-lock.json"
      via: "self-name must match in both files"
      pattern: "hefesto-front"
---

<objective>
Sweep the literal `nemea` → `hefesto` (case-sensitive, 3 rules) across the entire `nemea-front` repo: package identity (package.json + package-lock.json self-name), user-facing docs (README + ARCHITECTURE), and 4 test fixtures. Prove the unit suite stays green and the repo is grep-clean.

Purpose: nemea-front is the other clonable portfolio repo that must go public showing zero `nemea`. It has no infra/docker and no non-test source references — the footprint is package identity + docs + 4 test files only — so it is independent of the nemea-back sweep and runs in the same wave.
Output: A grep-clean nemea-front repo with a green unit suite and `hefesto-front` package identity.

Implements SPEC reqs 2 (package identity — front half), 4 (docs user-facing — front half), 5 (test fixtures — front half), 6 (no-collateral guard). Scope follows 12.7-CONTEXT.md (D-04 folder/.gitignore rename DEFERRED — NOT touched here; local folder stays `nemea-front` for this session).
</objective>

<artifacts_this_phase_produces>
This plan creates NO new files. Every change is an in-place case-sensitive token replacement (`nemea→hefesto`, `Nemea→Hefesto`, `NEMEA→HEFESTO`) on the 8 existing nemea-front files listed in `files_modified`. The local folder is NOT renamed (D-04 deferred).
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12.7-branding-hefesto/12.7-SPEC.md
@.planning/phases/12.7-branding-hefesto/12.7-CONTEXT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep nemea-front package identity + docs</name>
  <files>nemea-front/package.json, nemea-front/package-lock.json, nemea-front/README.md, nemea-front/docs/ARCHITECTURE.md</files>
  <read_first>
    - nemea-front/package.json (line 2: `"name": "nemea-front"`)
    - nemea-front/package-lock.json (lines 2 and 8: 2 self-name refs `"name": "nemea-front"`)
    - nemea-front/README.md (brand prose + clone instructions referencing nemea)
    - nemea-front/docs/ARCHITECTURE.md (brand refs)
    - .planning/phases/12.7-branding-hefesto/12.7-SPEC.md (reqs 2, 4)
  </read_first>
  <action>
    Apply the case-sensitive mapping (nemea→hefesto, Nemea→Hefesto, NEMEA→HEFESTO) with concrete targets:
    - package.json: `"name": "nemea-front"` → `"name": "hefesto-front"`.
    - package-lock.json: both self-name refs (lines 2 and 8) `"name": "nemea-front"` → `"name": "hefesto-front"`. Edit these 2 refs directly (per gotcha #4 — deterministic, no network; chosen over `npm install --package-lock-only`). Do NOT touch any dependency package names (none contain nemea).
    - README.md and docs/ARCHITECTURE.md: replace brand refs `Nemea`/`nemea` → `Hefesto`/`hefesto`. Note: the local-folder path token `nemea-front/` in clone/setup instructions follows the GitHub repo name; per D-04 the actual folder rename is deferred — but the DOC text should read the public-repo-consistent name `hefesto-front`. Replace path tokens in docs to `hefesto-front`/`hefesto-back` so a cloner reading the README sees the public name.
    Diff-review: confirm only brand tokens changed and no dependency identifier was touched.
  </action>
  <verify>
    <automated>cd nemea-front && git grep -i nemea -- package.json package-lock.json README.md docs/ARCHITECTURE.md; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `cd nemea-front && git grep -i nemea -- package.json package-lock.json README.md docs/ARCHITECTURE.md` returns empty (exit 1).
    - package.json `name` is `hefesto-front` and both package-lock.json self-name refs are `hefesto-front`.
    - `git diff` hunks show only brand-token replacements; no dependency name altered.
  </acceptance_criteria>
  <done>Package identity and docs are grep-clean of `nemea` with self-name `hefesto-front` consistent across package.json and package-lock.json.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep nemea-front 4 test fixtures + verify suite green</name>
  <files>nemea-front/src/app/(app)/calculadora/__tests__/CalculadoraClient.test.tsx, nemea-front/src/app/(app)/escenarios/[id]/__tests__/ScenarioEditorClient.test.tsx, nemea-front/src/app/(app)/usuarios/__tests__/DeleteUserAlertDialog.test.tsx, nemea-front/src/app/(app)/usuarios/__tests__/EditUserDialog.test.tsx</files>
  <read_first>
    - the 4 test files above (each contains `nemea` only as fixture/string data — emails, names, or labels — not as assertion logic)
    - .planning/phases/12.7-branding-hefesto/12.7-SPEC.md (req 5 acceptance: npm test green + grep-clean)
  </read_first>
  <action>
    Apply the case-sensitive mapping to the 4 test files, replacing fixture strings only: any `nemea`/`Nemea` in email fixtures, mock user names, or label strings → `hefesto`/`Hefesto` (e.g. `*@nemea.com` → `*@hefesto.com`). Do NOT change any expected behavior, query, or assertion structure — fixtures only. The mapping moves the fixtures consistently so the suite stays green.
    Diff-review each file to confirm the changes are string/fixture-only.
  </action>
  <verify>
    <automated>cd nemea-front && git grep -i nemea; test $? -eq 1 && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd nemea-front && git grep -i nemea` returns empty across the WHOLE repo (exit 1).
    - `npm test` exits 0 (unit suite green for the 4 swept test files; note any pre-existing unrelated failures from prior phases must be the same before and after — confirm via git stash round-trip if a failure appears, and do NOT attribute pre-existing NextAuth-ESM heritage failures to this plan).
    - Diff-review confirms test changes are fixture/string replacements only.
  </acceptance_criteria>
  <done>The whole nemea-front repo is grep-clean of `nemea` and the unit suite for the swept files is green (no new failures introduced).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public repo → cloner | A reviewer clones the public front repo; only package identity, docs, and test fixtures change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.7-F01 | Tampering | Test assertions in the 4 swept specs | mitigate | Diff-review in Task 2 confirms only fixture/string data (emails, names, labels) changed — no assertion, query, or expected-behavior logic altered. Suite stays green as the gate. |
| T-12.7-F02 | Information Disclosure | README/ARCHITECTURE docs | accept | Docs contain no secrets; brand-token replacement only. |
| T-12.7-SC | Tampering | npm installs | n/a | No package installs — lockfile self-name edited directly. Zero new dependencies. |

No high-severity threats — mechanical string rename on a frontend repo with no infra and no auth-logic source changes. nemea-front has no `src/*` non-test references to `nemea` (verified inventory: package + docs + 4 test files only).
</threat_model>

<verification>
- `cd nemea-front && git grep -i nemea` → empty (whole repo).
- `npm test` → exits 0 (no new failures vs pre-sweep baseline).
- package.json + package-lock.json self-name = `hefesto-front`.
</verification>

<success_criteria>
nemea-front is grep-clean of `nemea`, package identity is `hefesto-front`, docs read the public repo name, and the unit suite is green with no new failures.
</success_criteria>

<output>
Create `.planning/phases/12.7-branding-hefesto/12.7-02-SUMMARY.md` when done.
</output>

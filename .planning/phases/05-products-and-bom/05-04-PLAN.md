---
phase: 05-products-and-bom
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - nemea-front/src/components/products/BomEditorDialog.tsx
  - nemea-front/src/components/products/BomGroupEditorDialog.tsx
  - nemea-front/src/components/products/AddSellingPriceInline.tsx
  - nemea-front/src/components/products/PriceHistoryDialog.tsx
  - nemea-front/src/components/products/BatchPriceDialog.tsx
  - nemea-front/src/components/products/ProductExpandedRow.tsx
  - nemea-front/src/components/products/ProductTypeGroup.tsx
autonomous: false
requirements: [PROD-05, PROD-06, PROD-07]

must_haves:
  truths:
    - "Admin can edit the BOM of a product via modal with supply Combobox"
    - "Admin can group-edit BOM for all products of a type"
    - "Admin can add a selling price inline"
    - "Admin can view selling price history"
    - "Admin can batch-update selling prices for products of a type"
  artifacts:
    - path: "nemea-front/src/components/products/BomEditorDialog.tsx"
      provides: "BOM editor modal with supply Combobox, quantity input, unit auto-fill"
      contains: "Combobox"
    - path: "nemea-front/src/components/products/BomGroupEditorDialog.tsx"
      provides: "Group BOM editor with product checkboxes and warning indicators"
      contains: "batch-bom"
    - path: "nemea-front/src/components/products/AddSellingPriceInline.tsx"
      provides: "Inline price input with confirm/cancel"
      contains: "price"
    - path: "nemea-front/src/components/products/PriceHistoryDialog.tsx"
      provides: "Price history modal for product selling prices"
      contains: "prices"
  key_links:
    - from: "nemea-front/src/components/products/BomEditorDialog.tsx"
      to: "/api/products/:id/bom"
      via: "apiClientFetch PUT"
      pattern: "apiClientFetch.*bom"
    - from: "nemea-front/src/components/products/BomGroupEditorDialog.tsx"
      to: "/api/products/batch-bom"
      via: "apiClientFetch PUT"
      pattern: "batch-bom"
---

<objective>
Build the BOM editor modal, group BOM editor, selling price inline addition, price history dialog, and batch price update. Wire all action buttons in ProductExpandedRow and ProductTypeGroup headers.

Purpose: Completes the product management UI with material composition editing and selling price tracking.
Output: Fully functional BOM and price management UX with group operations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-products-and-bom/05-CONTEXT.md
@.planning/phases/05-products-and-bom/05-RESEARCH.md
@.planning/phases/05-products-and-bom/05-03-SUMMARY.md

<interfaces>
<!-- From Plan 05-03 (must exist before this plan runs) -->

From nemea-front/src/components/products/types.ts:
```typescript
export interface Product { id, skuCode, type, name, finish, color, size, isActive, currentPrice, lastPriceUpdate, createdAt }
export interface BomItem { id, supply: { id, name, unitType, isActive, type: { name } }, quantity: string, isActive }
export interface PriceRecord { id, price: string, createdAt: string }
export function getProductDisplayName(product): string
export function formatSellingPrice(price): string
```

From nemea-front/src/components/supplies/types.ts:
```typescript
export interface Supply { id, name, type, supplier, unitType, notes, isActive, currentPrice, lastPriceUpdate, createdAt }
export const UNIT_LABELS: Record<Supply['unitType'], string>
```

From nemea-front/src/components/supplies/AddPriceInline.tsx:
```typescript
// Pattern: inline input with confirm/cancel, POST to prices endpoint
```

From nemea-front/src/components/supplies/PriceHistoryDialog.tsx:
```typescript
// Pattern: fetch on open, display date+amount list newest first
```

API endpoints:
- GET /api/products/:id/bom -> { data: BomItem[] }
- PUT /api/products/:id/bom -> { data: BomItem[] } (body: { items: [{supplyId, quantity}] })
- PUT /api/products/batch-bom -> (body: { productIds[], items[] })
- GET /api/products/:id/prices -> { data: PriceRecord[] }
- POST /api/products/:id/prices -> { data: PriceRecord } (body: { price })
- POST /api/products/batch-prices -> { data: PriceRecord[] } (body: { productIds[], price })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BomEditorDialog + BomGroupEditorDialog + wire group header buttons</name>
  <files>
    nemea-front/src/components/products/BomEditorDialog.tsx,
    nemea-front/src/components/products/BomGroupEditorDialog.tsx,
    nemea-front/src/components/products/ProductExpandedRow.tsx,
    nemea-front/src/components/products/ProductTypeGroup.tsx
  </files>
  <action>
  **BomEditorDialog.tsx** -- client component:
  - Props: productId, productName (display), supplies[] (all active supplies), currentBom: BomItem[], open, onOpenChange, onSuccess
  - Modal with editable table of BOM rows
  - Each row: supply Combobox (search + grouped by supply type) | quantity input (number) | unit label (read-only, auto-filled from selected supply's unitType via UNIT_LABELS) | delete row button
  - Combobox: use Shadcn Command component (Popover + CommandInput + CommandList + CommandGroup per supply type + CommandItem). Filter supplies by search text. Show supply name + supplier name in each item.
  - "+ Agregar material" button adds empty row
  - Same supply CAN appear multiple times (no uniqueness enforcement in UI)
  - Pre-populate rows with currentBom data on open
  - On save: PUT /api/products/:id/bom with { items: [{supplyId, quantity}] }
  - Empty items array is valid (clears BOM)
  - Show toast on success, call onSuccess to refresh

  **BomGroupEditorDialog.tsx** -- client component:
  - Props: products[] (all products in the type group), supplies[], open, onOpenChange, onSuccess
  - Top section: checkboxes with all products in the group, all checked by default. Show product name via getProductDisplayName.
  - Majority BOM detection: fetch BOM for each product on dialog open (parallelize). Serialize each BOM as sorted JSON of [{supplyId, quantity}]. Find the most common BOM. Products with different BOM get a warning indicator (yellow triangle icon) next to checkbox.
  - Bottom section: standard BOM editor table (same pattern as BomEditorDialog), pre-populated with majority BOM
  - On save: PUT /api/products/batch-bom with { productIds: [checked product ids], items: [{supplyId, quantity}] }
  - Show toast with count of updated products. Call onSuccess.

  **Wire ProductExpandedRow.tsx**: Connect the [Editar BOM] button to open BomEditorDialog. Pass supplies, fetch current BOM for the dialog.

  **Wire ProductTypeGroup.tsx**: Connect the [Editar BOM grupal] button in the group header to open BomGroupEditorDialog. Pass all products in the group + supplies.
  </action>
  <verify>
    <automated>cd nemea-front && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>BOM editor modal opens from expanded row, shows supply Combobox grouped by type, saves BOM via transaction. Group BOM editor opens from header, shows product checkboxes with majority warning, applies BOM to selected products. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: AddSellingPriceInline + PriceHistoryDialog + BatchPriceDialog + wire remaining buttons</name>
  <files>
    nemea-front/src/components/products/AddSellingPriceInline.tsx,
    nemea-front/src/components/products/PriceHistoryDialog.tsx,
    nemea-front/src/components/products/BatchPriceDialog.tsx,
    nemea-front/src/components/products/ProductExpandedRow.tsx,
    nemea-front/src/components/products/ProductTypeGroup.tsx
  </files>
  <action>
  **AddSellingPriceInline.tsx** -- client component (mirror supplies AddPriceInline pattern):
  - Props: productId, onSuccess
  - Inline input that appears below the action buttons when [+ Precio venta] is clicked
  - Input for price amount + [Confirmar] + [Cancelar] buttons
  - On confirm: POST /api/products/:id/prices with { price }
  - Show toast "Precio de venta actualizado", call onSuccess

  **PriceHistoryDialog.tsx** -- client component (mirror supplies PriceHistoryDialog pattern):
  - Props: productId, productName (display), open, onOpenChange
  - Fetch price history on open via GET /api/products/:id/prices
  - Display list of prices: date (formatted) + amount (formatted), newest first
  - Use useCallback+useRef fetch pattern (same as supplies to satisfy lint rule)
  - Empty state: "Sin historial de precios"

  **BatchPriceDialog.tsx** -- client component:
  - Props: products[] (all products in the type group), open, onOpenChange, onSuccess
  - Checkboxes with all products, all checked by default. Show product name + current price.
  - Single price input field
  - On save: POST /api/products/batch-prices with { productIds: [checked ids], price }
  - Show toast with count. Call onSuccess.

  **Wire ProductExpandedRow.tsx**: Connect:
  - [+ Precio venta] toggles AddSellingPriceInline visibility
  - [Historial precios] opens PriceHistoryDialog

  **Wire ProductTypeGroup.tsx**: Connect:
  - [+ Precio grupal] button in header opens BatchPriceDialog
  </action>
  <verify>
    <automated>cd nemea-front && npx tsc --noEmit 2>&1 | head -20 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>Inline price addition works. Price history modal shows records newest first. Batch price dialog updates multiple products. All buttons in expanded row and group header are wired. TypeScript and ESLint pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete Products and BOM UI</name>
  <action>Human verifies the complete Products and BOM UI built across plans 05-03 and 05-04. What was built: product list grouped by type, batch creation (4-step modal), single edit, BOM editor with supply Combobox, group BOM editor with majority detection, selling price (inline add + history + batch), deactivation toggle, supply deactivation guard.</action>
  <verify>
    1. Navigate to /productos -- products listed grouped by type with collapsible sections
    2. Click "+ Crear productos" -- complete 4-step flow (type+name+finish -> colors -> sizes -> preview -> create)
    3. Click a product row to expand -- verify BOM table, price, and action buttons show
    4. Click "Editar BOM" -- verify supply Combobox works (search, grouped by type), add rows, save
    5. Click "Editar BOM grupal" in type header -- verify product checkboxes, majority BOM detection, save to multiple
    6. Click "+ Precio venta" -- verify inline input appears, save price
    7. Click "Historial precios" -- verify price history modal with correct records
    8. Click "+ Precio grupal" in header -- verify batch price dialog
    9. Click "Editar" on a product -- change a dimension, verify SKU regenerates
    10. Click "Desactivar" -- verify product disappears from active list, reappears with "Mostrar inactivos"
    11. Try to deactivate a supply that is in an active BOM -- should get error toast
  </verify>
  <done>All 11 verification steps pass. User types "approved" to continue.</done>
</task>

</tasks>

<verification>
1. `cd nemea-front && npx tsc --noEmit` -- zero errors
2. `cd nemea-front && npm run lint` -- zero errors
3. Full product lifecycle: create batch -> expand -> add BOM -> add price -> edit -> deactivate
4. BOM group edit with majority detection and warning indicators
5. Batch price update from group header
6. Supply deactivation guard returns error when supply is in active BOM
</verification>

<success_criteria>
- BOM editor saves atomically (old entries preserved, new entries active)
- Group BOM editor correctly detects majority BOM and shows warnings
- Selling price inline addition creates new price record
- Price history shows all records newest first
- All action buttons in expanded row and group header are functional
- Visual checkpoint passes human verification
</success_criteria>

<output>
After completion, create `.planning/phases/05-products-and-bom/05-04-SUMMARY.md`
</output>

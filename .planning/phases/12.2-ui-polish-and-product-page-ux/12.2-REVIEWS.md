---
phase: 12.2
reviewers: [gemini]
reviewed_at: "2026-04-10T16:30:00.000Z"
plans_reviewed: [12.2-01-PLAN.md, 12.2-02-PLAN.md, 12.2-03-PLAN.md]
---

# Cross-AI Plan Review — Phase 12.2

## Gemini Review

### Summary
The implementation plans for Phase 12.2 are exceptionally thorough and technically sound, addressing the 18/24 UI audit findings with surgical precision. The strategy effectively balances backend data integrity (size ordering) with frontend performance (RSC home page) and visual consistency (typography and branding). The transition to a two-weight typography system and the permission-aware RSC home page are particularly well-architected.

### Strengths
- **Architecture Integrity:** The decision to use `auth()` server-side for the home page (Plan 02) correctly satisfies the "permission-aware" requirement while adhering to the "RSC component" constraint, avoiding unnecessary client-side hydration for static navigation.
- **Data-Driven Ordering:** Implementing `sort_order` at the database level (Plan 01) rather than hardcoding frontend sorts ensures consistency across all catalog consumers and future-proofs the system for new size types.
- **Context-Aware Typography:** The typography audit (Plan 03) doesn't just do a "find and replace"; it applies specific rules based on the element's function (labels vs. data), maintaining visual hierarchy while simplifying the CSS.
- **Resilient Migrations:** The use of regex (`^[0-9]+$`) in the migration to handle numeric belt sizes is a robust way to sort varying size formats without manual entry.
- **Design System Fidelity:** Using the `group-data-[collapsible=icon]` pattern for sidebar logos maintains the zero-JS approach for sidebar state transitions.

### Concerns
- **Logo Aspect Ratios (MEDIUM):** `logo.png` is noted as a 1080x1080 square but described as "NEMEA text." If the text is centered in a large square, rendering it at `height={32}` in the sidebar might make the text illegible without cropping. Mitigation: The plan includes `object-contain`, but the executor should be prepared to adjust `width/height` or suggest a cropped asset if the white space is excessive.
- **Catalog CRUD for Sort Order (LOW):** As noted in the research, there is currently no UI to edit `sort_order` for new sizes. Mitigation: Acceptable for this phase as it defaults to 0, but it should be added to the `.planning/todos/pending/` list for a future catalog enhancement.
- **Mobile Table Alignment (LOW):** While `text-right` fixes the alignment on desktop, very narrow mobile screens might cause numeric headers to wrap awkwardly if padding isn't adjusted.

### Suggestions
- **Asset Verification:** Before applying Plan 02, verify if `leon+color` (lion with burgundy background) might actually look better on the dark-themed login page than the transparent `Isotipo.png`.
- **Tabular Nums:** Ensure that when replacing `font-medium` with `font-normal` in numeric cells, the `tabular-nums` class is consistently applied to prevent "jumping" text in dynamic tables.
- **Breadcrumb Consistency:** Since the home page is being simplified to cards, consider if the Breadcrumbs (if any) need a "Home" icon instead of text for a more polished look.

### Risk Assessment: LOW
The risk is low because: isolated CSS/state changes, zero new dependencies, proven patterns, no high-stakes logic changes.

**Gemini Verdict: READY FOR EXECUTION**

---

## Claude Review

Skipped — running inside Claude Code CLI (self-review provides no independence).

---

## Senior Pre-mortem

**Reviewer:** Senior (internal)
**Date:** 2026-04-10

### Will break

Nothing. All 3 plans verified against actual codebase — assumptions are correct.

### Might break

1. **Logo dimensions at sidebar scale.** `logo.png` is 1080x1080 square. Plan 02 renders it at `h-8 w-auto` (32px height). If the NEMEA text is centered in the square with padding, it'll be tiny. The `object-contain` helps but the executor may need to adjust dimensions after visual check. Not a code break — a visual tuning issue.

2. **`as any` on CatalogsService findAll.** Plan 01 Task 2 uses `as any` on the TypeORM order clause because `CatalogEntity` type doesn't include `sortOrder`. This works but violates CLAUDE.md's "no any" rule. The executor should either accept it (pragmatic) or create a type-safe alternative. Low risk — it's a read-only query.

3. **Plan 03's 23-file task.** The typography audit is 1 task touching 23 files with 48 specific replacements. If the executor loses context mid-task, partial replacements could leave inconsistent weights. Mitigation: the grep verification (`font-medium` count = 0) catches incomplete work. The executor would need to re-run the task if interrupted.

### Missing from the plans

1. **No `sort_order` UI for admin.** Plan 01 adds the column and seeds values, but there's no CRUD for it. New sizes default to `sort_order = 0` (appear first). An admin creating "Extra Grande" has no way to set its sort order. Acceptable for this phase (D-09 only asks for the column), but needs a todo.

2. **No `next build` verification step.** Plans verify with grep but don't run `next build` to confirm the home page RSC compiles. The executor's tsc check would catch type errors, but RSC-specific issues (like accidentally importing a client hook) might only surface at build time.

### Verdict

**SAFE TO EXECUTE**

The plans are solid. File paths verified, line numbers confirmed, assumptions match reality. The "might break" items are all low-risk with built-in mitigations (visual checkpoint, grep verification). The 23-file typography task is aggressive but the mechanical nature and exhaustive per-line guide make it manageable.

---

## Consensus Summary

### Agreed Strengths
- Plans are well-scoped with no new dependencies
- RSC home page with `auth()` is architecturally correct
- Database-level sort_order is future-proof
- Typography audit is systematic, not ad-hoc

### Agreed Concerns

| # | Issue | Severity | Source | Fix |
|---|-------|----------|--------|-----|
| 1 | Logo 1080x1080 square may render text illegibly at sidebar scale | MEDIUM | Gemini | Visual checkpoint — executor adjusts dimensions post-render |
| 2 | No UI to edit sort_order for new sizes | LOW | Gemini | Add to todos for future catalog enhancement |
| 3 | Mobile table alignment may wrap on narrow screens | LOW | Gemini | Check during visual verification |

### Divergent Views
N/A — single reviewer (Claude skipped for independence).

### Overall Assessment
**READY FOR EXECUTION** — Low risk, well-planned phase. The MEDIUM concern (logo aspect ratio) is already addressed by the visual checkpoint in Plan 02.

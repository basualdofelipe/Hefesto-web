---
phase: 12.2-ui-polish-and-product-page-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nemea-back/src/database/migrations/1773100000000-AddSortOrderToProductSizes.ts
  - nemea-back/src/catalogs/entities/product-size.entity.ts
  - nemea-back/src/catalogs/catalogs.service.ts
autonomous: true
requirements:
  - D-09

must_haves:
  truths:
    - "Sizes returned from GET /api/catalogs/product-sizes are ordered by sort_order ascending, not alphabetically"
    - "Existing seeded sizes (Unico/Chico/Mediano/Grande) have logical display order"
    - "Numeric belt sizes (if any exist) are ordered by their numeric value"
    - "New sizes created via the catalog page default to sort_order 0"
  artifacts:
    - path: "nemea-back/src/database/migrations/1773100000000-AddSortOrderToProductSizes.ts"
      provides: "sort_order column on product_sizes table with seeded values"
      contains: "sort_order"
    - path: "nemea-back/src/catalogs/entities/product-size.entity.ts"
      provides: "sortOrder column mapping on ProductSize entity"
      contains: "sortOrder"
    - path: "nemea-back/src/catalogs/catalogs.service.ts"
      provides: "product-sizes dimension returns sorted by sortOrder instead of name"
      contains: "sortOrder"
  key_links:
    - from: "catalogs.service.ts findAll"
      to: "product_sizes.sort_order"
      via: "TypeORM order clause"
      pattern: "sortOrder.*ASC"
---

<objective>
Add sort_order column to product_sizes table so sizes display in logical order (Unico, Chico, Mediano, Grande) instead of alphabetical order (Chico, Grande, Mediano, Unico).

Purpose: Per D-09, size ordering should be database-driven via a sort_order column, not frontend sorting. This is future-proof for non-numeric sizes and belt sizes (75, 80, 85...).
Output: Migration file, updated entity, updated service query.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.2-ui-polish-and-product-page-ux/12.2-CONTEXT.md
@.planning/phases/12.2-ui-polish-and-product-page-ux/12.2-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From nemea-back/src/catalogs/entities/product-size.entity.ts:
```typescript
import { Column, Entity } from 'typeorm';
import { BaseEntity } from '../../common/entities/base.entity';

@Entity('product_sizes')
export class ProductSize extends BaseEntity {
  @Column({ type: 'varchar', length: 100, unique: true })
  name!: string;

  @Column({ name: 'sku_code', type: 'smallint', unique: true })
  skuCode!: number;
}
```

From nemea-back/src/catalogs/catalogs.service.ts (findAll method, line 96-98):
```typescript
async findAll(dimension: string): Promise<CatalogEntity[]> {
  const repo = this.getRepository(dimension);
  return repo.find({ order: { name: 'ASC' } });
}
```

CatalogEntity type (line 19):
```typescript
type CatalogEntity = BaseEntity & { name: string };
```

Migration timestamp pattern: last migration is `1773000000000-CreateRolesAndMigrateUsers.ts`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sort_order migration for product_sizes</name>
  <files>nemea-back/src/database/migrations/1773100000000-AddSortOrderToProductSizes.ts</files>
  <read_first>
    - nemea-back/src/database/migrations/1773000000000-CreateRolesAndMigrateUsers.ts (migration pattern reference)
    - nemea-back/src/catalogs/entities/product-size.entity.ts (current entity)
  </read_first>
  <action>
Create migration file `nemea-back/src/database/migrations/1773100000000-AddSortOrderToProductSizes.ts` following the established migration pattern.

**IMPORTANT:** Before writing the UPDATE statements, run `SELECT name FROM product_sizes ORDER BY name;` against the dev database to confirm exact current size names. Use the actual names in the CASE statement.

The `up` method must:
1. `ALTER TABLE "product_sizes" ADD COLUMN "sort_order" SMALLINT NOT NULL DEFAULT 0`
2. UPDATE known sizes with logical display order via CASE statement:
   - 'Unico' (or actual DB name if different) → sort_order 0
   - 'Chico' → sort_order 10
   - 'Mediano' → sort_order 20
   - 'Grande' → sort_order 30
   - Any other non-numeric sizes → sort_order 50 (via ELSE clause)
3. For numeric belt sizes (names matching `^[0-9]+$` regex), set sort_order to their numeric value (e.g., '75' gets sort_order 75, '80' gets 80). Use: `UPDATE "product_sizes" SET "sort_order" = CAST("name" AS INTEGER) WHERE "name" ~ '^[0-9]+$'`

The `down` method must: `ALTER TABLE "product_sizes" DROP COLUMN "sort_order"`

Use spacing of 10 between named sizes so future insertions fit between (e.g., adding 'Extra Grande' at sort_order 25 between Mediano and Grande).

Per D-09: this is a SMALLINT column. Default 0 means newly created sizes appear first until admin reorders them.
  </action>
  <verify>
    <automated>cd nemea-back && npx ts-node -e "import('./src/database/migrations/1773100000000-AddSortOrderToProductSizes').then(m => { const inst = new (Object.values(m)[0] as any)(); console.log('name:', inst.name); console.log('has up:', typeof inst.up === 'function'); console.log('has down:', typeof inst.down === 'function'); })"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at nemea-back/src/database/migrations/1773100000000-AddSortOrderToProductSizes.ts
    - Migration class has `up` and `down` methods
    - `up` adds sort_order SMALLINT NOT NULL DEFAULT 0 column
    - `up` updates known sizes (Unico/Chico/Mediano/Grande) with sequential sort_order values
    - `up` handles numeric size names via regex pattern
    - `down` drops the sort_order column
  </acceptance_criteria>
  <done>Migration file exists with correct up/down methods. sort_order column will be added with sensible default values for all existing sizes.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Update ProductSize entity and CatalogsService, run migration</name>
  <files>nemea-back/src/catalogs/entities/product-size.entity.ts, nemea-back/src/catalogs/catalogs.service.ts</files>
  <read_first>
    - nemea-back/src/catalogs/entities/product-size.entity.ts (current entity)
    - nemea-back/src/catalogs/catalogs.service.ts (current findAll method)
  </read_first>
  <action>
**Part 1 — Entity update:**
Add `sortOrder` column to `ProductSize` entity:
```typescript
@Column({ name: 'sort_order', type: 'smallint', default: 0 })
sortOrder!: number;
```
Place it after the `skuCode` column.

**Part 2 — Service update:**
In `catalogs.service.ts`, modify the `findAll` method to use `sortOrder` ordering for `product-sizes` dimension. Since the `CatalogEntity` type is `BaseEntity & { name: string }` and does NOT include `sortOrder`, use a targeted approach:

```typescript
async findAll(dimension: string): Promise<CatalogEntity[]> {
  const repo = this.getRepository(dimension);
  if (dimension === 'product-sizes') {
    return repo.find({ order: { sortOrder: 'ASC', name: 'ASC' } as any });
  }
  return repo.find({ order: { name: 'ASC' } });
}
```

The `as any` cast is needed because `CatalogEntity` doesn't declare `sortOrder`. The secondary sort by `name` handles ties (multiple sizes with sort_order = 0). This is the same pattern used elsewhere in the codebase for type-narrowing with TypeORM.

**Part 3 — Run migration:**
Run `npm run migration:run` in the nemea-back directory to apply the migration against the dev database. Verify the migration ran successfully with no errors.
  </action>
  <verify>
    <automated>cd nemea-back && grep -n "sortOrder" src/catalogs/entities/product-size.entity.ts && grep -n "product-sizes" src/catalogs/catalogs.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - product-size.entity.ts contains `sortOrder` column with `name: 'sort_order'` and `type: 'smallint'` and `default: 0`
    - catalogs.service.ts findAll method has special case for 'product-sizes' using sortOrder ordering
    - Migration ran successfully (no error output from npm run migration:run)
    - `SELECT name, sort_order FROM product_sizes ORDER BY sort_order ASC;` returns sizes in logical order
  </acceptance_criteria>
  <done>ProductSize entity has sortOrder column. CatalogsService returns sizes ordered by sortOrder. Migration applied to dev database successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → DB | sort_order values set via migration, no user input validation needed for this column |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.2-01 | Tampering | sort_order column | accept | Column is not exposed via any user-facing input. Only set by migration and defaults. No API endpoint modifies sort_order in this phase. |
</threat_model>

<verification>
- `npm run migration:run` in nemea-back completes without errors
- `SELECT name, sort_order FROM product_sizes ORDER BY sort_order ASC, name ASC;` shows logical ordering
- grep confirms `sortOrder` in entity file and `product-sizes` special case in service
</verification>

<success_criteria>
- sort_order column exists on product_sizes table with correct values for all existing sizes
- GET /api/catalogs/product-sizes returns sizes in logical display order
- New sizes created via catalog CRUD default to sort_order = 0
</success_criteria>

<output>
After completion, create `.planning/phases/12.2-ui-polish-and-product-page-ux/12.2-01-SUMMARY.md`
</output>

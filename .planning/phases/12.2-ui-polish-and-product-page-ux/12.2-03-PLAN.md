---
phase: 12.2-ui-polish-and-product-page-ux
plan: 03
type: execute
wave: 2
depends_on:
  - "12.2-02"
files_modified:
  - nemea-front/src/components/calculadora/DesglosePanel.tsx
  - nemea-front/src/components/layout/Header.tsx
  - nemea-front/src/components/expenses/ExpenseMonthGroup.tsx
  - nemea-front/src/components/expenses/ExpenseTable.tsx
  - nemea-front/src/components/products/BatchPriceDialog.tsx
  - nemea-front/src/components/products/BomEditorDialog.tsx
  - nemea-front/src/components/products/BomGroupEditorDialog.tsx
  - nemea-front/src/components/products/PriceHistoryDialog.tsx
  - nemea-front/src/components/products/ProductExpandedRow.tsx
  - nemea-front/src/components/products/ProductNameGroup.tsx
  - nemea-front/src/components/products/ProductEditDialog.tsx
  - nemea-front/src/components/products/ProductDetailClient.tsx
  - nemea-front/src/components/products/ProductTypeGroup.tsx
  - nemea-front/src/components/tiendanube-config/GatewaySection.tsx
  - nemea-front/src/components/tiendanube-config/InstallmentsSection.tsx
  - nemea-front/src/components/tiendanube-config/PlansSection.tsx
  - nemea-front/src/components/suppliers/SupplierTable.tsx
  - nemea-front/src/components/supplies/SupplyExpandedRow.tsx
  - nemea-front/src/components/supplies/SupplyTypeGroup.tsx
  - nemea-front/src/components/supplies/PriceHistoryDialog.tsx
  - nemea-front/src/app/(app)/usuarios/UsersClient.tsx
  - nemea-front/src/app/(app)/roles/RolesClient.tsx
  - nemea-front/src/app/(app)/configuracion/tiendanube/TiendanubeConfigClient.tsx
autonomous: true
requirements:
  - D-08
  - D-10

must_haves:
  truths:
    - "Zero instances of font-medium in application code (components/ and app/ directories, excluding ui/ library files)"
    - "Zero instances of font-bold in application code (excluding ui/ library files)"
    - "All former font-medium on headers/labels/badges/table-headers are now font-semibold"
    - "All former font-medium on data cells/descriptions are now font-normal"
    - "font-bold instances (DesglosePanel, ExpenseTable) are now font-semibold"
    - "Shadcn ui/ component files are NOT modified (badge.tsx, button.tsx, etc. keep their original weights)"
  artifacts:
    - path: "nemea-front/src/components/products/ProductExpandedRow.tsx"
      provides: "Heaviest file (11 replacements) fully migrated"
      contains: "font-semibold"
    - path: "nemea-front/src/components/products/ProductDetailClient.tsx"
      provides: "5 replacements fully migrated"
      contains: "font-semibold"
  key_links:
    - from: "D-08 rule"
      to: "All 23 application files with font-medium/font-bold"
      via: "Mechanical replacement"
      pattern: "font-semibold|font-normal"
---

<objective>
Execute the typography weight audit per D-08: eliminate font-medium (500) and font-bold (700) from all application code, replacing with the 2-weight system: font-semibold (600) for emphasis and font-normal (400) for body text.

Purpose: The UI spec declares only 2 weights (400 regular, 600 semibold). In practice, font-medium (500) appears 63 times across 34 files. This creates a blurred 4-weight hierarchy. Consolidating to 2 weights produces clearer visual contrast.
Output: All application files use only font-semibold or font-normal. Shadcn ui/ component library files are untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.2-ui-polish-and-product-page-ux/12.2-CONTEXT.md
@.planning/phases/12.2-ui-polish-and-product-page-ux/12.2-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Typography audit — replace font-medium and font-bold across all application files</name>
  <files>nemea-front/src/components/calculadora/DesglosePanel.tsx, nemea-front/src/components/layout/Header.tsx, nemea-front/src/components/expenses/ExpenseMonthGroup.tsx, nemea-front/src/components/expenses/ExpenseTable.tsx, nemea-front/src/components/products/BatchPriceDialog.tsx, nemea-front/src/components/products/BomEditorDialog.tsx, nemea-front/src/components/products/BomGroupEditorDialog.tsx, nemea-front/src/components/products/PriceHistoryDialog.tsx, nemea-front/src/components/products/ProductExpandedRow.tsx, nemea-front/src/components/products/ProductNameGroup.tsx, nemea-front/src/components/products/ProductEditDialog.tsx, nemea-front/src/components/products/ProductDetailClient.tsx, nemea-front/src/components/products/ProductTypeGroup.tsx, nemea-front/src/components/tiendanube-config/GatewaySection.tsx, nemea-front/src/components/tiendanube-config/InstallmentsSection.tsx, nemea-front/src/components/tiendanube-config/PlansSection.tsx, nemea-front/src/components/suppliers/SupplierTable.tsx, nemea-front/src/components/supplies/SupplyExpandedRow.tsx, nemea-front/src/components/supplies/SupplyTypeGroup.tsx, nemea-front/src/components/supplies/PriceHistoryDialog.tsx, nemea-front/src/app/(app)/usuarios/UsersClient.tsx, nemea-front/src/app/(app)/roles/RolesClient.tsx, nemea-front/src/app/(app)/configuracion/tiendanube/TiendanubeConfigClient.tsx</files>
  <read_first>
    - Each file listed above MUST be read before modifying it
  </read_first>
  <action>
Execute a mechanical audit of every `font-medium` and `font-bold` usage in application code. Apply D-08 replacement rule:

**Rule:** headers / labels / badges / table-headers / group names / field captions / sidebar labels → `font-semibold`. Body text / descriptions / placeholders / regular table data cells → `font-normal`.

**CRITICAL:** Do NOT modify files in `nemea-front/src/components/ui/` — those are Shadcn library components that define their own weight conventions. Only modify application-level files.

**File-by-file replacement guide** (read each file first to confirm line numbers, then apply):

### font-medium → font-semibold (these are all headers/labels/badges/emphasis):

1. **Header.tsx:69** — `font-medium` on user name display → `font-semibold` (label)
2. **ExpenseMonthGroup.tsx:60** — `font-medium` on month label → `font-semibold` (group header)
3. **ExpenseMonthGroup.tsx:102** — `font-medium` on category badge → `font-semibold` (badge)
4. **ExpenseMonthGroup.tsx:107** — `font-medium` on amount table cell → `font-semibold` (emphasised numeric)
5. **BatchPriceDialog.tsx:104** — `font-medium` on "Productos" label → `font-semibold` (section label)
6. **BatchPriceDialog.tsx:127** — `font-medium` on "Nuevo precio de venta" label → `font-semibold` (field label)
7. **BomEditorDialog.tsx:137** — `font-medium` on grid column headers → `font-semibold` (table header)
8. **BomGroupEditorDialog.tsx:232** — `font-medium` on "Productos" label → `font-semibold` (section label)
9. **BomGroupEditorDialog.tsx:261** — `font-medium` on "Materiales" label → `font-semibold` (section label)
10. **BomGroupEditorDialog.tsx:269** — `font-medium` on grid column headers → `font-semibold` (table header)
11. **PriceHistoryDialog.tsx:94** (products) — `font-medium` on current price label → `font-semibold` (label)
12. **PriceHistoryDialog.tsx:93** (supplies) — `font-medium` on current price label → `font-semibold` (label)
13. **ProductExpandedRow.tsx:135** — `font-medium uppercase` field caption → `font-semibold` (label)
14. **ProductExpandedRow.tsx:138** — `font-medium` on product value → `font-semibold` (emphasis data)
15. **ProductExpandedRow.tsx:143** — `font-medium uppercase` field caption → `font-semibold` (label)
16. **ProductExpandedRow.tsx:152** — `font-medium uppercase` field caption → `font-semibold` (label)
17. **ProductExpandedRow.tsx:155** — `font-medium` on status value → `font-semibold` (emphasis data)
18. **ProductExpandedRow.tsx:161** — `font-medium uppercase` field caption → `font-semibold` (label)
19. **ProductExpandedRow.tsx:172** — `font-medium uppercase` section label → `font-semibold` (label)
20. **ProductExpandedRow.tsx:261** — `font-medium` on BOM total row → `font-semibold` (summary row)
21. **ProductExpandedRow.tsx:279** — `font-medium` on cost total → `font-semibold` (emphasis total)
22. **ProductExpandedRow.tsx:285** — `font-medium` on selling price total → `font-semibold` (emphasis total)
23. **ProductExpandedRow.tsx:291** — `font-medium` on margin total → `font-semibold` (emphasis total)
24. **ProductNameGroup.tsx:147** — `font-medium` on product name in group header → `font-semibold` (group header)
25. **ProductEditDialog.tsx:149** — `font-medium uppercase` SKU caption → `font-semibold` (label)
26. **ProductDetailClient.tsx:215** — `font-medium` on BOM total row → `font-semibold` (summary row)
27. **ProductDetailClient.tsx:240** — `font-medium uppercase` field caption → `font-semibold` (label)
28. **ProductDetailClient.tsx:250** — `font-medium uppercase` field caption → `font-semibold` (label)
29. **ProductDetailClient.tsx:260** — `font-medium uppercase` field caption → `font-semibold` (label)
30. **ProductDetailClient.tsx:276** — `font-medium` on warning text → `font-semibold` (alert emphasis)
31. **ProductTypeGroup.tsx:95** — `font-medium` on type name → `font-semibold` (group header)
32. **SupplyTypeGroup.tsx:52** — `font-medium` on type name → `font-semibold` (group header)
33. **SupplyTypeGroup.tsx:92** — `font-medium` on supply name cell → `font-semibold` (emphasis name)
34. **SupplyExpandedRow.tsx:69** — `font-medium uppercase` field caption → `font-semibold` (label)
35. **SupplyExpandedRow.tsx:75** — `font-medium uppercase` field caption → `font-semibold` (label)
36. **SupplyExpandedRow.tsx:90** — `font-medium uppercase` field caption → `font-semibold` (label)
37. **SupplyExpandedRow.tsx:93** — `font-medium` on supplier name → `font-semibold` (emphasis data)
38. **SupplyExpandedRow.tsx:99** — `font-medium uppercase` field caption → `font-semibold` (label)
39. **GatewaySection.tsx:131** — `font-medium` on payment method cell → `font-semibold` (row label)
40. **InstallmentsSection.tsx:96** — `font-medium` on installment label cell → `font-semibold` (row label)
41. **PlansSection.tsx:121** — `font-medium` on plan label cell → `font-semibold` (row label)
42. **SupplierTable.tsx:129** — `font-medium` on supplier name cell → `font-semibold` (emphasis name)
43. **UsersClient.tsx:164** — `font-medium` on user email cell → `font-semibold` (emphasis identifier)
44. **RolesClient.tsx:346** — `font-medium` on role name cell → `font-semibold` (emphasis identifier)
45. **TiendanubeConfigClient.tsx:82** — `font-medium` on "Plan Tiendanube:" label → `font-semibold` (label)

### font-bold → font-semibold:

46. **DesglosePanel.tsx:71** — `font-bold` in `${large ? 'text-lg font-bold' : ''}` → change to `${large ? 'text-lg font-semibold' : ''}` (emphasis in calculadora results, text-lg already provides size hierarchy)
47. **ExpenseTable.tsx:130** — `text-2xl font-bold` → `text-2xl font-semibold` (empty state stat number)
48. **ExpenseTable.tsx:136** — `text-2xl font-bold` → `text-2xl font-semibold` (empty state stat count)

### Verification after all replacements:

Run: `grep -rn "font-medium\|font-bold" nemea-front/src/components/ nemea-front/src/app/ --include="*.tsx" --include="*.ts" | grep -v "components/ui/"` — this MUST return 0 lines.

If any remain, they are in files not listed above — check and apply the same rule.

**D-10 note:** No skeleton changes in this phase — this is confirmed. Do not add or modify any Skeleton usage.
  </action>
  <verify>
    <automated>cd nemea-front && grep -rn "font-medium\|font-bold" src/components/ src/app/ --include="*.tsx" --include="*.ts" | grep -v "components/ui/" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "font-medium\|font-bold" nemea-front/src/components/ nemea-front/src/app/ --include="*.tsx" | grep -v "components/ui/"` returns 0 matches
    - All 23 application files listed above have been modified
    - No files in nemea-front/src/components/ui/ were modified
    - `grep -c "font-semibold" nemea-front/src/components/products/ProductExpandedRow.tsx` returns 11 or more
    - DesglosePanel.tsx line ~71 contains `font-semibold` not `font-bold`
    - ExpenseTable.tsx contains `font-semibold` not `font-bold` (2 instances)
  </acceptance_criteria>
  <done>Typography audit complete. Application code uses only font-semibold (600) and font-normal (400). Zero instances of font-medium or font-bold in app code. Shadcn ui/ files untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — this is a purely visual CSS class replacement.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.2-04 | N/A | Typography classes | accept | CSS-only changes with no security implications |
</threat_model>

<verification>
- `grep -rn "font-medium\|font-bold" nemea-front/src/components/ nemea-front/src/app/ --include="*.tsx" | grep -v "components/ui/"` returns 0 matches
- `grep -c "font-semibold" nemea-front/src/components/products/ProductExpandedRow.tsx` returns 11+
- `grep -c "font-semibold" nemea-front/src/components/products/ProductDetailClient.tsx` returns 5+
- Files in `nemea-front/src/components/ui/` are unmodified (git diff shows no changes)
</verification>

<success_criteria>
- The only font weights in application code are font-semibold and font-normal
- Shadcn ui/ library files retain their original font-medium/font-bold usage
- Visual hierarchy is preserved: labels/headers are bold (600), body text is regular (400)
</success_criteria>

<output>
After completion, create `.planning/phases/12.2-ui-polish-and-product-page-ux/12.2-03-SUMMARY.md`
</output>

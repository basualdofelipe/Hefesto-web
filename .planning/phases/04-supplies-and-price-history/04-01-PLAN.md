---
phase: 04-supplies-and-price-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nemea-back/src/supplies/entities/supply.entity.ts
  - nemea-back/src/supplies/entities/supply-price-history.entity.ts
  - nemea-back/src/supplies/dto/create-supply.dto.ts
  - nemea-back/src/supplies/dto/update-supply.dto.ts
  - nemea-back/src/supplies/dto/create-supply-price.dto.ts
  - nemea-back/src/supplies/supplies.controller.ts
  - nemea-back/src/supplies/supplies.service.ts
  - nemea-back/src/supplies/supplies.module.ts
  - nemea-back/src/suppliers/suppliers.service.ts
  - nemea-back/src/suppliers/suppliers.module.ts
  - nemea-back/src/database/migrations/XXXXXXXXX-CreateSuppliesAndPriceHistory.ts
  - nemea-back/src/app.module.ts
autonomous: true
requirements: [SPPL-01, SPPL-02, SPPL-03, SPPL-04, SPPL-05]

must_haves:
  truths:
    - "Admin can create a supply with name, type, supplier, unit_type, optional notes, and optional initial price"
    - "Admin can update a supply's name, type, unit_type, and notes (NOT supplier)"
    - "Admin can add a new price to a supply, creating an immutable historical record"
    - "User can view all historical prices for a supply sorted newest to oldest"
    - "Current price of a supply is always the most recently created price record"
    - "Admin can toggle a supply's active status (soft delete)"
    - "Deactivating a supplier cascades to deactivate all its active supplies"
    - "Reactivating a supplier does NOT reactivate its supplies"
  artifacts:
    - path: "nemea-back/src/supplies/entities/supply.entity.ts"
      provides: "Supply entity with ManyToOne to SupplyType and Supplier, UnitType enum, is_active"
      contains: "class Supply extends BaseEntity"
    - path: "nemea-back/src/supplies/entities/supply-price-history.entity.ts"
      provides: "Append-only price history entity with ManyToOne to Supply"
      contains: "class SupplyPriceHistory extends BaseEntity"
    - path: "nemea-back/src/supplies/supplies.service.ts"
      provides: "CRUD + toggle-status + price history + current price subquery"
      exports: ["SuppliesService"]
    - path: "nemea-back/src/supplies/supplies.controller.ts"
      provides: "REST endpoints for supplies and nested price history"
      exports: ["SuppliesController"]
  key_links:
    - from: "nemea-back/src/supplies/supplies.service.ts"
      to: "Supply entity + SupplyPriceHistory entity"
      via: "TypeORM repositories"
      pattern: "InjectRepository.*Supply"
    - from: "nemea-back/src/supplies/supplies.service.ts"
      to: "Transaction for create with initial price"
      via: "entityManager.transaction"
      pattern: "transaction"
    - from: "nemea-back/src/suppliers/suppliers.service.ts"
      to: "Supply repository for cascade deactivation"
      via: "InjectRepository(Supply)"
      pattern: "deactivateBySupplier"
    - from: "nemea-back/src/supplies/supplies.service.ts"
      to: "Latest price subquery"
      via: "QueryBuilder or second query"
      pattern: "DISTINCT ON|ORDER BY.*created_at.*DESC.*LIMIT"
---

<objective>
Backend SuppliesModule: Supply and SupplyPriceHistory entities with migration, full CRUD endpoints, append-only price history, current price via efficient query, soft delete with supplier cascade.

Purpose: Provide the REST API for all supply and price data, consumed by the Phase 4 frontend and later by Phase 6 cost calculation.
Output: Working `/api/supplies` and `/api/supplies/:id/prices` endpoints with Swagger docs, all existing tests still passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-supplies-and-price-history/04-CONTEXT.md
@.planning/phases/04-supplies-and-price-history/04-RESEARCH.md
@.planning/phases/03-catalogs-and-suppliers/03-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From nemea-back/src/common/entities/base.entity.ts:
```typescript
export abstract class BaseEntity {
  @PrimaryGeneratedColumn('uuid')
  id!: string;
  @CreateDateColumn({ name: 'created_at', type: 'timestamptz' })
  createdAt!: Date;
  @UpdateDateColumn({ name: 'updated_at', type: 'timestamptz' })
  updatedAt!: Date;
}
```

From nemea-back/src/catalogs/entities/supply-type.entity.ts:
```typescript
@Entity('supply_types')
export class SupplyType extends BaseEntity {
  @Column({ type: 'varchar', length: 100, unique: true })
  name!: string;
}
```

From nemea-back/src/suppliers/entities/supplier.entity.ts:
```typescript
@Entity('suppliers')
@Index('idx_suppliers_name_active', ['name'], { where: '"is_active" = true', unique: true })
export class Supplier extends BaseEntity {
  name!: string;
  address!: string | null;
  email!: string | null;
  phone!: string | null;
  whatsapp!: string | null;
  description!: string | null;
  @Column({ name: 'is_active', type: 'boolean', default: true })
  isActive!: boolean;
}
```

From nemea-back/src/suppliers/suppliers.module.ts:
```typescript
@Module({
  imports: [TypeOrmModule.forFeature([Supplier])],
  controllers: [SuppliersController],
  providers: [SuppliersService],
  exports: [SuppliersService],
})
export class SuppliersModule {}
```

From nemea-back/src/suppliers/suppliers.service.ts:
```typescript
@Injectable()
export class SuppliersService {
  async toggleStatus(id: string): Promise<Supplier> {
    const supplier = await this.findOne(id);
    supplier.isActive = !supplier.isActive;
    return this.supplierRepo.save(supplier);
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supply and SupplyPriceHistory entities + migration + UnitType enum</name>
  <files>
    nemea-back/src/supplies/entities/supply.entity.ts,
    nemea-back/src/supplies/entities/supply-price-history.entity.ts,
    nemea-back/src/database/migrations/XXXXXXXXX-CreateSuppliesAndPriceHistory.ts
  </files>
  <action>
    Create the `nemea-back/src/supplies/entities/` directory with two entity files.

    **Supply entity** (`supply.entity.ts`):
    - Extends BaseEntity (UUID id, created_at, updated_at)
    - `name`: varchar(255), not null
    - `type`: @ManyToOne to SupplyType, eager: true, nullable: false, @JoinColumn name: 'type_id'
    - `supplier`: @ManyToOne to Supplier, eager: true, nullable: false, @JoinColumn name: 'supplier_id'
    - `unitType`: enum column (name: 'unit_type'), using a UnitType enum defined in the same file with values: M2 = 'm2', UNIDAD = 'unidad', METRO = 'metro', KG = 'kg'. Default: UNIDAD
    - `notes`: text, nullable
    - `isActive`: boolean (name: 'is_active'), default true
    - Do NOT define the partial unique index via @Index decorator — define it in migration SQL directly per Pitfall 1 (TypeORM @Index with composite columns + ManyToOne FK can generate incorrect SQL)

    **UnitType enum** (export from supply.entity.ts):
    ```typescript
    export enum UnitType {
      M2 = 'm2',
      UNIDAD = 'unidad',
      METRO = 'metro',
      KG = 'kg',
    }
    ```

    **SupplyPriceHistory entity** (`supply-price-history.entity.ts`):
    - Extends BaseEntity
    - `supply`: @ManyToOne to Supply, nullable: false, onDelete: 'CASCADE', @JoinColumn name: 'supply_id'
    - `price`: decimal(12,2), not null. Use `type: 'decimal', precision: 12, scale: 2`. TypeORM returns decimal as string — this is fine
    - Do NOT add eager loading on this entity — prices are fetched via dedicated endpoints, never auto-loaded with supply

    **Migration** (raw SQL, NOT auto-generated — same approach as Phase 3 migrations):
    - Create the `supplies_unit_type_enum` type explicitly: `CREATE TYPE "supplies_unit_type_enum" AS ENUM ('m2', 'unidad', 'metro', 'kg')`
    - Create `supplies` table with all columns, PK constraint named `PK_supplies_id`, FK constraints `FK_supplies_type` and `FK_supplies_supplier`
    - Create partial unique index: `CREATE UNIQUE INDEX "idx_supplies_name_supplier_active" ON "supplies" ("name", "supplier_id") WHERE "is_active" = true`
    - Create `supply_price_history` table with all columns, PK `PK_supply_price_history_id`, FK `FK_supply_price_history_supply` with ON DELETE CASCADE
    - Create composite index: `CREATE INDEX "idx_supply_price_history_supply_date" ON "supply_price_history" ("supply_id", "created_at" DESC)`
    - Migration down: drop tables and enum type in reverse order
    - Use timestamp-based name matching project convention (e.g., `1772400000000-CreateSuppliesAndPriceHistory.ts`)

    Refer to 04-RESEARCH.md "Backend: Migration SQL Pattern" section for exact SQL.
  </action>
  <verify>
    <automated>cd nemea-back && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Supply and SupplyPriceHistory entities compile with zero TS errors. Migration file exists with both tables, enum, indexes, and FK constraints. UnitType enum is exported.</done>
</task>

<task type="auto">
  <name>Task 2: SuppliesModule with CRUD + price history endpoints + supplier cascade</name>
  <files>
    nemea-back/src/supplies/dto/create-supply.dto.ts,
    nemea-back/src/supplies/dto/update-supply.dto.ts,
    nemea-back/src/supplies/dto/create-supply-price.dto.ts,
    nemea-back/src/supplies/supplies.service.ts,
    nemea-back/src/supplies/supplies.controller.ts,
    nemea-back/src/supplies/supplies.module.ts,
    nemea-back/src/suppliers/suppliers.service.ts,
    nemea-back/src/suppliers/suppliers.module.ts,
    nemea-back/src/app.module.ts
  </files>
  <action>
    **DTOs:**

    `create-supply.dto.ts`:
    - name: @IsString @IsNotEmpty @MaxLength(255) — message: 'El nombre es obligatorio'
    - typeId: @IsUUID @IsNotEmpty — message: 'El tipo es obligatorio'
    - supplierId: @IsUUID @IsNotEmpty — message: 'El proveedor es obligatorio'
    - unitType: @IsEnum(UnitType) — message: 'Unidad de medida invalida'
    - notes?: @IsString @IsOptional
    - initialPrice?: @IsNumber @Min(0) @IsOptional — message: 'El precio debe ser un numero'
    - All fields with @ApiProperty decorators (examples: 'Cuero Vacheta', UnitType enum)

    `update-supply.dto.ts`:
    - name, typeId, unitType, notes — same validators as create but all @IsOptional
    - NO supplierId — supplier is NOT editable after creation (locked decision)
    - Use PartialType(OmitType(CreateSupplyDto, ['supplierId', 'initialPrice']))

    `create-supply-price.dto.ts`:
    - price: @IsNumber @Min(0) @IsNotEmpty — message: 'El precio es obligatorio'
    - @ApiProperty with example: 15200

    **SuppliesService** (`supplies.service.ts`):
    - Inject: `@InjectRepository(Supply)`, `@InjectRepository(SupplyPriceHistory)`, `EntityManager` (for transactions)
    - `findAll(includeInactive: boolean = false)`: Return all supplies (or active only) with type and supplier relations. Use a second query to batch-fetch the latest price per supply (DISTINCT ON pattern or subquery). Return supplies with `currentPrice` and `lastPriceUpdate` mapped. Order by type name ASC, then supply name ASC.
    - `findOne(id: string)`: Find supply by id. Throw NotFoundException if not found. Also fetch current price.
    - `create(dto: CreateSupplyDto)`: If `initialPrice` is provided, use `entityManager.transaction()` to create both supply and price record atomically (per Pitfall 6). Validate FK existence for typeId and supplierId by attempting find. Catch 23505 for unique constraint violation.
    - `update(id: string, dto: UpdateSupplyDto)`: Find then merge+save. Catch 23505.
    - `toggleStatus(id: string)`: Same pattern as SuppliersService — flip isActive, save.
    - `addPrice(supplyId: string, dto: CreateSupplyPriceDto)`: Verify supply exists, create SupplyPriceHistory record. Return the new price record.
    - `getPriceHistory(supplyId: string)`: Verify supply exists, return all price records ordered by created_at DESC.
    - `deactivateBySupplier(supplierId: string)`: Update all active supplies for this supplier to isActive = false. Used by cascade. Returns count of deactivated supplies.

    For the "current price" batch query, use the simpler 2-query approach:
    1. Fetch all supplies with find() and relations
    2. Run a raw query or QueryBuilder to get latest price per supply_id using DISTINCT ON:
       ```sql
       SELECT DISTINCT ON (supply_id) supply_id, price, created_at
       FROM supply_price_history
       ORDER BY supply_id, created_at DESC
       ```
    3. Map prices onto supplies in-memory

    Error messages in Spanish (project convention).

    **SuppliesController** (`supplies.controller.ts`):
    - @Controller('supplies') with @ApiTags('Supplies') and @ApiBearerAuth()
    - GET /supplies — optional query param `includeInactive` (boolean). No @Roles (all authenticated users can read)
    - GET /supplies/:id — ParseUUIDPipe on id. No @Roles
    - POST /supplies — @Roles(Role.ADMIN). Body: CreateSupplyDto
    - PUT /supplies/:id — @Roles(Role.ADMIN). Body: UpdateSupplyDto
    - PATCH /supplies/:id/toggle-status — @Roles(Role.ADMIN)
    - GET /supplies/:id/prices — No @Roles (all can read)
    - POST /supplies/:id/prices — @Roles(Role.ADMIN). Body: CreateSupplyPriceDto
    - Swagger decorators on all endpoints (@ApiOperation, @ApiResponse)

    **SuppliesModule** (`supplies.module.ts`):
    - imports: TypeOrmModule.forFeature([Supply, SupplyPriceHistory])
    - controllers: [SuppliesController]
    - providers: [SuppliesService]
    - exports: [SuppliesService]

    **Supplier cascade deactivation:**
    Per Research recommendation (Pitfall 7 simplest approach): Add `TypeOrmModule.forFeature([Supply])` to SuppliersModule imports. Inject `Repository<Supply>` into SuppliersService. Modify `toggleStatus()`: after flipping isActive, if the new status is `false` (deactivating), run `this.supplyRepo.update({ supplier: { id }, isActive: true }, { isActive: false })` to cascade-deactivate all active supplies. If reactivating — do nothing to supplies (locked decision). This avoids circular module dependencies.

    Update SuppliersModule imports to include `TypeOrmModule.forFeature([Supplier, Supply])`.

    **AppModule**: Add SuppliesModule to imports array.
  </action>
  <verify>
    <automated>cd nemea-back && npx tsc --noEmit 2>&1 | head -20 && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Server boots with `npm run start:dev` and all routes are mapped (verify in console output)
    - Swagger at /api/docs shows all 7 supply endpoints
    - All existing tests still pass
    - TypeScript compiles with zero errors
    - POST /api/supplies creates a supply (with optional initial price in a transaction)
    - GET /api/supplies returns supplies grouped with currentPrice and lastPriceUpdate
    - POST /api/supplies/:id/prices appends a price record
    - GET /api/supplies/:id/prices returns history newest-first
    - PATCH /api/supplies/:id/toggle-status flips isActive
    - Deactivating a supplier via existing endpoint cascades to deactivate its active supplies
  </done>
</task>

</tasks>

<verification>
1. `cd nemea-back && npx tsc --noEmit` — zero TypeScript errors
2. `cd nemea-back && npm test` — all existing tests pass
3. Server boots: `npm run start:dev` shows supply routes mapped in console
4. Swagger docs at /api/docs show all 7 endpoints under "Supplies" tag
</verification>

<success_criteria>
- Supply entity with ManyToOne to SupplyType and Supplier, UnitType enum, is_active with partial unique index
- SupplyPriceHistory entity with append-only records, composite index on (supply_id, created_at DESC)
- 7 REST endpoints: GET/POST supplies, GET/PUT/PATCH single supply, GET/POST prices
- Current price returned efficiently (2-query pattern, not N+1)
- Supplier deactivation cascades to supplies (one direction only)
- All existing tests pass, zero TS errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-supplies-and-price-history/04-01-SUMMARY.md`
</output>
